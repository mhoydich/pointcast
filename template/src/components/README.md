# template/src/components — fetched at init time

`RoomRenderer.astro` is copied here by `scripts/init-node.mjs` from the canonical pointcast repo. Same reasoning as `src/lib/README.md` — the renderer evolves with the v1 contract; pin a version or pull the latest at fork time.
