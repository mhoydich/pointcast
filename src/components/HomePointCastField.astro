<section class="pc-today-home" aria-labelledby="pc-today-home-title">
  <a class="pc-today-home__copy" href="/field">
    <span class="pc-today-home__kicker">
      <i aria-hidden="true"></i>
      PointCast Field · Observation 01 · 90 seconds
    </span>
    <span>
      <h2 id="pc-today-home-title">
        Find the seat
        <em>nobody designed.</em>
      </h2>
      <p>
        Look for something that was not sold as furniture, but quietly gives a
        person permission to stop.
      </p>
    </span>
    <span class="pc-today-home__action">
      Take today’s field prompt
      <b aria-hidden="true">↘</b>
    </span>
  </a>

  <a class="pc-today-home__visual" href="/field" aria-label="Open today’s ninety-second field invitation">
    <img
      src="/beach-commons/v17/assets/poster-01.png"
      alt="An illustrated public science fair beside the beach with seven colorful gateways"
      width="1024"
      height="1536"
      loading="eager"
    />
    <span class="pc-today-home__stamp">
      <strong>90</strong>
      seconds<br />outside the feed
    </span>
    <span class="pc-today-home__facts">
      <span>No account</span>
      <span>No upload</span>
      <span>One observation</span>
    </span>
  </a>
</section>

<style>
  .pc-today-home {
    --today-navy: #0b2632;
    --today-paper: #f4efd9;
    --today-orange: #f06a3f;
    --today-yellow: #f2c85f;
    display: grid;
    min-width: 0;
    min-height: clamp(480px, 64vw, 700px);
    grid-template-columns: minmax(0, 0.94fr) minmax(360px, 1.06fr);
    overflow: hidden;
    border-right: 1px solid rgba(11, 38, 50, 0.22);
    border-bottom: 1px solid rgba(11, 38, 50, 0.22);
    border-left: 1px solid rgba(11, 38, 50, 0.22);
    background: var(--today-navy);
    color: #fffdf5;
  }

  .pc-today-home a {
    color: inherit;
    text-decoration: none;
  }

  .pc-today-home__copy {
    display: flex;
    min-width: 0;
    flex-direction: column;
    justify-content: space-between;
    padding: clamp(30px, 5vw, 76px);
  }

  .pc-today-home__kicker {
    display: flex;
    align-items: center;
    gap: 11px;
    color: var(--today-yellow);
    font: 750 10px/1.35 "JetBrains Mono Variable", monospace;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .pc-today-home__kicker i {
    width: 9px;
    height: 9px;
    flex: 0 0 auto;
    border-radius: 50%;
    background: var(--today-orange);
    box-shadow: 0 0 0 6px rgba(240, 106, 63, 0.16);
  }

  .pc-today-home h2 {
    max-width: 8ch;
    margin: clamp(32px, 6vw, 82px) 0 0;
    color: #fffdf5;
    font-size: clamp(62px, 7.6vw, 120px);
    font-weight: 900;
    letter-spacing: -0.078em;
    line-height: 0.78;
  }

  .pc-today-home h2 em {
    display: block;
    color: var(--today-yellow);
    font-family: Iowan Old Style, Palatino, Georgia, serif;
    font-size: 0.72em;
    font-weight: 400;
    letter-spacing: -0.045em;
    line-height: 1.05;
  }

  .pc-today-home__copy p {
    max-width: 31ch;
    margin: 27px 0 0;
    color: rgba(255, 253, 245, 0.75);
    font-size: clamp(17px, 1.55vw, 24px);
    font-weight: 540;
    letter-spacing: -0.025em;
    line-height: 1.25;
  }

  .pc-today-home__action {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 20px;
    margin-top: 38px;
    border: 1px solid rgba(255, 255, 255, 0.35);
    padding: 17px 18px;
    color: #fffdf5;
    font: 780 10px/1 "JetBrains Mono Variable", monospace;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition: border-color 150ms ease, background 150ms ease, color 150ms ease;
  }

  .pc-today-home__action b {
    font-size: 18px;
  }

  .pc-today-home__copy:hover .pc-today-home__action {
    border-color: var(--today-orange);
    background: var(--today-orange);
  }

  .pc-today-home__visual {
    position: relative;
    min-width: 0;
    min-height: 480px;
    overflow: hidden;
    background: #ceb47d;
  }

  .pc-today-home__visual::after {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, transparent 42%, rgba(11, 38, 50, 0.58)),
      linear-gradient(90deg, rgba(11, 38, 50, 0.13), transparent 28%);
    content: "";
    pointer-events: none;
  }

  .pc-today-home__visual img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 700ms cubic-bezier(0.2, 0.8, 0.2, 1);
  }

  .pc-today-home__visual:hover img {
    transform: scale(1.025);
  }

  .pc-today-home__stamp {
    position: absolute;
    z-index: 2;
    top: clamp(20px, 3vw, 34px);
    right: clamp(20px, 3vw, 34px);
    display: grid;
    width: clamp(108px, 11vw, 156px);
    aspect-ratio: 1;
    place-items: center;
    border: 2px solid #fffdf5;
    border-radius: 50%;
    background: var(--today-orange);
    color: #fffdf5;
    font: 750 8px/1.3 "JetBrains Mono Variable", monospace;
    letter-spacing: 0.06em;
    text-align: center;
    text-transform: uppercase;
    transform: rotate(7deg);
  }

  .pc-today-home__stamp strong {
    display: block;
    margin-bottom: 4px;
    font-family: Inter, sans-serif;
    font-size: clamp(30px, 3.4vw, 48px);
    font-weight: 900;
    letter-spacing: -0.06em;
    line-height: 0.8;
  }

  .pc-today-home__facts {
    position: absolute;
    z-index: 2;
    right: 18px;
    bottom: 18px;
    left: 18px;
    display: flex;
    justify-content: space-between;
    gap: 10px;
    padding: 14px 15px;
    background: rgba(11, 38, 50, 0.9);
    color: #fffdf5;
    font: 700 9px/1.3 "JetBrains Mono Variable", monospace;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  @media (max-width: 900px) {
    .pc-today-home {
      grid-template-columns: 1fr;
    }

    .pc-today-home__copy {
      min-height: 650px;
    }

    .pc-today-home__visual {
      min-height: 76svh;
    }
  }

  @media (max-width: 520px) {
    .pc-today-home__copy {
      min-height: 640px;
      padding: 28px 20px;
    }

    .pc-today-home h2 {
      font-size: clamp(58px, 19vw, 82px);
    }

    .pc-today-home__copy p {
      font-size: 17px;
    }

    .pc-today-home__facts {
      flex-direction: column;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .pc-today-home__visual img {
      transition: none;
    }
  }
</style>
