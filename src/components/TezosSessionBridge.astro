---
/**
 * Layout-backed copy of the global PointCast Tezos session bridge.
 * Cloudflare middleware injects the same guarded script into standalone pages.
 */
import { POINTCAST_TEZOS_SESSION_BRIDGE_SCRIPT } from '../lib/auth/session-bridge-script';
---

<script is:inline set:html={POINTCAST_TEZOS_SESSION_BRIDGE_SCRIPT} />
