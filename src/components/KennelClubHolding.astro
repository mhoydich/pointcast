<section class="kennel-holding" data-kennel-holding hidden>
  <p class="kennel-holding__line" data-kennel-holding-line role="status" aria-live="polite"></p>
  <div class="kennel-holding__card">
    <p class="kennel-holding__summary" data-kennel-holding-summary></p>
    <p class="kennel-holding__where" data-kennel-holding-where></p>
    <div class="kennel-holding__links">
      <a data-kennel-holding-receipt href="https://tzkt.io" target="_blank" rel="noopener">receipt</a>
      <a data-kennel-holding-move href="/me?link=wallet" hidden>Move it to a wallet you control →</a>
    </div>
  </div>
</section>

<script>
  import { getSession } from '../lib/auth/client';
  import { mountKennelClaimedState } from '../lib/kennel-club-claimed-client';
  mountKennelClaimedState(getSession);
</script>

<style>
  .kennel-holding { display: grid; gap: 10px; }
  .kennel-holding__line { margin: 0; font: 500 12px/1.45 var(--pc-font-mono); }
  .kennel-holding__card { display: grid; gap: 7px; padding: 14px; border: 1.5px solid currentColor; background: rgba(255,255,255,.1); }
  .kennel-holding__card p { margin: 0; }
  .kennel-holding__summary { font: 500 13px/1.4 var(--pc-font-mono); }
  .kennel-holding__where { font: 400 10px/1.4 var(--pc-font-mono); letter-spacing: .08em; text-transform: uppercase; }
  .kennel-holding__links { display: flex; flex-wrap: wrap; justify-content: space-between; gap: 9px 16px; padding-top: 4px; }
  .kennel-holding__links a { color: inherit; font: 500 10px/1.4 var(--pc-font-mono); letter-spacing: .06em; text-transform: uppercase; }
  [hidden] { display: none !important; }
</style>
