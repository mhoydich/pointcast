<section class="super-auth-ribbon" aria-labelledby="super-auth-ribbon-title">
  <a class="super-auth-ribbon__inner" href="/auth">
    <span class="super-auth-ribbon__copy">
      <span class="super-auth-ribbon__kicker">POINTCAST ACCOUNT</span>
      <strong id="super-auth-ribbon-title">Identity becomes broadcast.</strong>
      <span>Google names it. Spotify sounds it. Shopify opens the shop. Tezos signs the object.</span>
    </span>
    <span class="super-auth-ribbon__nodes" aria-label="Google, Spotify, Shopify, and Tezos connections">
      <span style="--node-color:#4285f4"><b>ID</b><small>GOOGLE</small></span>
      <i aria-hidden="true"></i>
      <span style="--node-color:#1ed760"><b>SP</b><small>SPOTIFY</small></span>
      <i aria-hidden="true"></i>
      <span style="--node-color:#95bf47"><b>SH</b><small>SHOPIFY</small></span>
      <i aria-hidden="true"></i>
      <span style="--node-color:#ffd43b"><b>TZ</b><small>TEZOS</small></span>
    </span>
    <span class="super-auth-ribbon__action">Open account ↗</span>
  </a>
</section>

<style>
  .super-auth-ribbon {
    --auth-acid: #eaff57;
    margin: 0;
    border: 1px solid #171717;
    background:
      linear-gradient(90deg, rgba(255,255,255,0.55) 1px, transparent 1px) 0 0 / 34px 34px,
      var(--auth-acid);
  }
  .super-auth-ribbon__inner {
    min-height: 134px;
    display: grid;
    grid-template-columns: minmax(280px, 1.2fr) minmax(420px, 1.5fr) auto;
    align-items: stretch;
    color: #171717;
    text-decoration: none;
  }
  .super-auth-ribbon__copy {
    display: grid;
    align-content: center;
    gap: 8px;
    padding: 22px;
    border-right: 1px solid #171717;
  }
  .super-auth-ribbon__kicker,
  .super-auth-ribbon__copy > span:last-child,
  .super-auth-ribbon__nodes small,
  .super-auth-ribbon__action {
    font-family: var(--pc-font-mono, ui-monospace, monospace);
    text-transform: uppercase;
  }
  .super-auth-ribbon__kicker { color: #3150d8; font-size: 9px; font-weight: 800; letter-spacing: 0.12em; }
  .super-auth-ribbon__copy strong { max-width: 480px; font-size: clamp(24px, 2.3vw, 38px); line-height: 0.95; letter-spacing: -0.035em; }
  .super-auth-ribbon__copy > span:last-child { max-width: 520px; font-size: 8px; line-height: 1.55; letter-spacing: 0.04em; }
  .super-auth-ribbon__nodes {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 18px;
  }
  .super-auth-ribbon__nodes > span {
    width: 62px;
    height: 72px;
    display: grid;
    place-items: center;
    align-content: center;
    gap: 7px;
    border: 1px solid #171717;
    background: #fffdf2;
    box-shadow: 5px 5px 0 var(--node-color);
    transform: rotate(-2deg);
  }
  .super-auth-ribbon__nodes > span:nth-of-type(even) { transform: rotate(2deg); }
  .super-auth-ribbon__nodes b { font: 800 18px/1 var(--pc-font-mono, ui-monospace, monospace); }
  .super-auth-ribbon__nodes small { font-size: 6px; letter-spacing: 0.06em; }
  .super-auth-ribbon__nodes i { width: clamp(14px, 2vw, 36px); height: 1px; margin-inline: 5px; background: #171717; }
  .super-auth-ribbon__action {
    min-width: 124px;
    display: grid;
    place-items: center;
    padding: 18px;
    border-left: 1px solid #171717;
    font-size: 9px;
    font-weight: 800;
    letter-spacing: 0.08em;
    text-align: center;
  }
  .super-auth-ribbon__inner:hover .super-auth-ribbon__action { background: #171717; color: var(--auth-acid); }

  @media (max-width: 980px) {
    .super-auth-ribbon__inner { grid-template-columns: 1fr auto; }
    .super-auth-ribbon__nodes { grid-column: 1 / -1; grid-row: 2; border-top: 1px solid #171717; }
  }

  @media (max-width: 620px) {
    .super-auth-ribbon__inner { grid-template-columns: 1fr; }
    .super-auth-ribbon__copy { border-right: 0; }
    .super-auth-ribbon__action { min-height: 50px; border-top: 1px solid #171717; border-left: 0; }
    .super-auth-ribbon__nodes {
      grid-column: auto;
      grid-row: auto;
      justify-content: space-between;
      overflow: hidden;
      padding: 18px 12px 22px;
    }
    .super-auth-ribbon__nodes > span { width: 55px; height: 64px; box-shadow: 4px 4px 0 var(--node-color); }
    .super-auth-ribbon__nodes i { flex: 1; min-width: 6px; margin-inline: 4px; }
  }
</style>
