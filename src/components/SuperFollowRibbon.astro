<section class="super-follow-ribbon" aria-labelledby="super-follow-ribbon-title">
  <a class="super-follow-ribbon__inner" href="/super-follow">
    <span class="super-follow-ribbon__copy">
      <span class="super-follow-ribbon__kicker">SUPER FOLLOW · PERSONAL BROADCASTER</span>
      <strong id="super-follow-ribbon-title">A company is not one feed.</strong>
      <span>Tune Sony—or build a local shelf for any subject with public source doors.</span>
    </span>
    <span class="super-follow-ribbon__nodes" aria-label="Sources become a lens, signal, want, and receipt">
      <span style="--node-color:#1746ff"><b>01</b><small>SOURCES</small></span>
      <i aria-hidden="true"></i>
      <span style="--node-color:#ff8cc6"><b>02</b><small>LENS</small></span>
      <i aria-hidden="true"></i>
      <span style="--node-color:#5ce1e6"><b>03</b><small>SIGNAL</small></span>
      <i aria-hidden="true"></i>
      <span style="--node-color:#eaff57"><b>04</b><small>WANT</small></span>
      <i aria-hidden="true"></i>
      <span style="--node-color:#ffb347"><b>05</b><small>RECEIPT</small></span>
    </span>
    <span class="super-follow-ribbon__action">Build a follow ↗</span>
  </a>
</section>

<style>
  .super-follow-ribbon {
    --follow-ink: #121212;
    --follow-paper: #f7f1ff;
    margin: 0;
    border: 1px solid var(--follow-ink);
    border-top: 0;
    background:
      radial-gradient(circle at 85% 15%, rgba(255, 140, 198, 0.85) 0 11%, transparent 11.2%),
      linear-gradient(135deg, #5ce1e6 0 28%, #f7f1ff 28% 70%, #1746ff 70%);
  }
  .super-follow-ribbon__inner {
    min-height: 126px;
    display: grid;
    grid-template-columns: minmax(290px, 1.1fr) minmax(450px, 1.5fr) auto;
    align-items: stretch;
    color: var(--follow-ink);
    text-decoration: none;
  }
  .super-follow-ribbon__copy {
    display: grid;
    align-content: center;
    gap: 8px;
    padding: 22px;
    border-right: 1px solid var(--follow-ink);
    background: rgba(247, 241, 255, 0.94);
  }
  .super-follow-ribbon__kicker,
  .super-follow-ribbon__copy > span:last-child,
  .super-follow-ribbon__nodes small,
  .super-follow-ribbon__action {
    font-family: var(--pc-font-mono, ui-monospace, monospace);
    text-transform: uppercase;
  }
  .super-follow-ribbon__kicker {
    color: #1746ff;
    font-size: 9px;
    font-weight: 850;
    letter-spacing: 0.12em;
  }
  .super-follow-ribbon__copy strong {
    max-width: 520px;
    font-size: clamp(24px, 2.2vw, 36px);
    line-height: 0.95;
    letter-spacing: -0.035em;
  }
  .super-follow-ribbon__copy > span:last-child {
    max-width: 520px;
    font-size: 8px;
    line-height: 1.55;
    letter-spacing: 0.04em;
  }
  .super-follow-ribbon__nodes {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 18px;
  }
  .super-follow-ribbon__nodes > span {
    width: 58px;
    height: 70px;
    display: grid;
    place-items: center;
    align-content: center;
    gap: 7px;
    border: 1px solid var(--follow-ink);
    background: var(--follow-paper);
    box-shadow: 5px 5px 0 var(--node-color);
    transform: rotate(-2deg);
  }
  .super-follow-ribbon__nodes > span:nth-of-type(even) { transform: rotate(2deg); }
  .super-follow-ribbon__nodes b { font: 850 17px/1 var(--pc-font-mono, ui-monospace, monospace); }
  .super-follow-ribbon__nodes small { font-size: 6px; letter-spacing: 0.05em; }
  .super-follow-ribbon__nodes i {
    width: clamp(7px, 1.4vw, 24px);
    height: 1px;
    margin-inline: 5px;
    background: var(--follow-ink);
  }
  .super-follow-ribbon__action {
    min-width: 116px;
    display: grid;
    place-items: center;
    padding: 18px;
    border-left: 1px solid var(--follow-ink);
    background: #eaff57;
    font-size: 9px;
    font-weight: 850;
    letter-spacing: 0.08em;
    text-align: center;
  }
  .super-follow-ribbon__inner:hover .super-follow-ribbon__action {
    background: var(--follow-ink);
    color: #eaff57;
  }

  @media (max-width: 1040px) {
    .super-follow-ribbon__inner { grid-template-columns: 1fr auto; }
    .super-follow-ribbon__nodes {
      grid-column: 1 / -1;
      grid-row: 2;
      border-top: 1px solid var(--follow-ink);
    }
  }

  @media (max-width: 680px) {
    .super-follow-ribbon__inner { grid-template-columns: 1fr; }
    .super-follow-ribbon__copy { border-right: 0; }
    .super-follow-ribbon__action {
      min-height: 50px;
      border-top: 1px solid var(--follow-ink);
      border-left: 0;
    }
    .super-follow-ribbon__nodes {
      grid-column: auto;
      grid-row: auto;
      justify-content: space-between;
      overflow: hidden;
      padding: 18px 10px 22px;
    }
    .super-follow-ribbon__nodes > span {
      width: 47px;
      height: 60px;
      box-shadow: 4px 4px 0 var(--node-color);
    }
    .super-follow-ribbon__nodes i { flex: 1; min-width: 2px; margin-inline: 3px; }
  }
</style>
