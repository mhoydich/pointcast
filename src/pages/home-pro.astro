---
/**
 * /home-pro — stable alias for the homepage.
 *
 * Renders the same <HomeProfessional /> component as /. Kept as a named
 * route so the professional home has a durable URL independent of whatever
 * is at / in the future.
 */
import HomeProfessional from '../components/HomeProfessional.astro';
---
<HomeProfessional />
