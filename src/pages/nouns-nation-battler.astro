---
/**
 * /nouns-nation-battler - PointCast Battle Desk for the v30 Nouns Nation
 * automated league battler.
 */
import BlockLayout from '../layouts/BlockLayout.astro';
import { CHANNELS } from '../lib/channels';

const ch = CHANNELS.BTL;

const tickerItems = [
  '30 vs 30 auto-battle live from CH.BTL',
  'Two-week table rolls toward the Nouns Bowl',
  'Scout cards, boss fields, and season challenges are active',
  'Scorebug listens to the embedded v30 match engine',
  'Desk Wall, TV Cast, posters, and JSON are live',
];

const featureStories = [
  {
    label: 'Top Story',
    title: 'The Nouns Bowl path starts with one live slate',
    dek: 'Every matchup moves the local table: wins, points, fans, rivalry heat, and challenge marks.',
    image: '/games/nouns-nation-battler/assets/noun-12.svg',
  },
  {
    label: 'Film Room',
    title: 'Boss fields are where the desk earns its keep',
    dek: 'Monsoon Rift, Neon Crown, Scrap Storm, and Blackout Fog turn normal roles into weird reads.',
    image: '/games/nouns-nation-battler/assets/noun-41.svg',
  },
  {
    label: 'Root Line',
    title: 'Pick a gang, then watch the table remember',
    dek: 'Rooting is browser-local, but it makes an automated field feel like a room with sides.',
    image: '/games/nouns-nation-battler/assets/noun-27.svg',
  },
];

const schedule = [
  { slot: 'Now', matchup: 'Live league slate', status: 'Main feed' },
  { slot: 'Next', matchup: 'Winner reports to table', status: 'Auto Next queue' },
  { slot: 'TV', matchup: 'Director Mode Cast', status: '/nouns-nation-battler-tv' },
  { slot: 'Wall', matchup: 'Season Desk report', status: '/nouns-nation-battler-desk' },
];

const fallbackStandings = [
  { short: 'TN', name: 'Tomato Noggles', record: '0-0', detail: '0 fans', color: '#e45745' },
  { short: 'CF', name: 'Cobalt Frames', record: '0-0', detail: '0 fans', color: '#3677e0' },
  { short: 'GN', name: 'Golden Nouncil', record: '0-0', detail: '0 fans', color: '#d49b19' },
  { short: 'GS', name: 'Garden Stack', record: '0-0', detail: '0 fans', color: '#3f9b54' },
];

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'VideoGame',
  name: 'PointCast Battle Desk: Nouns Nation Battler',
  description: 'A sports-desk style live hub for the v30 automated 30v30 Nouns Nation Battler league.',
  url: 'https://pointcast.xyz/nouns-nation-battler/',
  gamePlatform: 'Web browser',
  genre: 'Auto battler',
  inLanguage: 'en-US',
};
---

<BlockLayout
  title="PointCast Battle Desk"
  description="A sports-desk style hub for Nouns Nation Battler v30: live scorebug, league table, top nouns, match queue, live calls, TV cast, desk wall, posters, and the automated 30 vs 30 field feed."
  image="/images/og/battle.png"
  jsonLd={jsonLd}
  alternates={[{ type: 'application/json', href: '/nouns-nation-battler.json', title: 'Nouns Nation Battler manifest (JSON)' }]}
  frame={{
    image: 'https://pointcast.xyz/images/og/battle.png',
    buttons: [
      { label: 'Watch desk', action: 'link', target: 'https://pointcast.xyz/nouns-nation-battler/' },
      { label: 'TV Cast', action: 'link', target: 'https://pointcast.xyz/nouns-nation-battler-tv/' },
      { label: 'Desk Wall', action: 'link', target: 'https://pointcast.xyz/nouns-nation-battler-desk/' },
      { label: 'Game JSON', action: 'link', target: 'https://pointcast.xyz/nouns-nation-battler.json' },
    ],
  }}
>
  <main
    class="battle-desk"
    data-battle-desk
    style={`--btl: ${ch.color600}; --btl-dark: ${ch.color800}; --btl-soft: ${ch.color50}; --left-team: #e45745; --right-team: #3677e0; --momentum-left: 50%;`}
  >
    <nav class="crumb" aria-label="Breadcrumb">
      <a href="/">Home</a>
      <span aria-hidden="true">/</span>
      <a href="/c/battler/">CH.BTL</a>
      <span aria-hidden="true">/</span>
      <span>Battle Desk</span>
    </nav>

    <section class="network-strip" aria-label="Battle desk ticker">
      <div class="network-brand">
        <span>PointCast</span>
        <strong>BTL Network</strong>
      </div>
      <div class="ticker" aria-label="Live ticker">
        <span class="ticker__live">Live</span>
        <div class="ticker__viewport">
          <div class="ticker__track">
            {tickerItems.concat(tickerItems).map((item) => <span>{item}</span>)}
          </div>
        </div>
      </div>
      <a class="data-link" href="/nouns-nation-battler.json">Desk JSON</a>
    </section>

    <section class="desk-head" aria-label="Battle desk header">
      <div>
        <p class="kicker">CH.BTL / Nouns Nation v30</p>
        <h1>PointCast Battle Desk</h1>
      </div>
      <div class="desk-head__meta" aria-label="Live desk status">
        <span data-live-field="state">LIVE</span>
        <strong data-live-field="match">Match 1</strong>
        <em data-live-field="last-update">Waiting for feed</em>
      </div>
    </section>

    <section class="scorebug" aria-label="Live match score">
      <article class="score-team score-team--left">
        <div class="score-team__id">
          <span data-live-field="left-short">TN</span>
          <strong data-live-field="left-name">Tomato Noggles</strong>
          <em data-live-field="left-mark">split tomato noggles</em>
        </div>
        <div class="score-team__score">
          <span>Alive</span>
          <strong data-live-field="left-alive">30</strong>
        </div>
      </article>

      <div class="score-center">
        <div class="score-center__top">
          <span data-live-field="field">Open Field</span>
          <span data-live-field="speed">Live speed</span>
          <span data-live-field="auto-next">Auto next on</span>
        </div>
        <div class="momentum" aria-label="Momentum meter">
          <i></i>
        </div>
        <strong data-live-field="momentum-label">Even field</strong>
        <em data-live-field="league-line">Day 1 / 14 · Slate 1 of 4</em>
      </div>

      <article class="score-team score-team--right">
        <div class="score-team__id">
          <span data-live-field="right-short">CF</span>
          <strong data-live-field="right-name">Cobalt Frames</strong>
          <em data-live-field="right-mark">blue square lenses</em>
        </div>
        <div class="score-team__score">
          <span>Alive</span>
          <strong data-live-field="right-alive">30</strong>
        </div>
      </article>
    </section>

    <section class="broadcast-layout">
      <section class="game-stage" aria-label="Live Nouns Nation Battler feed">
        <header class="stage-header">
          <div>
            <p class="panel-label">Main Feed</p>
            <h2 data-live-field="matchup">Nouns Nation Battler</h2>
          </div>
          <div class="stage-actions" aria-label="Desk controls">
            <button type="button" data-desk-command="newMatch">Next</button>
            <button type="button" data-desk-command="quickSim">Quick Sim</button>
            <button type="button" data-desk-command="simDay">Sim Day</button>
            <button type="button" data-desk-command="togglePause">Pause</button>
            <button type="button" data-desk-command="setSpeed" data-speed="0.75">Slow</button>
            <button type="button" class="is-active" data-desk-command="setSpeed" data-speed="1">Live</button>
            <button type="button" data-desk-command="setSpeed" data-speed="1.55">Rush</button>
          </div>
        </header>
        <iframe
          id="battleFrame"
          src="/games/nouns-nation-battler/index.html"
          title="Nouns Nation Battler live match feed"
          loading="eager"
          allow="fullscreen"
        ></iframe>
      </section>

      <aside class="studio-rail" aria-label="Studio desk">
        <section class="panel panel--live-log">
          <header class="panel-head">
            <p class="panel-label">Live Blog</p>
            <h2>Latest Calls</h2>
          </header>
          <div class="live-log" data-live-field="log">
            <p>Feed warming up.</p>
          </div>
        </section>

        <section class="panel">
          <header class="panel-head">
            <p class="panel-label">Leaders</p>
            <h2>Top Nouns</h2>
          </header>
          <div class="top-nouns" data-live-field="top-nouns">
            <article>
              <img src="/games/nouns-nation-battler/assets/noun-0.svg" alt="" loading="lazy" />
              <span>Awaiting first push</span>
              <strong>Pregame</strong>
            </article>
          </div>
        </section>

        <section class="panel">
          <header class="panel-head">
            <p class="panel-label">Table</p>
            <h2>League Standings</h2>
          </header>
          <div class="standings" data-live-field="standings">
            {fallbackStandings.map((row, index) => (
              <article style={`--team:${row.color}`}>
                <span>{index + 1}</span>
                <strong>{row.short}</strong>
                <em>{row.name}</em>
                <b>{row.record}</b>
                <i>{row.detail}</i>
              </article>
            ))}
          </div>
        </section>
      </aside>
    </section>

    <section class="coverage-grid" aria-label="Battle desk coverage">
      <section class="panel panel--wide">
        <header class="panel-head">
          <p class="panel-label">Root Line</p>
          <h2 data-live-field="root-line">Pick a gang</h2>
        </header>
        <div class="root-buttons">
          <button type="button" data-desk-command="root" data-team="0">Root Left</button>
          <button type="button" data-desk-command="root" data-team="1">Root Right</button>
          <a href="/nouns-nation-battler-tv/" target="_blank" rel="noreferrer">TV Cast</a>
          <a href="/nouns-nation-battler-desk/">Desk Wall</a>
          <a href="/nouns-nation-battler-posters/">Posters</a>
        </div>
        <p class="desk-copy" data-live-field="challenge">
          Season challenge loading. The desk will pull the live side quest from the embedded match.
        </p>
      </section>

      <section class="panel">
        <header class="panel-head">
          <p class="panel-label">Board</p>
          <h2>Match Queue</h2>
        </header>
        <div class="schedule">
          {schedule.map((item) => (
            <article>
              <span>{item.slot}</span>
              <strong>{item.matchup}</strong>
              <em>{item.status}</em>
            </article>
          ))}
        </div>
      </section>

      <section class="panel panel--features">
        <header class="panel-head">
          <p class="panel-label">NounCenter</p>
          <h2>Desk Notes</h2>
        </header>
        <div class="feature-stack">
          {featureStories.map((story) => (
            <article>
              <img src={story.image} alt="" loading="lazy" />
              <div>
                <span>{story.label}</span>
                <strong>{story.title}</strong>
                <p>{story.dek}</p>
              </div>
            </article>
          ))}
        </div>
      </section>
    </section>
  </main>
</BlockLayout>

<script is:inline>
(function () {
  'use strict';

  var SOURCE = 'pointcast:nouns-nation-battler';
  var CONTROL_SOURCE = 'pointcast:battle-desk';
  var desk = document.querySelector('[data-battle-desk]');
  var frame = document.getElementById('battleFrame');
  if (!desk || !frame) return;

  function find(selector) {
    return desk.querySelector(selector);
  }

  function setText(selector, value) {
    var node = find(selector);
    if (node) node.textContent = value;
  }

  function escapeHtml(value) {
    return String(value == null ? '' : value).replace(/[&<>"']/g, function (char) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char];
    });
  }

  function assetPath(asset) {
    if (!asset) return '/games/nouns-nation-battler/assets/noun-0.svg';
    if (/^https?:\/\//.test(asset) || asset.charAt(0) === '/') return asset;
    return '/games/nouns-nation-battler/' + asset.replace(/^\.?\//, '');
  }

  function postCommand(command, extra) {
    if (!frame.contentWindow) return;
    frame.contentWindow.postMessage(Object.assign({
      source: CONTROL_SOURCE,
      type: 'command',
      command: command,
    }, extra || {}), window.location.origin);
  }

  function speedLabel(speed) {
    if (speed >= 1.4) return 'Rush speed';
    if (speed <= 0.8) return 'Slow speed';
    return 'Live speed';
  }

  function renderTopNouns(leaders) {
    var node = find('[data-live-field="top-nouns"]');
    if (!node) return;
    var rows = []
      .concat((leaders && leaders.left) || [])
      .concat((leaders && leaders.right) || [])
      .sort(function (a, b) {
        var aScore = ((a.stats && a.stats.damage) || 0) + ((a.stats && a.stats.kos) || 0) * 34 + ((a.stats && a.stats.heals) || 0) * 0.12;
        var bScore = ((b.stats && b.stats.damage) || 0) + ((b.stats && b.stats.kos) || 0) * 34 + ((b.stats && b.stats.heals) || 0) * 0.12;
        return bScore - aScore;
      })
      .slice(0, 6);

    if (!rows.length) return;
    node.innerHTML = rows.map(function (unit) {
      var line = '#' + escapeHtml(unit.number || '?') + ' / ' + escapeHtml(unit.role || 'noun') + ' / ' + escapeHtml(unit.hp || 0) + '% HP';
      var stat = unit.stats ? escapeHtml(unit.stats.damage || 0) + ' dmg · ' + escapeHtml(unit.stats.kos || 0) + ' KO' : 'warming up';
      return '<article>' +
        '<img src="' + escapeHtml(assetPath(unit.asset)) + '" alt="" loading="lazy" />' +
        '<span>' + line + '</span>' +
        '<strong>' + escapeHtml(unit.name || 'Noun') + '</strong>' +
        '<em>' + stat + '</em>' +
      '</article>';
    }).join('');
  }

  function renderStandings(rows) {
    var node = find('[data-live-field="standings"]');
    if (!node || !rows || !rows.length) return;
    node.innerHTML = rows.slice(0, 8).map(function (row, index) {
      var diff = Number(row.pf || 0) - Number(row.pa || 0);
      var diffLabel = (diff >= 0 ? '+' : '') + diff;
      return '<article style="--team:' + escapeHtml(row.color || '#8A2432') + '">' +
        '<span>' + String(index + 1) + '</span>' +
        '<strong>' + escapeHtml(row.short || '--') + '</strong>' +
        '<em>' + escapeHtml(row.name || 'Gang') + '</em>' +
        '<b>' + escapeHtml(row.wins || 0) + '-' + escapeHtml(row.losses || 0) + '</b>' +
        '<i>' + diffLabel + ' / ' + escapeHtml(row.fans || 0) + ' fans</i>' +
      '</article>';
    }).join('');
  }

  function renderLog(logs) {
    var node = find('[data-live-field="log"]');
    if (!node || !logs || !logs.length) return;
    node.innerHTML = logs.slice(0, 7).map(function (line) {
      return '<p>' + escapeHtml(line) + '</p>';
    }).join('');
  }

  function renderSnapshot(payload) {
    if (!payload || !payload.gangs || payload.gangs.length < 2) return;

    var leftGang = payload.gangs[0];
    var rightGang = payload.gangs[1];
    var left = payload.alive ? payload.alive.left : 30;
    var right = payload.alive ? payload.alive.right : 30;
    var total = Math.max(1, left + right);
    var leftShare = (left / total) * 100;
    var stateLabel = payload.finished ? 'FINAL' : (payload.running ? 'LIVE' : 'PAUSED');
    var fieldName = payload.field && payload.field.boss ? payload.field.boss : ((payload.field && payload.field.name) || 'Open Field');
    var momentumLabel = Math.abs(left - right) < 2
      ? 'Even field'
      : (left > right ? leftGang.short + ' pressure' : rightGang.short + ' pressure');
    var challenge = payload.challenge
      ? payload.challenge.name + ': ' + payload.challenge.line + '. ' + payload.challenge.rule
      : 'Challenge loading from the match engine.';

    desk.style.setProperty('--left-team', leftGang.color || '#e45745');
    desk.style.setProperty('--right-team', rightGang.color || '#3677e0');
    desk.style.setProperty('--momentum-left', leftShare.toFixed(1) + '%');

    setText('[data-live-field="state"]', stateLabel);
    setText('[data-live-field="match"]', 'Match ' + (payload.match || 1));
    setText('[data-live-field="last-update"]', new Date(payload.timestamp || Date.now()).toLocaleTimeString([], {
      hour: 'numeric',
      minute: '2-digit',
      second: '2-digit',
    }));

    setText('[data-live-field="left-short"]', leftGang.short || 'L');
    setText('[data-live-field="left-name"]', leftGang.name || 'Left Gang');
    setText('[data-live-field="left-mark"]', leftGang.mark || '');
    setText('[data-live-field="left-alive"]', String(left));

    setText('[data-live-field="right-short"]', rightGang.short || 'R');
    setText('[data-live-field="right-name"]', rightGang.name || 'Right Gang');
    setText('[data-live-field="right-mark"]', rightGang.mark || '');
    setText('[data-live-field="right-alive"]', String(right));

    setText('[data-live-field="field"]', fieldName);
    setText('[data-live-field="speed"]', speedLabel(Number(payload.speed || 1)));
    setText('[data-live-field="auto-next"]', payload.autoNext ? 'Auto next on' : 'Auto next off');
    setText('[data-live-field="momentum-label"]', momentumLabel);
    setText('[data-live-field="league-line"]', payload.league && payload.league.line ? payload.league.line : 'League loading');
    setText('[data-live-field="matchup"]', payload.league && payload.league.matchup ? payload.league.matchup : 'Nouns Nation Battler');
    setText('[data-live-field="root-line"]', payload.rootingFor ? 'Rooting for ' + payload.rootingFor : 'Pick a gang');
    setText('[data-live-field="challenge"]', challenge);

    Array.prototype.forEach.call(desk.querySelectorAll('[data-speed]'), function (button) {
      button.classList.toggle('is-active', Number(button.dataset.speed) === Number(payload.speed || 1));
    });

    renderTopNouns(payload.leaders);
    renderStandings(payload.standings);
    renderLog(payload.logs);
  }

  desk.addEventListener('click', function (event) {
    var target = event.target;
    if (!target || !target.closest) return;
    var button = target.closest('[data-desk-command]');
    if (!button || !desk.contains(button)) return;
    var command = button.dataset.deskCommand;

    if (command === 'setSpeed') {
      postCommand(command, { value: Number(button.dataset.speed || 1) });
    } else if (command === 'root') {
      postCommand(command, { team: Number(button.dataset.team || 0) });
    } else {
      postCommand(command);
    }
  });

  window.addEventListener('message', function (event) {
    if (event.origin !== window.location.origin) return;
    var message = event.data || {};
    if (message.source !== SOURCE || message.type !== 'snapshot') return;
    renderSnapshot(message.payload);
  });

  frame.addEventListener('load', function () {
    window.setTimeout(function () {
      postCommand('snapshot');
    }, 350);
  });

  window.setInterval(function () {
    postCommand('snapshot');
  }, 2500);
})();
</script>

<style>
  .battle-desk {
    --paper: #f7f3e7;
    --ink: #111318;
    --muted: #5b6170;
    --line: rgba(17, 19, 24, 0.14);
    --panel: #fffdf7;
    --gold: #f3bf3f;
    --green: #16845d;
    min-height: 100vh;
    padding: 18px clamp(14px, 3vw, 30px) 42px;
    color: var(--ink);
    background:
      linear-gradient(90deg, rgba(17, 19, 24, 0.045) 1px, transparent 1px),
      linear-gradient(rgba(17, 19, 24, 0.045) 1px, transparent 1px),
      var(--paper);
    background-size: 24px 24px;
  }

  .crumb {
    display: flex;
    align-items: center;
    gap: 9px;
    flex-wrap: wrap;
    margin: 0 0 10px;
    color: var(--muted);
    font-family: var(--pc-font-mono, ui-monospace, monospace);
    font-size: 0.78rem;
    font-weight: 500;
  }

  .network-strip,
  .scorebug,
  .game-stage,
  .panel {
    border: 2px solid var(--ink);
    background: var(--panel);
    box-shadow: 5px 5px 0 var(--ink);
  }

  .network-strip {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    gap: 14px;
    min-height: 54px;
    margin-bottom: 14px;
    background: #151820;
    color: #fffaf0;
  }

  .network-brand {
    display: grid;
    align-content: center;
    min-height: 54px;
    padding: 7px 14px;
    background: var(--btl);
    color: #fffdf7;
    text-transform: uppercase;
  }

  .network-brand span,
  .panel-label,
  .ticker__live,
  .score-center__top,
  .score-center em,
  .score-team__score span,
  .desk-head__meta span,
  .standings span,
  .standings i,
  .feature-stack span,
  .schedule span,
  .schedule em,
  .top-nouns span,
  .top-nouns em,
  .live-log p {
    font-family: var(--pc-font-mono, ui-monospace, monospace);
  }

  .network-brand span {
    font-size: 0.64rem;
    letter-spacing: 0.16em;
  }

  .network-brand strong {
    font-size: 1rem;
    line-height: 1;
  }

  .ticker {
    min-width: 0;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    align-items: center;
    gap: 12px;
    overflow: hidden;
  }

  .ticker__live {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    color: #fffdf7;
    font-size: 0.75rem;
    text-transform: uppercase;
  }

  .ticker__live::before {
    content: "";
    width: 9px;
    height: 9px;
    border-radius: 50%;
    background: #ff4d5f;
    box-shadow: 0 0 0 3px rgba(255, 77, 95, 0.18);
  }

  .ticker__viewport {
    overflow: hidden;
  }

  .ticker__track {
    display: inline-flex;
    gap: 42px;
    min-width: max-content;
    color: #f5e8c8;
    font-family: var(--pc-font-mono, ui-monospace, monospace);
    font-size: 0.78rem;
    animation: ticker 44s linear infinite;
  }

  .data-link {
    margin-right: 12px;
    padding: 8px 10px;
    border: 1px solid rgba(255, 250, 240, 0.5);
    color: #fffaf0;
    font-family: var(--pc-font-mono, ui-monospace, monospace);
    font-size: 0.72rem;
    text-transform: uppercase;
  }

  .desk-head {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 18px;
    margin: 16px 0 12px;
  }

  .kicker,
  .panel-label {
    margin: 0 0 6px;
    color: var(--btl-dark);
    font-size: 0.74rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h1,
  h2,
  p {
    margin: 0;
  }

  h1 {
    font-size: 3.1rem;
    line-height: 0.95;
  }

  h2 {
    font-size: 1.18rem;
    line-height: 1.05;
  }

  .desk-head__meta {
    min-width: 190px;
    display: grid;
    justify-items: end;
    gap: 3px;
    text-align: right;
  }

  .desk-head__meta span {
    padding: 4px 8px;
    border: 2px solid var(--ink);
    background: #d7ff3f;
    color: var(--ink);
    font-size: 0.72rem;
    text-transform: uppercase;
  }

  .desk-head__meta strong {
    font-size: 1.3rem;
  }

  .desk-head__meta em {
    color: var(--muted);
    font-size: 0.78rem;
    font-style: normal;
  }

  .scorebug {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(260px, 0.72fr) minmax(0, 1fr);
    align-items: stretch;
    margin-bottom: 14px;
    overflow: hidden;
  }

  .score-team {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    align-items: center;
    min-width: 0;
    padding: 15px;
    gap: 14px;
  }

  .score-team--left {
    border-left: 14px solid var(--left-team);
  }

  .score-team--right {
    border-right: 14px solid var(--right-team);
  }

  .score-team__id {
    min-width: 0;
  }

  .score-team__id span {
    display: inline-grid;
    place-items: center;
    min-width: 42px;
    min-height: 34px;
    margin-bottom: 7px;
    border: 2px solid var(--ink);
    background: var(--ink);
    color: #fffdf7;
    font-weight: 500;
  }

  .score-team__id strong {
    display: block;
    font-size: 1.85rem;
    line-height: 0.98;
  }

  .score-team__id em {
    display: block;
    margin-top: 4px;
    color: var(--muted);
    font-size: 0.83rem;
    font-style: normal;
  }

  .score-team__score {
    min-width: 92px;
    display: grid;
    justify-items: center;
    padding: 9px 10px;
    border: 2px solid var(--ink);
    background: #fff;
  }

  .score-team__score span {
    color: var(--muted);
    font-size: 0.66rem;
    text-transform: uppercase;
  }

  .score-team__score strong {
    font-size: 3.2rem;
    line-height: 0.9;
    font-variant-numeric: tabular-nums;
  }

  .score-center {
    display: grid;
    align-content: center;
    gap: 8px;
    padding: 14px;
    border-left: 2px solid var(--ink);
    border-right: 2px solid var(--ink);
    background: #f1f4f8;
    text-align: center;
  }

  .score-center__top {
    display: flex;
    justify-content: center;
    gap: 8px;
    flex-wrap: wrap;
    color: var(--muted);
    font-size: 0.68rem;
    text-transform: uppercase;
  }

  .momentum {
    position: relative;
    height: 18px;
    border: 2px solid var(--ink);
    background: linear-gradient(90deg, var(--left-team) 0 var(--momentum-left), var(--right-team) var(--momentum-left) 100%);
    overflow: hidden;
  }

  .momentum i {
    position: absolute;
    top: -6px;
    bottom: -6px;
    left: calc(var(--momentum-left) - 2px);
    width: 4px;
    background: var(--ink);
  }

  .score-center strong {
    font-size: 0.86rem;
    text-transform: uppercase;
  }

  .score-center em {
    color: var(--muted);
    font-size: 0.68rem;
    font-style: normal;
    text-transform: uppercase;
  }

  .broadcast-layout {
    display: grid;
    grid-template-columns: minmax(0, 1.7fr) minmax(320px, 0.7fr);
    gap: 16px;
    align-items: start;
  }

  .game-stage {
    min-width: 0;
    overflow: hidden;
  }

  .stage-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    padding: 13px 14px;
    border-bottom: 2px solid var(--ink);
    background: #fffdf7;
  }

  .stage-actions,
  .root-buttons {
    display: flex;
    align-items: center;
    gap: 7px;
    flex-wrap: wrap;
  }

  button,
  .root-buttons a {
    min-height: 34px;
    border: 2px solid var(--ink);
    border-radius: 4px;
    background: #fff;
    color: var(--ink);
    font: inherit;
    font-size: 0.82rem;
    font-weight: 500;
    cursor: pointer;
    box-shadow: 2px 2px 0 var(--ink);
    padding: 7px 9px;
    text-decoration: none;
  }

  button:hover,
  button:focus-visible,
  button.is-active,
  .root-buttons a:hover,
  .root-buttons a:focus-visible {
    background: var(--gold);
    outline: none;
  }

  .game-stage iframe {
    display: block;
    width: 100%;
    height: min(780px, 80vh);
    min-height: 700px;
    border: 0;
    background: #f2eddc;
  }

  .studio-rail,
  .coverage-grid {
    display: grid;
    gap: 14px;
  }

  .panel {
    padding: 14px;
  }

  .panel-head {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 12px;
  }

  .live-log {
    display: grid;
    gap: 8px;
  }

  .live-log p {
    border-left: 8px solid var(--gold);
    background: #fff;
    padding: 8px 9px;
    font-size: 0.78rem;
    line-height: 1.32;
  }

  .top-nouns {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 8px;
  }

  .top-nouns article {
    min-width: 0;
    min-height: 132px;
    display: grid;
    align-content: start;
    gap: 5px;
    border: 1px solid var(--line);
    background: #fff;
    padding: 8px;
  }

  .top-nouns img {
    width: 48px;
    height: 48px;
    image-rendering: pixelated;
  }

  .top-nouns span,
  .top-nouns em {
    color: var(--muted);
    font-size: 0.64rem;
    font-style: normal;
    text-transform: uppercase;
  }

  .top-nouns strong {
    font-size: 0.85rem;
    line-height: 1.06;
  }

  .standings {
    display: grid;
    gap: 7px;
  }

  .standings article {
    display: grid;
    grid-template-columns: 28px 44px minmax(0, 1fr) 54px 80px;
    align-items: center;
    gap: 7px;
    min-height: 36px;
    border-left: 8px solid var(--team, var(--btl));
    background: #fff;
    padding: 6px 8px;
  }

  .standings span,
  .standings i {
    color: var(--muted);
    font-size: 0.68rem;
    font-style: normal;
    text-transform: uppercase;
  }

  .standings strong,
  .standings b {
    font-size: 0.9rem;
  }

  .standings em {
    overflow: hidden;
    color: var(--ink);
    font-size: 0.82rem;
    font-style: normal;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .coverage-grid {
    grid-template-columns: minmax(260px, 0.8fr) minmax(260px, 0.7fr) minmax(360px, 1.25fr);
    margin-top: 16px;
  }

  .panel--wide {
    border-top: 8px solid var(--green);
  }

  .desk-copy {
    margin-top: 12px;
    color: var(--muted);
    font-size: 0.95rem;
    line-height: 1.48;
  }

  .schedule {
    display: grid;
    gap: 9px;
  }

  .schedule article {
    display: grid;
    grid-template-columns: 52px minmax(0, 1fr);
    gap: 4px 10px;
    border-bottom: 1px solid var(--line);
    padding-bottom: 8px;
  }

  .schedule span {
    grid-row: span 2;
    color: var(--btl-dark);
    font-size: 0.72rem;
    text-transform: uppercase;
  }

  .schedule strong {
    font-size: 0.95rem;
  }

  .schedule em {
    color: var(--muted);
    font-size: 0.72rem;
    font-style: normal;
    text-transform: uppercase;
  }

  .feature-stack {
    display: grid;
    gap: 10px;
  }

  .feature-stack article {
    display: grid;
    grid-template-columns: 72px minmax(0, 1fr);
    gap: 10px;
    align-items: center;
    min-height: 90px;
    border: 1px solid var(--line);
    background: #fff;
    padding: 9px;
  }

  .feature-stack img {
    width: 70px;
    height: 70px;
    image-rendering: pixelated;
  }

  .feature-stack span {
    display: block;
    margin-bottom: 3px;
    color: var(--btl-dark);
    font-size: 0.66rem;
    text-transform: uppercase;
  }

  .feature-stack strong {
    display: block;
    font-size: 0.96rem;
    line-height: 1.12;
  }

  .feature-stack p {
    margin-top: 4px;
    color: var(--muted);
    font-size: 0.85rem;
    line-height: 1.35;
  }

  @keyframes ticker {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }

  @media (prefers-reduced-motion: reduce) {
    .ticker__track {
      animation: none;
    }
  }

  @media (max-width: 1160px) {
    h1 {
      font-size: 2.45rem;
    }

    .broadcast-layout,
    .coverage-grid {
      grid-template-columns: 1fr;
    }

    .studio-rail {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }

  @media (max-width: 820px) {
    .battle-desk {
      padding: 12px 10px 34px;
    }

    .network-strip,
    .scorebug,
    .desk-head,
    .stage-header,
    .studio-rail {
      grid-template-columns: 1fr;
    }

    .network-strip,
    .scorebug {
      display: grid;
    }

    .desk-head,
    .stage-header {
      align-items: stretch;
    }

    h1 {
      font-size: 2rem;
    }

    .desk-head__meta {
      justify-items: start;
      text-align: left;
    }

    .score-center {
      border-left: 0;
      border-right: 0;
      border-top: 2px solid var(--ink);
      border-bottom: 2px solid var(--ink);
    }

    .score-team__id strong {
      font-size: 1.35rem;
    }

    .game-stage iframe {
      min-height: 900px;
      height: 900px;
    }

    .top-nouns {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 560px) {
    .stage-actions,
    .root-buttons {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .standings article {
      grid-template-columns: 24px 38px minmax(0, 1fr) 48px;
    }

    .standings i {
      display: none;
    }
  }
</style>
