---
/**
 * /gandalf — the PointCast-native companion page.
 *
 * cc pass. Shipped in parallel with Codex's Issue #15 brief — this one
 * gives the companion room the /gandalf front door. The actual sit is the
 * first viewport; the Samwise Sigil remains below as a secondary keepsake.
 *
 * The companion (Codex's sitting-with-gandalf microsite) stays untouched
 * at public/sitting-with-gandalf/. This page is the on-site entry — it
 * credits Codex, explains the Gamgee ↔ Samwise ↔ Gandalf pairing, and
 * offers the keepsake.
 *
 * The Samwise Sigil:
 *   - Deterministic per-visit ID generated client-side (time-seeded hash)
 *   - Stored in localStorage under pc:gandalf:sigil
 *   - Rendered as an SVG glyph
 *   - "Keep it" = saved locally. "Mint on Tezos" = Phase 2 hook, explains
 *     it's coming in an FA2 contract later
 *   - Non-transferable by design. One keepsake per browser.
 */
import BlockLayout from '../layouts/BlockLayout.astro';

const title = "Sitting with Gandalf";
const description = "The PointCast Gandalf companion room: V7 by default, rebuilt around actual CC0 Noun-backed Wizard Nouns, the WN-7.1 creation protocol, Noun story pages, councils, keepsakes, resource sharpening, a Myth listen-along, and a Gandalf-adjacent image generation prompt series.";
const canonical = 'https://pointcast.xyz/gandalf';

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebPage',
  '@id': canonical,
  name: title,
  description,
  url: canonical,
  inLanguage: 'en-US',
  about: ['gandalf', 'samwise', 'companion', 'gamgee', 'sigil', 'keepsake', 'pointcast'],
  isPartOf: {
    '@type': 'WebSite',
    '@id': 'https://pointcast.xyz/#website',
    name: 'PointCast',
  },
  mainEntity: {
    '@type': 'CreativeWork',
    name: 'Sitting with Gandalf (companion)',
    creator: [{ '@type': 'SoftwareApplication', name: 'Codex' }],
    url: 'https://pointcast.xyz/sitting-with-gandalf/',
    datePublished: '2026-04-22',
  },
};
---

<BlockLayout title={title} description={description} jsonLd={jsonLd}>
  <div class="gf">
    <nav class="gf__crumb" aria-label="Breadcrumb">
      <a href="/">Home</a>
      <span aria-hidden="true">›</span>
      <a href="/gamgee">gamgee</a>
      <span aria-hidden="true">›</span>
      <span>gandalf</span>
    </nav>

    <section class="gf__room" aria-labelledby="room-title">
      <header class="gf__hero">
        <p class="gf__kicker mono">COMPANION ROOM · V7 WIZARD NOUNS</p>
        <h1 id="room-title" class="gf__title">Sitting with Gandalf</h1>
        <p class="gf__thesis">
          Start here. V7 turns the room into actual Noun-backed Wizard Nouns:
          real CC0 Noun SVGs, wizard hats, staffs, relics, realms, and the
          WN-7.1 attribute protocol: aura, charm, atmosphere, offering, lesson,
          resource, and relic pair. Forge one Gandalf; roll the actual Noun;
          summon a three-card council; tell a tiny Noun story; pair a keepsake;
          sharpen one resource; cast a protocol spell; then sit with it. V6's
          clear sit and the deeper V5 spell table are still there, along with 30 tiny Gandalf companions, 12
          keepsakes, a Myth listen-along, and a
          15-card image prompt series:
          Mondrian road maps, pop screenprints, Hoydich SoCal, forest, ocean,
          Buddha head, blockprint waves, tankers, airplanes, pixel campfires,
          Monet Paris rain, France, and red wine. The version switch still
          keeps V1 classic, V2 Gandalf ritual, V3 storybook nature, V4 pixel
          campfire, V5 spell table, and V6 Gandalf sit within reach.
        </p>
        <div class="gf__actions" aria-label="Gandalf actions">
          <a class="gf__btn gf__btn--primary" href="/sitting-with-gandalf/">Open full room</a>
          <a class="gf__btn gf__btn--ghost" href="#sigil-head">Samwise Sigil</a>
        </div>
      </header>

      <iframe
        class="gf__room-frame"
        src="/sitting-with-gandalf/"
        title="Sitting with Gandalf companion room"
        loading="eager"
        referrerpolicy="same-origin"
      ></iframe>
    </section>

    <section class="gf__sigil-section" aria-labelledby="sigil-head">
      <h2 id="sigil-head" class="gf__h2">The Samwise Sigil</h2>
      <p class="gf__p">
        A small keepsake for visitors of this page. Every sigil is generated
        when you claim it — time-seeded, local, and yours. Non-transferable by
        design. One per browser.
      </p>

      <div class="sigil" id="sigil-panel">
        <div class="sigil__stage" id="sigil-stage">
          <div class="sigil__placeholder mono" id="sigil-placeholder">
            NO SIGIL YET · TAP TO CONJURE
          </div>
          <svg class="sigil__svg" id="sigil-svg" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" aria-hidden="true" style="display:none;">
            <defs>
              <radialGradient id="sigil-bg" cx="50%" cy="40%" r="70%">
                <stop offset="0%" stop-color="#faefc7" stop-opacity="0.9"/>
                <stop offset="100%" stop-color="#4e3a16" stop-opacity="1"/>
              </radialGradient>
            </defs>
            <circle cx="60" cy="60" r="58" fill="url(#sigil-bg)" stroke="#2d1f08" stroke-width="1.5"/>
            <g id="sigil-glyph" stroke="#2d1f08" stroke-width="2" fill="none" stroke-linecap="round" stroke-linejoin="round"></g>
            <text x="60" y="108" font-family="JetBrains Mono, monospace" font-size="9" letter-spacing="2" text-anchor="middle" fill="#2d1f08" id="sigil-label">S-000-000</text>
          </svg>
        </div>

        <div class="sigil__meta" id="sigil-meta">
          <p class="sigil__line mono"><span class="sigil__key">ID</span><span class="sigil__val" id="sigil-id">—</span></p>
          <p class="sigil__line mono"><span class="sigil__key">FOUND</span><span class="sigil__val" id="sigil-found">—</span></p>
          <p class="sigil__line mono"><span class="sigil__key">STATE</span><span class="sigil__val" id="sigil-state">unclaimed</span></p>
        </div>

        <div class="sigil__actions" id="sigil-actions">
          <button class="gf__btn gf__btn--primary" id="btn-claim" type="button">Conjure a sigil</button>
          <button class="gf__btn" id="btn-mint" type="button" disabled>Mint on Tezos (Phase 2)</button>
          <button class="gf__btn gf__btn--ghost" id="btn-release" type="button" style="display:none;">Release sigil</button>
        </div>
      </div>

      <details class="gf__details">
        <summary class="mono">WHAT IS THIS, EXACTLY?</summary>
        <div class="gf__details-body">
          <p>
            A <strong>sigil</strong> is a small deterministic glyph — three
            strokes on a warm parchment background, labeled with a three-part
            ID like <code>S-042-7F3</code>. The ID encodes the moment you
            conjured it, keyed to your browser. It cannot be transferred.
          </p>
          <p>
            It lives in <code>localStorage</code> under
            <code>pc:gandalf:sigil</code>. Clear site data and it's gone. Mint
            the <strong>on-chain edition</strong> later (Tezos FA2, Phase 2
            of this token primitive) and the sigil becomes portable — until
            then, it's a local keepsake.
          </p>
          <p>
            Why bother? Because <em>keep going</em> is the release's thesis,
            and a sigil is a tiny, un-ceremonious way to mark that you sat
            with the companion. No account. No scoring. No signal back to us.
          </p>
        </div>
      </details>
    </section>

    <section class="gf__pairing">
      <h2 class="gf__h2">The pairing</h2>
      <p class="gf__p">
        Samwise Gamgee carries the ring-bearer through Mordor when everything
        else has failed. Gandalf sits with Frodo and Pippin and Merry when
        they need counsel, not motion. The release is named for the first;
        this companion is named for the second. Together — a step, a breath.
        <em>Small hands change the course of the future because the great
        have other things to do.</em>
      </p>
    </section>

    <section class="gf__related">
      <h2 class="gf__h2">Related</h2>
      <ul class="gf__links">
        <li><a href="/gamgee"><code class="mono">/gamgee</code></a> — the release front door</li>
        <li><a href="/farm"><code class="mono">/farm</code></a> — Sam's Plot (a small, careful garden)</li>
        <li><a href="/sitting-with-gandalf/"><code class="mono">/sitting-with-gandalf/</code></a> — the full companion microsite</li>
        <li><a href="/meditate"><code class="mono">/meditate</code></a> — the ocean-room, a different quiet</li>
        <li><a href="/tonight"><code class="mono">/tonight</code></a> — marine layer, a song on</li>
      </ul>
    </section>

    <footer class="gf__foot mono">
      <span>COMPANION · GAMGEE ↔ GANDALF · EL SEGUNDO · 2026-04-23</span>
    </footer>
  </div>
</BlockLayout>

<script is:inline>
  (function () {
    'use strict';
    var KEY = 'pc:gandalf:sigil';

    function loadSigil() {
      try {
        var raw = localStorage.getItem(KEY);
        if (!raw) return null;
        var parsed = JSON.parse(raw);
        if (parsed && parsed.version === 1 && parsed.id) return parsed;
      } catch (e) {}
      return null;
    }

    function saveSigil(s) {
      try { localStorage.setItem(KEY, JSON.stringify(s)); } catch (e) {}
    }

    function clearSigil() {
      try { localStorage.removeItem(KEY); } catch (e) {}
    }

    // Simple deterministic hash — xoroshiro-ish, enough for visual variance
    function hash32(str) {
      var h = 2166136261;
      for (var i = 0; i < str.length; i++) {
        h = (h ^ str.charCodeAt(i)) >>> 0;
        h = Math.imul(h, 16777619) >>> 0;
      }
      return h >>> 0;
    }

    function idFor(nowMs, entropy) {
      var seed = nowMs.toString(36) + ':' + entropy;
      var h = hash32(seed);
      var part1 = (h & 0xfff).toString(16).padStart(3, '0').toUpperCase();
      var part2 = ((h >> 12) & 0xfff).toString(16).padStart(3, '0').toUpperCase();
      return 'S-' + part1 + '-' + part2;
    }

    function generateGlyph(seed) {
      // Three strokes at deterministic positions across the 120x120 canvas.
      var h = hash32(seed);
      var strokes = [];
      for (var i = 0; i < 3; i++) {
        var a = (h >> (i * 6)) & 0x3f;   // 0-63
        var b = (h >> (i * 6 + 3)) & 0x3f;
        var startX = 30 + (a % 16) * 4;
        var startY = 30 + Math.floor(a / 16) * 15;
        var cx = 40 + (b % 16) * 2.5;
        var cy = 40 + Math.floor(b / 16) * 10;
        var endX = 30 + ((h >> (i * 4)) & 0x3f) % 60;
        var endY = 30 + ((h >> (i * 4 + 3)) & 0x3f) % 60;
        strokes.push('M ' + startX + ' ' + startY + ' Q ' + cx + ' ' + cy + ' ' + endX + ' ' + endY);
      }
      return strokes.map(function (d) { return '<path d="' + d + '"/>'; }).join('');
    }

    function formatDate(ms) {
      var d = new Date(ms);
      var parts = d.toISOString().replace('T', ' ').slice(0, 16);
      return parts + ' UTC';
    }

    var $svg = document.getElementById('sigil-svg');
    var $placeholder = document.getElementById('sigil-placeholder');
    var $glyph = document.getElementById('sigil-glyph');
    var $label = document.getElementById('sigil-label');
    var $id = document.getElementById('sigil-id');
    var $found = document.getElementById('sigil-found');
    var $state = document.getElementById('sigil-state');
    var $btnClaim = document.getElementById('btn-claim');
    var $btnMint = document.getElementById('btn-mint');
    var $btnRelease = document.getElementById('btn-release');

    function render(sigil) {
      if (!sigil) {
        $svg.style.display = 'none';
        $placeholder.style.display = 'block';
        $id.textContent = '—';
        $found.textContent = '—';
        $state.textContent = 'unclaimed';
        $btnClaim.style.display = 'inline-block';
        $btnClaim.textContent = 'Conjure a sigil';
        $btnRelease.style.display = 'none';
        $btnMint.disabled = true;
        return;
      }
      $svg.style.display = 'block';
      $placeholder.style.display = 'none';
      $glyph.innerHTML = generateGlyph(sigil.id);
      $label.textContent = sigil.id;
      $id.textContent = sigil.id;
      $found.textContent = formatDate(sigil.foundAt);
      $state.textContent = 'carried';
      $btnClaim.style.display = 'none';
      $btnRelease.style.display = 'inline-block';
      $btnMint.disabled = false;
    }

    $btnClaim.addEventListener('click', function () {
      var now = Date.now();
      var entropy = Math.random().toString(36).slice(2) + ':' + navigator.userAgent.slice(0, 20);
      var sigil = {
        version: 1,
        id: idFor(now, entropy),
        foundAt: now,
        browserMark: entropy.slice(0, 8),
      };
      saveSigil(sigil);
      render(sigil);
    });

    $btnMint.addEventListener('click', function () {
      alert('Mint on Tezos — Phase 2 of the sigil primitive.\n\nThis opens a Beacon wallet flow, mints an FA2 token on a forthcoming contract, and imports the sigil ID as its token metadata. Until the contract is deployed, your sigil stays local.\n\nWatch the release notes at /gamgee/changelog.');
    });

    $btnRelease.addEventListener('click', function () {
      if (!confirm('Release this sigil? It cannot be recovered — the ID is unique to this moment.')) return;
      clearSigil();
      render(null);
    });

    render(loadSigil());
  })();
</script>

<style>
  .gf {
    max-width: 1180px;
    margin: 0 auto;
    padding: 18px 18px 88px;
    font-family: 'Inter Variable', Inter, system-ui, sans-serif;
    color: #2d2419;
    background: linear-gradient(180deg, #f5ecd7 0%, #e7d9b3 100%);
    min-height: calc(100vh - 60px);
  }
  .mono { font-family: 'JetBrains Mono Variable', 'JetBrains Mono', ui-monospace, monospace; }

  .gf__crumb {
    font-size: 11px;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: #6b5a3a;
    margin-bottom: 22px;
  }
  .gf__crumb a { color: inherit; text-decoration: none; border-bottom: 1px solid currentColor; padding-bottom: 1px; }
  .gf__crumb span { margin: 0 6px; }

  .gf__room {
    min-height: calc(100vh - 150px);
    margin: 0 0 34px;
  }
  .gf__hero {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 10px 24px;
    align-items: end;
    padding-bottom: 18px;
    border-bottom: 2px solid #4e3a16;
    margin-bottom: 18px;
  }
  .gf__kicker {
    grid-column: 1 / -1;
    font-size: 10.5px;
    letter-spacing: 0.24em;
    font-weight: 700;
    color: #8a2432;
    margin: 0;
  }
  .gf__title {
    font-family: 'DM Serif Display', 'Lora', Georgia, serif;
    font-size: clamp(32px, 5vw, 64px);
    line-height: 1.1;
    letter-spacing: -0.01em;
    margin: 0;
    font-weight: 400;
    color: #2d1f08;
  }
  .gf__thesis {
    grid-column: 1;
    font-family: Georgia, serif;
    font-size: 16px;
    line-height: 1.55;
    margin: 0;
    max-width: 60ch;
    color: #3a3022;
  }
  .gf__thesis strong { color: #2d1f08; }

  .gf__actions {
    grid-row: 2 / span 2;
    grid-column: 2;
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;
    gap: 8px;
  }

  .gf__room-frame {
    display: block;
    width: 100%;
    min-height: min(760px, calc(100vh - 210px));
    border: 1px solid #4e3a16;
    border-radius: 6px;
    background: #0d100e;
    box-shadow: 0 22px 60px rgba(45, 31, 8, 0.22);
  }

  .gf__h2 {
    font-family: 'DM Serif Display', 'Lora', Georgia, serif;
    font-size: 24px;
    font-weight: 400;
    margin: 0 0 14px;
    color: #2d1f08;
  }
  .gf__p {
    font-family: Georgia, serif;
    font-size: 15px;
    line-height: 1.6;
    max-width: 62ch;
    margin: 0 0 14px;
    color: #3a3022;
  }
  .gf__p code {
    font-family: 'JetBrains Mono Variable', ui-monospace, monospace;
    font-size: 12.5px;
    background: rgba(45,31,8,0.1);
    padding: 1px 5px;
    border-radius: 2px;
  }
  .gf__p em { font-style: italic; color: #2d1f08; }

  .gf__sigil-section { margin: 0 0 36px; padding-bottom: 24px; border-bottom: 1px solid #c5b284; }
  .gf__sigil-section,
  .gf__pairing,
  .gf__related,
  .gf__foot {
    max-width: 760px;
    margin-left: auto;
    margin-right: auto;
  }

  .sigil {
    display: grid;
    grid-template-columns: 140px 1fr;
    gap: 18px;
    align-items: center;
    padding: 18px;
    background: #fffdf6;
    border: 1px solid #c5b284;
    border-radius: 3px;
    margin: 6px 0 16px;
  }
  @media (max-width: 560px) {
    .sigil { grid-template-columns: 1fr; text-align: center; }
  }
  .sigil__stage {
    width: 120px;
    height: 120px;
    position: relative;
    justify-self: center;
    align-self: center;
  }
  .sigil__placeholder {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    text-align: center;
    font-size: 9.5px;
    letter-spacing: 0.16em;
    color: #6b5a3a;
    padding: 0 8px;
    background: repeating-linear-gradient(45deg, #f5ecd7, #f5ecd7 6px, #eadbb4 6px, #eadbb4 12px);
    border-radius: 50%;
    border: 2px dashed #c5b284;
    font-weight: 700;
  }
  .sigil__svg { width: 120px; height: 120px; }
  .sigil__meta { font-size: 11px; }
  .sigil__line {
    display: grid;
    grid-template-columns: 58px 1fr;
    gap: 8px;
    padding: 3px 0;
    border-bottom: 1px dashed #d4c6a4;
  }
  .sigil__line:last-child { border-bottom: 0; }
  .sigil__key {
    font-size: 9px;
    letter-spacing: 0.14em;
    color: #8a7854;
    font-weight: 700;
  }
  .sigil__val {
    font-size: 11.5px;
    color: #2d1f08;
  }

  .sigil__actions {
    grid-column: 1 / -1;
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    justify-content: center;
    padding-top: 10px;
    border-top: 1px dashed #d4c6a4;
  }

  .gf__btn {
    appearance: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    background: #2d1f08;
    color: #f5ecd7;
    border: 1px solid #2d1f08;
    padding: 9px 14px;
    font-family: 'JetBrains Mono Variable', ui-monospace, monospace;
    font-size: 11px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    font-weight: 700;
    cursor: pointer;
    border-radius: 3px;
    transition: background 0.15s ease;
    text-decoration: none;
  }
  .gf__btn:hover { background: #4e3a16; text-decoration: none; }
  .gf__btn:disabled { opacity: 0.45; cursor: not-allowed; }
  .gf__btn--primary { background: #8a2432; border-color: #8a2432; }
  .gf__btn--primary:hover { background: #a6303f; }
  .gf__btn--ghost { background: transparent; color: #6b5a3a; border-color: #c5b284; }
  .gf__btn--ghost:hover { background: #f5ecd7; color: #2d1f08; }

  .gf__details {
    margin: 6px 0 0;
    padding: 12px 14px;
    background: rgba(255,253,246,0.6);
    border: 1px solid #d4c6a4;
    border-radius: 3px;
  }
  .gf__details summary {
    cursor: pointer;
    font-size: 10.5px;
    letter-spacing: 0.2em;
    font-weight: 700;
    color: #4e3a16;
  }
  .gf__details-body {
    margin-top: 12px;
    padding-top: 10px;
    border-top: 1px dashed #c5b284;
  }
  .gf__details-body p {
    font-family: Georgia, serif;
    font-size: 13.5px;
    line-height: 1.55;
    margin: 0 0 8px;
    color: #3a3022;
  }
  .gf__details-body p:last-child { margin: 0; }
  .gf__details-body code {
    font-family: 'JetBrains Mono Variable', ui-monospace, monospace;
    font-size: 12px;
    background: rgba(45,31,8,0.09);
    padding: 1px 5px;
    border-radius: 2px;
  }
  .gf__details-body em { font-style: italic; color: #2d1f08; }
  .gf__details-body strong { color: #2d1f08; }

  .gf__pairing { margin: 0 0 32px; padding-bottom: 24px; border-bottom: 1px solid #c5b284; }
  .gf__pairing .gf__p em {
    display: block;
    margin-top: 8px;
    font-style: italic;
    color: #6b5a3a;
    font-size: 14px;
  }

  .gf__related { margin: 0 0 24px; padding-bottom: 20px; border-bottom: 1px solid #c5b284; }
  .gf__links {
    list-style: none;
    padding: 0;
    margin: 0;
    display: flex;
    flex-direction: column;
    gap: 5px;
    font-family: Georgia, serif;
    font-size: 14px;
  }
  .gf__links a { color: #8a2432; text-decoration: none; border-bottom: 1px solid transparent; }
  .gf__links a:hover { border-bottom-color: #8a2432; }
  .gf__links code { font-size: 12.5px; background: rgba(45,31,8,0.08); padding: 1px 5px; border-radius: 2px; }

  .gf__foot {
    font-size: 10.5px;
    letter-spacing: 0.2em;
    color: #6b5a3a;
    text-align: center;
  }

  @media (max-width: 820px) {
    .gf__hero {
      grid-template-columns: 1fr;
      align-items: start;
    }
    .gf__thesis,
    .gf__actions {
      grid-column: 1;
      grid-row: auto;
    }
    .gf__actions {
      justify-content: flex-start;
    }
    .gf__room-frame {
      min-height: 760px;
    }
  }
</style>
