---
/**
 * pointcast.xyz v2 — home grid.
 *
 * Every content surface is a Block (per BLOCKS.md). The home page is a
 * dense auto-fit grid of BlockCards, most recent first. Size variants
 * are honored on desktop; mobile collapses to a single column with a
 * sticky channel chip bar at top.
 */
import BlockLayout from '../layouts/BlockLayout.astro';
import BlockCard from '../components/BlockCard.astro';
import BlockReorder from '../components/BlockReorder.astro';
import PresenceBar from '../components/PresenceBar.astro';
import WalletChip from '../components/WalletChip.astro';
import HomeMajors from '../components/HomeMajors.astro';
import FreshDeck from '../components/FreshDeck.astro';
import PollsOnHome from '../components/PollsOnHome.astro';
import MoodChip from '../components/MoodChip.astro';
import FreshStrip from '../components/FreshStrip.astro';
import VisitorHereStrip from '../components/VisitorHereStrip.astro';
import NetworkStrip from '../components/NetworkStrip.astro';
import TodayOnPointCast from '../components/TodayOnPointCast.astro';
import DailyDropStrip from '../components/DailyDropStrip.astro';
import SportsStrip from '../components/SportsStrip.astro';
import MorningPara from '../components/MorningPara.astro';
import NativePlantingYield from '../components/NativePlantingYield.astro';
import AgentLedger from '../components/AgentLedger.astro';
import HomeFireplace from '../components/HomeFireplace.astro';
import AppLaunchStrip from '../components/AppLaunchStrip.astro';
import BuddhaHeadRotator from '../components/BuddhaHeadRotator.astro';
import PointCastPlayStrip from '../components/PointCastPlayStrip.astro';
import ZenCatTodayCard from '../components/ZenCatTodayCard.astro';
import WalletShelfModule from '../components/WalletShelfModule.astro';
import NounsPortraitStrip from '../components/NounsPortraitStrip.astro';
import DrumCrewStrip from '../components/DrumCrewStrip.astro';
import FeedbackBlock from '../components/FeedbackBlock.astro';

// Removed 2026-04-19 21:55 PT per Mike: "six things isn't working,
// morning brief isn't working, novice voter isn't working, remove
// for now". Old MorningBrief, TodayStrip, VoterStats components still
// on disk; not imported. Can return when a new shape is agreed.
import { CHANNEL_LIST } from '../lib/channels';
import { getCollection } from 'astro:content';

const HELLO_MIKE_BLOCK_ID = '0334';
const HOUSEPLANTS_FIRST_BLOCK_ID = '0333';

const allBlocks = (await getCollection('blocks', ({ data }) => !data.draft))
  .sort((a, b) => b.data.timestamp.getTime() - a.data.timestamp.getTime());

const firstBlock = allBlocks.find((block) => block.data.id === HOUSEPLANTS_FIRST_BLOCK_ID) ?? allBlocks[0] ?? null;
const helloMikeCandidate = allBlocks.find((block) => block.data.id === HELLO_MIKE_BLOCK_ID) ?? null;
const helloMikeBlock = helloMikeCandidate?.data.id !== firstBlock?.data.id ? helloMikeCandidate : null;
const pinnedBlockIds = new Set([firstBlock?.data.id, helloMikeBlock?.data.id].filter(Boolean));
const blocks = allBlocks.filter((block) => !pinnedBlockIds.has(block.data.id));
const blockCount = allBlocks.length;
const homeDescription = 'PointCast is an agent-native broadcast from El Segundo: live posts, internships, participation memory, Nouns experiments, and machine-readable feeds in one place.';

const participationCards = [
  {
    href: '/internships/',
    label: 'Internships 2026',
    title: 'Build like you will remember this moment.',
    note: 'A structured, warm program for people who want to gather information, make artifacts, leave receipts, help others, and see their work matter.',
  },
  {
    href: '/internships/#participation-moment',
    label: 'Participation memory',
    title: 'Genius is something you practice.',
    note: 'Weekly memory notes turn the work into a record: what changed, who it helped, and what the next person can use.',
  },
  {
    href: '/internships/#nouns-interest',
    label: 'Nouns interest',
    title: 'Configure, stamp, message.',
    note: 'Pick a Nouns path, stamp a local receipt, gather source notes, and ping PointCast for review.',
  },
  {
    href: '/internships/roles.json',
    label: 'Machine readable',
    title: 'Roles and pathways as data.',
    note: 'Agents can parse roles for Information Gatherer, Participation Memory Keeper, Nouns configurator, rewards, grants, rooms, and more.',
  },
];

const todayPostIdeas = [
  {
    channel: 'Internship',
    title: 'The Week You Started Becoming Useful',
    brief: 'A post about participation as a first real receipt: one question, one source, one action, one memory note.',
    action: 'Draft from /internships/#participation-moment',
  },
  {
    channel: 'Nouns',
    title: 'Nouns Interest Configurator Field Log',
    brief: 'Walk through Visit Nouns, cards, portraits, cola, and the stamp path as a practical onboarding map.',
    action: 'Gather links and send a reviewed Nouns brief',
  },
  {
    channel: 'Research',
    title: 'Information Gatherer: Five Sources Before One Claim',
    brief: 'Make the research lane concrete: credible sources, uncertainty notes, privacy check, next action.',
    action: 'Use the information-gatherer runner output',
  },
  {
    channel: 'Rewards',
    title: 'Proof Of Help Beats Proof Of Hype',
    brief: 'Explain how task rewards can honor useful contributions without pretending collectibles are investment products.',
    action: 'Link /rewards and /internships/program.json',
  },
  {
    channel: 'Mindfulness',
    title: 'The Five-Minute Reset Before You Build',
    brief: 'A simple breath, gratitude, and contentment ritual for interns and builders between tasks.',
    action: 'Pair with /meditate',
  },
  {
    channel: 'Local',
    title: 'El Segundo Field Notes For Internet People',
    brief: 'A local-signal post: one place, one observation, one photo/screenshot receipt, one useful map note.',
    action: 'Route to Local Signal Scout',
  },
  {
    channel: 'Grants',
    title: 'Grant Quest: What PointCast Can Ask For Today',
    brief: 'A fresh shortlist of funder angles: public goods, AI media literacy, Nouns, Tezos, learning pathways.',
    action: 'Update /grants/applications.json',
  },
  {
    channel: 'Audio',
    title: 'Road-Song Energy For Builders',
    brief: 'A soundtrack-style post: why the day needs punch, motion, and confidence without losing care.',
    action: 'Open with the user-shared Spotify reference',
  },
];

const studioSignals = [
  {
    href: '/now',
    label: 'El Segundo',
    title: 'Pacific air, useful links.',
    note: 'Local signal, light touch, practical updates.',
  },
  {
    href: '/internships/',
    label: 'Learn',
    title: 'Small useful reps.',
    note: 'Gather a source, make a thing, leave a receipt.',
  },
  {
    href: '/grants/',
    label: 'Grants',
    title: 'Quiet proof.',
    note: 'Show the work, ask clearly, keep moving.',
  },
  {
    href: '/ping/',
    label: 'Pickleball',
    title: 'Send the next serve.',
    note: 'One clean note, one friendly follow-through.',
  },
];

// JSON-LD — make the home a CollectionPage so agents can parse the list.
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'CollectionPage',
  '@id': 'https://pointcast.xyz/#home',
  name: 'PointCast Gamgee front door',
  description: homeDescription,
  url: 'https://pointcast.xyz/',
  inLanguage: 'en-US',
  hasPart: allBlocks.slice(0, 20).map((b) => ({
    '@type': 'CreativeWork',
    '@id': `https://pointcast.xyz/b/${b.data.id}`,
    name: b.data.title,
    datePublished: b.data.timestamp.toISOString(),
    url: `https://pointcast.xyz/b/${b.data.id}`,
  })),
};

const alternates = [
  { type: 'application/json', href: '/blocks.json', title: 'All blocks (JSON)' },
  { type: 'application/rss+xml', href: '/feed.xml', title: 'PointCast (RSS · all blocks)' },
  { type: 'application/feed+json', href: '/feed.json', title: 'PointCast (JSON Feed 1.1)' },
  ...CHANNEL_LIST.map((c) => ({
    type: 'application/rss+xml',
    href: `/c/${c.slug}.rss`,
    title: `PointCast · ${c.name} (RSS)`,
  })),
];

const contents = [
  { href: '#release', label: 'Release', note: 'Gamgee RC0 and agent paths' },
  { href: '#studio-signal', label: 'El Segundo', note: 'Ocean air and light work' },
  { href: '#participation', label: 'Participate', note: 'Internships, memory, Nouns' },
  { href: '#post-ideas', label: 'Post Ideas', note: 'Fresh seeds for today' },
  { href: '#pattern', label: 'Pattern', note: 'Agent-native publishing' },
  { href: '#featured', label: 'Featured', note: 'Pinned blocks' },
  { href: '#fresh', label: 'Fresh', note: 'New, mood, and presence' },
  { href: '#nouns-portraits', label: 'Portraits', note: 'Five Tezos-mintable Nouns' },
  { href: '#network', label: 'Network', note: 'People, apps, and activity' },
  { href: '#play', label: 'Play', note: 'Passport, quests, walk, radio' },
  { href: '#today', label: 'Today', note: 'Highlights, drop, sports' },
  { href: '#interact', label: 'Interact', note: 'Polls, random deck, channels' },
  { href: '#feedback', label: 'Feedback', note: 'Share what should change' },
  { href: '#majors', label: 'Majors', note: 'Drum, Cast, Meditate' },
  { href: '#blocks', label: 'Blocks', note: 'Full public grid' },
  { href: '#endpoints', label: 'Endpoints', note: 'Site map and feeds' },
];
---

<BlockLayout
  title="Home"
  description={homeDescription}
  jsonLd={jsonLd}
  alternates={alternates}
  frame={{
    image: 'https://pointcast.xyz/images/og/og-home-v2.png',
    buttons: [
      { label: 'Open /internships', action: 'link', target: 'https://pointcast.xyz/internships' },
      { label: 'Open /gamgee', action: 'link', target: 'https://pointcast.xyz/gamgee' },
      { label: 'Open /now', action: 'link', target: 'https://pointcast.xyz/now' },
      { label: '/for-agents', action: 'link', target: 'https://pointcast.xyz/for-agents' },
      { label: '/agents.json', action: 'link', target: 'https://pointcast.xyz/agents.json' },
    ],
  }}
>
  <div class="page">

    <h1 class="sr-only">PointCast Gamgee - an agent-native broadcast from El Segundo with human and machine-readable paths.</h1>

    <!-- Masthead — one line. Wordmark, block count, current time, agent link. -->
    <header class="masthead">
      <a href="/" class="wordmark" aria-label="PointCast home">POINTCAST</a>
      <span class="sep" aria-hidden="true">·</span>
      <span class="meta">{blockCount} BLOCKS</span>
      <span class="sep" aria-hidden="true">·</span>
      <time class="meta time" id="pc-clock">— : —</time>
      <PresenceBar />
      <span class="spacer" aria-hidden="true"></span>
      <WalletChip />
      <a href="/for-agents" class="agent-link">/for-agents →</a>
    </header>

    <section id="studio-signal" class="studio-signal" aria-labelledby="studio-signal-title">
      <div class="studio-signal__line" aria-hidden="true">
        <span></span>
        <span></span>
        <span></span>
      </div>
      <div class="studio-signal__inner">
        <div class="studio-signal__copy">
          <p class="studio-signal__kicker">El Segundo / Ocean / Pickleball</p>
          <h2 id="studio-signal-title" class="studio-signal__title">PointCast, lightly.</h2>
          <p class="studio-signal__dek">
            Ocean air, useful links, small posts, a little pickleball. Build the thing, then go outside for a minute.
          </p>
        </div>
        <nav class="studio-signal__links" aria-label="Light PointCast paths">
          {studioSignals.map((signal) => (
            <a href={signal.href} class="studio-signal__link">
              <span>{signal.label}</span>
              <strong>{signal.title}</strong>
              <em>{signal.note}</em>
            </a>
          ))}
        </nav>
      </div>
    </section>

    <nav class="contents" aria-labelledby="contents-title">
      <div class="contents__header">
        <p class="contents__eyebrow">Table of Contents</p>
        <h2 id="contents-title" class="contents__title">What is on PointCast</h2>
      </div>
      <ol class="contents__list">
        {contents.map((item, index) => (
          <li class="contents__item">
            <a href={item.href} class="contents__link">
              <span class="contents__number">{String(index + 1).padStart(2, '0')}</span>
              <span class="contents__copy">
                <span class="contents__label">{item.label}</span>
                <span class="contents__note">{item.note}</span>
              </span>
            </a>
          </li>
        ))}
      </ol>
      <div class="contents__routes" aria-label="Common PointCast routes">
        <a href="/now">/now</a>
        <a href="/for-agents">/for-agents</a>
        <a href="/search">/search</a>
        <a href="/archive">/archive</a>
        <a href="/blocks.json">/blocks.json</a>
      </div>
    </nav>

    <HomeFireplace />

    <section id="release" class="gamgee-front-door" aria-labelledby="gamgee-front-door-title">
      <div class="gamgee-front-door__copy">
        <p class="gamgee-front-door__kicker">Fresh home signal</p>
        <h2 id="gamgee-front-door-title" class="gamgee-front-door__title">PointCast is live work, posted while the day is still moving.</h2>
        <p class="gamgee-front-door__dek">Today the front door points at participation: internships, memory notes, Nouns interest, information gathering, rewards, grants, rooms, and the confidence to make something useful enough to remember.</p>
        <ul class="gamgee-front-door__signals" aria-label="Gamgee release signals">
          <li>{blockCount} blocks</li>
          <li>Claude + Codex + Manus</li>
          <li>Build it, stamp it, remember it</li>
        </ul>
      </div>
      <nav class="gamgee-front-door__paths" aria-label="Gamgee entry points">
        <a href="/internships/" class="gamgee-front-door__path">
          <span class="gamgee-front-door__label">For interns</span>
          <span class="gamgee-front-door__target">/internships</span>
          <span class="gamgee-front-door__note">Build like you will remember this moment.</span>
        </a>
        <a href="/internships/#nouns-interest" class="gamgee-front-door__path">
          <span class="gamgee-front-door__label">For Nouns</span>
          <span class="gamgee-front-door__target">/internships/#nouns-interest</span>
          <span class="gamgee-front-door__note">Configure, stamp, message PointCast.</span>
        </a>
        <a href="/now" class="gamgee-front-door__path">
          <span class="gamgee-front-door__label">For humans</span>
          <span class="gamgee-front-door__target">/now</span>
          <span class="gamgee-front-door__note">See what is live today.</span>
        </a>
        <a href="/rooms" class="gamgee-front-door__path">
          <span class="gamgee-front-door__label">For rooms</span>
          <span class="gamgee-front-door__target">/rooms</span>
          <span class="gamgee-front-door__note">Bath · Drum · Anytime — all rooms.</span>
        </a>
        <a href="/marketplace" class="gamgee-front-door__path">
          <span class="gamgee-front-door__label">For collectors</span>
          <span class="gamgee-front-door__target">/marketplace</span>
          <span class="gamgee-front-door__note">Buy and list PointCast NFTs on Tezos.</span>
        </a>
        <a href="/profile" class="gamgee-front-door__path">
          <span class="gamgee-front-door__label">For you</span>
          <span class="gamgee-front-door__target">/profile</span>
          <span class="gamgee-front-door__note">Your collection, mood, rooms visited.</span>
        </a>
        <a href="/for-agents" class="gamgee-front-door__path">
          <span class="gamgee-front-door__label">For agents</span>
          <span class="gamgee-front-door__target">/for-agents</span>
          <span class="gamgee-front-door__note">Read the operating surface.</span>
        </a>
        <a href="/agents.json" class="gamgee-front-door__path">
          <span class="gamgee-front-door__label">For tools</span>
          <span class="gamgee-front-door__target">/agents.json</span>
          <span class="gamgee-front-door__note">Parse the canonical manifest.</span>
        </a>
      </nav>
      <AgentLedger />
    </section>

    <section id="participation" class="participation-home" aria-labelledby="participation-home-title">
      <div class="participation-home__head">
        <p class="participation-home__kicker">Participation engine</p>
        <h2 id="participation-home-title" class="participation-home__title">Do the work you will want to remember.</h2>
        <p class="participation-home__dek">
          The new PointCast path is simple: gather the information, make the useful thing, leave the receipt, help the next person, and write down the moment you became sharper.
        </p>
      </div>
      <div class="participation-home__grid">
        {participationCards.map((card) => (
          <a href={card.href} class="participation-home__card">
            <span class="participation-home__label">{card.label}</span>
            <strong>{card.title}</strong>
            <span>{card.note}</span>
          </a>
        ))}
      </div>
      <div class="participation-home__actions" aria-label="Participation actions">
        <a href="/internships/program.json">program.json</a>
        <a href="/internships/roles.json">roles.json</a>
        <a href="/rewards/">task rewards</a>
        <a href="/grants/">grant quest</a>
        <a href="/ping/">ping to participate</a>
      </div>
    </section>

    <section id="post-ideas" class="today-ideas" aria-labelledby="today-ideas-title">
      <div class="today-ideas__head">
        <p class="today-ideas__kicker">Today fresh post ideas</p>
        <h2 id="today-ideas-title" class="today-ideas__title">Eight sparks with road-song confidence.</h2>
        <p class="today-ideas__dek">
          Quick post seeds for the next PointCast run: lively, useful, source-backed, and ready to become blocks, pings, or grant/reward artifacts.
        </p>
      </div>
      <div class="today-ideas__rail">
        {todayPostIdeas.map((idea, index) => (
          <article class="today-ideas__card">
            <span class="today-ideas__number">{String(index + 1).padStart(2, '0')}</span>
            <span class="today-ideas__channel">{idea.channel}</span>
            <h3>{idea.title}</h3>
            <p>{idea.brief}</p>
            <strong>{idea.action}</strong>
          </article>
        ))}
      </div>
    </section>

    <section id="pattern" class="agent-native-brief" aria-labelledby="agent-native-brief-title">
      <div class="agent-native-brief__copy">
        <p class="agent-native-brief__kicker">Agent-native publishing · new explainer</p>
        <h2 id="agent-native-brief-title" class="agent-native-brief__title">A website for people and agents at the same time.</h2>
        <p class="agent-native-brief__dek">
          The new pattern page turns PointCast into a reference implementation:
          canonical human pages, JSON mirrors, discovery manifests, feeds,
          sitemaps, LLM context, and citation-ready permalinks.
        </p>
        <div class="agent-native-brief__nouns" aria-label="Nouns signal markers">
          {[313, 523, 742, 1042].map((nounId) => (
            <a href={`https://noun.pics/${nounId}.svg`} target="_blank" rel="noopener" class="agent-native-brief__noun">
              <img src={`https://noun.pics/${nounId}.svg`} alt={`Noun ${nounId}`} width="44" height="44" loading="lazy" />
              <span>N{nounId}</span>
            </a>
          ))}
        </div>
      </div>
      <div class="agent-native-brief__matrix" aria-label="Agent-native publishing surfaces">
        <a href="/agent-native-publishing" class="agent-native-brief__cell agent-native-brief__cell--primary">
          <span class="agent-native-brief__label">Pattern</span>
          <strong>/agent-native-publishing</strong>
          <span>Six rules plus the reusable brief.</span>
        </a>
        <a href="/agents.json" class="agent-native-brief__cell">
          <span class="agent-native-brief__label">Manifest</span>
          <strong>/agents.json</strong>
          <span>One request maps the surface.</span>
        </a>
        <a href="/llms.txt" class="agent-native-brief__cell">
          <span class="agent-native-brief__label">Context</span>
          <strong>/llms.txt</strong>
          <span>Short answer and retrieval order.</span>
        </a>
        <a href="/BLOCKS.md" class="agent-native-brief__cell">
          <span class="agent-native-brief__label">Schema</span>
          <strong>/BLOCKS.md</strong>
          <span>Stable Block primitive.</span>
        </a>
      </div>
      <ul class="agent-native-brief__signals" aria-label="Agent-native publishing checklist">
        <li>human HTML</li>
        <li>JSON mirrors</li>
        <li>stable IDs</li>
        <li>RSS + JSON Feed</li>
        <li>LLM context</li>
        <li>citation contract</li>
      </ul>
    </section>

    {helloMikeBlock && (
      <section id="featured" class="hello-module" aria-label="Hello Mike module">
        <BlockCard block={helloMikeBlock} />
      </section>
    )}

    {firstBlock && (
      <section id={helloMikeBlock ? undefined : 'featured'} class="first-block" aria-label="Featured block on PointCast">
        <BlockCard block={firstBlock} />
      </section>
    )}

    <div id="fresh" class="section-anchor" aria-hidden="true"></div>

    <!-- Fresh strip — top-of-page freshness signal + one-tap action.
         Per Mike 2026-04-19 morning feedback "top not fresh enough and
         nothing to do". Shows HELLO / N NEW / CAUGHT UP badges based on
         localStorage lastVisit; CTA routes to newest or random older. -->
    <FreshStrip />

    <!-- MoodChip — moved to top per Mike 2026-04-20 13:55 PT: "yah let
         move mood up, and then yah make the selection of the mood change
         the mood, that animation, and yah the ability to play music,
         neater ui and then you are rolling thru the site in that mood."
         Setting your mood first means every page that follows renders
         with the tint + the soundtrack follows you. -->
    <MoodChip />

    <!-- Visitor "peoples here" strip — represents YOU as a noun avatar +
         ghost slots that light up when other visitors connect. Per Mike
         2026-04-19 20:30 PT. -->
    <VisitorHereStrip />

    <NounsPortraitStrip context="home" />

    <!-- Drum Crew — six mintable Nouns, one per drum room. Per Mike
         2026-04-27: "have a look at the homepage, try to add some
         nouns blocks for collecting." Sits right under
         NounsPortraitStrip so the two collecting modules share the
         same visual neighborhood. Same Visit Nouns FA2, different
         seeds, themed to the nine drum surfaces + /api/mcp. -->
    <DrumCrewStrip context="home" />

    <div id="network" class="section-anchor" aria-hidden="true"></div>

    <!-- Network strip — surfaces /here (live congregation), /for-nodes
         (become a node), /workbench (who's building what). Per Mike
         2026-04-20 11:15 PT: "look to refresh homepage, include new
         blocks, approach, etc · i'm pushing claude, codex and soon manus
         to start interacting, let me know if there is a page we can
         track that activity". -->
    <NetworkStrip />

    <AppLaunchStrip />

    <WalletShelfModule />

    <div id="play" class="section-anchor" aria-hidden="true"></div>

    <ZenCatTodayCard />

    <PointCastPlayStrip />

    <div id="today" class="section-anchor" aria-hidden="true"></div>

    <!-- Today on PointCast — hand-curated day-specific highlights.
         Per Mike 2026-04-20 12:20 PT: "on homepage, it should likely
         be all the interesting things we were talking about today from
         clock to bitcoin, to 420, to feature updates, to things to try,
         to learnings, to how to learn, to entertainment, kinda thing,
         and very monday stuffs as, today is monday." -->
    <TodayOnPointCast />

    <!-- Daily drop collect inline. Per Mike 2026-04-20 13:20 PT:
         "have on homepage, ability to collect daily drop." Shared
         localStorage with /today so claim here, claim there, same count. -->
    <DailyDropStrip />

    <!-- Sports — recent global-major results. Per Mike 2026-04-20
         13:20 PT: "try a couple of sports updates of the latest results
         from last night or today, start with global major." Client-side
         fetches ESPN scoreboards, 10-min cache. -->
    <SportsStrip />

    <div id="interact" class="section-anchor" aria-hidden="true"></div>

    {/* MoodChip moved above VisitorHereStrip per Mike 2026-04-20 13:55 PT. */}

    {/* MorningPara removed 2026-04-20 13:00 PT per Mike: "remove brief
         from homepage." TodayOnPointCast is the current editorial-strip
         surface; the morning paragraph was redundant. Component file
         stays on disk. */}

    <!-- Live polls strip — pool now includes 4 fresh polls (codex-project
         bet, sunday-ES, april-register, sunday-soundtrack) on top of the
         existing 12. Per Mike 2026-04-19 21:55 PT "more fresh polls". -->
    <PollsOnHome />

    <!-- Fresh deck — 3 random blocks cloned client-side from the grid below
         so every page-view gets a different top. Per Mike 2026-04-18 block
         0272 + /sprint pick. -->
    <FreshDeck />

    <BuddhaHeadRotator context="home" />

    <FeedbackBlock />

    <!-- Channel legend — sticky-horizontal on mobile, flex-wrap on desktop. -->
    <nav class="channels" aria-label="Channels">
      {CHANNEL_LIST.map((ch) => (
        <a class="chip" href={`/c/${ch.slug}`} style={`--ch-600: ${ch.color600}; --ch-800: ${ch.color800}; --ch-50: ${ch.color50};`}>
          <span class="chip__dot" aria-hidden="true"></span>
          <span class="chip__code">CH.{ch.code}</span>
          <span class="chip__name">{ch.name}</span>
        </a>
      ))}
    </nav>

    <!-- Majors — inline modules for /drum + /cast + /meditate.
         Mike 2026-04-17: "make sure the majors, like drum or pool
         together, have some type of module on the home page prior to
         click" + "likely have ability to drum get started". -->
    <div id="majors" class="section-anchor" aria-hidden="true"></div>

    <HomeMajors />

    <NativePlantingYield />

    <!-- The grid — every block. Drag-and-drop reordering is client-side +
         localStorage-only; no server state. -->
    <BlockReorder>
      <section id="blocks" class="grid" role="main">
        {blocks.map((block) => <BlockCard block={block} />)}
      </section>
    </BlockReorder>

    <!-- Footer — endpoint list. Agent-oriented, no social nav. -->
    <footer id="endpoints" class="endpoints">
      <p class="endpoints__label">ENDPOINTS</p>
      <ul class="endpoints__list">
        <li><a href="/manifesto">/manifesto</a></li>
        <li><a href="/dao">/dao</a></li>
        <li><a href="/yield">/yield</a></li>
        <li><a href="/nouns-cola">/nouns-cola</a></li>
        <li><a href="/nouns-cola-crush">/nouns-cola-crush</a></li>
        <li><a href="/nouns-cards">/nouns-cards</a></li>
        <li><a href="/nouns-cards-v2">/nouns-cards-v2</a></li>
        <li><a href="/nouns-cards-v3">/nouns-cards-v3</a></li>
        <li><a href="/nouns-portraits">/nouns-portraits</a></li>
        <li><a href="/internships">/internships</a></li>
        <li><a href="/internships/program.json">/internships/program.json</a></li>
        <li><a href="/internships/roles.json">/internships/roles.json</a></li>
        <li><a href="/rewards">/rewards</a></li>
        <li><a href="/grants">/grants</a></li>
        <li><a href="/linktree">/linktree</a></li>
        <li><a href="/publish">/publish</a></li>
        <li><a href="/beacon">/beacon</a></li>
        <li><a href="/nature">/nature</a></li>
        <li><a href="/garden-yield">/garden-yield</a></li>
        <li><a href="/houseplants">/houseplants</a></li>
        <li><a href="/nature-yield.json">/nature-yield.json</a></li>
        <li><a href="/gamgee">/gamgee</a></li>
        <li><a href="/meditate">/meditate</a></li>
        <li><a href="/protocol">/protocol</a></li>
        <li><a href="/agent-native-publishing">/agent-native-publishing</a></li>
        <li><a href="/glossary">/glossary</a></li>
        <li><a href="/now">/now</a></li>
        <li><a href="/search">/search</a></li>
        <li><a href="/archive">/archive</a></li>
        <li><a href="/timeline">/timeline</a></li>
        <li><a href="/editions">/editions</a></li>
        <li><a href="/subscribe">/subscribe</a></li>
        <li><a href="/changelog">/changelog</a></li>
        <li><a href="/stack">/stack</a></li>
        <li><a href="/ai-stack">/ai-stack</a></li>
        <li><a href="/yee">/yee</a></li>
        <li><a href="/mesh">/mesh</a></li>
        <li><a href="/collabs">/collabs</a></li>
        <li><a href="/ping">/ping</a></li>
        <li><a href="/sprint">/sprint</a></li>
        <li><a href="/sprints">/sprints</a></li>
        <li><a href="/drop">/drop</a></li>
        <li><a href="/products">/products</a></li>
        <li><a href="/polls">/polls</a></li>
        <li><a href="/briefs">/briefs</a></li>
        <li><a href="/gallery">/gallery</a></li>
        <li><a href="/moods">/moods</a></li>
        <li><a href="/local">/local</a></li>
        <li><a href="/play">/play</a></li>
        <li><a href="/play.json">/play.json</a></li>
        <li><a href="/passport">/passport</a></li>
        <li><a href="/quests">/quests</a></li>
        <li><a href="/walk">/walk</a></li>
        <li><a href="/room-weather">/room-weather</a></li>
        <li><a href="/radio">/radio</a></li>
        <li><a href="/routes">/routes</a></li>
        <li><a href="/builders">/builders</a></li>
        <li><a href="/civic">/civic</a></li>
        <li><a href="/pet">/pet</a></li>
        <li><a href="/zen-cats">/zen-cats</a></li>
        <li><a href="/zen-cats.json">/zen-cats.json</a></li>
        <li><a href="/today">/today ★</a></li>
        <li><a href="/tv">/tv ↗</a></li>
        <li><a href="/eth-legacy">/eth-legacy</a></li>
        <li><a href="https://shop.getgoodfeels.com" target="_blank" rel="noopener">shop ↗</a></li>
        <li><a href="/agents.json">/agents.json</a></li>
        <li><a href="/blocks.json">/blocks.json</a></li>
        <li><a href="/for-agents">/for-agents</a></li>
        <li><a href="/agent-derby">/agent-derby</a></li>
        <li><a href="/agent-derby.json">/agent-derby.json</a></li>
        <li><a href="/battle">/battle</a></li>
        <li><a href="/cast">/cast</a></li>
        <li><a href="/drum">/drum</a></li>
        <li><a href="/status">/status</a></li>
        <li><a href="/collection">/collection</a></li>
        <li><a href="/profile">/profile</a></li>
        <li><a href="/collect">/collect</a></li>
        <li><a href="/about">/about</a></li>
      </ul>
    </footer>

  </div>
</BlockLayout>

<script is:inline>
  (function() {
    const el = document.getElementById('pc-clock');
    if (!el) return;
    const fmt = new Intl.DateTimeFormat('en-US', {
      hour: '2-digit', minute: '2-digit', hour12: false, timeZone: 'America/Los_Angeles'
    });
    function tick() {
      const parts = fmt.formatToParts(new Date());
      const h = parts.find(p => p.type === 'hour')?.value ?? '—';
      const m = parts.find(p => p.type === 'minute')?.value ?? '—';
      el.textContent = `${h}:${m} PT`;
    }
    tick();
    setInterval(tick, 10_000);
  })();
</script>

<style>
  .page {
    max-width: 1480px;
    margin: 0 auto;
    padding: 16px 20px 80px;
    background:
      radial-gradient(circle at 10% 0%, rgba(255, 201, 128, 0.18), transparent 220px),
      radial-gradient(circle at 84% 2%, rgba(138, 46, 98, 0.08), transparent 260px),
      linear-gradient(180deg, rgba(255, 248, 225, 0.4), transparent 420px);
  }

  /* Zone dividers — thin mono kickers marking the verb-flow between
     groups of components on the home. Per Mike 2026-04-19 21:35 PT
     "visit → update my stuffs → daily content → interaction → scroll". */
  .zone-divider {
    display: flex;
    align-items: center;
    gap: 10px;
    margin: 14px 0 6px;
    padding: 0;
    font-size: 9px;
    letter-spacing: 0.24em;
    color: var(--pc-ink-soft);
    font-weight: 600;
    text-transform: uppercase;
  }
  .zone-divider__line {
    flex: 1;
    height: 1px;
    background: linear-gradient(to right, transparent, var(--pc-rule, #d6d4cd), transparent);
  }
  .zone-divider__label {
    padding: 0 4px;
    white-space: nowrap;
  }
  @media (max-width: 620px) {
    .zone-divider { gap: 6px; font-size: 8.5px; letter-spacing: 0.2em; }
    .zone-divider__label { padding: 0 2px; }
  }

  /* Visually-hidden h1 — semantic document outline for a11y + SEO without
     disrupting the masthead-as-title visual design. Technique: Bootstrap
     sr-only pattern. Screen readers announce, sighted users don't see it. */
  .sr-only {
    position: absolute;
    width: 1px; height: 1px;
    padding: 0; margin: -1px;
    overflow: hidden;
    clip: rect(0, 0, 0, 0);
    white-space: nowrap;
    border: 0;
  }

  .masthead {
    position: relative;
    display: flex;
    align-items: baseline;
    gap: 10px;
    margin-bottom: 12px;
    padding: 12px 14px;
    border: 1px solid rgba(196, 149, 46, 0.24);
    border-bottom-color: rgba(106, 36, 78, 0.18);
    background:
      linear-gradient(90deg, rgba(255, 244, 203, 0.72), rgba(255, 255, 255, 0.5) 45%, rgba(248, 223, 228, 0.36)),
      #fffdf7;
    box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.72), 0 12px 34px rgba(61, 39, 18, 0.05);
    font-family: var(--pc-font-mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .masthead::before {
    content: '';
    position: absolute;
    left: 14px;
    right: 14px;
    bottom: -2px;
    height: 2px;
    background: linear-gradient(90deg, #e4a84b, rgba(138, 46, 98, 0.5), rgba(78, 126, 169, 0.4));
    opacity: 0.62;
  }

  .wordmark {
    font-family: var(--pc-font-mono);
    font-size: 22px;
    font-weight: 500;
    letter-spacing: 0.22em;
    color: #1f160c;
    text-shadow: 0 1px 0 rgba(255, 244, 203, 0.7);
  }
  .wordmark:hover { color: #8a2e62; text-decoration: none; }

  .meta { color: #6d6257; font-weight: 400; }
  .time { min-width: 60px; }
  .sep { color: rgba(138, 46, 98, 0.22); }
  .spacer { flex: 1; }

  .agent-link { color: #2f5f9f; font-weight: 600; }

  .section-anchor {
    scroll-margin-top: 72px;
  }

  @media (max-width: 767px) {
    .masthead {
      flex-wrap: wrap;
      align-items: center;
      gap: 8px 10px;
    }

    .masthead .spacer {
      display: none;
    }

    .agent-link {
      margin-left: auto;
    }
  }

  .contents {
    display: grid;
    grid-template-columns: minmax(180px, 0.28fr) minmax(0, 1fr);
    gap: 14px 20px;
    margin: 12px 0 16px;
    padding: 14px;
    border: 1px solid rgba(196, 149, 46, 0.2);
    background:
      linear-gradient(90deg, rgba(255, 248, 225, 0.58), transparent 38%),
      rgba(255, 253, 247, 0.72);
    font-family: var(--pc-font-mono);
  }

  .contents__header {
    min-width: 0;
  }

  .contents__eyebrow {
    margin: 0 0 6px;
    color: #8a2e62;
    font-size: 9px;
    font-weight: 600;
    letter-spacing: 0.16em;
    text-transform: uppercase;
  }

  .contents__title {
    margin: 0;
    color: #1f160c;
    font-size: 18px;
    font-weight: 500;
    letter-spacing: 0.04em;
    text-transform: uppercase;
  }

  .contents__list {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 6px;
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .contents__item {
    min-width: 0;
  }

  .contents__link {
    display: grid;
    grid-template-columns: 28px minmax(0, 1fr);
    gap: 8px;
    min-height: 58px;
    padding: 8px;
    border: 1px solid rgba(196, 149, 46, 0.2);
    background:
      linear-gradient(135deg, rgba(255, 255, 255, 0.64), rgba(255, 244, 203, 0.2)),
      #faf8f0;
    color: var(--pc-ink);
    transition:
      background-color 900ms ease,
      border-color 900ms ease,
      transform 1200ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .contents__link:hover {
    border-color: rgba(138, 46, 98, 0.28);
    background:
      linear-gradient(135deg, rgba(248, 223, 228, 0.42), rgba(255, 244, 203, 0.36)),
      #fffdf7;
    transform: translateY(-1px);
    text-decoration: none;
  }

  .contents__number {
    color: rgba(138, 46, 98, 0.52);
    font-size: 10px;
    letter-spacing: 0.08em;
  }

  .contents__copy {
    display: grid;
    gap: 3px;
    min-width: 0;
  }

  .contents__label {
    overflow-wrap: anywhere;
    color: #1f160c;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .contents__note {
    overflow-wrap: anywhere;
    color: #6d6257;
    font-family: var(--pc-font-sans);
    font-size: 12px;
    line-height: 1.3;
  }

  .contents__routes {
    grid-column: 2;
    display: flex;
    flex-wrap: wrap;
    gap: 8px 12px;
    min-width: 0;
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .contents__routes a {
    color: #2f5f9f;
  }

  @media (max-width: 980px) {
    .contents {
      grid-template-columns: 1fr;
    }

    .contents__list {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    .contents__routes {
      grid-column: 1;
    }
  }

  @media (max-width: 620px) {
    .contents {
      gap: 12px;
    }

    .contents__list {
      grid-template-columns: 1fr;
    }

    .contents__link {
      min-height: 0;
    }
  }

  .gamgee-front-door {
    display: grid;
    grid-template-columns: minmax(0, 1.35fr) minmax(320px, 0.65fr);
    gap: 24px;
    margin: 14px 0 18px;
    padding: 18px 0;
    border-bottom: 1.5px solid var(--pc-ink);
    font-family: var(--pc-font-sans);
  }

  .gamgee-front-door__copy {
    min-width: 0;
  }

  .gamgee-front-door__kicker {
    margin: 0 0 8px;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--pc-accent);
  }

  .gamgee-front-door__title {
    max-width: 820px;
    margin: 0;
    font-size: 40px;
    line-height: 0.98;
    letter-spacing: 0;
    font-weight: 500;
    color: var(--pc-ink);
  }

  .gamgee-front-door__dek {
    max-width: 760px;
    margin: 12px 0 0;
    font-size: 15px;
    line-height: 1.5;
    color: var(--pc-ink-soft);
  }

  .gamgee-front-door__signals {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    list-style: none;
    padding: 0;
    margin: 14px 0 0;
  }

  .gamgee-front-door__signals li {
    border: 1px solid var(--pc-rule);
    padding: 5px 8px;
    background: #fafaf7;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--pc-ink-soft);
  }

  .gamgee-front-door__paths {
    display: grid;
    gap: 8px;
    align-content: start;
    min-width: 0;
  }

  .gamgee-front-door__path {
    display: grid;
    grid-template-columns: minmax(82px, auto) minmax(0, 1fr);
    gap: 3px 12px;
    padding: 10px 0 10px 12px;
    border-left: 3px solid var(--pc-accent);
    background: #f4f8fb;
    color: var(--pc-ink);
    min-width: 0;
  }

  .gamgee-front-door__path:hover {
    background: #eef4fa;
    text-decoration: none;
  }

  .gamgee-front-door__label,
  .gamgee-front-door__target {
    font-family: var(--pc-font-mono);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .gamgee-front-door__label {
    color: var(--pc-ink-muted);
  }

  .gamgee-front-door__target {
    min-width: 0;
    overflow-wrap: anywhere;
    color: var(--pc-accent);
    font-weight: 500;
  }

  .gamgee-front-door__note {
    grid-column: 1 / -1;
    font-size: 13px;
    line-height: 1.35;
    color: var(--pc-ink-soft);
    min-width: 0;
    overflow-wrap: anywhere;
  }

  @media (max-width: 820px) {
    .gamgee-front-door {
      grid-template-columns: 1fr;
      gap: 14px;
    }
  }

  @media (max-width: 639px) {
    .gamgee-front-door {
      margin-top: 12px;
      padding: 14px 0 16px;
      width: calc(100vw - 40px);
      max-width: calc(100vw - 40px);
      overflow: hidden;
    }

    .gamgee-front-door__copy,
    .gamgee-front-door__paths {
      width: min(100%, 350px);
      max-width: 100%;
    }

    .gamgee-front-door__title {
      font-size: 29px;
      line-height: 1.02;
      max-width: 100%;
      overflow-wrap: anywhere;
    }

    .gamgee-front-door__dek {
      font-size: 14px;
      max-width: 100%;
      overflow-wrap: anywhere;
    }

    .gamgee-front-door__signals {
      display: grid;
      grid-template-columns: 1fr;
    }

    .gamgee-front-door__signals li {
      max-width: 100%;
      overflow-wrap: anywhere;
    }

    .gamgee-front-door__path {
      grid-template-columns: 1fr;
      gap: 2px;
    }
  }

  .studio-signal {
    position: relative;
    margin: 14px 0 16px;
    padding: 20px 20px 22px;
    border: 1px solid rgba(92, 153, 170, 0.22);
    background:
      linear-gradient(180deg, rgba(255, 255, 255, 0.94), rgba(245, 251, 250, 0.86)),
      linear-gradient(90deg, rgba(215, 235, 239, 0.28), rgba(244, 238, 202, 0.2));
    color: var(--pc-ink);
    font-family: "Avenir Next Condensed", "Arial Narrow", "Helvetica Neue", var(--pc-font-sans);
    overflow: hidden;
  }

  .studio-signal__line {
    display: grid;
    grid-template-columns: 2fr 1fr 0.7fr;
    width: min(260px, 46%);
    min-width: 164px;
    height: 3px;
    margin-bottom: 26px;
  }

  .studio-signal__line span:nth-child(1) {
    background: #68a9bb;
  }

  .studio-signal__line span:nth-child(2) {
    background: #7cae65;
  }

  .studio-signal__line span:nth-child(3) {
    background: #e5cc66;
  }

  .studio-signal__inner {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(260px, 380px);
    gap: 34px;
    align-items: center;
  }

  .studio-signal__copy {
    min-width: 0;
  }

  .studio-signal__kicker {
    margin: 0 0 10px;
    color: #3d8190;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .studio-signal__title {
    max-width: 760px;
    margin: 0;
    color: var(--pc-ink);
    font-size: 68px;
    font-stretch: condensed;
    font-weight: 400;
    letter-spacing: 0.01em;
    line-height: 0.96;
    overflow-wrap: anywhere;
  }

  .studio-signal__dek {
    max-width: 540px;
    margin: 14px 0 0;
    color: var(--pc-ink-soft);
    font-family: var(--pc-font-sans);
    font-size: 15px;
    line-height: 1.5;
  }

  .studio-signal__links {
    display: grid;
    align-self: center;
    border-top: 1px solid rgba(92, 153, 170, 0.18);
    font-family: var(--pc-font-sans);
  }

  .studio-signal__link {
    display: grid;
    grid-template-columns: 88px minmax(0, 1fr);
    gap: 3px 14px;
    padding: 10px 0;
    border-bottom: 1px solid rgba(92, 153, 170, 0.18);
    color: var(--pc-ink);
  }

  .studio-signal__link:hover {
    color: #3d8190;
    text-decoration: none;
  }

  .studio-signal__link span {
    grid-row: 1 / span 2;
    align-self: start;
    padding-top: 3px;
    color: #6c8f3b;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    font-weight: 700;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .studio-signal__link strong {
    font-size: 15px;
    line-height: 1.2;
  }

  .studio-signal__link em {
    grid-column: 2;
    color: var(--pc-ink-soft);
    font-size: 12px;
    font-style: normal;
    line-height: 1.42;
  }

  @media (max-width: 1120px) {
    .studio-signal__inner {
      grid-template-columns: 1fr;
      gap: 22px;
      align-items: start;
    }

    .studio-signal__title {
      font-size: 58px;
    }

    .studio-signal__links {
      max-width: 720px;
    }
  }

  @media (max-width: 720px) {
    .studio-signal {
      padding: 16px 14px 18px;
    }

    .studio-signal__line {
      width: 100%;
      min-width: 0;
      margin-bottom: 20px;
    }

    .studio-signal__title {
      font-size: 40px;
      line-height: 1;
    }

    .studio-signal__dek {
      font-size: 14px;
    }

    .studio-signal__link {
      grid-template-columns: 1fr;
      gap: 5px;
    }

    .studio-signal__link span,
    .studio-signal__link em {
      grid-column: auto;
      grid-row: auto;
    }
  }

  .participation-home,
  .today-ideas {
    position: relative;
    margin: 18px 0 24px;
    padding: 18px;
    border: 1px solid rgba(196, 149, 46, 0.28);
    background:
      linear-gradient(90deg, rgba(255, 244, 203, 0.5), transparent 42%),
      linear-gradient(135deg, rgba(251, 232, 219, 0.82), rgba(245, 251, 238, 0.7) 56%, rgba(237, 244, 250, 0.72));
    font-family: var(--pc-font-sans);
    overflow: hidden;
  }

  .participation-home::before,
  .today-ideas::before {
    content: '';
    position: absolute;
    inset: 0 auto 0 0;
    width: 7px;
    background: linear-gradient(180deg, #ee5877, #e4a84b, #4f8b5b, #4e7ea9);
  }

  .participation-home__head,
  .today-ideas__head {
    position: relative;
    z-index: 1;
    max-width: 860px;
    margin-bottom: 14px;
    padding-left: 8px;
  }

  .participation-home__kicker,
  .today-ideas__kicker {
    margin: 0 0 8px;
    color: #8a2e62;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    font-weight: 600;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  .participation-home__title,
  .today-ideas__title {
    max-width: 820px;
    margin: 0;
    color: var(--pc-ink);
    font-size: clamp(30px, 5vw, 58px);
    font-weight: 500;
    line-height: 0.98;
    letter-spacing: 0;
  }

  .participation-home__dek,
  .today-ideas__dek {
    max-width: 720px;
    margin: 12px 0 0;
    color: var(--pc-ink-soft);
    font-size: 15px;
    line-height: 1.5;
  }

  .participation-home__grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
  }

  .participation-home__card {
    display: grid;
    grid-template-rows: auto auto 1fr;
    gap: 8px;
    min-height: 190px;
    padding: 14px;
    border: 1px solid rgba(31, 22, 12, 0.18);
    background: rgba(255, 253, 247, 0.82);
    color: var(--pc-ink);
    box-shadow: 0 12px 28px rgba(61, 39, 18, 0.04);
  }

  .participation-home__card:hover {
    border-color: rgba(138, 46, 98, 0.34);
    background: #fffdf7;
    text-decoration: none;
  }

  .participation-home__label,
  .today-ideas__channel,
  .today-ideas__number {
    font-family: var(--pc-font-mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .participation-home__label {
    color: #8a2e62;
  }

  .participation-home__card strong {
    color: var(--pc-ink);
    font-size: 20px;
    line-height: 1.08;
    font-weight: 600;
  }

  .participation-home__card span:last-child {
    color: var(--pc-ink-soft);
    font-size: 14px;
    line-height: 1.45;
  }

  .participation-home__actions {
    position: relative;
    z-index: 1;
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-top: 12px;
    padding-left: 8px;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .participation-home__actions a {
    padding: 7px 9px;
    border: 1px solid rgba(47, 95, 159, 0.22);
    background: rgba(244, 248, 251, 0.82);
    color: #2f5f9f;
  }

  .participation-home__actions a:hover {
    border-color: rgba(47, 95, 159, 0.42);
    background: #eef4fa;
    text-decoration: none;
  }

  .today-ideas {
    background:
      linear-gradient(90deg, rgba(31, 22, 12, 0.045) 1px, transparent 1px) 0 0 / 18px 18px,
      linear-gradient(0deg, rgba(31, 22, 12, 0.045) 1px, transparent 1px) 0 0 / 18px 18px,
      linear-gradient(135deg, rgba(255, 244, 203, 0.68), rgba(248, 223, 228, 0.62) 45%, rgba(237, 244, 250, 0.78));
  }

  .today-ideas__rail {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 10px;
  }

  .today-ideas__card {
    min-height: 210px;
    padding: 12px;
    border: 1px solid rgba(31, 22, 12, 0.2);
    background: rgba(255, 253, 247, 0.86);
  }

  .today-ideas__number {
    display: inline-block;
    margin-right: 7px;
    color: rgba(138, 46, 98, 0.62);
  }

  .today-ideas__channel {
    color: #4f8b5b;
  }

  .today-ideas__card h3 {
    margin: 12px 0 8px;
    color: var(--pc-ink);
    font-size: 19px;
    line-height: 1.08;
    font-weight: 600;
    letter-spacing: 0;
  }

  .today-ideas__card p {
    margin: 0;
    color: var(--pc-ink-soft);
    font-size: 14px;
    line-height: 1.45;
  }

  .today-ideas__card strong {
    display: block;
    margin-top: 12px;
    color: #2f5f9f;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    line-height: 1.35;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  @media (max-width: 1100px) {
    .participation-home__grid,
    .today-ideas__rail {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 620px) {
    .participation-home,
    .today-ideas {
      padding: 14px 12px 14px 16px;
    }

    .participation-home__grid,
    .today-ideas__rail {
      grid-template-columns: 1fr;
    }

    .participation-home__card,
    .today-ideas__card {
      min-height: 0;
    }
  }

  .agent-native-brief {
    position: relative;
    display: grid;
    grid-template-columns: minmax(0, 0.78fr) minmax(380px, 1.22fr);
    gap: 22px 28px;
    margin: 18px 0 24px;
    padding: 20px;
    border: 1px solid rgba(196, 149, 46, 0.26);
    border-bottom-color: rgba(106, 36, 78, 0.32);
    background:
      linear-gradient(90deg, rgba(255, 244, 203, 0.54) 0 19%, transparent 19% 100%),
      linear-gradient(180deg, rgba(104, 64, 146, 0.1) 0 28%, transparent 28% 100%),
      linear-gradient(135deg, rgba(238, 88, 119, 0.16), rgba(236, 214, 112, 0.24) 34%, rgba(80, 139, 91, 0.18) 62%, rgba(78, 126, 169, 0.16));
    background-position: 0 0, 0 0, 50% 50%;
    background-size: auto, auto, 145% 145%;
    font-family: var(--pc-font-sans);
    overflow: hidden;
  }

  .agent-native-brief::before,
  .agent-native-brief::after {
    content: '';
    position: absolute;
    pointer-events: none;
  }

  .agent-native-brief::before {
    inset: 14px auto auto 47%;
    width: 156px;
    height: 44px;
    background: rgba(238, 88, 119, 0.13);
    border: 1px solid rgba(238, 88, 119, 0.18);
    transform: rotate(-1deg);
  }

  .agent-native-brief::after {
    right: 18px;
    bottom: 18px;
    width: 98px;
    height: 62px;
    background: rgba(104, 64, 146, 0.11);
    border: 1px solid rgba(104, 64, 146, 0.16);
    transform: rotate(1deg);
  }

  .agent-native-brief__copy {
    position: relative;
    z-index: 1;
    display: grid;
    align-content: start;
    min-width: 0;
  }

  .agent-native-brief__kicker {
    margin: 0 0 9px;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    color: #8a2e62;
  }

  .agent-native-brief__title {
    max-width: 640px;
    margin: 0;
    font-size: 32px;
    line-height: 1.02;
    font-weight: 500;
    letter-spacing: 0;
    color: var(--pc-ink);
  }

  .agent-native-brief__dek {
    max-width: 660px;
    margin: 12px 0 0;
    font-size: 15px;
    line-height: 1.52;
    color: var(--pc-ink-soft);
  }

  .agent-native-brief__nouns {
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    margin-top: 16px;
    padding: 8px;
    width: fit-content;
    max-width: 100%;
    background: rgba(255, 248, 225, 0.42);
    border: 1px solid rgba(196, 149, 46, 0.2);
  }

  .agent-native-brief__noun {
    display: inline-grid;
    grid-template-columns: 28px auto;
    align-items: center;
    gap: 7px;
    min-height: 34px;
    padding: 3px 8px 3px 3px;
    border: 1px solid rgba(196, 149, 46, 0.26);
    background: rgba(255, 255, 255, 0.42);
    color: var(--pc-ink-soft);
    font-family: var(--pc-font-mono);
    font-size: 9px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition:
      background-color 1200ms ease,
      border-color 1200ms ease,
      color 1200ms ease,
      transform 1600ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .agent-native-brief__noun:hover {
    border-color: rgba(104, 64, 146, 0.36);
    background: rgba(248, 223, 228, 0.72);
    color: #684092;
    transform: translateY(-1px);
    text-decoration: none;
  }

  .agent-native-brief__noun img {
    display: block;
    width: 28px;
    height: 28px;
    image-rendering: pixelated;
  }

  .agent-native-brief__matrix {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
    min-width: 0;
  }

  .agent-native-brief__cell {
    display: grid;
    grid-template-rows: auto auto 1fr;
    gap: 7px;
    min-width: 0;
    min-height: 126px;
    padding: 14px;
    border: 1px solid rgba(196, 149, 46, 0.25);
    border-top: 0;
    background:
      linear-gradient(180deg, rgba(238, 88, 119, 0.18) 0 7px, transparent 7px),
      linear-gradient(135deg, rgba(251, 232, 219, 0.86), rgba(248, 223, 228, 0.62));
    color: var(--pc-ink);
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.35);
    transition:
      border-color 1400ms ease,
      box-shadow 1800ms ease,
      transform 1800ms cubic-bezier(0.16, 1, 0.3, 1);
  }

  .agent-native-brief__cell:nth-child(2) {
    background:
      linear-gradient(180deg, rgba(78, 126, 169, 0.18) 0 7px, transparent 7px),
      linear-gradient(135deg, rgba(235, 244, 247, 0.82), rgba(246, 240, 216, 0.62));
  }

  .agent-native-brief__cell:nth-child(3) {
    background:
      linear-gradient(180deg, rgba(79, 139, 91, 0.18) 0 7px, transparent 7px),
      linear-gradient(135deg, rgba(241, 248, 232, 0.82), rgba(255, 248, 225, 0.62));
  }

  .agent-native-brief__cell:nth-child(4) {
    background:
      linear-gradient(180deg, rgba(196, 149, 46, 0.22) 0 7px, transparent 7px),
      linear-gradient(135deg, rgba(255, 247, 210, 0.86), rgba(237, 229, 247, 0.58));
  }

  .agent-native-brief__cell:hover {
    border-color: rgba(104, 64, 146, 0.34);
    box-shadow: inset 0 0 0 1px rgba(255, 255, 255, 0.54), 0 14px 34px rgba(106, 36, 78, 0.06);
    transform: translateY(-2px);
    text-decoration: none;
  }

  .agent-native-brief__cell--primary {
    background:
      linear-gradient(180deg, rgba(238, 88, 119, 0.2) 0 7px, transparent 7px),
      linear-gradient(135deg, rgba(251, 232, 219, 0.9), rgba(248, 223, 228, 0.66));
  }

  .agent-native-brief__label,
  .agent-native-brief__cell strong,
  .agent-native-brief__signals {
    font-family: var(--pc-font-mono);
  }

  .agent-native-brief__label {
    color: var(--pc-ink-muted);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .agent-native-brief__cell strong {
    min-width: 0;
    overflow-wrap: anywhere;
    color: #8a2e62;
    font-size: 13px;
    font-weight: 500;
    letter-spacing: 0.04em;
  }

  .agent-native-brief__cell span:last-child {
    color: var(--pc-ink-soft);
    font-size: 13px;
    line-height: 1.35;
  }

  .agent-native-brief__signals {
    position: relative;
    z-index: 1;
    grid-column: 1 / -1;
    display: flex;
    flex-wrap: wrap;
    gap: 7px;
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .agent-native-brief__signals li {
    border: 1px solid rgba(196, 149, 46, 0.24);
    padding: 5px 8px;
    background: rgba(255, 248, 225, 0.58);
    color: var(--pc-ink-soft);
    font-size: 10px;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    transition: background-color 1400ms ease, border-color 1400ms ease;
  }

  .agent-native-brief__signals li:nth-child(2n) {
    background: rgba(237, 229, 247, 0.58);
  }

  .agent-native-brief__signals li:nth-child(3n) {
    background: rgba(240, 247, 236, 0.62);
  }

  .agent-native-brief__signals li:nth-child(4n) {
    background: rgba(248, 223, 228, 0.58);
  }

  @keyframes agentNativeBriefGlow {
    0% {
      background-position: 0 0, 0 0, 38% 46%;
    }

    100% {
      background-position: 0 0, 0 0, 62% 54%;
    }
  }

  @media (prefers-reduced-motion: no-preference) {
    .agent-native-brief {
      animation: agentNativeBriefGlow 28s ease-in-out infinite alternate;
    }
  }

  @media (max-width: 920px) {
    .agent-native-brief {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 639px) {
    .agent-native-brief {
      width: calc(100vw - 40px);
      max-width: calc(100vw - 40px);
      padding: 16px;
      overflow: hidden;
    }

    .agent-native-brief__title {
      font-size: 27px;
    }

    .agent-native-brief__matrix {
      grid-template-columns: 1fr;
    }

    .agent-native-brief__cell {
      min-height: 0;
    }
  }

  .hello-module {
    display: grid;
    grid-template-columns: 1fr;
    gap: 8px;
    margin: 14px 0;
  }

  .hello-module :global(.block-card) {
    min-height: 168px;
  }

  /* Channel chip bar — flex-wrap desktop, horizontal-scroll + sticky on mobile */
  .channels {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    padding: 14px 0;
    border-bottom: 1px dashed var(--pc-rule);
    margin-bottom: 14px;
    background: var(--pc-bg);
  }

  @media (max-width: 767px) {
    .channels {
      width: 100%;
      max-width: 100%;
      flex-wrap: nowrap;
      overflow-x: auto;
      overflow-y: hidden;
      position: sticky;
      top: 0;
      z-index: 10;
      -webkit-overflow-scrolling: touch;
      scrollbar-width: none;
    }
    .channels::-webkit-scrollbar { display: none; }
    .chip { flex-shrink: 0; }
  }

  .chip {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 4px 8px;
    border: 1px solid var(--ch-600);
    background: var(--ch-50);
    color: var(--ch-800);
    font-family: var(--pc-font-mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
    border-radius: 2px;
    transition: background-color 0.12s;
  }
  .chip:hover {
    background: color-mix(in oklab, var(--ch-600) 20%, #fff);
    text-decoration: none;
  }
  .chip__dot {
    display: inline-block;
    width: 7px; height: 7px;
    background: var(--ch-600);
    border-radius: 50%;
  }
  .chip__code { font-weight: 500; }
  .chip__name { font-weight: 400; color: var(--pc-ink-soft); }

  .first-block {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    margin-top: 14px;
  }

  .first-block :global(.block-card) {
    min-height: 210px;
  }

  .first-block :global(.block-card.span-2x1),
  .first-block :global(.block-card.span-2x2),
  .first-block :global(.block-card.span-3x2) {
    grid-column: 1;
    grid-row: auto;
  }

  @media (max-width: 639px) {
    .first-block {
      margin-top: 12px;
    }

    .first-block :global(.block-card) {
      min-height: 180px;
    }
  }

  /* Self-healing grid per BLOCKS.md: auto-fit + minmax(220px, 1fr) */
  .grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    grid-auto-rows: minmax(170px, auto);
    grid-auto-flow: dense;
    gap: 8px;
    margin-top: 14px;
  }

  @media (max-width: 639px) {
    .grid {
      grid-template-columns: 1fr;
      /* Mike 2026-04-18 home-mobile-lighten: more breathing room between
         cards on mobile. The horizontal gap is moot at single-column;
         the vertical gap is what makes the scroll feel airier. */
      gap: 16px;
      grid-auto-rows: minmax(120px, auto);
    }
  }

  /* ══ SCROLL RHYTHM ═════════════════════════════════════════════════════
     Per-position decoration so the feed doesn't read as 48 identical tiles.
     Rules layer in source order — each block matches based on its DOM
     index (grid-auto-flow:dense doesn't change nth-child). Deliberately
     subtle: varies tone, weight, and micro-detail, never layout. */

  /* Every 5th card gets a paper-tint fill over the standard white. */
  .grid :global(.block-card:nth-child(5n+2):not(.type-visit):not(.type-note):not(.block-card--detail)) {
    background: #fafaf7;
  }

  /* Every 7th card gets a thicker 3px left accent (was 6px only on READ). */
  .grid :global(.block-card:nth-child(7n+3):not(.type-visit):not(.block-card--detail)) {
    border-left-width: 3px;
  }

  /* Every 9th card drops the border entirely and relies on a faint
     shadow instead — reads as an "open" card amid the bordered ones. */
  .grid :global(.block-card:nth-child(9n+4):not(.type-visit):not(.type-note):not(.type-link):not(.type-mint):not(.type-faucet):not(.block-card--detail)) {
    border-color: transparent;
    box-shadow: 0 0 0 1px var(--pc-rule, #C4C2BC), inset 4px 0 0 var(--ch-600);
  }

  /* Every 11th card inverts: dark bg + light text. Only applies where
     it won't collide with VISIT (which is already dark). */
  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail)) {
    background: #12110E;
    border-color: var(--ch-800);
  }

  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail) .block-card__title) {
    color: #f2efe9;
  }

  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail) .block-card__dek),
  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail) .block-card__body) {
    color: #c4c2bc;
  }

  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail) .block-card__header),
  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail) .block-card__code),
  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail) .block-card__time),
  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail) .block-card__kicker) {
    color: color-mix(in oklab, var(--ch-600) 55%, #fff);
  }

  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail) .block-card__meta) {
    color: color-mix(in oklab, var(--ch-600) 55%, #fff);
  }

  .grid :global(.block-card:nth-child(11n+6):not(.type-visit):not(.type-mint):not(.type-faucet):not(.type-listen):not(.type-watch):not(.block-card--detail):hover .block-card__title) {
    color: #fff;
  }

  .endpoints {
    margin-top: 48px;
    padding-top: 16px;
    border-top: 1.5px solid var(--pc-ink);
    display: flex;
    align-items: center;
    gap: 16px;
    flex-wrap: wrap;
    font-family: var(--pc-font-mono);
    font-size: 10px;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .endpoints__label { margin: 0; color: var(--pc-ink-soft); font-weight: 500; }
  .endpoints__list {
    list-style: none;
    margin: 0; padding: 0;
    display: flex;
    gap: 12px;
    flex-wrap: wrap;
  }
  .endpoints__list a { color: var(--pc-accent); }
</style>
