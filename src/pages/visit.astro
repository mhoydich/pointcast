---
/**
 * /visit — homepage candidate (the "nice visit experience", 2026-05-29).
 *
 * Lineage: this builds directly on /window-home (the 2026-05-23 Option A
 * first-principles home). Window-home's diagnosis still holds — the
 * editorial / had re-bloated to ~15 sections, so window-home cut back to
 * "the homepage IS a live window onto El Segundo" with two sections.
 *
 * What window-home got right and we keep wholesale: the CSS pixel-art
 * window (sky tinted by time-of-day, weather-aware clouds, marine layer,
 * coastline, seven chunky buildings with lit windows + ambient motion).
 * That sense-of-place is the best asset PointCast's homepage has ever
 * had, and it's reused here verbatim (same `wh-` classes, Astro-scoped).
 *
 * What window-home got wrong for a FIRST visit: the seven buildings were
 * the only navigation, and their labels were hover-only tooltips. A
 * first-timer — or anyone on a phone, where there is no hover — saw seven
 * abstract colored shapes and could not tell the red dome was the drum.
 * The town looked great but didn't welcome or orient you.
 *
 * So /visit adds the "visit" layer on top of the window:
 *   1. ARRIVAL    — warm, on-voice copy that says where you are.
 *   2. THE WINDOW — window-home's live town, untouched.
 *   3. THE DOORS  — the same seven rooms as ALWAYS-VISIBLE labeled cards
 *                   below the window. Legible on touch, color-matched to
 *                   their building, and hovering a door lifts its building
 *                   so the two nav layers read as one place.
 *   4. THE DESK   — one quiet footer band: today's digest, latest block,
 *                   a first-timer path to the mythos, and the exits.
 *
 * Lives at /visit so Mike can A/B against / and /window-home. Promotion
 * is one PR swapping this into /index.astro.
 */
import BlockLayout from '../layouts/BlockLayout.astro';
import { getCollection } from 'astro:content';
import shipsData from '../data/recent-ships.json';

interface Weather {
  ok: boolean; name?: string; tempF?: number; condition?: string;
}
let weather: Weather = { ok: false };
try {
  const res = await fetch('https://pointcast.xyz/api/weather?lat=33.92&lng=-118.42&label=el-segundo', {
    signal: AbortSignal.timeout(2500),
  });
  if (res.ok) weather = await res.json();
} catch { /* fall through to stub */ }
const tempF = weather.tempF ?? 62;
const condition = (weather.condition ?? 'overcast').toLowerCase();

function timeOfDayFromHour(h: number) {
  if (h >= 5  && h <  7)  return 'dawn';
  if (h >= 7  && h < 11)  return 'morning';
  if (h >= 11 && h < 14)  return 'midday';
  if (h >= 14 && h < 17)  return 'afternoon';
  if (h >= 17 && h < 19)  return 'sunset';
  if (h >= 19 && h < 21)  return 'dusk';
  return 'night';
}
const ptHourFmt = new Intl.DateTimeFormat('en-US', {
  timeZone: 'America/Los_Angeles', hour: 'numeric', hour12: false,
});
const ptHour = parseInt(ptHourFmt.format(new Date()), 10);
const tod = timeOfDayFromHour(ptHour);

// A one-line greeting that tracks the hour — the page should feel like it
// noticed you arrived. Server-rendered for first paint; refreshed client
// side alongside the sky.
function greetingFor(t: string): string {
  switch (t) {
    case 'dawn':      return 'Early. The marine layer hasn’t burned off yet.';
    case 'morning':   return 'Morning. The coffee pot’s on.';
    case 'midday':    return 'Midday sun over the refinery and the sea.';
    case 'afternoon': return 'Afternoon light, long and gold.';
    case 'sunset':    return 'Sunset over the Pacific. Best hour in town.';
    case 'dusk':      return 'Dusk. The windows are starting to light up.';
    default:          return 'Late. The town’s quiet, but the pot’s still on.';
  }
}
const greeting = greetingFor(tod);

function conditionTag(c: string) {
  if (/fog|mist|marine/.test(c)) return 'foggy';
  if (/clear|sun/.test(c))       return 'clear';
  if (/overcast/.test(c))        return 'overcast';
  return 'cloudy';
}
const cond = conditionTag(condition);

const allBlocks = (await getCollection('blocks', ({ data }) => !data.draft))
  .sort((a, b) => +new Date(b.data.timestamp) - +new Date(a.data.timestamp));
const blockCount = allBlocks.length;
const latestBlock = allBlocks[0];

const now = new Date();
const startOfToday = new Date(now.getFullYear(), now.getMonth(), now.getDate());
const todayBlocks = allBlocks.filter(b => new Date(b.data.timestamp) >= startOfToday);

interface ShipItem { ref: string; title: string; href: string; tag: string; at: string; }
const ships = (shipsData as { ships: ShipItem[] }).ships
  .slice().sort((a, b) => +new Date(b.at) - +new Date(a.at));
const sevenDaysAgo = new Date(startOfToday);
sevenDaysAgo.setDate(sevenDaysAgo.getDate() - 7);
const shipsLast7 = ships.filter(s => +new Date(s.at) >= +sevenDaysAgo).length;

const firstBlock = allBlocks[allBlocks.length - 1];
const sinceDate = firstBlock
  ? new Date(firstBlock.data.timestamp).toISOString().slice(0, 10)
  : '2025-01-14';

// Seven rooms along the skyline AND in the doors strip below. Order is
// L→R on desktop. `shape` styles the CSS silhouette; the door card reuses
// `color` for its chip so a door and its building read as the same place.
const ROOMS = [
  { id: 'drum',   shape: 'dome',   href: '/drum',         label: 'The drum',      sub: 'one tap, the whole room hears',  color: '#cf3c3c' },
  { id: 'bar',    shape: 'awning', href: '/bar-v6',       label: 'The bar',       sub: 'walk the street, cast a +cat',   color: '#c4952e' },
  { id: 'coffee', shape: 'steam',  href: '/coffee',       label: 'The coffee',    sub: 'pour from the live shelf',       color: '#7a4a1f' },
  { id: 'arcade', shape: 'crt',    href: '/win95-games',  label: 'The arcade',    sub: 'solitaire · mines · memory',     color: '#5a5aaa' },
  { id: 'mythos', shape: 'peak',   href: '/mythos',       label: 'The mythos',    sub: '60-second read · 15 worlds',     color: '#1f5b48' },
  { id: 'wire',   shape: 'tower',  href: '/wire',         label: 'The wire',      sub: 'live commits + new blocks',      color: '#1b3a5b' },
  { id: 'house',  shape: 'house',  href: '/residents',    label: 'The residents', sub: 'four agents, two open slots',    color: '#5b2a4a' },
] as const;

const title = 'PointCast — a small internet town from El Segundo';
const description = `Come visit. ${blockCount} blocks since ${sinceDate}. It's ${tempF}°F and ${condition} in El Segundo. Every door is open.`;
---

<BlockLayout title={title} description={description}>
  <main class="wv">

    {/* ============== 1 · ARRIVAL ============== */}
    <header class="wv__head">
      <p class="wv__kicker mono">
        EL&nbsp;SEGUNDO · 33.92°N&nbsp;118.42°W · <span id="wv-temp">{tempF}</span>°F · <span id="wv-cond">{condition}</span>
      </p>
      <h1 class="wv__title">A small internet town.</h1>
      <p class="wv__sub">
        You’re in El Segundo now. <span id="wv-greeting">{greeting}</span> Every door below is open —
        have a look around.
      </p>
    </header>

    {/* ============== 2 · THE WINDOW (from /window-home) ============== */}
    <section class="wh-stage" aria-label="A live view from El Segundo">
      <div class="wh-frame" data-tod={tod} data-cond={cond} id="wh-frame">

        <div class="wh-sky">
          <div class="wh-sun"  aria-hidden="true"></div>
          <div class="wh-moon" aria-hidden="true"></div>
          <div class="wh-stars" aria-hidden="true">
            <span class="wh-star" style="left:6%;  top:14%; animation-delay: 0s;"></span>
            <span class="wh-star" style="left:14%; top:28%; animation-delay: 1.2s;"></span>
            <span class="wh-star" style="left:22%; top:8%;  animation-delay: 0.5s;"></span>
            <span class="wh-star" style="left:34%; top:22%; animation-delay: 1.9s;"></span>
            <span class="wh-star" style="left:46%; top:6%;  animation-delay: 0.3s;"></span>
            <span class="wh-star" style="left:58%; top:18%; animation-delay: 2.6s;"></span>
            <span class="wh-star" style="left:68%; top:30%; animation-delay: 1.4s;"></span>
            <span class="wh-star" style="left:78%; top:10%; animation-delay: 0.8s;"></span>
            <span class="wh-star" style="left:88%; top:24%; animation-delay: 2.2s;"></span>
            <span class="wh-star" style="left:94%; top:14%; animation-delay: 0.1s;"></span>
          </div>
          <div class="wh-cloud wh-cloud--1" aria-hidden="true"></div>
          <div class="wh-cloud wh-cloud--2" aria-hidden="true"></div>
          <div class="wh-cloud wh-cloud--3" aria-hidden="true"></div>
          <div class="wh-cloud wh-cloud--4" aria-hidden="true"></div>
          <div class="wh-marine"  aria-hidden="true"></div>
          <div class="wh-horizon" aria-hidden="true"></div>
        </div>

        <div class="wh-ground" aria-hidden="true"></div>

        {/* SKYLINE — seven clickable buildings. Mirrors the doors strip
            below; hovering a door lifts the matching building. */}
        <nav class="wh-skyline" aria-label="Rooms in town">
          {ROOMS.map((r, i) => (
            <a
              class={`wh-bldg wh-bldg--${r.shape}`}
              href={r.href}
              style={`--room: ${r.color}; --idx: ${i};`}
              data-room={r.id}
              aria-label={`${r.label} — ${r.sub}`}
            >
              <span class="wh-bldg__shape" aria-hidden="true">
                <span class="wh-bldg__body"></span>
                <span class="wh-bldg__roof"></span>
                <span class="wh-bldg__sign"></span>
                <span class="wh-bldg__door"></span>
                <span class="wh-bldg__night" aria-hidden="true"></span>
              </span>
              <span class="wh-bldg__lit" aria-hidden="true"></span>
              <span class="wh-bldg__tip">
                <span class="wh-bldg__tip-label">{r.label}</span>
                <span class="wh-bldg__tip-sub">{r.sub}</span>
              </span>
            </a>
          ))}
        </nav>
      </div>
    </section>

    {/* ============== 3 · THE DOORS ============== */}
    <section class="wv-doors" aria-label="Doors into the town">
      <p class="wv-doors__kicker mono">Every door is open</p>
      <ul class="wv-doors__grid">
        {ROOMS.map((r) => (
          <li>
            <a class="wv-door" href={r.href} data-room={r.id} style={`--room: ${r.color};`}>
              <span class="wv-door__chip" aria-hidden="true"></span>
              <span class="wv-door__text">
                <span class="wv-door__label">{r.label}</span>
                <span class="wv-door__sub mono">{r.sub}</span>
              </span>
              <span class="wv-door__go mono" aria-hidden="true">→</span>
            </a>
          </li>
        ))}
      </ul>
    </section>

    {/* ============== 4 · THE DESK ============== */}
    <section class="wv-desk" aria-label="Today on PointCast">
      <p class="wv-desk__line">
        <span class="wv-desk__date mono">TODAY · {now.toISOString().slice(0,10)}</span>
        <span class="wv-desk__sep">·</span>
        <span class="wv-desk__stat"><strong>{todayBlocks.length}</strong>&nbsp;new&nbsp;block{todayBlocks.length === 1 ? '' : 's'}</span>
        <span class="wv-desk__sep">·</span>
        <span class="wv-desk__stat"><strong>{shipsLast7}</strong>&nbsp;ships&nbsp;this&nbsp;week</span>
        <span class="wv-desk__sep">·</span>
        <span class="wv-desk__stat"><strong>{blockCount}</strong>&nbsp;blocks&nbsp;since&nbsp;{sinceDate}</span>
      </p>
      {latestBlock && (
        <p class="wv-desk__latest">
          ↪ latest: <a href={`/b/${latestBlock.data.id}`}>/{latestBlock.data.id} — {latestBlock.data.title}</a>
        </p>
      )}

      <p class="wv-desk__newhere">
        First time in town? <a href="/mythos">Read the mythos</a> — it’s a 60-second read.
      </p>

      <nav class="wv-exits" aria-label="More from the town">
        <a href="/wire"       class="wv-exit mono">→ the wire</a>
        <a href="/archive"    class="wv-exit mono">browse the archive</a>
        <a href="/for-agents" class="wv-exit mono">/for-agents</a>
      </nav>
    </section>

  </main>
</BlockLayout>

<script is:inline>
  (function () {
    'use strict';
    var frame  = document.getElementById('wh-frame');
    var tempEl = document.getElementById('wv-temp');
    var condEl = document.getElementById('wv-cond');
    var greetEl = document.getElementById('wv-greeting');
    if (!frame) return;

    function ptHour() {
      try {
        var f = new Intl.DateTimeFormat('en-US', { timeZone: 'America/Los_Angeles', hour: 'numeric', hour12: false });
        return parseInt(f.format(new Date()), 10);
      } catch (e) { return new Date().getHours(); }
    }
    function tod(h) {
      if (h >= 5  && h <  7) return 'dawn';
      if (h >= 7  && h < 11) return 'morning';
      if (h >= 11 && h < 14) return 'midday';
      if (h >= 14 && h < 17) return 'afternoon';
      if (h >= 17 && h < 19) return 'sunset';
      if (h >= 19 && h < 21) return 'dusk';
      return 'night';
    }
    function greetingFor(t) {
      switch (t) {
        case 'dawn':      return 'Early. The marine layer hasn’t burned off yet.';
        case 'morning':   return 'Morning. The coffee pot’s on.';
        case 'midday':    return 'Midday sun over the refinery and the sea.';
        case 'afternoon': return 'Afternoon light, long and gold.';
        case 'sunset':    return 'Sunset over the Pacific. Best hour in town.';
        case 'dusk':      return 'Dusk. The windows are starting to light up.';
        default:          return 'Late. The town’s quiet, but the pot’s still on.';
      }
    }
    function ctag(c) {
      c = (c || '').toLowerCase();
      if (/fog|mist|marine/.test(c)) return 'foggy';
      if (/clear|sun/.test(c))       return 'clear';
      if (/overcast/.test(c))        return 'overcast';
      return 'cloudy';
    }
    function refreshTod() {
      var t = tod(ptHour());
      frame.setAttribute('data-tod', t);
      if (greetEl) greetEl.textContent = greetingFor(t);
    }
    function refreshWeather() {
      fetch('/api/weather?lat=33.92&lng=-118.42&label=el-segundo', { cache: 'no-store' })
        .then(function (r) { return r.ok ? r.json() : null; })
        .then(function (d) {
          if (!d || !d.ok) return;
          if (tempEl && d.tempF != null) tempEl.textContent = String(d.tempF);
          if (condEl && d.condition)     condEl.textContent = String(d.condition).toLowerCase();
          frame.setAttribute('data-cond', ctag(d.condition));
        }).catch(function () { /* keep current state */ });
    }
    refreshTod();
    setInterval(refreshTod,     60000);
    setInterval(refreshWeather, 300000);

    // Tie the two nav layers together: hovering/focusing a door lifts the
    // matching building in the window, and vice-versa. Makes the doors and
    // the skyline read as one place rather than two parallel menus.
    var doors = document.querySelectorAll('.wv-door');
    function setLit(id, on) {
      var b = frame.querySelector('.wh-bldg[data-room="' + id + '"]');
      if (b) b.classList.toggle('is-lit', on);
      var d = document.querySelector('.wv-door[data-room="' + id + '"]');
      if (d) d.classList.toggle('is-lit', on);
    }
    doors.forEach(function (d) {
      var id = d.getAttribute('data-room');
      d.addEventListener('mouseenter', function () { setLit(id, true); });
      d.addEventListener('mouseleave', function () { setLit(id, false); });
      d.addEventListener('focus',      function () { setLit(id, true); });
      d.addEventListener('blur',       function () { setLit(id, false); });
    });
    var blds = frame.querySelectorAll('.wh-bldg');
    blds.forEach(function (b) {
      var id = b.getAttribute('data-room');
      b.addEventListener('mouseenter', function () { setLit(id, true); });
      b.addEventListener('mouseleave', function () { setLit(id, false); });
    });
  })();
</script>

<style>
  .wv {
    max-width: 1180px;
    margin: 0 auto;
    padding: 28px 20px 60px;
    font-family: var(--font-body);
  }
  .wv .mono { font-family: var(--font-mono); }

  /* ============== 1 · ARRIVAL ============== */
  .wv__head { text-align: center; margin: 0 0 22px; }
  .wv__kicker {
    font-size: 10px;
    letter-spacing: 0.22em;
    color: #6b6256;
    text-transform: uppercase;
    margin: 0 0 14px;
  }
  .wv__title {
    font-family: 'DM Serif Display', 'Lora', Georgia, serif;
    font-size: clamp(42px, 7vw, 76px);
    font-weight: 400;
    line-height: 1.02;
    letter-spacing: -0.018em;
    margin: 0 0 12px;
    color: #1f1b15;
  }
  .wv__sub {
    font-family: var(--font-serif);
    font-size: clamp(15px, 1.6vw, 18px);
    line-height: 1.5;
    color: #6b6256;
    margin: 0 auto;
    max-width: 580px;
  }
  #wv-greeting { color: #1f1b15; }

  /* ============== 2 · THE WINDOW (verbatim from /window-home) ========= */
  .wh-stage {
    margin: 28px auto 0;
    display: flex;
    justify-content: center;
  }
  .wh-frame {
    position: relative;
    width: 100%;
    max-width: 1080px;
    aspect-ratio: 16 / 9;
    border: 12px solid #f3ece0;
    border-bottom-width: 14px;
    border-radius: 4px;
    box-shadow:
      inset 0 0 0 2px #d8cdb8,
      inset 0 0 0 4px rgba(0, 0, 0, 0.04),
      0 12px 36px rgba(31, 27, 21, 0.18),
      0 1px 0 rgba(255, 255, 255, 0.7);
    overflow: hidden;
    background: #b8e0f0;
  }

  .wh-sky { position: absolute; inset: 0; transition: background 4s ease-in-out; }
  .wh-frame[data-tod="dawn"]      .wh-sky { background: linear-gradient(180deg, #f7c0a0 0%, #ffd5b4 35%, #c8b495 75%, #a89880 100%); }
  .wh-frame[data-tod="morning"]   .wh-sky { background: linear-gradient(180deg, #b8d8e6 0%, #d4e4ec 50%, #e0d8c4 100%); }
  .wh-frame[data-tod="midday"]    .wh-sky { background: linear-gradient(180deg, #6cb4d8 0%, #a8d4e8 50%, #c8e0ec 100%); }
  .wh-frame[data-tod="afternoon"] .wh-sky { background: linear-gradient(180deg, #88b8d4 0%, #c4d4e0 60%, #e6d8c0 100%); }
  .wh-frame[data-tod="sunset"]    .wh-sky { background: linear-gradient(180deg, #d68a4e 0%, #e87a5a 30%, #c46478 60%, #6c4a78 100%); }
  .wh-frame[data-tod="dusk"]      .wh-sky { background: linear-gradient(180deg, #3c3680 0%, #5a4488 35%, #7c4878 70%, #a06082 100%); }
  .wh-frame[data-tod="night"]     .wh-sky { background: linear-gradient(180deg, #0a1428 0%, #1a2440 60%, #2a3058 100%); }

  .wh-sun {
    position: absolute; width: 52px; height: 52px; border-radius: 50%;
    background: #ffe07a; box-shadow: 0 0 30px rgba(255, 224, 122, 0.6);
    left: 60%; top: 20%; opacity: 0;
    transition: opacity 4s, top 4s ease-in-out, left 4s ease-in-out, background 4s, box-shadow 4s;
  }
  .wh-frame[data-tod="dawn"]      .wh-sun { opacity: 1;   left: 12%; top: 58%; background: #ffb088; box-shadow: 0 0 28px rgba(255, 130, 90, 0.55); }
  .wh-frame[data-tod="morning"]   .wh-sun { opacity: 0.9; left: 26%; top: 30%; background: #fff0c8; }
  .wh-frame[data-tod="midday"]    .wh-sun { opacity: 1;   left: 50%; top: 12%; background: #fff5b0; box-shadow: 0 0 40px rgba(255, 245, 176, 0.65); }
  .wh-frame[data-tod="afternoon"] .wh-sun { opacity: 1;   left: 72%; top: 24%; background: #ffe07a; }
  .wh-frame[data-tod="sunset"]    .wh-sun { opacity: 1;   left: 86%; top: 56%; background: #ff7a4e; box-shadow: 0 0 44px rgba(255, 122, 78, 0.75); }
  .wh-frame[data-tod="dusk"]      .wh-sun { opacity: 0.3; left: 92%; top: 74%; background: #c46478; }
  .wh-frame[data-tod="night"]     .wh-sun { opacity: 0; }

  .wh-moon {
    position: absolute; width: 40px; height: 40px; border-radius: 50%;
    background: #f0ebd8;
    box-shadow: inset -10px -2px 0 0 rgba(0, 0, 0, 0.15), 0 0 18px rgba(240, 235, 216, 0.45);
    left: 30%; top: 22%; opacity: 0;
    transition: opacity 4s, top 4s ease-in-out, left 4s ease-in-out;
  }
  .wh-frame[data-tod="dusk"]  .wh-moon { opacity: 0.7; left: 26%; top: 26%; }
  .wh-frame[data-tod="night"] .wh-moon { opacity: 1;   left: 34%; top: 16%; }
  .wh-frame[data-tod="dawn"]  .wh-moon { opacity: 0.4; left: 72%; top: 28%; }

  .wh-stars { position: absolute; inset: 0; opacity: 0; transition: opacity 4s; }
  .wh-frame[data-tod="dusk"]  .wh-stars { opacity: 0.5; }
  .wh-frame[data-tod="night"] .wh-stars { opacity: 1; }
  .wh-frame[data-tod="dawn"]  .wh-stars { opacity: 0.3; }
  .wh-star {
    position: absolute; width: 2px; height: 2px; background: #fffaf0; border-radius: 50%;
    box-shadow: 0 0 3px rgba(255, 250, 240, 0.7);
    animation: wh-twinkle 3.4s ease-in-out infinite;
  }
  @keyframes wh-twinkle {
    0%, 100% { opacity: 0.3; transform: scale(1); }
    50%      { opacity: 1;   transform: scale(1.4); }
  }

  .wh-cloud { position: absolute; background: rgba(255, 255, 255, 0.85); border-radius: 50%; filter: blur(0.5px); will-change: transform; }
  .wh-cloud--1 { width: 110px; height: 26px; top: 18%; left: -120px; animation: wh-drift 80s linear infinite; }
  .wh-cloud--2 { width:  78px; height: 20px; top: 32%; left: -90px;  animation: wh-drift 62s linear infinite; animation-delay: -25s; }
  .wh-cloud--3 { width: 140px; height: 30px; top: 10%; left: -160px; animation: wh-drift 100s linear infinite; animation-delay: -40s; }
  .wh-cloud--4 { width:  64px; height: 18px; top: 26%; left: -80px;  animation: wh-drift 55s linear infinite; animation-delay: -10s; }
  @keyframes wh-drift { 0% { transform: translateX(0); } 100% { transform: translateX(calc(100vw + 200px)); } }
  .wh-frame[data-cond="clear"]    .wh-cloud { opacity: 0.18; }
  .wh-frame[data-cond="cloudy"]   .wh-cloud { opacity: 0.7;  }
  .wh-frame[data-cond="overcast"] .wh-cloud { opacity: 0.88; background: rgba(220, 220, 220, 0.92); }
  .wh-frame[data-cond="foggy"]    .wh-cloud { opacity: 0.55; filter: blur(2px); }
  .wh-frame[data-tod="night"]     .wh-cloud { background: rgba(140, 140, 160, 0.72); }
  .wh-frame[data-tod="dusk"]      .wh-cloud { background: rgba(180, 160, 180, 0.72); }

  .wh-marine {
    position: absolute; left: 0; right: 0; bottom: 0; height: 22%;
    background: linear-gradient(180deg, transparent 0%, rgba(220, 224, 230, 0.45) 60%, rgba(220, 224, 230, 0.7) 100%);
    transition: height 4s, opacity 4s; opacity: 0.55;
  }
  .wh-frame[data-cond="foggy"]    .wh-marine { height: 56%; opacity: 0.9; }
  .wh-frame[data-cond="overcast"] .wh-marine { height: 32%; opacity: 0.7; }
  .wh-frame[data-cond="clear"]    .wh-marine { opacity: 0.22; }

  .wh-horizon {
    position: absolute; left: 0; right: 0; bottom: 30%; height: 2px;
    background: rgba(255, 220, 180, 0.4); opacity: 0; transition: opacity 4s;
  }
  .wh-frame[data-tod="sunset"] .wh-horizon,
  .wh-frame[data-tod="dawn"]   .wh-horizon { opacity: 1; }

  .wh-ground {
    position: absolute; left: 0; right: 0; bottom: 0; height: 18%;
    background: linear-gradient(180deg,
      transparent 0%, rgba(212, 184, 138, 0.18) 8%, rgba(196, 168, 116, 0.72) 30%,
      #c4a874 55%, #b09465 60%, rgba(255, 248, 216, 0.88) 61%,
      #4868a8 62%, #2a4878 78%, #1a3868 100%);
    pointer-events: none; transition: background 4s ease-in-out;
  }
  .wh-frame[data-tod="sunset"] .wh-ground {
    background: linear-gradient(180deg, transparent 0%, rgba(180, 130, 95, 0.40) 16%, rgba(160, 105, 75, 0.85) 44%,
      #8c5638 55%, #6e4030 60%, rgba(255, 200, 150, 0.95) 61%, #c46478 62%, #6c4a78 80%, #3a2c5a 100%);
  }
  .wh-frame[data-tod="dusk"] .wh-ground {
    background: linear-gradient(180deg, transparent 0%, rgba(108, 88, 110, 0.45) 14%, rgba(86, 68, 96, 0.85) 40%,
      #4a3a58 55%, #38283e 60%, rgba(220, 200, 220, 0.78) 61%, #2a2858 62%, #1a1838 100%);
  }
  .wh-frame[data-tod="night"] .wh-ground {
    background: linear-gradient(180deg, transparent 0%, rgba(50, 44, 38, 0.45) 16%, rgba(44, 36, 30, 0.80) 40%,
      #2a2218 55%, #1c160e 60%, rgba(180, 200, 220, 0.55) 61%, #0e1830 62%, #050c1e 100%);
  }
  .wh-frame[data-tod="dawn"] .wh-ground {
    background: linear-gradient(180deg, transparent 0%, rgba(180, 140, 110, 0.30) 14%, rgba(170, 130, 100, 0.78) 38%,
      #b08a6a 55%, #957254 60%, rgba(255, 220, 180, 0.85) 61%, #6a7898 62%, #4a5878 100%);
  }

  /* SKYLINE */
  .wh-skyline {
    position: absolute; left: 0; right: 0; bottom: 8%; height: 38%;
    display: flex; align-items: flex-end; justify-content: space-around; padding: 0 4%; z-index: 2;
  }
  .wh-bldg {
    position: relative; display: block; flex: 0 0 auto;
    width: clamp(40px, 9vw, 92px); height: 100%;
    text-decoration: none; color: inherit; transform: translateY(0);
    transition: transform 0.22s cubic-bezier(0.16, 0.84, 0.30, 1.00); will-change: transform;
  }
  .wh-bldg:hover, .wh-bldg:focus-visible, .wh-bldg.is-lit { transform: translateY(-5px); outline: none; }

  .wh-bldg__shape { position: absolute; inset: 0; display: block; }
  .wh-bldg__body, .wh-bldg__roof, .wh-bldg__sign, .wh-bldg__door {
    display: block; position: absolute; background: var(--room); box-shadow: 0 1px 0 rgba(0, 0, 0, 0.18);
  }

  .wh-bldg__night {
    position: absolute; inset: 0; background: rgba(15, 18, 28, 0);
    pointer-events: none; transition: background 4s ease-in-out; z-index: 5;
  }
  .wh-frame[data-tod="dusk"]  .wh-bldg__night { background: rgba(15, 18, 28, 0.32); }
  .wh-frame[data-tod="night"] .wh-bldg__night { background: rgba(15, 18, 28, 0.58); }
  .wh-frame[data-tod="dawn"]  .wh-bldg__night { background: rgba(15, 18, 28, 0.15); }

  .wh-bldg__lit {
    position: absolute; inset: 0; opacity: 0; pointer-events: none;
    transition: opacity 4s ease-in-out; z-index: 6;
    filter: drop-shadow(0 0 3px rgba(255, 213, 110, 0.55));
  }
  .wh-frame[data-tod="dusk"]  .wh-bldg__lit { opacity: 0.9; }
  .wh-frame[data-tod="night"] .wh-bldg__lit { opacity: 1; }
  .wh-frame[data-tod="dawn"]  .wh-bldg__lit { opacity: 0.3; }

  .wh-bldg--tower .wh-bldg__lit {
    background:
      radial-gradient(circle at 50% 52%, #ffd56a 0 2.5px, transparent 3.5px),
      radial-gradient(circle at 50% 66%, #ffd56a 0 2.5px, transparent 3.5px),
      radial-gradient(circle at 50% 80%, #ffd56a 0 2.5px, transparent 3.5px),
      radial-gradient(circle at 50% 94%, #ffd56a 0 2.5px, transparent 3.5px);
  }
  .wh-bldg--dome .wh-bldg__lit {
    background:
      radial-gradient(circle at 30% 80%, #ffd56a 0 3.5px, transparent 4.5px),
      radial-gradient(circle at 70% 80%, #ffd56a 0 3.5px, transparent 4.5px);
  }
  .wh-bldg--peak .wh-bldg__lit {
    background:
      radial-gradient(circle at 30% 55%, #ffd56a 0 2.5px, transparent 3.5px),
      radial-gradient(circle at 50% 55%, #ffd56a 0 2.5px, transparent 3.5px),
      radial-gradient(circle at 70% 55%, #ffd56a 0 2.5px, transparent 3.5px),
      radial-gradient(circle at 30% 76%, #ffd56a 0 2.5px, transparent 3.5px),
      radial-gradient(circle at 50% 76%, #ffd56a 0 2.5px, transparent 3.5px),
      radial-gradient(circle at 70% 76%, #ffd56a 0 2.5px, transparent 3.5px);
  }
  .wh-bldg--awning .wh-bldg__lit {
    background:
      radial-gradient(circle at 25% 60%, #ffd56a 0 3px, transparent 4px),
      radial-gradient(circle at 50% 60%, #ffd56a 0 3px, transparent 4px),
      radial-gradient(circle at 75% 60%, #ffd56a 0 3px, transparent 4px),
      radial-gradient(circle at 50% 92%, #ffeaa0 0 5px, transparent 6px);
  }
  .wh-bldg--steam .wh-bldg__lit {
    background:
      radial-gradient(circle at 50% 56%, #ffd56a 0 3.5px, transparent 4.5px),
      radial-gradient(circle at 50% 78%, #ffeaa0 0 5px, transparent 6px);
  }
  .wh-bldg--crt .wh-bldg__lit {
    background: radial-gradient(ellipse 26% 18% at 50% 68%, rgba(160, 215, 250, 0.75) 0 60%, transparent 100%);
  }
  .wh-bldg--house .wh-bldg__lit {
    background:
      radial-gradient(circle at 30% 80%, #ffd56a 0 3.5px, transparent 4.5px),
      radial-gradient(circle at 70% 80%, #ffd56a 0 3.5px, transparent 4.5px),
      radial-gradient(circle at 50% 95%, #ffeaa0 0 4.5px, transparent 5.5px);
  }

  .wh-bldg--tower .wh-bldg__body { left: 30%; right: 30%; bottom: 0; height: 70%; background: var(--room); }
  .wh-bldg--tower .wh-bldg__roof { left: 42%; right: 42%; bottom: 70%; height: 25%; background: var(--room); }
  .wh-bldg--tower .wh-bldg__sign {
    left: 48%; right: 48%; bottom: 95%; height: 12%; width: 4%;
    background: #fffaf0; border-radius: 50%; min-width: 4px; box-shadow: 0 0 6px rgba(255, 250, 240, 0.8);
  }
  .wh-bldg--tower .wh-bldg__door { display: none; }

  .wh-bldg--dome .wh-bldg__body { left: 10%; right: 10%; bottom: 0; height: 50%; }
  .wh-bldg--dome .wh-bldg__roof { left: 5%; right: 5%; bottom: 50%; height: 50%; border-radius: 50% 50% 0 0 / 100% 100% 0 0; }
  .wh-bldg--dome .wh-bldg__sign { left: 45%; right: 45%; bottom: 96%; height: 8%; width: 10%; background: var(--room); border-radius: 50%; min-width: 6px; }
  .wh-bldg--dome .wh-bldg__door { left: 40%; right: 40%; bottom: 0; height: 18%; background: rgba(0, 0, 0, 0.35); }

  .wh-bldg--peak .wh-bldg__body { left: 18%; right: 18%; bottom: 0; height: 70%; }
  .wh-bldg--peak .wh-bldg__roof {
    left: 8%; right: 8%; bottom: 70%; height: 22%; background: transparent;
    border-style: solid; border-color: transparent transparent var(--room) transparent;
    border-width: 0 calc(min(40px, 4vw)) min(28px, 3vw) calc(min(40px, 4vw)); box-shadow: none; width: auto;
  }
  .wh-bldg--peak .wh-bldg__sign { display: none; }
  .wh-bldg--peak .wh-bldg__door { left: 38%; right: 38%; bottom: 0; height: 20%; background: rgba(0, 0, 0, 0.35); }

  .wh-bldg--awning .wh-bldg__body { left: 8%; right: 8%; bottom: 0; height: 60%; }
  .wh-bldg--awning .wh-bldg__roof { left: 0; right: 0; bottom: 60%; height: 12%; background: repeating-linear-gradient(90deg, var(--room) 0 8px, #fff8e0 8px 16px); }
  .wh-bldg--awning .wh-bldg__sign { left: 12%; right: 12%; bottom: 72%; height: 22%; background: #fff8e0; }
  .wh-bldg--awning .wh-bldg__door { left: 40%; right: 40%; bottom: 0; height: 30%; background: rgba(0, 0, 0, 0.4); }

  .wh-bldg--steam .wh-bldg__body { left: 22%; right: 22%; bottom: 0; height: 65%; }
  .wh-bldg--steam .wh-bldg__roof { left: 15%; right: 15%; bottom: 65%; height: 10%; background: #5a3818; }
  .wh-bldg--steam .wh-bldg__sign {
    left: 30%; right: 30%; bottom: 80%; height: 18%;
    background:
      radial-gradient(ellipse at 30% 50%, rgba(255, 255, 255, 0.7) 0 5px, transparent 6px),
      radial-gradient(ellipse at 65% 70%, rgba(255, 255, 255, 0.55) 0 4px, transparent 5px),
      radial-gradient(ellipse at 50% 30%, rgba(255, 255, 255, 0.5) 0 4px, transparent 5px);
    background-color: transparent; box-shadow: none;
  }
  .wh-bldg--steam .wh-bldg__door { left: 42%; right: 42%; bottom: 0; height: 22%; background: rgba(0, 0, 0, 0.4); }

  .wh-bldg--crt .wh-bldg__body { left: 8%; right: 8%; bottom: 0; height: 55%; border-radius: 4px 4px 0 0; }
  .wh-bldg--crt .wh-bldg__roof { left: 18%; right: 18%; bottom: 12%; height: 38%; background: #b0d8e8; box-shadow: inset 0 0 0 2px rgba(0, 0, 0, 0.18); border-radius: 2px; }
  .wh-bldg--crt .wh-bldg__sign { left: 48%; right: 48%; bottom: 55%; height: 38%; width: 4%; background: var(--room); min-width: 3px; }
  .wh-bldg--crt .wh-bldg__door { display: none; }

  .wh-bldg--house .wh-bldg__body { left: 10%; right: 10%; bottom: 0; height: 50%; }
  .wh-bldg--house .wh-bldg__roof {
    left: 0; right: 0; bottom: 50%; height: 28%; background: transparent;
    border-style: solid; border-color: transparent transparent var(--room) transparent;
    border-width: 0 calc(min(46px, 4.6vw)) min(34px, 3.4vw) calc(min(46px, 4.6vw)); box-shadow: none;
  }
  .wh-bldg--house .wh-bldg__sign { left: 68%; right: 12%; bottom: 55%; height: 22%; background: var(--room); }
  .wh-bldg--house .wh-bldg__door { left: 42%; right: 42%; bottom: 0; height: 28%; background: rgba(0, 0, 0, 0.35); }

  /* Ambient animations */
  .wh-bldg--dome .wh-bldg__roof { transform-origin: 50% 100%; animation: wh-drum-pulse 2.4s ease-in-out infinite; }
  @keyframes wh-drum-pulse { 0%, 100% { transform: scale(1); } 50% { transform: scale(1.025); } }
  .wh-bldg--steam .wh-bldg__sign { transform-origin: 50% 100%; animation: wh-steam-rise 3.6s ease-in-out infinite; }
  @keyframes wh-steam-rise { 0%, 100% { transform: translateY(0) scale(1); opacity: 0.85; } 50% { transform: translateY(-6px) scale(1.08); opacity: 1; } }
  .wh-bldg--tower .wh-bldg__sign { animation: wh-wire-blink 2.8s ease-in-out infinite; }
  @keyframes wh-wire-blink { 0%, 100% { opacity: 0.4; box-shadow: 0 0 4px rgba(255, 250, 240, 0.4); } 50% { opacity: 1; box-shadow: 0 0 10px rgba(255, 250, 240, 1); } }
  .wh-bldg--crt .wh-bldg__roof { animation: wh-crt-flicker 4.2s ease-in-out infinite; }
  @keyframes wh-crt-flicker { 0%, 100% { opacity: 1; filter: brightness(1); } 47% { opacity: 0.93; filter: brightness(1.02); } 53% { opacity: 1; filter: brightness(0.98); } }
  .wh-bldg--house .wh-bldg__sign { transform-origin: 50% 100%; animation: wh-house-smoke 5s ease-in-out infinite; }
  @keyframes wh-house-smoke { 0%, 100% { transform: scaleY(1) translateY(0); opacity: 1; } 50% { transform: scaleY(1.15) translateY(-2px); opacity: 0.85; } }
  @media (prefers-reduced-motion: reduce) {
    .wh-bldg--dome .wh-bldg__roof, .wh-bldg--steam .wh-bldg__sign, .wh-bldg--tower .wh-bldg__sign,
    .wh-bldg--crt .wh-bldg__roof, .wh-bldg--house .wh-bldg__sign { animation: none !important; }
  }

  /* Tooltip — still present on the window for pointer users */
  .wh-bldg__tip {
    position: absolute; bottom: calc(100% + 6px); left: 50%;
    transform: translateX(-50%) translateY(4px);
    display: flex; flex-direction: column; align-items: center; gap: 2px;
    padding: 7px 10px; background: #fff8e0; border: 1.5px solid #1f1b15; border-radius: 3px;
    box-shadow: 2px 2px 0 rgba(31, 27, 21, 0.85); white-space: nowrap;
    opacity: 0; pointer-events: none; transition: opacity 0.18s ease, transform 0.18s ease; z-index: 10;
  }
  .wh-bldg:hover .wh-bldg__tip, .wh-bldg:focus-visible .wh-bldg__tip {
    opacity: 1; transform: translateX(-50%) translateY(0);
  }
  .wh-bldg__tip-label { font-family: var(--font-heading); font-size: 14px; font-weight: 600; color: #1f1b15; line-height: 1.1; }
  .wh-bldg__tip-sub { font-family: var(--font-mono); font-size: 10px; letter-spacing: 0.06em; color: #6b6256; line-height: 1.2; }

  /* ============== 3 · THE DOORS ============== */
  .wv-doors { max-width: 1080px; margin: 26px auto 0; }
  .wv-doors__kicker {
    text-align: center; font-size: 10px; letter-spacing: 0.22em; text-transform: uppercase;
    color: #6b6256; margin: 0 0 14px;
  }
  .wv-doors__grid {
    list-style: none; margin: 0; padding: 0;
    display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px;
  }
  /* Seven rooms in a 4-wide grid leaves the last cell empty; pull the
     7th (residents) to span the gap so the row stays balanced. */
  .wv-doors__grid > li:nth-child(7) { grid-column: span 1; }
  .wv-door {
    display: flex; align-items: center; gap: 11px;
    padding: 12px 13px; height: 100%;
    background: #fbf6ec; border: 1.5px solid #e4dac6; border-radius: 6px;
    text-decoration: none; color: inherit;
    transition: transform 0.16s ease, border-color 0.16s ease, box-shadow 0.16s ease, background 0.16s ease;
  }
  .wv-door:hover, .wv-door:focus-visible, .wv-door.is-lit {
    transform: translateY(-3px);
    border-color: var(--room);
    box-shadow: 0 6px 16px rgba(31, 27, 21, 0.12);
    background: #fffdf8; outline: none;
  }
  .wv-door__chip {
    flex: 0 0 auto; width: 14px; height: 28px; border-radius: 3px;
    background: var(--room); box-shadow: inset 0 -6px 0 rgba(0, 0, 0, 0.18);
  }
  .wv-door__text { display: flex; flex-direction: column; gap: 2px; min-width: 0; }
  .wv-door__label { font-family: var(--font-heading); font-size: 15px; font-weight: 600; color: #1f1b15; line-height: 1.1; }
  .wv-door__sub { font-size: 10.5px; letter-spacing: 0.03em; color: #7a715f; line-height: 1.25; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .wv-door__go { margin-left: auto; color: #c8bda6; font-size: 14px; transition: color 0.16s ease, transform 0.16s ease; }
  .wv-door:hover .wv-door__go, .wv-door:focus-visible .wv-door__go, .wv-door.is-lit .wv-door__go {
    color: var(--room); transform: translateX(2px);
  }

  /* ============== 4 · THE DESK ============== */
  .wv-desk { margin: 34px auto 0; max-width: 880px; text-align: center; padding-top: 22px; border-top: 1px dashed #d8cdb8; }
  .wv-desk__line { margin: 0 0 10px; font-family: var(--font-mono); font-size: 12px; letter-spacing: 0.06em; color: #1f1b15; line-height: 1.6; }
  .wv-desk__line strong { color: #c4952e; font-weight: 700; font-size: 14px; }
  .wv-desk__date { font-size: 10px; letter-spacing: 0.18em; color: #6b6256; text-transform: uppercase; }
  .wv-desk__sep { color: #c8bda6; margin: 0 4px; }
  .wv-desk__stat { white-space: nowrap; }
  .wv-desk__latest { margin: 6px 0 0; font-family: var(--font-serif); font-size: 14px; color: #6b6256; }
  .wv-desk__latest a { color: #1f1b15; text-decoration: none; border-bottom: 1px solid #d8cdb8; }
  .wv-desk__latest a:hover { color: #c4952e; border-bottom-color: currentColor; }
  .wv-desk__newhere { margin: 16px 0 0; font-family: var(--font-serif); font-size: 15px; color: #6b6256; }
  .wv-desk__newhere a { color: #1f1b15; text-decoration: none; border-bottom: 1px solid #c4952e; font-weight: 600; }
  .wv-desk__newhere a:hover { color: #c4952e; }

  .wv-exits { display: flex; flex-wrap: wrap; gap: 22px; justify-content: center; margin-top: 20px; }
  .wv-exit { font-size: 10px; letter-spacing: 0.18em; color: #6b6256; text-transform: uppercase; font-weight: 600; text-decoration: none; border-bottom: 1px solid currentColor; }
  .wv-exit:hover { color: #c4952e; }

  /* ============== RESPONSIVE ============== */
  @media (max-width: 720px) {
    .wv { padding: 18px 14px 48px; }
    .wh-frame { border-width: 8px; border-bottom-width: 10px; }
    .wh-skyline { padding: 0 2%; bottom: 3%; }
    .wh-bldg__tip { font-size: 11px; padding: 5px 8px; }
    .wh-bldg__tip-label { font-size: 12px; }
    .wh-bldg__tip-sub { display: none; }
    .wv-doors__grid { grid-template-columns: repeat(2, 1fr); }
    .wv-door__sub { white-space: normal; }
    .wv-desk__line { font-size: 11px; }
    .wv-desk__line strong { font-size: 12px; }
  }
  @media (max-width: 420px) {
    .wv-doors__grid { grid-template-columns: 1fr; }
  }

  @media (prefers-reduced-motion: reduce) {
    .wh-cloud, .wh-star, .wh-sky, .wh-sun, .wh-moon, .wh-bldg {
      animation: none !important; transition: none !important;
    }
  }
</style>
