---
/**
 * /nouns-nation-battler-v3 - federation desk for the Nouns Nation
 * automated league battler.
 */
import BlockLayout from '../layouts/BlockLayout.astro';
import { CHANNELS } from '../lib/channels';

const ch = CHANNELS.BTL;

const tickerItems = [
  'V3 frames the game as a federation desk, not just a match viewer',
  'The thought: make the sport watchable, make the receipts portable, make the door easy to enter',
  'Nations, teams, gangs, clubs, crews, DAOs, shops, schools, and local leagues can bring identity',
  'PointCast supplies the desk, schedule grammar, result envelope, and agent-readable rails',
  'Signed: Michael Hoydich x Codex 5.5 extra-high',
];

const thoughtCards = [
  {
    label: '01',
    title: 'Make the game legible first',
    body:
      'The field can stay chaotic. The desk should make the score, pressure, stakes, and next action understandable in one glance.',
  },
  {
    label: '02',
    title: 'Federate receipts, not vibes',
    body:
      'Every outside nation needs a stable manifest, a result envelope, and a public home link. Culture stays local; proof travels.',
  },
  {
    label: '03',
    title: 'Let identity enter sideways',
    body:
      'A nation can be a school, crew, shop, DAO, local league, art collective, or fandom. The intake shape should not overfit one social form.',
  },
  {
    label: '04',
    title: 'Agents become staff',
    body:
      'Codex, Claude, Manus, Cursor, and MCP agents should be able to scout, score, QA, package assets, and write handoff notes without a meeting.',
  },
];

const federationLanes = [
  {
    code: 'FOUND',
    title: 'Founding gangs',
    body: 'The eight built-in gangs become the house league: stable colors, marks, standings, and rivalry memory.',
  },
  {
    code: 'BRING',
    title: 'Imported nations',
    body: 'Outside groups arrive with a manifest, roster mode, colors, proof note, and the event level they want to try.',
  },
  {
    code: 'CAST',
    title: 'Broadcast desks',
    body: 'Every nation can get a desk surface, a TV cut, and shareable cards without needing to fork the whole game.',
  },
  {
    code: 'CUP',
    title: 'Opt-in cups',
    body: 'The first federation events should be small: exhibitions, rivalry nights, cups, and one clear bowl.',
  },
];

const operatorLoops = [
  { stamp: 'WATCH', title: 'Observe the slate', note: 'Use live snapshots to call match pressure, field type, and top Nouns.' },
  { stamp: 'WRITE', title: 'Turn match into receipt', note: 'Publish result cards, desk snapshots, and CH.BTL blocks for the archive.' },
  { stamp: 'INVITE', title: 'Open the intake lane', note: 'Point people to the manifest shape before promising a custom backend.' },
  { stamp: 'FEDERATE', title: 'Schedule the right event', note: 'Start with exhibition and cup formats, then graduate stable nations.' },
];

const outputLinks = [
  { label: 'Hub', title: 'Nouns Nation', href: '/nouns-nation/' },
  { label: 'V2', title: 'Battle Desk V2', href: '/nouns-nation-battler-v2/' },
  { label: 'Strategy', title: 'Federation', href: '/nouns-nation/federation/' },
  { label: 'Intake', title: 'Bring a Nation', href: '/nouns-nation/join/' },
  { label: 'Capital', title: 'Investment Thesis', href: '/investment-thesis' },
  { label: 'Agents', title: 'Sideline Desk', href: '/nouns-nation-battler-agents/desk/' },
  { label: 'Data', title: 'Nation JSON', href: '/nouns-nation.json' },
  { label: 'Game', title: 'Battler JSON', href: '/nouns-nation-battler.json' },
];

const fallbackTeams = [
  { short: 'TN', name: 'Tomato Noggles', alive: 30, color: '#e45745' },
  { short: 'CF', name: 'Cobalt Frames', alive: 30, color: '#3677e0' },
];

const signature = {
  line: 'Signed Michael Hoydich x Codex 5.5 extra-high',
  place: 'El Segundo, California',
  date: '2026-04-29',
  posture:
    'Build the sport as a public desk first. Then let people bring their own nations, teams, gangs, clubs, crews, DAOs, schools, shops, and leagues into the schedule when their receipts are legible.',
};

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'VideoGame',
  name: 'PointCast Battle Desk V3: Nouns Nation Federation Desk',
  description:
    'A federation-minded V3 control room for Nouns Nation Battler with live match feed, operating thought, signed posture, and bring-your-own-nation rails.',
  url: 'https://pointcast.xyz/nouns-nation-battler-v3/',
  gamePlatform: 'Web browser',
  genre: 'Auto battler',
  inLanguage: 'en-US',
};
---

<BlockLayout
  title="PointCast Battle Desk V3"
  description="A federation-minded V3 desk for Nouns Nation Battler: live match feed, portable receipts, bring-your-own-nation rails, and a signed Michael Hoydich x Codex 5.5 extra-high operating thought."
  image="/images/og/battle.png"
  jsonLd={jsonLd}
  alternates={[
    { type: 'application/json', href: '/nouns-nation.json', title: 'Nouns Nation federation manifest (JSON)' },
    { type: 'application/json', href: '/nouns-nation-battler.json', title: 'Nouns Nation Battler manifest (JSON)' },
  ]}
  frame={{
    image: 'https://pointcast.xyz/images/og/battle.png',
    buttons: [
      { label: 'Watch V3', action: 'link', target: 'https://pointcast.xyz/nouns-nation-battler-v3/' },
      { label: 'Nouns Nation', action: 'link', target: 'https://pointcast.xyz/nouns-nation/' },
      { label: 'Bring a Nation', action: 'link', target: 'https://pointcast.xyz/nouns-nation/join/' },
      { label: 'Nation JSON', action: 'link', target: 'https://pointcast.xyz/nouns-nation.json' },
    ],
  }}
>
  <main
    class="battle-v3"
    data-battle-v3
    style={`--btl: ${ch.color600}; --btl-dark: ${ch.color800}; --btl-soft: ${ch.color50}; --left-team: ${fallbackTeams[0].color}; --right-team: ${fallbackTeams[1].color}; --left-share: 50%; --right-share: 50%;`}
  >
    <nav class="crumb" aria-label="Breadcrumb">
      <a href="/">Home</a>
      <span aria-hidden="true">/</span>
      <a href="/nouns-nation/">Nouns Nation</a>
      <span aria-hidden="true">/</span>
      <a href="/nouns-nation-battler-v2/">Battle Desk V2</a>
      <span aria-hidden="true">/</span>
      <span>V3</span>
    </nav>

    <section class="signal-bar" aria-label="V3 ticker">
      <div class="signal-bar__brand">
        <span>PointCast BTL</span>
        <strong>Federation Desk V3</strong>
      </div>
      <div class="signal-bar__ticker">
        <span data-live-field="state">LIVE</span>
        <div>
          <p>
            {tickerItems.concat(tickerItems).map((item) => <span>{item}</span>)}
          </p>
        </div>
      </div>
      <a href="/nouns-nation/join/">Bring</a>
    </section>

    <section class="hero" aria-labelledby="battle-v3-title">
      <div class="hero__copy">
        <p class="kicker">NEXT VERSION / FEDERATION THOUGHT</p>
        <h1 id="battle-v3-title">Battle Desk V3 is the league operating room.</h1>
        <p>
          The thought is simple: ESPN-style watchability is the front door, but federation is the
          long game. Give every match a clean desk, every result a portable receipt, and every outside
          nation a way to show up without losing its own identity.
        </p>
        <nav class="hero__actions" aria-label="Battle Desk V3 actions">
          <a class="primary" href="#live">Watch live</a>
          <a href="/nouns-nation/federation/">Read strategy</a>
          <a href="/nouns-nation/join/">Bring a nation</a>
        </nav>
      </div>

      <aside class="signature" aria-label="Signed thought">
        <p class="kicker">Operator signature</p>
        <strong>{signature.line}</strong>
        <p>{signature.posture}</p>
        <span>{signature.place} / {signature.date}</span>
      </aside>
    </section>

    <section class="scorebar" aria-label="Live matchup summary">
      <article>
        <span data-live-field="left-short">{fallbackTeams[0].short}</span>
        <strong data-live-field="left-name">{fallbackTeams[0].name}</strong>
        <em><b data-live-field="left-alive">{fallbackTeams[0].alive}</b> alive</em>
      </article>
      <div>
        <span data-live-field="league-line">Day 1 / Slate 1</span>
        <strong data-live-field="matchup">Nouns Nation Battler</strong>
        <i><span data-live-field="left-pressure">50%</span><span data-live-field="right-pressure">50%</span></i>
      </div>
      <article>
        <span data-live-field="right-short">{fallbackTeams[1].short}</span>
        <strong data-live-field="right-name">{fallbackTeams[1].name}</strong>
        <em><b data-live-field="right-alive">{fallbackTeams[1].alive}</b> alive</em>
      </article>
    </section>

    <section class="controls" aria-label="Desk controls">
      <button type="button" data-desk-command="newMatch">Next</button>
      <button type="button" data-desk-command="quickSim">Quick Sim</button>
      <button type="button" data-desk-command="simDay">Sim Day</button>
      <button type="button" data-desk-command="togglePause">Pause</button>
      <button type="button" data-desk-command="setAutoNext">Auto Next</button>
      <button type="button" data-desk-command="setSpeed" data-speed="0.75">Slow</button>
      <button type="button" class="is-active" data-desk-command="setSpeed" data-speed="1">Live</button>
      <button type="button" data-desk-command="setSpeed" data-speed="1.55">Rush</button>
      <button type="button" data-desk-command="root" data-team="0">Root Left</button>
      <button type="button" data-desk-command="root" data-team="1">Root Right</button>
    </section>

    <section id="live" class="live-grid" aria-label="Live Federation Desk">
      <section class="field-panel" aria-label="Embedded match field">
        <header class="section-head">
          <div>
            <p class="kicker">Main feed</p>
            <h2 data-live-field="control-copy">Live engine locked</h2>
          </div>
          <span data-live-field="field">Open Field</span>
        </header>
        <iframe
          id="battleFrameV3"
          src="about:blank"
          data-battle-src="/games/nouns-nation-battler/index.html"
          title="Nouns Nation Battler live field feed"
          loading="eager"
          allow="fullscreen"
        ></iframe>
      </section>

      <aside class="desk-panel" aria-label="Live desk readout">
        <section class="desk-card desk-card--hot">
          <p class="kicker">Match thought</p>
          <h2 data-live-field="analyst-headline">Even field</h2>
          <p data-live-field="challenge">
            Season challenge loading from the match engine.
          </p>
          <div class="mini-grid">
            <span><b data-live-field="match">1</b> match</span>
            <span><b data-live-field="total-live">60</b> live</span>
            <span><b data-live-field="speed">Live</b> speed</span>
            <span><b data-live-field="weather">Clear</b> weather</span>
          </div>
        </section>

        <section class="desk-card">
          <p class="kicker">Top Nouns</p>
          <div class="leader-board" data-live-field="top-nouns">
            <article>
              <img src="/games/nouns-nation-battler/assets/noun-0.svg" alt="" loading="lazy" />
              <span>#0 / pregame</span>
              <strong>Noun on deck</strong>
              <em>0 dmg / 0 KO</em>
            </article>
          </div>
        </section>

        <section class="desk-card">
          <p class="kicker">Replay calls</p>
          <div class="replay-log" data-live-field="log">
            <p>Waiting for first push.</p>
          </div>
        </section>
      </aside>
    </section>

    <section class="thought-board" aria-labelledby="thought-title">
      <div class="section-head">
        <div>
          <p class="kicker">The thought</p>
          <h2 id="thought-title">Federation starts as product clarity.</h2>
        </div>
      </div>
      <div class="thought-grid">
        {thoughtCards.map((card) => (
          <article>
            <span>{card.label}</span>
            <strong>{card.title}</strong>
            <p>{card.body}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="federation-lanes" aria-labelledby="lanes-title">
      <div class="section-head">
        <div>
          <p class="kicker">Federation lanes</p>
          <h2 id="lanes-title">Who gets to enter the room?</h2>
        </div>
        <a href="/nouns-nation/join/">Open intake</a>
      </div>
      <div class="lane-grid">
        {federationLanes.map((lane) => (
          <article>
            <span>{lane.code}</span>
            <strong>{lane.title}</strong>
            <p>{lane.body}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="operator-loop" aria-labelledby="loop-title">
      <div>
        <p class="kicker">Producer loop</p>
        <h2 id="loop-title">Run the league like a public newsroom.</h2>
        <p>
          V3 treats every match as source material. Watch the field, write the receipt,
          invite the next nation, and only then expand the event format.
        </p>
      </div>
      <div class="loop-list">
        {operatorLoops.map((loop) => (
          <article>
            <span>{loop.stamp}</span>
            <strong>{loop.title}</strong>
            <p>{loop.note}</p>
          </article>
        ))}
      </div>
    </section>

    <section class="outputs" aria-labelledby="outputs-title">
      <div class="section-head">
        <div>
          <p class="kicker">Desk outputs</p>
          <h2 id="outputs-title">Where the next click goes.</h2>
        </div>
      </div>
      <div class="output-grid">
        {outputLinks.map((link) => (
          <a href={link.href}>
            <span>{link.label}</span>
            <strong>{link.title}</strong>
          </a>
        ))}
      </div>
    </section>
  </main>
</BlockLayout>

<script is:inline>
(function () {
  'use strict';

  var SOURCE = 'pointcast:nouns-nation-battler';
  var CONTROL_SOURCE = 'pointcast:battle-desk';
  var desk = document.querySelector('[data-battle-v3]');
  var frame = document.getElementById('battleFrameV3');
  if (!desk || !frame) return;

  function find(selector) {
    return desk.querySelector(selector);
  }

  function setText(selector, value) {
    var node = find(selector);
    if (node) node.textContent = value;
  }

  function escapeHtml(value) {
    return String(value == null ? '' : value).replace(/[&<>"']/g, function (char) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char];
    });
  }

  function assetPath(asset) {
    if (!asset) return '/games/nouns-nation-battler/assets/noun-0.svg';
    if (/^https?:\/\//.test(asset) || asset.charAt(0) === '/') return asset;
    return '/games/nouns-nation-battler/' + asset.replace(/^\.?\//, '');
  }

  function postCommand(command, extra) {
    if (!frame.contentWindow) return;
    frame.contentWindow.postMessage(Object.assign({
      source: CONTROL_SOURCE,
      type: 'command',
      command: command,
    }, extra || {}), window.location.origin);
  }

  function speedLabel(speed) {
    if (speed >= 1.4) return 'Rush';
    if (speed <= 0.8) return 'Slow';
    return 'Live';
  }

  function scoreUnit(unit) {
    var stats = unit && unit.stats ? unit.stats : {};
    return Number(stats.damage || 0) + Number(stats.kos || 0) * 34 + Number(stats.heals || 0) * 0.12;
  }

  function renderTopNouns(leaders) {
    var node = find('[data-live-field="top-nouns"]');
    if (!node) return;
    var rows = []
      .concat((leaders && leaders.left) || [])
      .concat((leaders && leaders.right) || [])
      .sort(function (a, b) { return scoreUnit(b) - scoreUnit(a); })
      .slice(0, 6);

    if (!rows.length) return;
    node.innerHTML = rows.map(function (unit, index) {
      var stats = unit.stats || {};
      return '<article>' +
        '<img src="' + escapeHtml(assetPath(unit.asset)) + '" alt="" loading="lazy" />' +
        '<span>' + String(index + 1) + ' / #' + escapeHtml(unit.number || '?') + ' / ' + escapeHtml(unit.role || 'noun') + '</span>' +
        '<strong>' + escapeHtml(unit.name || 'Noun') + '</strong>' +
        '<em>' + escapeHtml(stats.damage || 0) + ' dmg / ' + escapeHtml(stats.kos || 0) + ' KO</em>' +
      '</article>';
    }).join('');
  }

  function renderLog(logs) {
    var node = find('[data-live-field="log"]');
    if (!node || !logs || !logs.length) return;
    node.innerHTML = logs.slice(0, 7).map(function (line) {
      return '<p>' + escapeHtml(line) + '</p>';
    }).join('');
  }

  function renderSnapshot(payload) {
    if (!payload || !payload.gangs || payload.gangs.length < 2) return;

    var leftGang = payload.gangs[0];
    var rightGang = payload.gangs[1];
    var left = payload.alive ? Number(payload.alive.left || 0) : 30;
    var right = payload.alive ? Number(payload.alive.right || 0) : 30;
    var total = Math.max(1, left + right);
    var leftShare = (left / total) * 100;
    var rightShare = 100 - leftShare;
    var speed = Number(payload.speed || 1);
    var stateLabel = payload.finished ? 'FINAL' : (payload.running ? 'LIVE' : 'PAUSED');
    var fieldName = payload.field && payload.field.boss ? payload.field.boss : ((payload.field && payload.field.name) || 'Open Field');
    var pressureLabel = Math.abs(left - right) < 2
      ? 'Even field'
      : (left > right ? leftGang.short + ' controls tempo' : rightGang.short + ' controls tempo');
    var challenge = payload.challenge
      ? payload.challenge.name + ': ' + payload.challenge.line + '. ' + payload.challenge.rule
      : 'Challenge loading from the match engine.';
    var weather = payload.weather && payload.weather.name ? payload.weather.name : (payload.weather || 'Clear');

    desk.style.setProperty('--left-team', leftGang.color || '#e45745');
    desk.style.setProperty('--right-team', rightGang.color || '#3677e0');
    desk.style.setProperty('--left-share', leftShare.toFixed(1) + '%');
    desk.style.setProperty('--right-share', rightShare.toFixed(1) + '%');

    setText('[data-live-field="state"]', stateLabel);
    setText('[data-live-field="match"]', String(payload.match || 1));
    setText('[data-live-field="left-short"]', leftGang.short || 'L');
    setText('[data-live-field="left-name"]', leftGang.name || 'Left Gang');
    setText('[data-live-field="left-alive"]', String(left));
    setText('[data-live-field="left-pressure"]', Math.round(leftShare) + '%');
    setText('[data-live-field="right-short"]', rightGang.short || 'R');
    setText('[data-live-field="right-name"]', rightGang.name || 'Right Gang');
    setText('[data-live-field="right-alive"]', String(right));
    setText('[data-live-field="right-pressure"]', Math.round(rightShare) + '%');
    setText('[data-live-field="league-line"]', payload.league && payload.league.line ? payload.league.line : 'League loading');
    setText('[data-live-field="matchup"]', payload.league && payload.league.matchup ? payload.league.matchup : 'Nouns Nation Battler');
    setText('[data-live-field="field"]', fieldName);
    setText('[data-live-field="control-copy"]', stateLabel === 'LIVE' ? 'Live engine locked' : 'Engine hold');
    setText('[data-live-field="analyst-headline"]', pressureLabel);
    setText('[data-live-field="challenge"]', challenge);
    setText('[data-live-field="total-live"]', String(total));
    setText('[data-live-field="speed"]', speedLabel(speed));
    setText('[data-live-field="weather"]', weather);

    Array.prototype.forEach.call(desk.querySelectorAll('[data-speed]'), function (button) {
      button.classList.toggle('is-active', Number(button.dataset.speed) === speed);
    });

    renderTopNouns(payload.leaders);
    renderLog(payload.logs);
  }

  desk.addEventListener('click', function (event) {
    var target = event.target;
    if (!target || !target.closest) return;
    var button = target.closest('[data-desk-command]');
    if (!button || !desk.contains(button)) return;
    var command = button.dataset.deskCommand;

    if (command === 'setSpeed') {
      postCommand(command, { value: Number(button.dataset.speed || 1) });
    } else if (command === 'root') {
      postCommand(command, { team: Number(button.dataset.team || 0) });
    } else {
      postCommand(command);
    }
  });

  window.addEventListener('message', function (event) {
    if (event.origin !== window.location.origin) return;
    var message = event.data || {};
    if (message.source !== SOURCE || message.type !== 'snapshot') return;
    renderSnapshot(message.payload);
  });

  frame.addEventListener('load', function () {
    window.setTimeout(function () {
      postCommand('snapshot');
    }, 350);
  });

  try {
    localStorage.setItem('pc:nouns-nation-guide-v1', 'seen');
  } catch (error) {
    /* LocalStorage can be unavailable in hardened browsers. The field still loads. */
  }

  var battleSrc = frame.getAttribute('data-battle-src');
  if (battleSrc && frame.getAttribute('src') !== battleSrc) {
    frame.setAttribute('src', battleSrc);
  }

  window.setInterval(function () {
    postCommand('snapshot');
  }, 2500);
})();
</script>

<style>
  .battle-v3 {
    --paper: #f7f2e8;
    --ink: #101216;
    --muted: #5e6470;
    --line: rgba(16, 18, 22, 0.16);
    --panel: #fffdf8;
    --mint: #d8f4df;
    --blue: #dbeafe;
    --amber: #ffe9ad;
    min-height: 100vh;
    padding: 16px clamp(12px, 2.5vw, 30px) 42px;
    color: var(--ink);
    background:
      linear-gradient(90deg, rgba(16, 18, 22, 0.04) 1px, transparent 1px),
      linear-gradient(rgba(16, 18, 22, 0.04) 1px, transparent 1px),
      radial-gradient(circle at 12% 4%, rgba(216, 244, 223, 0.95), transparent 28%),
      radial-gradient(circle at 92% 14%, rgba(255, 233, 173, 0.85), transparent 30%),
      var(--paper);
    background-size: 24px 24px, 24px 24px, auto, auto, auto;
  }

  .battle-v3 * {
    box-sizing: border-box;
  }

  .battle-v3 a {
    color: inherit;
    text-decoration: none;
  }

  .battle-v3 p,
  .battle-v3 h1,
  .battle-v3 h2 {
    margin: 0;
  }

  .crumb,
  .kicker,
  .signal-bar,
  .controls,
  .scorebar span,
  .scorebar em,
  .mini-grid,
  .leader-board span,
  .leader-board em,
  .replay-log,
  .thought-grid span,
  .lane-grid span,
  .loop-list span,
  .output-grid span,
  .signature span {
    font-family: var(--pc-font-mono, ui-monospace, monospace);
  }

  .crumb {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
    margin: 0 0 10px;
    color: var(--muted);
    font-size: 0.78rem;
  }

  .signal-bar,
  .signature,
  .scorebar,
  .controls,
  .field-panel,
  .desk-card,
  .thought-grid article,
  .lane-grid article,
  .operator-loop,
  .output-grid a {
    border: 2px solid var(--ink);
    background: var(--panel);
    box-shadow: 4px 4px 0 var(--ink);
  }

  .signal-bar {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr) auto;
    align-items: center;
    min-height: 56px;
    background: #151922;
    color: #fffaf0;
    overflow: hidden;
  }

  .signal-bar__brand {
    display: grid;
    align-content: center;
    min-height: 56px;
    padding: 8px 14px;
    background: var(--btl);
    text-transform: uppercase;
  }

  .signal-bar__brand span {
    font-size: 0.64rem;
    letter-spacing: 0.14em;
  }

  .signal-bar__brand strong {
    font-size: 1.02rem;
    line-height: 1;
  }

  .signal-bar__ticker {
    min-width: 0;
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);
    align-items: center;
    gap: 12px;
    padding: 0 12px;
  }

  .signal-bar__ticker > span {
    display: inline-flex;
    align-items: center;
    gap: 7px;
    font-size: 0.72rem;
    letter-spacing: 0.12em;
  }

  .signal-bar__ticker > span::before {
    content: "";
    width: 9px;
    height: 9px;
    border-radius: 999px;
    background: #ff4d5f;
    box-shadow: 0 0 0 3px rgba(255, 77, 95, 0.18);
  }

  .signal-bar__ticker div {
    overflow: hidden;
  }

  .signal-bar__ticker p {
    display: inline-flex;
    min-width: max-content;
    gap: 36px;
    color: #f8eeca;
    font-size: 0.78rem;
    animation: ticker 52s linear infinite;
  }

  .signal-bar > a {
    margin-right: 12px;
    padding: 8px 10px;
    border: 1px solid rgba(255, 250, 240, 0.5);
    font-size: 0.72rem;
    text-transform: uppercase;
  }

  .hero {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(320px, 0.46fr);
    gap: 18px;
    align-items: stretch;
    margin: 18px 0;
  }

  .hero__copy {
    min-width: 0;
    padding: clamp(18px, 3vw, 36px) 0;
  }

  .kicker {
    color: var(--btl-dark);
    font-size: 0.72rem;
    font-weight: 700;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  h1 {
    max-width: 960px;
    margin-top: 8px;
    font-size: clamp(2.6rem, 7vw, 6.8rem);
    line-height: 0.9;
  }

  .hero__copy > p {
    max-width: 820px;
    margin-top: 16px;
    color: #2b3038;
    font-size: clamp(1rem, 1.35vw, 1.25rem);
    line-height: 1.5;
  }

  .hero__actions {
    display: flex;
    gap: 10px;
    flex-wrap: wrap;
    margin-top: 22px;
  }

  .hero__actions a,
  .section-head > a {
    min-height: 40px;
    display: inline-flex;
    align-items: center;
    border: 2px solid var(--ink);
    background: #fff;
    box-shadow: 3px 3px 0 var(--ink);
    font-weight: 800;
    padding: 9px 12px;
  }

  .hero__actions .primary,
  .section-head > a {
    background: var(--amber);
  }

  .signature {
    display: grid;
    align-content: end;
    gap: 12px;
    min-height: 360px;
    padding: 20px;
    background:
      linear-gradient(135deg, rgba(16, 18, 22, 0.08), transparent 42%),
      #fef8df;
  }

  .signature strong {
    font-size: clamp(1.8rem, 3vw, 3rem);
    line-height: 0.94;
  }

  .signature p {
    color: #2c313a;
    line-height: 1.45;
  }

  .signature span {
    color: var(--muted);
    font-size: 0.76rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }

  .scorebar {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(280px, 0.7fr) minmax(0, 1fr);
    margin-bottom: 12px;
    overflow: hidden;
  }

  .scorebar article {
    min-width: 0;
    display: grid;
    gap: 4px;
    padding: 14px;
  }

  .scorebar article:first-child {
    border-left: 14px solid var(--left-team);
  }

  .scorebar article:last-child {
    border-right: 14px solid var(--right-team);
    text-align: right;
  }

  .scorebar span {
    color: var(--muted);
    font-size: 0.72rem;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .scorebar strong {
    overflow-wrap: anywhere;
    font-size: clamp(1.28rem, 2.1vw, 2rem);
    line-height: 1;
  }

  .scorebar em {
    color: var(--muted);
    font-size: 0.75rem;
    font-style: normal;
    text-transform: uppercase;
  }

  .scorebar b {
    color: var(--ink);
    font-size: 1rem;
  }

  .scorebar > div {
    display: grid;
    align-content: center;
    gap: 8px;
    padding: 14px;
    border-left: 2px solid var(--ink);
    border-right: 2px solid var(--ink);
    background: var(--blue);
    text-align: center;
  }

  .scorebar i {
    display: grid;
    grid-template-columns: var(--left-share) var(--right-share);
    height: 18px;
    border: 2px solid var(--ink);
    background: linear-gradient(90deg, var(--left-team), var(--right-team));
    font-size: 0;
  }

  .controls {
    display: flex;
    flex-wrap: wrap;
    gap: 8px;
    margin-bottom: 16px;
    padding: 10px;
    background: #fff9de;
  }

  button {
    min-height: 34px;
    border: 2px solid var(--ink);
    border-radius: 4px;
    background: #fff;
    color: var(--ink);
    box-shadow: 2px 2px 0 var(--ink);
    cursor: pointer;
    font: inherit;
    font-size: 0.82rem;
    font-weight: 700;
    padding: 7px 9px;
  }

  button:hover,
  button:focus-visible,
  button.is-active {
    background: var(--amber);
    outline: none;
  }

  .live-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.45fr) minmax(330px, 0.64fr);
    gap: 16px;
    align-items: start;
  }

  .section-head {
    display: flex;
    align-items: end;
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 14px;
  }

  .section-head h2,
  .desk-card h2,
  .operator-loop h2 {
    font-size: clamp(1.35rem, 2.3vw, 2.2rem);
    line-height: 0.98;
  }

  .field-panel {
    min-width: 0;
    overflow: hidden;
  }

  .field-panel .section-head {
    margin: 0;
    padding: 12px 14px;
    border-bottom: 2px solid var(--ink);
  }

  .field-panel iframe {
    display: block;
    width: 100%;
    height: min(760px, 80vh);
    min-height: 690px;
    border: 0;
    background: #f2eddc;
  }

  .desk-panel {
    display: grid;
    gap: 14px;
  }

  .desk-card {
    padding: 14px;
  }

  .desk-card--hot {
    border-top: 8px solid var(--btl);
  }

  .desk-card > p:not(.kicker) {
    margin-top: 10px;
    color: var(--muted);
    line-height: 1.45;
  }

  .mini-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 8px;
    margin-top: 14px;
  }

  .mini-grid span {
    display: grid;
    min-height: 58px;
    align-content: center;
    border: 1px solid var(--line);
    background: #fff;
    color: var(--muted);
    font-size: 0.72rem;
    padding: 8px;
    text-transform: uppercase;
  }

  .mini-grid b {
    color: var(--ink);
    font-size: 1.08rem;
  }

  .leader-board {
    display: grid;
    gap: 8px;
    margin-top: 10px;
  }

  .leader-board article {
    display: grid;
    grid-template-columns: 42px minmax(0, 1fr);
    grid-template-areas:
      "img meta"
      "img name"
      "img stat";
    gap: 2px 10px;
    align-items: center;
    border: 1px solid var(--line);
    background: #fff;
    padding: 8px;
  }

  .leader-board img {
    grid-area: img;
    width: 42px;
    height: 42px;
    image-rendering: pixelated;
  }

  .leader-board span {
    grid-area: meta;
    color: var(--muted);
    font-size: 0.66rem;
    text-transform: uppercase;
  }

  .leader-board strong {
    grid-area: name;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .leader-board em {
    grid-area: stat;
    color: var(--muted);
    font-size: 0.68rem;
    font-style: normal;
  }

  .replay-log {
    display: grid;
    gap: 7px;
    margin-top: 10px;
  }

  .replay-log p {
    border-left: 4px solid var(--btl);
    background: #fff;
    color: #343944;
    font-size: 0.78rem;
    line-height: 1.35;
    padding: 7px 8px;
  }

  .thought-board,
  .federation-lanes,
  .operator-loop,
  .outputs {
    margin-top: 22px;
  }

  .thought-grid,
  .lane-grid,
  .output-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 12px;
  }

  .thought-grid article,
  .lane-grid article {
    min-height: 210px;
    display: grid;
    align-content: start;
    gap: 12px;
    padding: 16px;
  }

  .thought-grid span,
  .lane-grid span,
  .loop-list span,
  .output-grid span {
    color: var(--btl-dark);
    font-size: 0.7rem;
    font-weight: 800;
    letter-spacing: 0.12em;
    text-transform: uppercase;
  }

  .thought-grid strong,
  .lane-grid strong {
    font-size: 1.22rem;
    line-height: 1;
  }

  .thought-grid p,
  .lane-grid p {
    color: var(--muted);
    line-height: 1.42;
  }

  .lane-grid article:nth-child(2),
  .thought-grid article:nth-child(2) {
    background: var(--mint);
  }

  .lane-grid article:nth-child(4),
  .thought-grid article:nth-child(4) {
    background: #eef2ff;
  }

  .operator-loop {
    display: grid;
    grid-template-columns: minmax(0, 0.56fr) minmax(0, 1fr);
    gap: 18px;
    padding: 18px;
    background: #151922;
    color: #fffaf0;
  }

  .operator-loop > div:first-child p:not(.kicker) {
    margin-top: 12px;
    color: #d8ddea;
    line-height: 1.45;
  }

  .operator-loop .kicker {
    color: #f6d365;
  }

  .loop-list {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 10px;
  }

  .loop-list article {
    min-height: 134px;
    display: grid;
    gap: 8px;
    align-content: start;
    border: 1px solid rgba(255, 250, 240, 0.26);
    background: rgba(255, 255, 255, 0.06);
    padding: 12px;
  }

  .loop-list span {
    color: #f6d365;
  }

  .loop-list strong {
    font-size: 1.05rem;
  }

  .loop-list p {
    color: #d8ddea;
    line-height: 1.38;
  }

  .output-grid a {
    min-height: 116px;
    display: grid;
    align-content: end;
    gap: 8px;
    padding: 12px;
  }

  .output-grid a:hover,
  .output-grid a:focus-visible {
    background: var(--amber);
    outline: none;
  }

  .output-grid strong {
    font-size: 1.2rem;
    line-height: 1;
  }

  @keyframes ticker {
    from { transform: translateX(0); }
    to { transform: translateX(-50%); }
  }

  @media (max-width: 980px) {
    .signal-bar,
    .hero,
    .scorebar,
    .live-grid,
    .operator-loop {
      grid-template-columns: 1fr;
    }

    .signal-bar {
      align-items: stretch;
    }

    .signal-bar__brand,
    .signal-bar > a {
      margin: 0;
    }

    .scorebar article:last-child {
      text-align: left;
      border-right: 0;
      border-left: 14px solid var(--right-team);
    }

    .scorebar > div {
      border-left: 0;
      border-right: 0;
      border-top: 2px solid var(--ink);
      border-bottom: 2px solid var(--ink);
    }

    .thought-grid,
    .lane-grid,
    .output-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }

  @media (max-width: 620px) {
    .battle-v3 {
      padding-inline: 10px;
    }

    .signal-bar__ticker {
      grid-template-columns: 1fr;
      padding-block: 10px;
    }

    h1 {
      font-size: clamp(2.35rem, 17vw, 4.2rem);
    }

    .signature {
      min-height: 260px;
    }

    .controls {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }

    button {
      width: 100%;
    }

    .field-panel iframe {
      min-height: 560px;
      height: 70vh;
    }

    .thought-grid,
    .lane-grid,
    .loop-list,
    .output-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
