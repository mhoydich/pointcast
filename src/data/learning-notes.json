{
  "$comment": "Hand-curated 'what we learned tonight' notes, surfaced on the homepage LEARNINGS band. Editorial signal that the project is improving even before there's traffic. Add new notes at top; cap at ~12 (older ones drop off the visible list but stay in the file for the record).",
  "updatedAt": "2026-05-08T22:00:00Z",
  "updatedBy": "cc",
  "notes": [
    {
      "id": "import-meta-glob-for-routes",
      "at": "2026-05-08",
      "title": "import.meta.glob is the right tool for build-time route discovery.",
      "body": "We had ~376 hand-curated places + ~9 routes that slipped through. Globbing src/pages/**/*.{astro,md,ts} at build time lets us diff against the curated set and surface unfiled routes automatically. Editorial shape stays opinionated; nothing slips."
    },
    {
      "id": "presence-do-room-key",
      "at": "2026-05-08",
      "title": "PresenceRoom DO accepts arbitrary room keys.",
      "body": "The walking bar is global (not per-page) so we open a WebSocket to /api/room?url=/__bar__ — synthetic key. The DO doesn't care it's not a real route. One line of client code, zero new server work, cross-page presence."
    },
    {
      "id": "no-per-frame-sync",
      "at": "2026-05-08",
      "title": "You don't need per-frame position sync for shared loops.",
      "body": "Cross-visitor walking bar: each peer's bar runs the same 60s CSS walk on its own clock with a randomized animation-delay. They look like they're moving together because the loop is identical, not because they broadcast. Cheap, no animation thrash."
    },
    {
      "id": "two-storage-keys-not-one",
      "at": "2026-05-08",
      "title": "Don't bloat one localStorage key when two will do.",
      "body": "pc:me:v1 (identity-and-day) and pc:inventory:v1 (gear) are separate. The /me reset button doesn't touch inventory — identity churn shouldn't wipe your gear list. Same for pc:pet:v1, pc:streak. Reset semantics drove the split."
    },
    {
      "id": "operating-mode-vs-ships",
      "at": "2026-05-08",
      "title": "Operating mode IN FLIGHT and RECENT SHIPS are different contracts.",
      "body": "Stale plans in operating-mode.json (\"studio publish hardening — queued\" still showing weeks after shipping) misled visitors. Now: open work lives in operating-mode.json, ships move out to recent-ships.json on merge. Two files, clean handoff."
    },
    {
      "id": "tailwind-v4-theme-vars",
      "at": "2026-05-07",
      "title": "Tailwind v4 @theme tokens don't auto-emit as runtime :root vars.",
      "body": "Bit us when community pills tried var(--color-ink) and got transparent. Fix: hardcode hex literals for components that need explicit theme values. Document the gotcha in code comments so the next agent doesn't re-discover it."
    },
    {
      "id": "cf-pages-detached-connector",
      "at": "2026-05-08",
      "title": "Detached CF Pages git connector still costs us 2-3 min per merge.",
      "body": "4 manual `wrangler pages deploy` tonight. Each deploy uploads ~1140 new files (the asset hashes change every build). Reconnecting the dashboard webhook is the one fix Mike actually has to do — agents can't fix it. Surfaced as INCIDENT in operating-mode."
    },
    {
      "id": "right-click-as-affordance",
      "at": "2026-05-08",
      "title": "Right-click is a real affordance if you label it.",
      "body": "Pet adoption uses right-click on a companion. Two things made it discoverable: (1) the title attribute says \"+cat · right-click to keep as pet\", (2) a toast confirms when you do it. Without the label nobody finds it; with the label it feels secret-but-fair."
    }
  ]
}
