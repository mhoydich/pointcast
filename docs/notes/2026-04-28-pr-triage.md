# Open PR triage · 2026-04-28

**Author:** cc · **Read-only.** No closures or merges executed by cc — flagging for Mike's review on return.

10 PRs open. Grouped by recommendation.

---

## Active — keep open, awaiting Mike review

### #58 · `codex/agent-derby-v3` · feat(agent-derby): v3 — stables, fandom, daily races, badges, Tezos receipt
- **Author:** Codex (filed by mhoydich)
- **Age:** 4 days, +1,567/-45, 6 files
- **Status:** Codex tagged "do not auto-merge — review." All build + QA passes per Codex's body.
- **Surface:** new daily Derby module on home, expanded stables/racers, Tezos receipt v0.5.0-v3 wiring.
- **cc action:** Left a [surfacing comment](https://github.com/mhoydich/pointcast/pull/58#issuecomment-4331600369) walking through the merge plan + risk.
- **Recommendation:** **review + merge**. Highest-priority pending review. Largest stale PR by velocity-of-fresh-context.

### #126 · `cc/marketplace-launch-docs` · docs(launch): marketplace press release + channel matrix + 4-week cadence
- **Author:** cc
- **Age:** 2 days, +305/-0, docs only
- **Status:** Press release was already updated to v3 KT1 in PR #146 (merged 2026-04-26). The channel matrix at `docs/launch/2026-04-26-free-publishing-channels.md` may overlap with my D1-1 day-1 launch posts at `docs/launch/2026-04-27-day1-posts/`.
- **Recommendation:** **review for overlap, then merge or close**. If the channel matrix is broader-cadence (4-week) it's complementary to my day-1 set. If it duplicates, close in favor of the merged work.

### #130 · `cc/drawing-room` · feat(drawing-room): /drawing-room side pathway with two front-door drafts
- **Author:** cc
- **Age:** 2 days, +3,740/-0
- **Status:** New room concept. Large additive PR. cc has not merged because the front-door drafts are alternates that should get a Mike-eyes pass.
- **Recommendation:** **review on Mike's return**. Could be a "land it as preview-only at `/drawing-room`" merge; the home-page front-door variants are the touchier piece.

---

## Superseded by merged work — close as obsolete

### #133 · `feat/window-snapshots-and-market-mine` · feat(market): Window Snapshots collection + Your tokens panel
- **Author:** mhoydich
- **Age:** 2 days, +640/-27
- **Status:** **Almost certainly superseded.** "Window Snapshots collection" matches what Day 2's `/snapshots` (PR #161) and `/minted` (PR #162) shipped. "Your tokens panel" is what Codex's `/marketplace` already has + what my D3 cross-link (PR #169) now also surfaces. The contracts.json scaffold for `window_snapshots` is already in main.
- **Recommendation:** **close as obsolete**. Worth a 1-min diff scan first to make sure no unique scaffold is in this PR that didn't make it through the merged work — if any contract or metadata-endpoint code hasn't shipped elsewhere, cherry-pick it before closing.

### #138 · `chore/marketplace-v3-and-safety-net` · marketplace v3 + pre-sign layout safety net + Your tokens panel + Window Snapshots scaffold
- **Author:** mhoydich
- **Age:** 2 days, +882/-39
- **Status:** **Definitely superseded.** v3 is live on mainnet, pre-sign safety check landed in PR #135 / commit f71225b, "Your tokens panel" is on `/market` now, Window Snapshots scaffold is on `/snapshots` + `/minted`. Every named deliverable in the title is already shipped through other PRs.
- **Recommendation:** **close as obsolete**. The diff may have minor refinements; worth a quick scan but the headline work is all merged.

---

## Stale and likely abandoned — propose closing

### #1 · `manus/collab-paths-2026-04-21` · feat(manus): all seven collab paths
- **Author:** mhoydich (Manus-attributed)
- **Age:** 7 days untouched, +2,151/-1
- **Status:** Manus's framework PR for the seven collab paths. The actual collab work has been happening through individual PRs since (Codex landed gandalf, room, mythos, etc. directly). This umbrella PR is likely a victim of incremental merges rendering it duplicative.
- **Recommendation:** **propose close**. Confirm with Manus first via a comment — they may have referenced material here worth extracting before closing.

### #18 · `feat/race-system-rfc` · Race System — RFC 0002 + /race + /race/front-door (Phase 1)
- **Author:** mhoydich (Codex-touched)
- **Age:** 4 days, +779/-0
- **Status:** RFC + race system Phase 1. Race System Phase 2 already shipped (PR #46 per the handoff trail). Front Door is at `/race/front-door` and is live. RFC document may have been superseded by the actual implementation.
- **Recommendation:** **review the RFC vs. shipped state, then close or extract the RFC doc to `docs/rfcs/0002-race-system.md`**.

### #33 · `feat/rcs-collection-page` · feat(rcs): collection page for randomly common skeles
- **Author:** mhoydich
- **Age:** 4 days, +588/-0
- **Status:** RCS = Randomly Common Skeles, a Tezos collection. Collection page additive; no apparent conflict with current main but no recent activity.
- **Recommendation:** **review on Mike's return**. Either ship as-is, fold into the existing `/collection` surface, or close if no longer aligned with direction.

### #39 · `fix/visitor-intel-css` · fix(here-grid): add footprint + edge-chip CSS missed from visitor-intel
- **Author:** mhoydich
- **Age:** 4 days, +50/-0
- **Status:** Tiny CSS fix. Likely either still applies or has been silently fixed elsewhere.
- **Recommendation:** **fast review**. If the CSS classes still exist in `here-grid`, merge; if the visitor-intel feature has moved, close.

---

## Recently filed — fine, awaiting normal review

### #103 · `fix/manus-qa-2026-04-24` · fix(farm,race): hydrate ripe state + clean unicode escapes
- **Author:** mhoydich (Manus-QA-derived)
- **Age:** 3 days, +6/-3
- **Status:** Tiny fix from Manus QA findings. Low risk.
- **Recommendation:** **merge** if `/farm` and `/race` smoke-test clean.

---

## Summary recommendation matrix

| Action | PRs | Count |
|---|---|---|
| **Review + likely merge** | #58, #126, #130, #103 | 4 |
| **Close as superseded** | #133, #138 | 2 |
| **Triage + likely close** | #1, #18, #33, #39 | 4 |

If Mike spends 30 min on the triage queue, the 10-PR open list collapses to ~3-4 PRs (the actively-relevant ones), and the visual signal in the GitHub queue improves significantly.

---

## Process note

This triage is read-only by design. cc did not:
- Close any PR
- Comment on any PR (except the existing surfacing comment on #58 from earlier in the sprint)
- Merge any PR

Mike approves close decisions. The triage doc is the recommendation; the click is yours.
