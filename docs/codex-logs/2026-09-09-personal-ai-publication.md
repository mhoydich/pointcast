# Personal AI publication and live verification corrections

Mike approved publication after reviewing PR #1084. Its merge revision `b4d335eabeed4096902d205233508737b4f8ff89` was built in a clean temporary checkout and deployed to Cloudflare Pages as `8de8bdd1-7b38-4660-a0a7-4735df9dd72d`. The canonical `/me` and connector assets matched the generated build. The static build produced 2,155 pages; the Pages Functions bundle compiled; 223 focused tests passed.

Auth migrations 0015–0018 were applied to the configured production `AUTH_DB`. All four tables, eight explicit indexes, and four migration journal records were verified against the tested schema. The older reward migration was intentionally excluded: its objects already exist through guarded runtime provisioning, and its unconditional DDL must not be replayed just because its journal entry is absent. No unrelated reward records or migration journal entries were changed.

Live verification found two corrections:

- `/login/` returned 404 while `/login` redirected. Both GET and HEAD aliases now redirect to the same-origin `/auth` before directory rewriting, preserving the query string. Existing profile aliases retain their destinations.
- Multiple PointCast tabs can cause the existing wallet bridge to repeatedly confirm the same signed-in owner. The new AI and purchase panels previously aborted and restarted their initial read on every confirmation, remaining at “Checking.” The X panel had the same repeated invalidation path. They now ignore duplicate bridge confirmations for the same nonempty owner ID. Changed owners, sign-out, ordinary auth actions, malformed notifications, and explicit refresh still clear private state and invalidate pending work. X identity changes also refresh the connection view.

The repeated-event regressions failed before the UI fix. Fable then caught a fixture mismatch: the real session identifies its owner with `userId`. The follow-up uses that contract and includes recovery when the same owner returns after an unauthorized response or an initial network failure. The corrected AI and purchase controller suites passed 63/63, including real-shape repeated-event and recovery regressions. The redirect/admin/sitemap cases passed 16/16. The combined AI/auth/payment/routing suite passed 257/257 after the X correction. Fable confirmed both reported blockers resolved, and a separate read-only review found no X blocker. No real provider consent, new production pairing, wallet authorization, payment, or external message was performed. X credentials remain absent and new purchases remain disabled. Setup links and clone instructions use `main`.

A final concurrent-request review found that a successful save could arrive after another read rejected the session, restoring old private content. Before the final upload, both AI controllers were changed so an authoritative 401 invalidates and aborts all pending work, clears the owner and private state, and cancels pending wallet approval. Held-response regression tests cover that boundary as well as restoration of the same signed-in owner. The final combined suite passed 260/260; both controller suites passed 66/66. An independent review replayed the original late-save race and a stale old-owner 401 against the corrected modules, and both passed.

The final deployment and canonical browser verification receipt is retained with the release handoff. Temporary dependencies, build output, and checkouts are removed after verified publication.
