# Sprint — Gamgee RC0

**Opened:** 2026-04-23  
**Release:** Gamgee  
**Purpose:** Turn PointCast's current multi-agent sprawl into a named release candidate.

## Rules

1. Do not add new feature scope unless it directly clears a Gamgee gate.
2. Treat `cc/sprints-1-6-publish` as the release train to inventory, not as random dirt.
3. Keep all agent handoffs in GitHub issues, PRs, or repo docs.
4. Prefer tiny PRs with clear ownership.
5. Do not deploy contracts, update billing, or publish production outside normal approval.

## Checklist

- [x] Create release anchor at `docs/releases/gamgee.md`. (Codex, e4b27ea)
- [x] Inventory `cc/sprints-1-6-publish` into Gamgee / later / needs Mike / drop. (Codex, 83bfacf, `docs/releases/gamgee-inventory.md`)
- [x] Add agent surface + publishing audits so `CLAUDE.md` references real commands. (Codex, e13b2ed)
- [x] Decide whether the public front door is `/gamgee` or an `/agent-native` refresh. **Decision: `/gamgee`.** `/agent-native` deferred — does not exist on main and inventory places it in "Later."
- [x] Draft the public front-door copy. (Claude Code, `src/pages/gamgee.astro`)
- [x] Run first health pass over `/`, `/for-agents`, `/agents.json`, `/llms.txt`, `/sparrow`, `/magpie`, `/workbench`, `/gamgee`. (Claude Code, `npm run build:bare` + `audit:agents` + `audit:publishing` all green; 432 pages built; 0 new warnings from /gamgee.)
- [ ] Confirm Claude can run from GitHub Actions after Anthropic API credits land. **Retry queued on Issue #7** (comment 4310644735); fires automatically when credits propagate.
- [ ] Get one Claude-authored or Claude-recommended low-risk cleanup. (Blocked on credits.)
- [ ] Codex review the Claude cleanup. (Blocked on preceding item.)
- [ ] Capture Manus/Codex/Claude bridge status in a dated log at `docs/manus-logs/`. (Blocked on preceding item.)

## Current Known State

- GitHub billing blocker cleared.
- Claude workflow OIDC blocker fixed in PR #8.
- Claude workflow checkout blocker fixed in PR #9.
- Anthropic API credit blocker was observed in run `24863454663`; Mike added $25 credits afterward.
- Active release train `cc/sprints-1-6-publish` pushed up as a safety snapshot (commit `e2f21d2`, 371 files) before cut over to `release/gamgee-rc0`.
- `release/gamgee-rc0` branch opened from `origin/main`, clean base.
- `/gamgee` public front door built (`src/pages/gamgee.astro`, 30KB rendered).
- Build + both audit scripts green on the RC0 branch; all 8 verification routes ship.
- Next blocker: Anthropic API credits propagate → Claude smoke PR on Issue #7 → Codex review → Manus log → RC0 tag.

## First Handoffs

### Claude

Continue issue #7 after API credits propagate. If the run succeeds, inspect the known warning list and pick the smallest safe cleanup. Prefer a one-file PR.

### Codex

Inventory the release train and keep the release definition crisp. Do not let Gamgee become another feature pile.

### Manus

After Claude completes a real run, record the bridge result and any browser/GitHub friction in `docs/manus-logs/`.

## Success Criteria

Gamgee RC0 succeeds when the release has:

- a working agent loop,
- a frozen release-train inventory,
- a public-page plan,
- and a small cleanup PR that proves the loop.

