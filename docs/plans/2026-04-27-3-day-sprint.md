# 3-Day Sprint · 2026-04-27 → 2026-04-29

**Context:** Mike is offline 2026-04-26 PM through 2026-04-27. v3 marketplace is live. v4 source is staged but un-compiled (needs SmartPy IDE drive → Kukai sign). Window Snapshots same status.

**Constraint:** All Day 1 + Day 2 work must be doable without a Kukai signature. Day 3 is Mike-ready handoff.

**Author:** cc · **Status:** active · **Source:** session handoff 2026-04-26 PM

---

## Priorities (cross-day)

1. **Don't break the live marketplace.** v3 is shipping real sales. Any change that touches `/market` flows through legacy-mode plumbing already in place — no schema migrations to v3 contract reads.
2. **Pre-flight v4 so the cutover is one PR.** When Mike signs v4, the only commit needed should be (a) move v3 KT1 to `_legacy_marketplace`, (b) set new `mainnet`. All UI changes for `royalty_receiver` should already be feature-flagged on disk.
3. **Keep the daily cadence.** One Block per day minimum.
4. **No destructive PR sweeps without approval.** Other agents (Codex, Manus) own some of the open PRs — flag for Mike's review, don't close.

---

## Day 1 · Mon 2026-04-27 · Content + v4 prep

### D1-1 · Launch posts (autonomous, ship-ready)
**Owner:** cc · **Blocking:** none · **Outcome:** four files at `docs/launch/2026-04-27-day1-posts/`
- Farcaster cast (≤320 chars, frame-friendly link to /market)
- Bluesky post (300 char limit)
- r/tezos draft (longer-form, links to press release + first-sale screenshot)
- Tezos Discord one-liner
- All four reference v3 KT1 (already correct in press release)
- Mike-side action: paste, post. Estimated 15 min when he's back.

### D1-2 · v4 royalty UI plumbing (feature-flagged)
**Owner:** cc · **Blocking:** none · **Outcome:** PR ready to merge before v4 sign
- Add optional `<input>` for `royalty_receiver` to the list-token form, hidden behind `marketplace.version >= 4` check on `contracts.json`
- Default value when shown: configured `contracts[slug].royaltyReceiver` for known PointCast collections, seller's own wallet for external KT1s
- Update `listToken()` in `market.astro`: when v4 active, include `royalty_receiver` in the `list_ask` batch call. When v3 (current), skip the param entirely (v3's entrypoint signature doesn't accept it).
- Add `marketplace.version` field to `contracts.json` (set to 3 today)
- Test: build clean, `data-marketplace-version="3"` in HTML, royalty input stays hidden

### D1-3 · Block 0373 — sprint kickoff
**Owner:** cc · **Blocking:** none · **Outcome:** `src/content/blocks/0373.json`
- Channel: FD · Type: READ · ~3 min read
- "Three days alone in El Segundo" — what shipped 2026-04-26 (rooms, profile v2, marketplace v3), what cc will autonomously ship while Mike is offline, what waits for his return
- Not in Mike's voice — cc-attributed per AGENTS.md

### D1-4 · `/admin/deploy/new` doc cross-link
**Owner:** cc · **Blocking:** none · **Outcome:** small docs commit
- Add a "v4 marketplace · ready to compile" callout panel to `/admin/deploy/new` when the `?prefill=marketplace` URL is hit
- Surfaces: "v4 source is at `contracts/v2/marketplace.py` · open SmartPy IDE → paste → run → deploy → stage artifacts → return here"
- Removes ambiguity when Mike comes back to the page

### D1-5 · `/api/bath` smoke test
**Owner:** cc · **Blocking:** none · **Outcome:** confirms KV degradation path
- Hit `https://pointcast.xyz/api/bath` with GET (no params) — should return `{ok: false, reason: 'kv-not-bound', ...}` until PC_BATH_KV provisioned
- POST a sample save — same fallback
- Post results to a new `docs/notes/2026-04-27-bath-api-smoke.md` so Mike can decide whether to provision PC_BATH_KV

---

## Day 2 · Tue 2026-04-28 · New surfaces + polish

### D2-1 · `/window` room (Window Snapshots preview surface)
**Owner:** cc · **Blocking:** none · **Outcome:** new page + redirect-aware
- Renders the 3 painted-interior JPGs (`public/images/window-snapshots/*.jpg`) as a triptych preview
- Mint button stays disabled with "originating soon · ${date}" label until `contracts.window_snapshots.mainnet` is populated
- Once Mike originates and pastes the KT1, the same page automatically becomes a live mint surface (no code change)
- Style: matches `/coffee` mintables strip aesthetic
- Adds `/window` to the room nav links across `/anytime`, `/bath`, `/room`

### D2-2 · `/bath/recent` global feed
**Owner:** cc · **Blocking:** PC_BATH_KV bound (graceful empty if not) · **Outcome:** new page
- Reads `/api/bath` GET (no wallet) → renders the global recent saves
- Cassette-style cards (matching profile rotation grid)
- "Saves from the public bath" — opt-in only (saves auto-included since `pc:bath:saves` is local-first; the API gets them only when wallet POST happens)
- Falls back to "the bath log goes live when KV is bound" empty state

### D2-3 · Profile minted items section
**Owner:** cc · **Blocking:** none · **Outcome:** new section in `/profile`
- Below VAULT, add a **MINTED · POINTCAST** lane
- Reads tzkt for tokens the active wallet holds in PointCast contracts (coffee_mugs, visit_nouns, future window_snapshots)
- Distinct from VAULT (which shows all FA2s) — this is "what you've claimed via PointCast surfaces"
- Each tile links back to the originating page (`/coffee` for mugs, `/visit-nouns` for nouns)
- Visual hierarchy: smaller than VAULT but with the chunky border + offset shadow

### D2-4 · Mobile pass on profile v2
**Owner:** cc · **Blocking:** none · **Outcome:** CSS tweaks
- Test profile on 375px / 414px / 768px breakpoints
- Likely tweaks: hero stamp positioning, arcade grid 2-col on small, ledger row stacking
- Safari Mobile rendering of `aspect-ratio: 1200/630` — fall back to padding-bottom hack if needed

### D2-5 · Block 0374 — Window snapshots preview
**Owner:** cc · **Blocking:** none · **Outcome:** `src/content/blocks/0374.json`
- LOOK type · channel: SPN or new ART channel
- Showcase the 3 paintings from `/window`, mention they'll be mintable once originated
- Body: short prose on each painting + a link to `/window`

---

## Day 3 · Wed 2026-04-29 · Mike-ready handoff

### D3-1 · v4 origination runbook (lean)
**Owner:** cc · **Blocking:** none · **Outcome:** `docs/plans/2026-04-29-marketplace-v4-runbook.md`
- 7-step Mike-side checklist:
  1. Open https://smartpy.io/ide
  2. Paste `contracts/v2/marketplace.py`
  3. Run (verify all 7 tests pass)
  4. Click Deploy → Continue (populates origination tab)
  5. Download `step_*_cont_0_contract.json` + `step_*_cont_0_storage.json`
  6. Stage at `public/admin/_artifacts/marketplace-{contract,storage}.json`
  7. Visit `/admin/deploy/new?prefill=marketplace` → safety check goes green → sign
- Post-sign: paste new KT1 into `contracts.json:marketplace.mainnet`, move v3 to `_legacy_marketplace[0]`, set `version: 4`. Redeploy. Validate `/market` shows v3 ask with LEGACY badge.

### D3-2 · Stale PR triage (read-only, no closes)
**Owner:** cc · **Blocking:** none · **Outcome:** `docs/notes/2026-04-29-pr-triage.md`
- Per the handoff: PRs #1, #18, #33, #39 candidates for closure
- For each: list current state, last activity, owner, recommendation (close / keep / re-spec)
- Mike approves close decisions when he returns — cc does not close

### D3-3 · Three-day shipping log block
**Owner:** cc · **Blocking:** none · **Outcome:** Block 0375 + index update
- Wraps the autonomous run: every PR opened, every page touched, links to artifacts
- Recap of what Mike needs to do when he's back (priority-ordered)

### D3-4 · v4 cutover pre-staged PR
**Owner:** cc · **Blocking:** Mike originates v4 · **Outcome:** PR in draft mode
- Branch + diff already prepared: `contracts.json` change moving v3 → `_legacy_marketplace`, setting v4 KT1, bumping `version: 4`
- Mike opens, pastes the new v4 KT1 in the right field, marks ready, merges
- `market.astro` v4-conditional UI from D1-2 already merged — flips to "show royalty input" automatically

---

## Schedule loop

A `/schedule` agent will poll tzkt every 30 min for any new origination from Mike's wallet (`tz2FjJh…`) with marketplace-shaped storage (asks bigmap, platform_fee_bps, etc.). On detection, opens a draft PR pre-filled with the contracts.json delta.

---

## Done criteria

By Wed 2026-04-29 EOD, Mike returns to:
- 3 new daily Blocks (0373, 0374, 0375)
- 1 new room (`/window`)
- 1 new public surface (`/bath/recent`)
- Profile minted-items lane
- Mobile polish pass
- v4 prep PR ready to merge
- v4 cutover PR pre-staged
- Stale PR triage doc
- Day-1 launch posts ready to publish
- Zero broken pages, zero failing builds, zero rolled-back deploys
