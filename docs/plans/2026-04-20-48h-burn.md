# 48h compute burn — 2026-04-20 → 2026-04-22

**Trigger:** Mike 2026-04-20 ~18:40 PT: *"we have two days to use this 50% compute, make a plan"* + usage screenshot showing 48% of Weekly · all models burned, 52% remaining, ~48h to next reset.

**Available budget (Mike's Claude plan):**
- Weekly · all models — ~52% remaining (Opus + Sonnet combined)
- Weekly · Claude Design — ~86% remaining
- Sonnet-only — 100% remaining
- 5-hour limit — ~92% remaining, resets hourly

**Discipline:** *close threads, don't open more.* Every ship in this plan resolves an OPEN unresolved thread from the honest audit earlier this tick, or ships one of the 3 ambitious primitives sized to consume the budget productively. No new strips, no new retros-about-retros.

---

## The three ambitious targets (consume the real budget)

These are the compute-hungry ships that justify Opus capacity specifically:

### A. Drum Cookie Clicker — cc ships directly, not ChatGPT

The ChatGPT brief has been sitting since 16:15 PT. Instead of waiting on a manual handoff, cc builds the whole thing: `/drum` rewrite as a Web Audio cookie clicker with upgrade ladder (10 → 25k beats), prestige "cast a rhythm" mint, auto-drummer passive tick, mobile-first tap target, Farcaster Frame preserved. ~4h of focused compute; ~600 lines across `src/pages/drum/index.astro`, `src/lib/drum.ts`, `src/lib/drum-audio.ts`, `src/components/DrumClicker.astro`.

### B. Autonomous cadence dispatcher

Right now `/cadence` is visible but hand-driven. Real target: `functions/cron/cadence-tick.ts` that fires every 15 min on Cloudflare's cron binding, reads the queue, picks the next eligible ship, routes it (cc via internal webhook; Codex via MCP; Manus via ping; ChatGPT by dropping a brief). Ships under sizes `shy` and `modest` can auto-fire without my active session. This is the thing that makes PointCast "always shipping" by Day 2 even when I'm between sessions. ~3h.

### C. [DEFERRED per Mike 2026-04-20 18:50 PT] Kimi integration

Mike: *"lets skip kimi rn."* Removed from this window. Reallocating the ~4h to:
- Experimental cc-oversight of Manus + ChatGPT via computer-use / Chrome MCP (per Mike: *"consider manus and chatgpt have a lot of compute and you can oversee the computer tho that's not clear is going to work"*) — actually driving their sessions to execute the briefs that have been sitting. Modest attempt; if computer-use doesn't pan out, fall back to better briefs + Mike-handoff.
- If computer-use proves viable for Manus → run the Agentic.Market listing brief end-to-end (create Base wallet, register listings, screenshot + log).
- If computer-use proves viable for ChatGPT → paste the drum-cookie-clicker brief, drive execution, review PR, merge.

Total ambitious budget: **~11h** (now A + B + computer-use experiment).

---

## The backlog — close every thread (the rest of the budget)

From the honest audit, 18 mentions across 7 threads still open. Each gets shipped this window, not queued.

| # | Thread | Budget | Deliverable |
|---|---|---|---|
| 1 | In-flight agent-readiness: WebMCP + Markdown for Agents + OAuth PR | 2h | Ship + deploy; push score 7/9 → 9/9 (skip OIDC) |
| 2 | Codex retry w/ tight spec → PrizeCastChip | 1h | Fire with 20-line imperative spec; if timeout, cc ships directly |
| 3 | BlockReorder debug via Chrome MCP mobile emulation | 2h | Actually reproduce; fix; ship |
| 4 | SportsStrip v3 — team records + upsets + previews + trends | 3h | Real ESPN /teams fetch, upset math, previews row |
| 5 | Cloudflare KV overuse fix — visit.ts session dedup | 1h | 10-min cooldown per session ID; stop the billing bleed |
| 6 | Home rethink Phase 2 — HeroBlock + ActionDrawers | 2h | Execute approved Option A |
| 7 | Home rethink Phase 3 — /sports page + remove SportsStrip from home | 1h | Phase 3 |
| 8 | Home rethink Phase 4-5 — FreshDeck inline + masthead enrich | 1h | Phase 4-5 |
| 9 | Mobile formatting overflow + pinch-zoom issue | 1.5h | Chrome MCP mobile emulation; find + fix specific components |
| 10 | Workbench dedensify | 1h | Strip + rebuild /workbench |
| 11 | /here vs /drum differentiation | 0.5h | Copy + visual distinction |
| 12 | hello@pointcast.xyz email routing | 0.5h | Configure Cloudflare Email Routing myself; stop waiting on Manus |
| 13 | Favicon rasterization (proper .ico for legacy unfurl clients) | 0.5h | Use available image tooling, generate multi-size .ico |
| 14 | Manus kickoff ping for Agentic.Market listing | 0.2h | Just send the damn ping |
| 15 | ChatGPT drum handoff (after A ships — redundant, deferred or deleted) | 0 | Delete, not deferred |
| 16 | x402 fulfillment endpoint stubs | 2h | `functions/api/x402/[service].ts` with proper 402 flow |
| 17 | Fix sha256 placeholders in agent-skills/index.json | 0.5h | Build step that hashes URL content |
| 18 | PrizeCastChip (ship directly if Codex still fails) | 1h | Cc ships if retry times out |

Backlog budget: **~21h**.

---

## Editorial push — 3-5 research blocks (mid-window)

Compute is literal fuel for this. Draft long-form blocks on:

1. **"48h burn — what a small network can do in a weekend"** — retrospective at T+48h
2. **"Federated compute one week in"** — week-over-week deltas, actual federation interest data
3. **"The agent-readiness checklist, as a primitive"** — what /well-known/* actually changed for PointCast traffic
4. **"Drum as a liturgy"** — essay on cookie-clicker-as-meditation tied to drum deployment
5. **"Kimi comparison"** — Kimi-drafted block vs. cc-drafted block, side by side, for the same topic

Each ~45 min of compute. **~4h** total.

---

## Sequence — 48h wall clock

### Night 1 — 4/20 19:00–24:00 PT (5h active)
- 19:00: Deploy the in-flight middleware (Markdown for Agents + WebMCP inline in CoNav + OAuth PR stub). Close agent-readiness to 9/9.
- 20:00: Codex retry with tight spec → PrizeCastChip (or cc-direct fallback).
- 20:30: visit.ts KV dedup. Stop the Cloudflare billing.
- 21:00: BlockReorder debug via Chrome MCP mobile emulation. Fix or honest "I can't reproduce — need your device" with telemetry.
- 22:00: SportsStrip v3 (start) — ESPN /teams endpoint + upset math.
- 23:30: Write "drum as a liturgy" block 0336.
- 00:00: Retro + sleep window (cadence muted 2–6am PT).

### Day 2 Morning — 4/21 07:00–12:00 PT (5h active)
- 07:00: Finish SportsStrip v3.
- 08:00: Home rethink Phase 2 — HeroBlock + ActionDrawers.
- 09:30: Home rethink Phase 3 — /sports page + remove from home.
- 10:30: Home rethink Phase 4-5 — FreshDeck inline + masthead enrich.
- 11:00: Mobile overflow + pinch-zoom fix via Chrome MCP.
- 12:00: Lunch break.

### Day 2 Afternoon — 4/21 13:00–19:00 PT (6h active)
- 13:00: **Ambitious A — Drum Cookie Clicker.** Full ship. 4h focused.
- 17:00: **Ambitious B — Cadence autonomous dispatcher.** 2h. `functions/cron/cadence-tick.ts`, Cloudflare cron binding, queue-to-dispatch flow, retroactively marks shipped.

### Day 2 Evening — 4/21 19:00–23:00 PT (4h active)
- 19:00: **Ambitious C — Kimi integration.** Real API call. First Kimi-drafted block. Compute-ledger x402 entry.
- 22:00: Sports + Kimi comparison blocks.

### Day 3 Morning — 4/22 06:00–11:00 PT (5h active, before reset at ~T+48h)
- 06:00: Workbench dedensify + /here vs /drum distinction.
- 07:00: hello@pointcast.xyz email routing (Cloudflare Email Routing UI).
- 07:30: Favicon rasterization — proper multi-size .ico.
- 08:00: x402 fulfillment endpoint stubs.
- 10:00: Agent-skills sha256 build step.
- 10:30: Final audit + 48h-burn retro block 0340ish.
- 11:00: Reset hits. Ledger tally, screenshot the agentic score, call it.

---

## Total estimate

- Ambitious A+B+C: **~11h**
- Backlog close-out: **~21h**
- Editorial blocks: **~4h**
- Build/deploy + retros + coordination overhead: **~6h**
- **Total: ~42h of active compute over 48h of wall clock.**

Fits inside 52% of weekly allowance comfortably. The Sonnet-only bucket (100% remaining) can absorb the coordination + small ships if Opus runs thin.

---

## Risks + mitigations

1. **Codex keeps timing out.** Mitigation: tight spec. If still fails, cc ships everything. Codex operationalization was always a stretch claim.
2. **BlockReorder doesn't reproduce in Chrome MCP.** Mitigation: report that as the answer. Ask Mike for exact device/browser + a screen recording if needed.
3. **Kimi API key / auth friction.** Mitigation: if onboarding is more than 30 min, skip the real Kimi call, ship the comparison as a "why we didn't" block.
4. **Cadence autonomous dispatcher requires wrangler bindings** (cron triggers, KV for queue state). Mitigation: start with queue-in-localStorage-JSON committed-to-repo, add KV in a follow-up. Ship 80% first.
5. **Compute runs out mid-day 2.** Mitigation: prioritize ambitious A (drum — the most visible deliverable), then B (dispatcher — the actual leverage), then close out whatever backlog fits. C (Kimi) is first to cut.

---

## One open question for Mike

**Go with this plan or reshape it?** Specifically — should I swap any of ambitious A/B/C for something you'd rather have, and do you have opinions on order (A first for visible win, B first for leverage, C first for "operationalizing" a new collab)? Default if you say nothing: I start Night 1 at 19:00 PT with the agent-readiness close-out.

---

— cc, 18:45 PT (2026-04-20) · 48h, one plan, ship the backlog + the three big ones
