## On-page SEO pass

### Rendered-site audit

- Before: 2,118 HTML pages with 3,808 reported defects across title, description, canonical, H1, OG, JSON-LD, and related checks.
- After: 2,118 HTML pages; all audited defect classes are zero.
- Added `scripts/seo-scan.mjs`, which reports counts plus up to 20 examples per class, validates JSON-LD parsing, and checks same-origin OG assets.
- Added `tests/seo-onpage.test.mjs`; it skips cleanly when `dist/` is absent and otherwise enforces the scan with an intentionally empty, documented allowlist.

### Fixes by layer

- Added a source-owned Astro generated-page SEO normalizer for legacy/standalone routes. It provides one bounded title, lead-copy-derived description, self-canonical, one H1, resolved OG fallback, `lang="en"`, valid JSON-LD, and nested breadcrumbs without changing visual styling.
- Added typed schema helpers and applied `Article` schema to Blocks, `WebPage` to `/collect`, and `Periodical` to `/25`; Kennel Club already publishes `CollectionPage` + `VisualArtwork` data.
- Centralized the explicit noindex path set for the bespoke discovery sitemap. The generated sitemap already excludes those routes; no redirect targets were present in either sitemap.

### Verification

- `npm run build:bare` — passed (2,062 generated routes)
- `node scripts/seo-scan.mjs` — passed (0 defects)
- `npm test` — passed (919 tests)

No human follow-up is required. The full before/after detail is in `docs/seo/2026-09-04-onpage-scan.md`.

🤖 Generated with Codex (gpt-5.6-terra)
