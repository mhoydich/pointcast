{
  "name": "pointcast-presence",
  "version": "1.0.0",
  "description": "PointCast presence Durable Object — companion Worker for the Pages project.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
