{
  "name": "pointcast-tank",
  "version": "0.1.0",
  "description": "PointCast tank Durable Object — companion Worker for the Pages project.",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
