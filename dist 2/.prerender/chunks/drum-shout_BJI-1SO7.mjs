import { c as createComponent } from './astro-component_DWMcTjG3.mjs';
import 'piccolore';
import { a as renderTemplate, r as renderComponent, m as maybeRenderHead, b as addAttribute } from './prerender_CmTjnOuJ.mjs';
import { $ as $$BlockLayout } from './BlockLayout_DHviHHrD.mjs';
import { $ as $$DrumNav } from './DrumNav_D5cUUl3f.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$DrumShout = createComponent(async ($$result, $$props, $$slots) => {
  const title = "/drum-shout — take the mic";
  const description = "Take the mic. Type a 200-char shout. The room sees it scroll across every drum-hub cast surface.";
  const jsonLd = { "@context": "https://schema.org", "@type": "WebPage", "@id": "https://pointcast.xyz/drum-shout", name: "PointCast Drum · Shout", url: "https://pointcast.xyz/drum-shout", description };
  const MOODS = [
    { id: "warm", label: "WARM", color: "#ff8a4a" },
    { id: "bright", label: "BRIGHT", color: "#ffd400" },
    { id: "ocean", label: "OCEAN", color: "#5fbafd" },
    { id: "garden", label: "GARDEN", color: "#3b6e3b" },
    { id: "fog", label: "FOG", color: "#cfc6df" }
  ];
  return renderTemplate(_a || (_a = __template(["", " <script>\n(function () { 'use strict';\n  const COOLDOWN_MS = 30 * 1000;\n  const MAX = 200;\n  let currentMood = 'warm';\n  const colorByMood = { warm: '#ff8a4a', bright: '#ffd400', ocean: '#5fbafd', garden: '#3b6e3b', fog: '#cfc6df' };\n  const input = document.getElementById('sh-input');\n  const count = document.getElementById('sh-count');\n  const send = document.getElementById('sh-send');\n  const cooldownEl = document.getElementById('sh-cooldown');\n  const feed = document.getElementById('sh-feed');\n  const empty = document.getElementById('sh-empty');\n  const lamp = document.getElementById('sh-lamp');\n  const lampText = document.getElementById('sh-lamp-text');\n\n  function getSession() {\n    try { const k='pc:session'; let s=localStorage.getItem(k); if(!s){s=Math.random().toString(36).slice(2)+Date.now().toString(36); localStorage.setItem(k,s);} return s; }\n    catch { return 'sh-' + Math.random().toString(36).slice(2,10); }\n  }\n  function nounIdFor(sid) { let h=5381; for(let i=0;i<sid.length;i++) h=(h*33+sid.charCodeAt(i))&0x7fffffff; return h%1200; }\n  const session = getSession();\n  const myNoun = nounIdFor(session);\n\n  document.querySelectorAll('.sh__mood').forEach((b) => {\n    b.addEventListener('click', () => {\n      document.querySelectorAll('.sh__mood').forEach((x) => x.classList.remove('sh__mood--on'));\n      b.classList.add('sh__mood--on');\n      currentMood = b.getAttribute('data-mood') || 'warm';\n    });\n  });\n  input.addEventListener('input', () => { count.textContent = `${input.value.length} / ${MAX}`; });\n\n  function lastSentTs() { try { return Number(localStorage.getItem('pc:drum-shout:lastSent')) || 0; } catch { return 0; } }\n  function setLastSent(ts) { try { localStorage.setItem('pc:drum-shout:lastSent', String(ts)); } catch {} }\n  function refreshCooldown() {\n    const last = lastSentTs();\n    const remain = (last + COOLDOWN_MS) - Date.now();\n    if (remain > 0) {\n      send.disabled = true;\n      cooldownEl.textContent = `cooling ${Math.ceil(remain / 1000)}s`;\n    } else {\n      send.disabled = false;\n      cooldownEl.textContent = '';\n    }\n  }\n  setInterval(refreshCooldown, 500); refreshCooldown();\n\n  function flashLamp(text) {\n    lamp.classList.add('sh__lamp--on');\n    lampText.textContent = text || '— ON AIR —';\n    setTimeout(() => { lamp.classList.remove('sh__lamp--on'); lampText.textContent = '— OFF AIR —'; }, 8000);\n  }\n\n  function renderShout(s) {\n    if (empty && empty.parentNode) empty.remove();\n    const card = document.createElement('div');\n    card.className = 'sh__card';\n    const color = colorByMood[s.mood] || '#ff8a4a';\n    card.style.setProperty('--sh-card-color', color);\n    const t = new Date(s.t || Date.now());\n    const stamp = `${String(t.getHours()).padStart(2,'0')}:${String(t.getMinutes()).padStart(2,'0')}:${String(t.getSeconds()).padStart(2,'0')}`;\n    card.innerHTML = `\n      <div class=\"sh__card-head\">\n        <img src=\"https://noun.pics/${s.nounId || 0}.svg\" alt=\"\" width=\"40\" height=\"40\" loading=\"lazy\" />\n        <div>\n          <span class=\"sh__card-pid mono\">noun #${s.nounId || '—'} · pid ${(s.pid || '').slice(0,8) || '—'}</span>\n          <span class=\"sh__card-time mono\">${stamp}</span>\n        </div>\n        <span class=\"sh__card-mood mono\">${(s.mood || 'warm').toUpperCase()}</span>\n      </div>\n      <p class=\"sh__card-text\">${(s.text || '').replace(/[<>]/g, '')}</p>\n      <div class=\"sh__card-marquee\"><span>${(s.text || '').replace(/[<>]/g, '')}</span></div>\n    `;\n    feed.prepend(card);\n    const cards = feed.querySelectorAll('.sh__card');\n    if (cards.length > 8) for (let i = 8; i < cards.length; i++) cards[i].remove();\n    setTimeout(() => { card.classList.add('sh__card--fade'); }, 28000);\n  }\n\n  document.getElementById('sh-form').addEventListener('submit', (e) => {\n    e.preventDefault();\n    const text = (input.value || '').trim().slice(0, MAX);\n    if (!text) { input.focus(); return; }\n    if (Date.now() - lastSentTs() < COOLDOWN_MS) { refreshCooldown(); return; }\n    setLastSent(Date.now());\n    refreshCooldown();\n    flashLamp('— ON AIR —');\n    // Local optimistic render\n    renderShout({ text, mood: currentMood, t: Date.now(), pid: '', nounId: myNoun });\n    try {\n      fetch('/api/sounds', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ type: 'shout', sessionId: session, text, mood: currentMood, nounId: myNoun }),\n      });\n    } catch {}\n    input.value = ''; count.textContent = `0 / ${MAX}`;\n  });\n\n  // Poll for incoming shouts from other visitors\n  let lastTs = Date.now();\n  async function poll() {\n    try {\n      const r = await fetch(`/api/sounds?since=${lastTs}`, { cache: 'no-store' });\n      if (!r.ok) return;\n      const data = await r.json();\n      const events = Array.isArray(data.events) ? data.events : [];\n      if (!events.length) return;\n      lastTs = events[events.length - 1].t || Date.now();\n      for (const e of events) {\n        if (e.type === 'shout' && e.text) {\n          // skip own optimistic echo by comparing pid hash if available\n          renderShout({ text: e.text, mood: e.mood, t: e.t, pid: e.pid, nounId: e.nounId });\n          flashLamp(`— ${(e.mood || 'warm').toUpperCase()} ON AIR —`);\n        }\n      }\n    } catch {}\n  }\n  poll(); setInterval(poll, 1500);\n})();\n<\/script>"], ["", " <script>\n(function () { 'use strict';\n  const COOLDOWN_MS = 30 * 1000;\n  const MAX = 200;\n  let currentMood = 'warm';\n  const colorByMood = { warm: '#ff8a4a', bright: '#ffd400', ocean: '#5fbafd', garden: '#3b6e3b', fog: '#cfc6df' };\n  const input = document.getElementById('sh-input');\n  const count = document.getElementById('sh-count');\n  const send = document.getElementById('sh-send');\n  const cooldownEl = document.getElementById('sh-cooldown');\n  const feed = document.getElementById('sh-feed');\n  const empty = document.getElementById('sh-empty');\n  const lamp = document.getElementById('sh-lamp');\n  const lampText = document.getElementById('sh-lamp-text');\n\n  function getSession() {\n    try { const k='pc:session'; let s=localStorage.getItem(k); if(!s){s=Math.random().toString(36).slice(2)+Date.now().toString(36); localStorage.setItem(k,s);} return s; }\n    catch { return 'sh-' + Math.random().toString(36).slice(2,10); }\n  }\n  function nounIdFor(sid) { let h=5381; for(let i=0;i<sid.length;i++) h=(h*33+sid.charCodeAt(i))&0x7fffffff; return h%1200; }\n  const session = getSession();\n  const myNoun = nounIdFor(session);\n\n  document.querySelectorAll('.sh__mood').forEach((b) => {\n    b.addEventListener('click', () => {\n      document.querySelectorAll('.sh__mood').forEach((x) => x.classList.remove('sh__mood--on'));\n      b.classList.add('sh__mood--on');\n      currentMood = b.getAttribute('data-mood') || 'warm';\n    });\n  });\n  input.addEventListener('input', () => { count.textContent = \\`\\${input.value.length} / \\${MAX}\\`; });\n\n  function lastSentTs() { try { return Number(localStorage.getItem('pc:drum-shout:lastSent')) || 0; } catch { return 0; } }\n  function setLastSent(ts) { try { localStorage.setItem('pc:drum-shout:lastSent', String(ts)); } catch {} }\n  function refreshCooldown() {\n    const last = lastSentTs();\n    const remain = (last + COOLDOWN_MS) - Date.now();\n    if (remain > 0) {\n      send.disabled = true;\n      cooldownEl.textContent = \\`cooling \\${Math.ceil(remain / 1000)}s\\`;\n    } else {\n      send.disabled = false;\n      cooldownEl.textContent = '';\n    }\n  }\n  setInterval(refreshCooldown, 500); refreshCooldown();\n\n  function flashLamp(text) {\n    lamp.classList.add('sh__lamp--on');\n    lampText.textContent = text || '— ON AIR —';\n    setTimeout(() => { lamp.classList.remove('sh__lamp--on'); lampText.textContent = '— OFF AIR —'; }, 8000);\n  }\n\n  function renderShout(s) {\n    if (empty && empty.parentNode) empty.remove();\n    const card = document.createElement('div');\n    card.className = 'sh__card';\n    const color = colorByMood[s.mood] || '#ff8a4a';\n    card.style.setProperty('--sh-card-color', color);\n    const t = new Date(s.t || Date.now());\n    const stamp = \\`\\${String(t.getHours()).padStart(2,'0')}:\\${String(t.getMinutes()).padStart(2,'0')}:\\${String(t.getSeconds()).padStart(2,'0')}\\`;\n    card.innerHTML = \\`\n      <div class=\"sh__card-head\">\n        <img src=\"https://noun.pics/\\${s.nounId || 0}.svg\" alt=\"\" width=\"40\" height=\"40\" loading=\"lazy\" />\n        <div>\n          <span class=\"sh__card-pid mono\">noun #\\${s.nounId || '—'} · pid \\${(s.pid || '').slice(0,8) || '—'}</span>\n          <span class=\"sh__card-time mono\">\\${stamp}</span>\n        </div>\n        <span class=\"sh__card-mood mono\">\\${(s.mood || 'warm').toUpperCase()}</span>\n      </div>\n      <p class=\"sh__card-text\">\\${(s.text || '').replace(/[<>]/g, '')}</p>\n      <div class=\"sh__card-marquee\"><span>\\${(s.text || '').replace(/[<>]/g, '')}</span></div>\n    \\`;\n    feed.prepend(card);\n    const cards = feed.querySelectorAll('.sh__card');\n    if (cards.length > 8) for (let i = 8; i < cards.length; i++) cards[i].remove();\n    setTimeout(() => { card.classList.add('sh__card--fade'); }, 28000);\n  }\n\n  document.getElementById('sh-form').addEventListener('submit', (e) => {\n    e.preventDefault();\n    const text = (input.value || '').trim().slice(0, MAX);\n    if (!text) { input.focus(); return; }\n    if (Date.now() - lastSentTs() < COOLDOWN_MS) { refreshCooldown(); return; }\n    setLastSent(Date.now());\n    refreshCooldown();\n    flashLamp('— ON AIR —');\n    // Local optimistic render\n    renderShout({ text, mood: currentMood, t: Date.now(), pid: '', nounId: myNoun });\n    try {\n      fetch('/api/sounds', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ type: 'shout', sessionId: session, text, mood: currentMood, nounId: myNoun }),\n      });\n    } catch {}\n    input.value = ''; count.textContent = \\`0 / \\${MAX}\\`;\n  });\n\n  // Poll for incoming shouts from other visitors\n  let lastTs = Date.now();\n  async function poll() {\n    try {\n      const r = await fetch(\\`/api/sounds?since=\\${lastTs}\\`, { cache: 'no-store' });\n      if (!r.ok) return;\n      const data = await r.json();\n      const events = Array.isArray(data.events) ? data.events : [];\n      if (!events.length) return;\n      lastTs = events[events.length - 1].t || Date.now();\n      for (const e of events) {\n        if (e.type === 'shout' && e.text) {\n          // skip own optimistic echo by comparing pid hash if available\n          renderShout({ text: e.text, mood: e.mood, t: e.t, pid: e.pid, nounId: e.nounId });\n          flashLamp(\\`— \\${(e.mood || 'warm').toUpperCase()} ON AIR —\\`);\n        }\n      }\n    } catch {}\n  }\n  poll(); setInterval(poll, 1500);\n})();\n<\/script>"])), renderComponent($$result, "BlockLayout", $$BlockLayout, { "title": title, "description": description, "jsonLd": jsonLd, "data-astro-cid-w3lfqrr4": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<main class="sh" id="sh-main" data-astro-cid-w3lfqrr4> ${renderComponent($$result2, "DrumNav", $$DrumNav, { "current": "confessional", "data-astro-cid-w3lfqrr4": true })} <header class="sh__head" data-astro-cid-w3lfqrr4> <p class="sh__kicker" data-astro-cid-w3lfqrr4>DRUM HUB · COMMS · SHOUT · TAKE THE MIC · MULTIPLAYER</p> <h1 class="sh__title" data-astro-cid-w3lfqrr4><em data-astro-cid-w3lfqrr4>Step up, lean in, say it once.</em></h1> <p class="sh__dek" data-astro-cid-w3lfqrr4>
Type a shout. Pick a color. The room sees it scroll across every drum-hub cast surface — TV, marquee, visualizer. 200 characters, 30-second cooldown, no edit, no delete.
</p> </header> <section class="sh__booth" data-astro-cid-w3lfqrr4> <div class="sh__lamp" id="sh-lamp" aria-hidden="true" data-astro-cid-w3lfqrr4> <span class="sh__lamp-dot" data-astro-cid-w3lfqrr4></span> <span class="sh__lamp-text mono" id="sh-lamp-text" data-astro-cid-w3lfqrr4>— OFF AIR —</span> </div> <form class="sh__form" id="sh-form" autocomplete="off" data-astro-cid-w3lfqrr4> <textarea class="sh__input" id="sh-input" maxlength="200" placeholder="Take the mic — what do you want the room to hear?" rows="3" aria-label="Shout text" data-astro-cid-w3lfqrr4></textarea> <div class="sh__moodbar" role="radiogroup" aria-label="Shout color" data-astro-cid-w3lfqrr4> ${MOODS.map((m, i) => renderTemplate`<button type="button"${addAttribute(`sh__mood ${i === 0 ? "sh__mood--on" : ""}`, "class")}${addAttribute(m.id, "data-mood")}${addAttribute(`--sh-mood: ${m.color}`, "style")}${addAttribute(`Mood ${m.label}`, "aria-label")} data-astro-cid-w3lfqrr4> <span class="sh__mood-swatch" data-astro-cid-w3lfqrr4></span> <span class="sh__mood-label mono" data-astro-cid-w3lfqrr4>${m.label}</span> </button>`)} </div> <div class="sh__form-foot" data-astro-cid-w3lfqrr4> <span class="sh__count mono" id="sh-count" data-astro-cid-w3lfqrr4>0 / 200</span> <span class="sh__cooldown mono" id="sh-cooldown" aria-live="polite" data-astro-cid-w3lfqrr4></span> <button type="submit" class="sh__send mono" id="sh-send" data-astro-cid-w3lfqrr4>SHOUT →</button> </div> </form> <div class="sh__feed" id="sh-feed" aria-label="Recent shouts" data-astro-cid-w3lfqrr4> <p class="sh__empty mono" id="sh-empty" data-astro-cid-w3lfqrr4>— no shouts yet —</p> </div> </section> <footer class="sh__foot" data-astro-cid-w3lfqrr4> <p data-astro-cid-w3lfqrr4>
Each shout fires <code data-astro-cid-w3lfqrr4>type=shout</code> to <code data-astro-cid-w3lfqrr4>/api/sounds</code> with the text + mood color. The marquee surface (<a href="/drum-marquee" data-astro-cid-w3lfqrr4>/drum-marquee</a>) scrolls every shout right-to-left at full width. The visualizer (<a href="/drum-viz" data-astro-cid-w3lfqrr4>/drum-viz</a>) shows it as a colored ripple. The radio (<a href="/drum-radio" data-astro-cid-w3lfqrr4>/drum-radio</a>) translates it to a sweep tone.
</p> <p class="sh__credit mono" data-astro-cid-w3lfqrr4>v0.1 · 2026-04-28 · drum sprint · comms tool 1</p> </footer> </main> ` }));
}, "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-shout.astro", void 0);

const $$file = "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-shout.astro";
const $$url = "/drum-shout";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$DrumShout,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
