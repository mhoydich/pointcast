import { c as createComponent } from './astro-component_DWMcTjG3.mjs';
import 'piccolore';
import { a as renderTemplate, d as defineScriptVars, b as addAttribute, m as maybeRenderHead } from './prerender_CmTjnOuJ.mjs';
import 'clsx';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(cooked.slice()) }));
var _a;
const $$OceanMeditation = createComponent(($$result, $$props, $$slots) => {
  const Astro2 = $$result.createAstro($$props, $$slots);
  Astro2.self = $$OceanMeditation;
  const programs = [
    {
      id: "calm",
      name: "Calm Bay",
      label: "Calm Bay",
      pattern: [4, 2, 6, 2],
      tone: "soft shoreline",
      note: "A gentle shoreline rhythm for a quick nervous-system reset.",
      prompts: [
        "Let the inhale rise like a small wave.",
        "Rest at the crest without gripping.",
        "Exhale as the tide rolls back out."
      ]
    },
    {
      id: "current",
      name: "Deep Current",
      label: "Deep Current",
      pattern: [5, 2, 7, 2],
      tone: "long exhale",
      note: "A slower current for clearing mental noise before focused work.",
      prompts: [
        "Breathe below the surface of the feed.",
        "Let each thought pass like sea glass in the current.",
        "Lengthen the exhale until the water goes still."
      ]
    },
    {
      id: "moon",
      name: "Moon Tide",
      label: "Moon Tide",
      pattern: [4, 4, 4, 4],
      tone: "box breath",
      note: "A square breath under moonlight for evening decompression.",
      prompts: [
        "Inhale to the horizon.",
        "Pause in the silver quiet.",
        "Exhale into the dark water."
      ]
    }
  ];
  const durations = [
    { seconds: 120, label: "2 min", name: "Morning tide" },
    { seconds: 300, label: "5 min", name: "Deep reset" },
    { seconds: 600, label: "10 min", name: "Full drift" }
  ];
  const artifact = Astro2.props.artifact ?? {
    name: "Breathe El Segundo",
    image: "/images/tokens/breathe-el-segundo.webp",
    alt: "Breathe El Segundo token artwork",
    description: "Ocean-based attention training, folded back into the PointCast broadcast instead of another app to maintain."
  };
  return renderTemplate(_a || (_a = __template(["", '<section class="ocean-meditation" data-ocean-meditation data-program="calm" data-cue="inhale" aria-label="Ocean meditation timer" data-astro-cid-d4tjut42> <div class="session-shell" data-astro-cid-d4tjut42> <div class="ocean-stage" aria-hidden="true" data-astro-cid-d4tjut42> <div class="stage-skyline" data-astro-cid-d4tjut42></div> <div class="stage-sun" data-astro-cid-d4tjut42></div> <div class="stage-cloud stage-cloud--one" data-astro-cid-d4tjut42></div> <div class="stage-cloud stage-cloud--two" data-astro-cid-d4tjut42></div> <div class="breath-orb" data-astro-cid-d4tjut42> <span id="oceanCue" data-astro-cid-d4tjut42>Inhale</span> </div> <div class="stage-wave stage-wave--back" data-astro-cid-d4tjut42></div> <div class="stage-wave stage-wave--mid" data-astro-cid-d4tjut42></div> <div class="stage-wave stage-wave--front" data-astro-cid-d4tjut42></div> </div> <div class="session-panel" data-astro-cid-d4tjut42> <div class="panel-strip" data-astro-cid-d4tjut42> <span class="mono" data-astro-cid-d4tjut42>OCEAN RESET</span> <span class="mono" id="oceanState" data-astro-cid-d4tjut42>READY</span> </div> <div class="programs" aria-label="Meditation programs" data-astro-cid-d4tjut42> ', ' </div> <div class="duration-row" aria-label="Session duration" data-astro-cid-d4tjut42> ', ' </div> <div class="readout-grid" data-astro-cid-d4tjut42> <div data-astro-cid-d4tjut42> <span class="readout-label mono" data-astro-cid-d4tjut42>Session</span> <strong id="oceanSessionName" data-astro-cid-d4tjut42>Morning tide</strong> </div> <div data-astro-cid-d4tjut42> <span class="readout-label mono" data-astro-cid-d4tjut42>Remaining</span> <strong id="oceanTime" data-astro-cid-d4tjut42>02:00</strong> </div> <div data-astro-cid-d4tjut42> <span class="readout-label mono" data-astro-cid-d4tjut42>Completed</span> <strong id="oceanSessions" data-astro-cid-d4tjut42>0</strong> </div> <div data-astro-cid-d4tjut42> <span class="readout-label mono" data-astro-cid-d4tjut42>Minutes</span> <strong id="oceanMinutes" data-astro-cid-d4tjut42>0</strong> </div> </div> <div class="tide-meter" aria-hidden="true" data-astro-cid-d4tjut42> <span id="oceanProgress" data-astro-cid-d4tjut42></span> </div> <div class="action-row" data-astro-cid-d4tjut42> <button class="action-btn action-btn--primary" type="button" id="oceanStart" aria-pressed="false" data-astro-cid-d4tjut42>Start</button> <button class="action-btn" type="button" id="oceanReset" data-astro-cid-d4tjut42>Reset</button> <button class="action-btn" type="button" id="oceanFocus" data-astro-cid-d4tjut42>Focus</button> <label class="tone-toggle" data-astro-cid-d4tjut42> <input type="checkbox" id="oceanTone" data-astro-cid-d4tjut42> <span data-astro-cid-d4tjut42>Ocean tone</span> </label> </div> <p class="program-note" id="oceanNote" data-astro-cid-d4tjut42>', '</p> <ul class="prompt-list" id="oceanPrompts" data-astro-cid-d4tjut42> ', ' </ul> <div class="tide-log" data-astro-cid-d4tjut42> <div class="tide-log__head" data-astro-cid-d4tjut42> <span class="mono" data-astro-cid-d4tjut42>Tide Log</span> <button type="button" id="oceanClearLog" data-astro-cid-d4tjut42>Clear</button> </div> <ul id="oceanLog" data-astro-cid-d4tjut42> <li data-astro-cid-d4tjut42>No sessions completed yet.</li> </ul> </div> </div> </div> <div class="asset-card"', " data-astro-cid-d4tjut42> <img", "", ' loading="lazy" decoding="async" data-astro-cid-d4tjut42> <div data-astro-cid-d4tjut42> <p class="mono" data-astro-cid-d4tjut42>TOKEN ARTIFACT</p> <h2 data-astro-cid-d4tjut42>', "</h2> <p data-astro-cid-d4tjut42>", "</p> ", ' </div> </div> <div class="focus-overlay" id="oceanFocusOverlay" aria-hidden="true" data-astro-cid-d4tjut42> <div class="focus-window" role="dialog" aria-modal="true" aria-labelledby="oceanFocusTitle" data-astro-cid-d4tjut42> <button class="focus-close" type="button" id="oceanFocusClose" title="Close focus mode" data-astro-cid-d4tjut42>×</button> <div class="focus-scene" aria-hidden="true" data-astro-cid-d4tjut42> <div class="focus-sun" data-astro-cid-d4tjut42></div> <div class="focus-orb" data-astro-cid-d4tjut42> <span id="oceanFocusCue" data-astro-cid-d4tjut42>Inhale</span> </div> <div class="focus-wave focus-wave--back" data-astro-cid-d4tjut42></div> <div class="focus-wave focus-wave--front" data-astro-cid-d4tjut42></div> </div> <div class="focus-copy" data-astro-cid-d4tjut42> <span class="mono" id="oceanFocusTitle" data-astro-cid-d4tjut42>Ocean Focus</span> <strong id="oceanFocusTime" data-astro-cid-d4tjut42>02:00</strong> <em id="oceanFocusProgram" data-astro-cid-d4tjut42>Calm Bay</em> </div> </div> </div> </section> <script>(function(){', "\n  (function () {\n    const root = document.querySelector('[data-ocean-meditation]');\n    if (!root) return;\n\n    const STORAGE_KEY = 'pc:ocean-meditation';\n    const programMap = Object.fromEntries(programs.map((program) => [program.id, program]));\n    const programBtns = root.querySelectorAll('.program-btn');\n    const durationBtns = root.querySelectorAll('.duration-btn');\n    const startBtn = document.getElementById('oceanStart');\n    const resetBtn = document.getElementById('oceanReset');\n    const focusBtn = document.getElementById('oceanFocus');\n    const toneToggle = document.getElementById('oceanTone');\n    const clearLogBtn = document.getElementById('oceanClearLog');\n    const cueEl = document.getElementById('oceanCue');\n    const timeEl = document.getElementById('oceanTime');\n    const sessionNameEl = document.getElementById('oceanSessionName');\n    const stateEl = document.getElementById('oceanState');\n    const sessionsEl = document.getElementById('oceanSessions');\n    const minutesEl = document.getElementById('oceanMinutes');\n    const progressEl = document.getElementById('oceanProgress');\n    const noteEl = document.getElementById('oceanNote');\n    const promptsEl = document.getElementById('oceanPrompts');\n    const logEl = document.getElementById('oceanLog');\n    const focusOverlay = document.getElementById('oceanFocusOverlay');\n    const focusClose = document.getElementById('oceanFocusClose');\n    const focusCueEl = document.getElementById('oceanFocusCue');\n    const focusTimeEl = document.getElementById('oceanFocusTime');\n    const focusProgramEl = document.getElementById('oceanFocusProgram');\n\n    let currentProgram = 'calm';\n    let duration = Number(durations[0]?.seconds || 120);\n    let sessionName = durations[0]?.name || 'Morning tide';\n    let elapsed = 0;\n    let timer = null;\n    let audioCtx = null;\n    let toneNodes = null;\n    let tonePulse = null;\n    let stats = loadStats();\n\n    function loadStats() {\n      try {\n        const parsed = JSON.parse(localStorage.getItem(STORAGE_KEY) || 'null');\n        if (parsed && typeof parsed === 'object') {\n          return {\n            sessions: Number(parsed.sessions) || 0,\n            minutes: Number(parsed.minutes) || 0,\n            log: Array.isArray(parsed.log) ? parsed.log.slice(0, 5) : [],\n          };\n        }\n      } catch {}\n      return { sessions: 0, minutes: 0, log: [] };\n    }\n\n    function saveStats() {\n      try { localStorage.setItem(STORAGE_KEY, JSON.stringify(stats)); } catch {}\n    }\n\n    function formatTime(totalSeconds) {\n      const minutes = Math.floor(totalSeconds / 60).toString().padStart(2, '0');\n      const seconds = (totalSeconds % 60).toString().padStart(2, '0');\n      return minutes + ':' + seconds;\n    }\n\n    function getCue(seconds) {\n      const program = programMap[currentProgram] || programMap.calm;\n      const pattern = program.pattern;\n      const cycle = pattern.reduce((sum, value) => sum + Number(value), 0);\n      const phase = seconds % cycle;\n      if (phase < pattern[0]) return { key: 'inhale', text: 'Inhale' };\n      if (phase < pattern[0] + pattern[1]) return { key: 'hold', text: 'Hold' };\n      if (phase < pattern[0] + pattern[1] + pattern[2]) return { key: 'exhale', text: 'Exhale' };\n      return { key: 'soften', text: 'Soften' };\n    }\n\n    function renderStats() {\n      if (sessionsEl) sessionsEl.textContent = String(stats.sessions);\n      if (minutesEl) minutesEl.textContent = String(stats.minutes);\n      if (!logEl) return;\n      logEl.textContent = '';\n      if (!stats.log.length) {\n        const item = document.createElement('li');\n        item.textContent = 'No sessions completed yet.';\n        logEl.appendChild(item);\n        return;\n      }\n      stats.log.forEach((entry) => {\n        const item = document.createElement('li');\n        const name = document.createElement('span');\n        const meta = document.createElement('span');\n        name.textContent = entry.name || 'Ocean session';\n        meta.textContent = (entry.length || '0m') + ' ' + (entry.time || '');\n        item.append(name, meta);\n        logEl.appendChild(item);\n      });\n    }\n\n    function renderProgram() {\n      const program = programMap[currentProgram] || programMap.calm;\n      root.setAttribute('data-program', currentProgram);\n      if (focusOverlay) focusOverlay.setAttribute('data-program', currentProgram);\n      if (noteEl) noteEl.textContent = program.note;\n      if (focusProgramEl) focusProgramEl.textContent = program.name;\n      if (promptsEl) {\n        promptsEl.textContent = '';\n        program.prompts.forEach((prompt) => {\n          const item = document.createElement('li');\n          item.textContent = prompt;\n          promptsEl.appendChild(item);\n        });\n      }\n    }\n\n    function render() {\n      const remaining = Math.max(duration - elapsed, 0);\n      const progress = duration > 0 ? Math.min((elapsed / duration) * 100, 100) : 0;\n      const cue = getCue(elapsed);\n      const timeText = formatTime(remaining);\n      root.setAttribute('data-cue', cue.key);\n      if (focusOverlay) focusOverlay.setAttribute('data-cue', cue.key);\n      if (cueEl) cueEl.textContent = cue.text;\n      if (focusCueEl) focusCueEl.textContent = cue.text;\n      if (timeEl) timeEl.textContent = timeText;\n      if (focusTimeEl) focusTimeEl.textContent = timeText;\n      if (sessionNameEl) sessionNameEl.textContent = sessionName;\n      if (progressEl) progressEl.style.width = progress + '%';\n    }\n\n    function stopTone() {\n      if (tonePulse) {\n        clearInterval(tonePulse);\n        tonePulse = null;\n      }\n      if (toneNodes) {\n        toneNodes.forEach((node) => {\n          try {\n            if (typeof node.stop === 'function') node.stop();\n          } catch {}\n          try { node.disconnect(); } catch {}\n        });\n        toneNodes = null;\n      }\n    }\n\n    function startTone() {\n      const Ctor = window.AudioContext || window.webkitAudioContext;\n      if (!Ctor) {\n        if (toneToggle) toneToggle.checked = false;\n        return;\n      }\n      if (toneNodes) return;\n      try {\n        audioCtx = audioCtx || new Ctor();\n        if (audioCtx.state === 'suspended') audioCtx.resume();\n        const now = audioCtx.currentTime;\n        const low = audioCtx.createOscillator();\n        const shimmer = audioCtx.createOscillator();\n        const lowGain = audioCtx.createGain();\n        const shimmerGain = audioCtx.createGain();\n        const filter = audioCtx.createBiquadFilter();\n        const master = audioCtx.createGain();\n        const isMoon = currentProgram === 'moon';\n        const isCurrent = currentProgram === 'current';\n\n        low.type = 'sine';\n        shimmer.type = isMoon ? 'sine' : 'triangle';\n        low.frequency.value = isMoon ? 72 : 92;\n        shimmer.frequency.value = isCurrent ? 212 : 184;\n        lowGain.gain.value = isCurrent ? 0.05 : 0.035;\n        shimmerGain.gain.value = isMoon ? 0.005 : 0.009;\n        filter.type = 'lowpass';\n        filter.frequency.value = isMoon ? 380 : 540;\n        master.gain.value = 0.42;\n\n        low.connect(lowGain);\n        shimmer.connect(shimmerGain);\n        lowGain.connect(filter);\n        shimmerGain.connect(filter);\n        filter.connect(master);\n        master.connect(audioCtx.destination);\n        low.start(now);\n        shimmer.start(now);\n\n        tonePulse = setInterval(() => {\n          const t = audioCtx.currentTime;\n          master.gain.cancelScheduledValues(t);\n          master.gain.setValueAtTime(master.gain.value, t);\n          master.gain.linearRampToValueAtTime(isCurrent ? 0.72 : 0.64, t + 1.4);\n          master.gain.linearRampToValueAtTime(0.36, t + 3);\n        }, 3000);\n\n        toneNodes = [low, shimmer, lowGain, shimmerGain, filter, master];\n      } catch {\n        if (toneToggle) toneToggle.checked = false;\n      }\n    }\n\n    function pause(stopAudio) {\n      clearInterval(timer);\n      timer = null;\n      root.removeAttribute('data-running');\n      if (stateEl) stateEl.textContent = elapsed >= duration ? 'COMPLETE' : 'READY';\n      if (startBtn) {\n        startBtn.textContent = elapsed >= duration ? 'Done' : 'Start';\n        startBtn.setAttribute('aria-pressed', 'false');\n      }\n      if (stopAudio) {\n        if (toneToggle) toneToggle.checked = false;\n        stopTone();\n      }\n    }\n\n    function recordCompletion() {\n      const minutes = Math.round(duration / 60);\n      const time = new Date().toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' });\n      const program = programMap[currentProgram] || programMap.calm;\n      stats.sessions += 1;\n      stats.minutes += minutes;\n      stats.log.unshift({ name: program.name, length: minutes + 'm', time });\n      stats.log = stats.log.slice(0, 5);\n      saveStats();\n      renderStats();\n    }\n\n    function complete() {\n      pause(false);\n      recordCompletion();\n      root.setAttribute('data-cue', 'soften');\n      if (focusOverlay) focusOverlay.setAttribute('data-cue', 'soften');\n      if (cueEl) cueEl.textContent = 'Complete';\n      if (focusCueEl) focusCueEl.textContent = 'Complete';\n      if (toneToggle) toneToggle.checked = false;\n      stopTone();\n    }\n\n    function start() {\n      if (timer) {\n        pause(true);\n        return;\n      }\n      if (elapsed >= duration) elapsed = 0;\n      root.setAttribute('data-running', 'true');\n      if (stateEl) stateEl.textContent = 'BREATHING';\n      if (startBtn) {\n        startBtn.textContent = 'Pause';\n        startBtn.setAttribute('aria-pressed', 'true');\n      }\n      if (toneToggle && toneToggle.checked) startTone();\n      render();\n      timer = setInterval(() => {\n        elapsed += 1;\n        if (elapsed >= duration) {\n          elapsed = duration;\n          render();\n          complete();\n          return;\n        }\n        render();\n      }, 1000);\n    }\n\n    function reset() {\n      pause(true);\n      elapsed = 0;\n      if (stateEl) stateEl.textContent = 'READY';\n      if (startBtn) startBtn.textContent = 'Start';\n      render();\n    }\n\n    function openFocus() {\n      if (!focusOverlay) return;\n      if (!timer) start();\n      focusOverlay.classList.add('is-active');\n      focusOverlay.setAttribute('aria-hidden', 'false');\n      if (focusClose) focusClose.focus();\n    }\n\n    function closeFocus() {\n      if (!focusOverlay) return;\n      focusOverlay.classList.remove('is-active');\n      focusOverlay.setAttribute('aria-hidden', 'true');\n    }\n\n    programBtns.forEach((btn) => {\n      btn.addEventListener('click', () => {\n        const next = btn.getAttribute('data-program') || 'calm';\n        if (!programMap[next]) return;\n        currentProgram = next;\n        programBtns.forEach((item) => item.classList.toggle('is-active', item === btn));\n        reset();\n        renderProgram();\n        render();\n      });\n    });\n\n    durationBtns.forEach((btn) => {\n      btn.addEventListener('click', () => {\n        durationBtns.forEach((item) => item.classList.toggle('is-active', item === btn));\n        duration = Number(btn.getAttribute('data-duration') || 120);\n        sessionName = btn.getAttribute('data-session-name') || 'Ocean reset';\n        reset();\n      });\n    });\n\n    if (startBtn) startBtn.addEventListener('click', start);\n    if (resetBtn) resetBtn.addEventListener('click', reset);\n    if (focusBtn) focusBtn.addEventListener('click', openFocus);\n    if (focusClose) focusClose.addEventListener('click', closeFocus);\n    if (focusOverlay) {\n      focusOverlay.addEventListener('click', (event) => {\n        if (event.target === focusOverlay) closeFocus();\n      });\n    }\n    if (toneToggle) {\n      toneToggle.addEventListener('change', () => {\n        if (toneToggle.checked) startTone();\n        else stopTone();\n      });\n    }\n    if (clearLogBtn) {\n      clearLogBtn.addEventListener('click', () => {\n        stats = { sessions: 0, minutes: 0, log: [] };\n        saveStats();\n        renderStats();\n      });\n    }\n    document.addEventListener('keydown', (event) => {\n      if (event.key === 'Escape') closeFocus();\n    });\n\n    renderProgram();\n    renderStats();\n    render();\n  })();\n})();<\/script>"])), maybeRenderHead(), programs.map((program) => renderTemplate`<button${addAttribute(["program-btn", program.id === "calm" && "is-active"], "class:list")} type="button"${addAttribute(program.id, "data-program")} data-astro-cid-d4tjut42> <span data-astro-cid-d4tjut42>${program.label}</span> <small data-astro-cid-d4tjut42>${program.pattern.join("-")}</small> </button>`), durations.map((duration, index) => renderTemplate`<button${addAttribute(["duration-btn", index === 0 && "is-active"], "class:list")} type="button"${addAttribute(duration.seconds, "data-duration")}${addAttribute(duration.name, "data-session-name")} data-astro-cid-d4tjut42> ${duration.label} </button>`), programs[0].note, programs[0].prompts.map((prompt) => renderTemplate`<li data-astro-cid-d4tjut42>${prompt}</li>`), addAttribute(artifact.alt, "aria-label"), addAttribute(artifact.image, "src"), addAttribute(artifact.alt, "alt"), artifact.name, artifact.description, artifact.url && renderTemplate`<a class="asset-link"${addAttribute(artifact.url, "href")} target="_blank" rel="noopener" data-astro-cid-d4tjut42>View on objkt &nearr;</a>`, defineScriptVars({ programs, durations }));
}, "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/components/OceanMeditation.astro", void 0);

export { $$OceanMeditation as $ };
