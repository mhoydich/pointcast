import { c as createComponent } from './astro-component_DWMcTjG3.mjs';
import 'piccolore';
import { r as renderComponent, a as renderTemplate, u as unescapeHTML, b as addAttribute, m as maybeRenderHead } from './prerender_CmTjnOuJ.mjs';
import { $ as $$BlockLayout } from './BlockLayout_DHviHHrD.mjs';
import { c as NOC_ENDPOINTS, d as NOC_STORAGE_KEYS, e as NOC_TARGET_POLICIES, f as NOC_FORMATIONS, g as NOC_AGENT_HANDOFFS, h as NOC_AGENT_SHIFT_PROTOCOL, i as NOC_V2_RELEASE, N as NOC_FIXTURES, j as NOC_AGENTS, k as NOC_CLUBS, l as NOC_VERSION, m as NOC_PRINCIPLES, o as NOC_RECEIPT_SHAPE, p as NOC_PACKET_SHAPE } from './nouns-open-circuit_BaZ8Hd0n.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$NounsOpenCircuit = createComponent(($$result, $$props, $$slots) => {
  const clientPayload = {
    version: NOC_VERSION,
    clubs: NOC_CLUBS,
    agents: NOC_AGENTS,
    fixtures: NOC_FIXTURES,
    release: NOC_V2_RELEASE,
    agentShiftProtocol: NOC_AGENT_SHIFT_PROTOCOL,
    agentHandoffs: NOC_AGENT_HANDOFFS,
    formations: NOC_FORMATIONS,
    targetPolicies: NOC_TARGET_POLICIES,
    storageKeys: NOC_STORAGE_KEYS,
    endpoints: NOC_ENDPOINTS
  };
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "VideoGame",
    name: "Nouns Open Circuit",
    description: "A rival agent-operated league product for the same Nouns player pool: clubs, live agent shifts, tactics packets, fixture receipts, agent benches, and public JSON surfaces.",
    url: "https://pointcast.xyz/nouns-open-circuit",
    gamePlatform: "Web browser",
    genre: "Auto battler league desk",
    inLanguage: "en-US"
  };
  return renderTemplate`${renderComponent($$result, "BlockLayout", $$BlockLayout, { "title": "Nouns Open Circuit", "description": "A rival agent-operated league product for the same Nouns player pool: clubs, live agent shifts, tactics packets, fixture receipts, agent benches, and public JSON surfaces.", "image": "/images/og/battle.png", "jsonLd": jsonLd, "alternates": [{ type: "application/json", href: "/nouns-open-circuit.json", title: "Nouns Open Circuit manifest (JSON)" }], "frame": {
    image: "https://pointcast.xyz/images/og/battle.png",
    buttons: [
      { label: "Open Circuit", action: "link", target: "https://pointcast.xyz/nouns-open-circuit" },
      { label: "Circuit JSON", action: "link", target: "https://pointcast.xyz/nouns-open-circuit.json" },
      { label: "Nation Battler", action: "link", target: "https://pointcast.xyz/nouns-nation-battler" },
      { label: "Agents", action: "link", target: "https://pointcast.xyz/agents.json" }
    ]
  }, "data-astro-cid-ptwhsag3": true }, { "default": ($$result2) => renderTemplate(_a || (_a = __template([" ", '<main class="noc" data-astro-cid-ptwhsag3> <section class="desk" aria-label="Nouns Open Circuit league desk" data-astro-cid-ptwhsag3> <div class="desk__lead" data-astro-cid-ptwhsag3> <p class="kicker" data-astro-cid-ptwhsag3>CH.BTL / RIVAL LEAGUE / AGENT OPERATED / V2</p> <h1 data-astro-cid-ptwhsag3>Nouns Open Circuit</h1> <p data-astro-cid-ptwhsag3>\nSame Nouns. New league. Clubs publish agent tactics before kickoff, agents run visible shifts during the match,\n          and every fixture resolves into receipt-shaped records that humans can watch and machine readers can cite.\n</p> <div class="desk__links" aria-label="Machine-readable links" data-astro-cid-ptwhsag3> <a href="/nouns-open-circuit.json" data-astro-cid-ptwhsag3>/nouns-open-circuit.json</a> <a id="deskMatchLink"', ' data-astro-cid-ptwhsag3>match receipt</a> <a href="/nouns-nation-battler" data-astro-cid-ptwhsag3>incumbent league</a> <a href="/games/nouns-nation-battler/assets/manifest.json" data-astro-cid-ptwhsag3>sprite manifest</a> </div> </div> <div class="fixture-card" aria-label="Featured fixture" data-astro-cid-ptwhsag3> <span id="fixtureTag" data-astro-cid-ptwhsag3>Rivalry seed</span> <strong id="fixtureTitle" data-astro-cid-ptwhsag3>Opening Desk Match</strong> <div class="fixture-card__match" data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3> <img id="homeNoun"', ' alt="" loading="eager" data-astro-cid-ptwhsag3> <b id="homeCode" data-astro-cid-ptwhsag3>BPC</b> <strong id="fixtureHomeLiveScore" data-astro-cid-ptwhsag3>0</strong> </div> <em data-astro-cid-ptwhsag3>vs</em> <div data-astro-cid-ptwhsag3> <img id="awayNoun"', ` alt="" loading="eager" data-astro-cid-ptwhsag3> <b id="awayCode" data-astro-cid-ptwhsag3>PHC</b> <strong id="fixtureAwayLiveScore" data-astro-cid-ptwhsag3>0</strong> </div> </div> <div class="fixture-card__runner" aria-label="Visible match loop" data-astro-cid-ptwhsag3> <b id="fixtureRunnerState" data-astro-cid-ptwhsag3>auto loop armed</b> <span id="fixtureRunnerClock" data-astro-cid-ptwhsag3>00'</span> <em id="fixtureRunnerDelta" data-astro-cid-ptwhsag3>waiting for SCOUT-01</em> </div> <div class="fixture-card__meter" aria-hidden="true" data-astro-cid-ptwhsag3> <span id="fixtureMomentumBar" data-astro-cid-ptwhsag3></span> </div> <p id="fixtureSeed" data-astro-cid-ptwhsag3>seed: noc-open-001</p> </div> </section> <section class="club-switcher" aria-label="Club selection" data-astro-cid-ptwhsag3> `, ' </section> <section class="ops-grid" aria-label="League operations" data-astro-cid-ptwhsag3> <article class="club-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Selected Club</span> <strong id="clubName" data-astro-cid-ptwhsag3>', '</strong> </div> <div class="club-panel__body" data-astro-cid-ptwhsag3> <img id="clubImage"', ' alt="" loading="lazy" data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3> <p id="clubDoctrine" data-astro-cid-ptwhsag3>', '</p> <dl data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3><dt data-astro-cid-ptwhsag3>Operator</dt><dd id="clubOperator" data-astro-cid-ptwhsag3>', '</dd></div> <div data-astro-cid-ptwhsag3><dt data-astro-cid-ptwhsag3>Home</dt><dd id="clubHome" data-astro-cid-ptwhsag3>', '</dd></div> <div data-astro-cid-ptwhsag3><dt data-astro-cid-ptwhsag3>Record</dt><dd id="clubRecord" data-astro-cid-ptwhsag3>', '</dd></div> <div data-astro-cid-ptwhsag3><dt data-astro-cid-ptwhsag3>Heat</dt><dd id="clubHeat" data-astro-cid-ptwhsag3>', '</dd></div> </dl> </div> </div> <div class="noun-strip" id="nounStrip" aria-label="Club Noun pool" data-astro-cid-ptwhsag3></div> </article> <article class="agent-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Agent Bench</span> <strong data-astro-cid-ptwhsag3>', ' roles</strong> </div> <div class="agent-list" data-astro-cid-ptwhsag3> ', ` </div> </article> <article class="packet-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Tactics Packet</span> <strong id="packetStatus" data-astro-cid-ptwhsag3>ready</strong> </div> <div class="button-row" data-astro-cid-ptwhsag3> <button id="packetButton" type="button" data-astro-cid-ptwhsag3>Generate Packet</button> <button id="receiptButton" type="button" data-astro-cid-ptwhsag3>Issue Receipt</button> </div> <pre id="packetPreview" aria-label="Tactics packet preview" data-astro-cid-ptwhsag3></pre> </article> <article class="receipt-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Fixture Receipt</span> <strong id="receiptStatus" data-astro-cid-ptwhsag3>local preview</strong> </div> <div class="result-banner" id="resultBanner" data-astro-cid-ptwhsag3>Awaiting receipt.</div> <pre id="receiptPreview" aria-label="Receipt preview" data-astro-cid-ptwhsag3></pre> </article> <article class="live-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Live Agent Match</span> <strong id="liveStatus" data-astro-cid-ptwhsag3>shift 0</strong> </div> <div class="live-scoreboard" aria-label="Live match state" data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3> <span id="liveHomeCode" data-astro-cid-ptwhsag3>BPC</span> <strong id="liveHomeScore" data-astro-cid-ptwhsag3>0</strong> <small id="liveHomeMomentum" data-astro-cid-ptwhsag3>50 momentum</small> </div> <b id="liveClock" data-astro-cid-ptwhsag3>00'</b> <div data-astro-cid-ptwhsag3> <span id="liveAwayCode" data-astro-cid-ptwhsag3>PHC</span> <strong id="liveAwayScore" data-astro-cid-ptwhsag3>0</strong> <small id="liveAwayMomentum" data-astro-cid-ptwhsag3>50 momentum</small> </div> </div> <div class="button-row button-row--four" data-astro-cid-ptwhsag3> <button id="shiftButton" type="button" data-astro-cid-ptwhsag3>Run Agent Shift</button> <button id="loopButton" type="button" aria-pressed="true" data-astro-cid-ptwhsag3>Pause Loop</button> <button id="resetMatchButton" type="button" data-astro-cid-ptwhsag3>Reset Match</button> <a id="matchPageButton"`, ' data-astro-cid-ptwhsag3>Open Match Page</a> </div> <div class="proposal-grid" id="proposalGrid" aria-label="Agent proposals" data-astro-cid-ptwhsag3></div> <ol class="event-log" id="eventLog" aria-label="Agent event log" data-astro-cid-ptwhsag3></ol> </article> </section> <section class="table-grid" aria-label="League table and protocol" data-astro-cid-ptwhsag3> <article class="standings-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Club Table</span> <strong data-astro-cid-ptwhsag3>v0 desk</strong> </div> <div class="standings-list" id="standingsList" data-astro-cid-ptwhsag3></div> </article> <article class="protocol-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Protocol</span> <strong data-astro-cid-ptwhsag3>agent-readable</strong> </div> <div class="protocol-grid" data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>Packet shape</b> <code data-astro-cid-ptwhsag3>', "</code> </div> <div data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>Receipt shape</b> <code data-astro-cid-ptwhsag3>", "</code> </div> <div data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>Storage</b> <code data-astro-cid-ptwhsag3>", '</code> </div> <div data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>Discovery</b> <code data-astro-cid-ptwhsag3>/agents.json</code> </div> </div> </article> <article class="principles-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Constitution</span> <strong data-astro-cid-ptwhsag3>why rival</strong> </div> <ul data-astro-cid-ptwhsag3> ', ' </ul> </article> </section> </main> <script type="application/json" id="noc-data">', "<\/script> <script>\n    const dataNode = document.querySelector('#noc-data');\n    const data = JSON.parse(dataNode?.textContent || '{}');\n    const clubs = data.clubs || [];\n    const fixtures = data.fixtures || [];\n    const agents = data.agents || [];\n    const agentShiftProtocol = data.agentShiftProtocol || [];\n    const agentHandoffs = data.agentHandoffs || [];\n    const formations = data.formations || [];\n    const policies = data.targetPolicies || [];\n    const storageKeys = data.storageKeys || {};\n    const AUTO_SHIFT_MS = 1450;\n    const MATCH_TURNS = 8;\n    let loopTimer = null;\n    const state = {\n      clubSlug: localStorage.getItem(storageKeys.club) || clubs[0]?.slug || '',\n      packetNonce: 0,\n      packet: null,\n      receipt: null,\n      liveState: null,\n      looping: true,\n    };\n\n    const $ = (selector) => document.querySelector(selector);\n    const clubButtons = [...document.querySelectorAll('[data-club]')];\n\n    function hashText(text) {\n      let hash = 2166136261;\n      for (let index = 0; index < text.length; index += 1) {\n        hash ^= text.charCodeAt(index);\n        hash = Math.imul(hash, 16777619);\n      }\n      return hash >>> 0;\n    }\n\n    function shortHash(value) {\n      return hashText(JSON.stringify(value)).toString(36).padStart(7, '0').slice(0, 9);\n    }\n\n    function clubBySlug(slug) {\n      return clubs.find((club) => club.slug === slug) || clubs[0];\n    }\n\n    function fixtureForClub(club) {\n      return fixtures.find((fixture) => fixture.home === club.slug || fixture.away === club.slug) || fixtures[0];\n    }\n\n    function opponentFor(club, fixture) {\n      const opponentSlug = fixture.home === club.slug ? fixture.away : fixture.home;\n      return clubBySlug(opponentSlug);\n    }\n\n    function selectedClub() {\n      return clubBySlug(state.clubSlug);\n    }\n\n    function clamp(value, min, max) {\n      return Math.max(min, Math.min(max, value));\n    }\n\n    function initialLiveState(fixture = fixtureForClub(selectedClub())) {\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      return {\n        fixtureId: fixture.id,\n        turn: 0,\n        clock: 0,\n        final: false,\n        home: { slug: home.slug, score: 0, momentum: 50 },\n        away: { slug: away.slug, score: 0, momentum: 50 },\n        log: [],\n      };\n    }\n\n    function liveStateForFixture(fixture) {\n      if (!state.liveState || state.liveState.fixtureId !== fixture.id) {\n        state.liveState = initialLiveState(fixture);\n      }\n      return state.liveState;\n    }\n\n    function buildAgentShift(liveState) {\n      const fixture = fixtures.find((item) => item.id === liveState.fixtureId) || fixtureForClub(selectedClub());\n      const protocol = agentShiftProtocol[liveState.turn % agentShiftProtocol.length];\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      const packet = state.packet || buildPacket();\n      const base = hashText(`${fixture.seed}:${packet.formation}:${protocol.agent}:${liveState.turn}`);\n      const actor = base % 2 === 0 ? home : away;\n      const target = actor.slug === home.slug ? away : home;\n      const delta = base % 5 === 0 ? 2 : base % 3 === 0 ? 1 : 0;\n      const minute = Math.min(90, 8 + liveState.turn * 11);\n      return {\n        minute,\n        phase: protocol.phase,\n        agent: protocol.agent,\n        action: protocol.action,\n        artifact: protocol.artifact,\n        actor: actor.slug,\n        target: target.slug,\n        delta,\n        note: `${protocol.agent} turns ${protocol.artifact} into ${protocol.scoreboardEffect} for ${actor.code}`,\n      };\n    }\n\n    function applyAgentShift() {\n      const fixture = fixtureForClub(selectedClub());\n      const liveState = liveStateForFixture(fixture);\n      if (liveState.final) return;\n      const shift = buildAgentShift(liveState);\n      const side = shift.actor === liveState.home.slug ? liveState.home : liveState.away;\n      const other = shift.actor === liveState.home.slug ? liveState.away : liveState.home;\n      side.score += shift.delta;\n      side.momentum = clamp(side.momentum + 7 + shift.delta * 4, 0, 99);\n      other.momentum = clamp(other.momentum - 3, 0, 99);\n      liveState.turn += 1;\n      liveState.clock = shift.minute;\n      liveState.log.unshift(shift);\n      state.liveState = liveState;\n      localStorage.setItem(storageKeys.liveState, JSON.stringify(liveState));\n      localStorage.setItem(storageKeys.dispatch, JSON.stringify(shift));\n      if (liveState.turn >= MATCH_TURNS) finishLiveMatch(liveState);\n    }\n\n    function finishLiveMatch(liveState) {\n      if (liveState.final) return;\n      liveState.clock = 90;\n      liveState.final = true;\n      state.liveState = liveState;\n      state.receipt = buildReceipt(state.packet || buildPacket());\n      localStorage.setItem(storageKeys.liveState, JSON.stringify(liveState));\n      renderReceipt(state.receipt);\n    }\n\n    function buildPacket() {\n      const club = selectedClub();\n      const fixture = fixtureForClub(club);\n      const base = hashText(`${fixture.seed}:${club.slug}:${state.packetNonce}`);\n      const watchlist = club.nounIds.slice(0, 3).map((nounId, index) => ({\n        nounId,\n        note: `${agentShiftProtocol[index]?.phase || 'scout'} lane watch`,\n      }));\n      return {\n        schema: 'pointcast-nouns-open-circuit-tactics-v2',\n        fixtureId: fixture.id,\n        club: club.slug,\n        operatorAgent: club.operator,\n        nounPool: club.nounIds,\n        formation: formations[base % formations.length],\n        aggression: 42 + (base % 49),\n        riskBudget: 35 + ((base >> 4) % 46),\n        targetPolicy: policies[(base >> 3) % policies.length],\n        agentHandoffs,\n        watchlist,\n        proofRefs: [\n          data.endpoints.spriteManifest,\n          data.endpoints.json,\n          data.endpoints.incumbent,\n          `${data.endpoints.human}/match/${fixture.id}`,\n        ],\n      };\n    }\n\n    function buildReceipt(packet) {\n      const club = selectedClub();\n      const fixture = fixtureForClub(club);\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      const liveState = state.liveState?.fixtureId === fixture.id ? state.liveState : null;\n      const timeline = liveState?.log?.length ? [...liveState.log].reverse() : [];\n      const fallbackHome = 18 + (hashText(`${fixture.seed}:score:${home.slug}`) % 12);\n      const fallbackAway = Math.max(0, fallbackHome - (2 + (hashText(`${fixture.seed}:margin:${away.slug}`) % 9)));\n      const homeScore = liveState ? 12 + liveState.home.score : fallbackHome;\n      const awayScore = liveState ? 12 + liveState.away.score : fallbackAway;\n      const winner = homeScore >= awayScore ? home : away;\n      return {\n        schema: 'pointcast-nouns-open-circuit-receipt-v2',\n        fixtureId: fixture.id,\n        seed: fixture.seed,\n        winner: winner.slug,\n        score: `${homeScore}-${awayScore}`,\n        packetHash: shortHash(packet),\n        eventHash: shortHash({ fixture, winner: winner.slug, score: `${homeScore}-${awayScore}`, timeline }),\n        agentTrail: timeline.map((event) => ({\n          minute: event.minute,\n          agent: event.agent,\n          artifact: event.artifact,\n          hash: shortHash(event),\n        })),\n        timeline,\n        agents: agents.map((agent) => agent.slug),\n        citation: `${data.endpoints.human}/match/${fixture.id}`,\n      };\n    }\n\n    function renderFixture() {\n      const club = selectedClub();\n      const fixture = fixtureForClub(club);\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      $('#fixtureTag').textContent = fixture.tag;\n      $('#fixtureTitle').textContent = fixture.label;\n      $('#fixtureSeed').textContent = `seed: ${fixture.seed}`;\n      $('#homeNoun').src = home.image;\n      $('#homeCode').textContent = home.code;\n      $('#awayNoun').src = away.image;\n      $('#awayCode').textContent = away.code;\n      $('#deskMatchLink').href = fixture.route;\n      $('#matchPageButton').href = fixture.route;\n    }\n\n    function renderClub() {\n      const club = selectedClub();\n      $('#clubName').textContent = club.name;\n      $('#clubImage').src = club.image;\n      $('#clubDoctrine').textContent = club.doctrine;\n      $('#clubOperator').textContent = club.operator;\n      $('#clubHome').textContent = club.home;\n      $('#clubRecord').textContent = club.record;\n      $('#clubHeat').textContent = club.heat;\n      $('#nounStrip').innerHTML = club.nounIds.map((id) => `\n        <span>\n          <img src=\"/games/nouns-nation-battler/assets/noun-${id}.svg\" alt=\"\" loading=\"lazy\" />\n          <b>#${String(id).padStart(2, '0')}</b>\n        </span>\n      `).join('');\n      clubButtons.forEach((button) => {\n        button.classList.toggle('is-active', button.dataset.club === club.slug);\n      });\n    }\n\n    function renderTable() {\n      const sorted = [...clubs].sort((a, b) => b.heat - a.heat || a.name.localeCompare(b.name));\n      $('#standingsList').innerHTML = sorted.map((club, index) => `\n        <div class=\"standing-row\" style=\"--club:${club.color};\">\n          <b>${index + 1}</b>\n          <strong>${club.code}</strong>\n          <span>${club.record}</span>\n          <span>${club.heat} heat</span>\n          <em>${club.operator}</em>\n        </div>\n      `).join('');\n    }\n\n    function renderPacket(packet = state.packet || buildPacket()) {\n      state.packet = packet;\n      $('#packetPreview').textContent = JSON.stringify(packet, null, 2);\n      localStorage.setItem(storageKeys.packet, JSON.stringify(packet));\n    }\n\n    function renderReceipt(receipt = state.receipt) {\n      if (!receipt) {\n        $('#receiptPreview').textContent = JSON.stringify({\n          schema: 'pointcast-nouns-open-circuit-receipt-v2',\n          status: 'pending',\n        }, null, 2);\n        return;\n      }\n      const winner = clubBySlug(receipt.winner);\n      $('#receiptPreview').textContent = JSON.stringify(receipt, null, 2);\n      $('#receiptStatus').textContent = receipt.packetHash;\n      $('#resultBanner').textContent = `${winner.code} win ${receipt.score}`;\n      localStorage.setItem(storageKeys.receipt, JSON.stringify(receipt));\n    }\n\n    function renderLive() {\n      const fixture = fixtureForClub(selectedClub());\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      const liveState = liveStateForFixture(fixture);\n      $('#liveHomeCode').textContent = home.code;\n      $('#liveAwayCode').textContent = away.code;\n      $('#liveHomeScore').textContent = liveState.home.score;\n      $('#liveAwayScore').textContent = liveState.away.score;\n      $('#fixtureHomeLiveScore').textContent = liveState.home.score;\n      $('#fixtureAwayLiveScore').textContent = liveState.away.score;\n      $('#liveHomeMomentum').textContent = `${liveState.home.momentum} momentum`;\n      $('#liveAwayMomentum').textContent = `${liveState.away.momentum} momentum`;\n      $('#liveClock').textContent = `${String(liveState.clock).padStart(2, '0')}'`;\n      $('#fixtureRunnerClock').textContent = `${String(liveState.clock).padStart(2, '0')}'`;\n      $('#liveStatus').textContent = liveState.final ? 'final whistle' : `${state.looping ? 'loop' : 'paused'} shift ${liveState.turn}/${MATCH_TURNS}`;\n      const nextProtocol = agentShiftProtocol[liveState.turn % agentShiftProtocol.length];\n      const latestEvent = liveState.log[0];\n      const latestActor = latestEvent ? clubBySlug(latestEvent.actor) : null;\n      $('#fixtureRunnerState').textContent = liveState.final\n        ? 'final receipt issued'\n        : state.looping\n          ? 'auto loop running'\n          : 'loop paused';\n      $('#fixtureRunnerDelta').textContent = latestEvent\n        ? `${latestEvent.agent} -> ${latestActor.code} +${latestEvent.delta}`\n        : `waiting for ${nextProtocol.agent}`;\n      $('#fixtureMomentumBar').style.width = `${clamp(liveState.home.momentum, 8, 92)}%`;\n      $('#proposalGrid').innerHTML = `\n        <div>\n          <b>${nextProtocol.agent}</b>\n          <strong>${nextProtocol.action}</strong>\n          <span>${nextProtocol.artifact}</span>\n        </div>\n        <div>\n          <b>${fixture.tempo || 'open tempo'}</b>\n          <strong>${home.code} vs ${away.code}</strong>\n          <span>${fixture.seed}</span>\n        </div>\n        <div>\n          <b>${selectedClub().operator}</b>\n          <strong>${state.packet?.formation || 'packet pending'}</strong>\n          <span>${state.packet?.targetPolicy || 'target policy pending'}</span>\n        </div>\n      `;\n      $('#eventLog').innerHTML = liveState.log.length\n        ? liveState.log.map((event) => {\n            const actor = clubBySlug(event.actor);\n            return `<li><b>${event.minute}' ${event.agent}</b><span>${event.note}</span><em>${actor.code} +${event.delta}</em></li>`;\n          }).join('')\n        : '<li><b>00&#39; DESK</b><span>Agents waiting for first shift.</span><em>ready</em></li>';\n      localStorage.setItem(storageKeys.liveState, JSON.stringify(liveState));\n    }\n\n    function renderLoopButton() {\n      const loopButton = $('#loopButton');\n      loopButton.textContent = state.looping ? 'Pause Loop' : 'Start Loop';\n      loopButton.setAttribute('aria-pressed', String(state.looping));\n    }\n\n    function renderAll() {\n      const activeFixture = fixtureForClub(selectedClub());\n      if (state.packet && (state.packet.fixtureId !== activeFixture.id || state.packet.club !== state.clubSlug)) {\n        state.packet = null;\n      }\n      if (state.receipt && state.receipt.fixtureId !== activeFixture.id) {\n        state.receipt = null;\n      }\n      localStorage.setItem(storageKeys.club, state.clubSlug);\n      renderFixture();\n      renderClub();\n      renderTable();\n      renderPacket();\n      renderReceipt(state.receipt);\n      renderLive();\n      renderLoopButton();\n    }\n\n    function runLoopStep() {\n      const fixture = fixtureForClub(selectedClub());\n      const liveState = liveStateForFixture(fixture);\n      if (liveState.final) {\n        state.liveState = initialLiveState(fixture);\n        state.receipt = null;\n        $('#receiptStatus').textContent = 'local preview';\n        $('#resultBanner').textContent = 'Looping next match run.';\n        renderReceipt(null);\n      } else {\n        applyAgentShift();\n      }\n      renderLive();\n    }\n\n    function startMatchLoop() {\n      if (loopTimer) return;\n      state.looping = true;\n      renderLoopButton();\n      loopTimer = window.setInterval(runLoopStep, AUTO_SHIFT_MS);\n    }\n\n    function stopMatchLoop() {\n      if (loopTimer) {\n        window.clearInterval(loopTimer);\n        loopTimer = null;\n      }\n      state.looping = false;\n      renderLoopButton();\n    }\n\n    clubButtons.forEach((button) => {\n      button.addEventListener('click', () => {\n        state.clubSlug = button.dataset.club;\n        state.packetNonce += 1;\n        state.receipt = null;\n        state.liveState = null;\n        $('#receiptStatus').textContent = 'local preview';\n        $('#resultBanner').textContent = 'Awaiting receipt.';\n        renderAll();\n      });\n    });\n\n    $('#packetButton').addEventListener('click', () => {\n      state.packetNonce += 1;\n      const packet = buildPacket();\n      $('#packetStatus').textContent = shortHash(packet);\n      renderPacket(packet);\n      renderLive();\n    });\n\n    $('#receiptButton').addEventListener('click', () => {\n      const packet = state.packet || buildPacket();\n      state.receipt = buildReceipt(packet);\n      renderReceipt(state.receipt);\n    });\n\n    $('#shiftButton').addEventListener('click', () => {\n      applyAgentShift();\n      renderLive();\n    });\n\n    $('#loopButton').addEventListener('click', () => {\n      if (state.looping) {\n        stopMatchLoop();\n      } else {\n        startMatchLoop();\n      }\n      renderLive();\n    });\n\n    $('#resetMatchButton').addEventListener('click', () => {\n      state.liveState = initialLiveState(fixtureForClub(selectedClub()));\n      state.receipt = null;\n      localStorage.setItem(storageKeys.liveState, JSON.stringify(state.liveState));\n      $('#receiptStatus').textContent = 'local preview';\n      $('#resultBanner').textContent = 'Awaiting receipt.';\n      renderReceipt(null);\n      renderLive();\n    });\n\n    try {\n      state.packet = JSON.parse(localStorage.getItem(storageKeys.packet) || 'null');\n      state.receipt = JSON.parse(localStorage.getItem(storageKeys.receipt) || 'null');\n      state.liveState = JSON.parse(localStorage.getItem(storageKeys.liveState) || 'null');\n    } catch {\n      state.packet = null;\n      state.receipt = null;\n      state.liveState = null;\n    }\n    if (state.packet?.schema !== 'pointcast-nouns-open-circuit-tactics-v2') state.packet = null;\n    if (state.receipt?.schema !== 'pointcast-nouns-open-circuit-receipt-v2') state.receipt = null;\n    if (!state.liveState?.fixtureId) state.liveState = null;\n    if (state.liveState && typeof state.liveState.final !== 'boolean') state.liveState.final = false;\n\n    renderAll();\n    startMatchLoop();\n  <\/script> "], [" ", '<main class="noc" data-astro-cid-ptwhsag3> <section class="desk" aria-label="Nouns Open Circuit league desk" data-astro-cid-ptwhsag3> <div class="desk__lead" data-astro-cid-ptwhsag3> <p class="kicker" data-astro-cid-ptwhsag3>CH.BTL / RIVAL LEAGUE / AGENT OPERATED / V2</p> <h1 data-astro-cid-ptwhsag3>Nouns Open Circuit</h1> <p data-astro-cid-ptwhsag3>\nSame Nouns. New league. Clubs publish agent tactics before kickoff, agents run visible shifts during the match,\n          and every fixture resolves into receipt-shaped records that humans can watch and machine readers can cite.\n</p> <div class="desk__links" aria-label="Machine-readable links" data-astro-cid-ptwhsag3> <a href="/nouns-open-circuit.json" data-astro-cid-ptwhsag3>/nouns-open-circuit.json</a> <a id="deskMatchLink"', ' data-astro-cid-ptwhsag3>match receipt</a> <a href="/nouns-nation-battler" data-astro-cid-ptwhsag3>incumbent league</a> <a href="/games/nouns-nation-battler/assets/manifest.json" data-astro-cid-ptwhsag3>sprite manifest</a> </div> </div> <div class="fixture-card" aria-label="Featured fixture" data-astro-cid-ptwhsag3> <span id="fixtureTag" data-astro-cid-ptwhsag3>Rivalry seed</span> <strong id="fixtureTitle" data-astro-cid-ptwhsag3>Opening Desk Match</strong> <div class="fixture-card__match" data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3> <img id="homeNoun"', ' alt="" loading="eager" data-astro-cid-ptwhsag3> <b id="homeCode" data-astro-cid-ptwhsag3>BPC</b> <strong id="fixtureHomeLiveScore" data-astro-cid-ptwhsag3>0</strong> </div> <em data-astro-cid-ptwhsag3>vs</em> <div data-astro-cid-ptwhsag3> <img id="awayNoun"', ` alt="" loading="eager" data-astro-cid-ptwhsag3> <b id="awayCode" data-astro-cid-ptwhsag3>PHC</b> <strong id="fixtureAwayLiveScore" data-astro-cid-ptwhsag3>0</strong> </div> </div> <div class="fixture-card__runner" aria-label="Visible match loop" data-astro-cid-ptwhsag3> <b id="fixtureRunnerState" data-astro-cid-ptwhsag3>auto loop armed</b> <span id="fixtureRunnerClock" data-astro-cid-ptwhsag3>00'</span> <em id="fixtureRunnerDelta" data-astro-cid-ptwhsag3>waiting for SCOUT-01</em> </div> <div class="fixture-card__meter" aria-hidden="true" data-astro-cid-ptwhsag3> <span id="fixtureMomentumBar" data-astro-cid-ptwhsag3></span> </div> <p id="fixtureSeed" data-astro-cid-ptwhsag3>seed: noc-open-001</p> </div> </section> <section class="club-switcher" aria-label="Club selection" data-astro-cid-ptwhsag3> `, ' </section> <section class="ops-grid" aria-label="League operations" data-astro-cid-ptwhsag3> <article class="club-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Selected Club</span> <strong id="clubName" data-astro-cid-ptwhsag3>', '</strong> </div> <div class="club-panel__body" data-astro-cid-ptwhsag3> <img id="clubImage"', ' alt="" loading="lazy" data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3> <p id="clubDoctrine" data-astro-cid-ptwhsag3>', '</p> <dl data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3><dt data-astro-cid-ptwhsag3>Operator</dt><dd id="clubOperator" data-astro-cid-ptwhsag3>', '</dd></div> <div data-astro-cid-ptwhsag3><dt data-astro-cid-ptwhsag3>Home</dt><dd id="clubHome" data-astro-cid-ptwhsag3>', '</dd></div> <div data-astro-cid-ptwhsag3><dt data-astro-cid-ptwhsag3>Record</dt><dd id="clubRecord" data-astro-cid-ptwhsag3>', '</dd></div> <div data-astro-cid-ptwhsag3><dt data-astro-cid-ptwhsag3>Heat</dt><dd id="clubHeat" data-astro-cid-ptwhsag3>', '</dd></div> </dl> </div> </div> <div class="noun-strip" id="nounStrip" aria-label="Club Noun pool" data-astro-cid-ptwhsag3></div> </article> <article class="agent-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Agent Bench</span> <strong data-astro-cid-ptwhsag3>', ' roles</strong> </div> <div class="agent-list" data-astro-cid-ptwhsag3> ', ` </div> </article> <article class="packet-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Tactics Packet</span> <strong id="packetStatus" data-astro-cid-ptwhsag3>ready</strong> </div> <div class="button-row" data-astro-cid-ptwhsag3> <button id="packetButton" type="button" data-astro-cid-ptwhsag3>Generate Packet</button> <button id="receiptButton" type="button" data-astro-cid-ptwhsag3>Issue Receipt</button> </div> <pre id="packetPreview" aria-label="Tactics packet preview" data-astro-cid-ptwhsag3></pre> </article> <article class="receipt-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Fixture Receipt</span> <strong id="receiptStatus" data-astro-cid-ptwhsag3>local preview</strong> </div> <div class="result-banner" id="resultBanner" data-astro-cid-ptwhsag3>Awaiting receipt.</div> <pre id="receiptPreview" aria-label="Receipt preview" data-astro-cid-ptwhsag3></pre> </article> <article class="live-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Live Agent Match</span> <strong id="liveStatus" data-astro-cid-ptwhsag3>shift 0</strong> </div> <div class="live-scoreboard" aria-label="Live match state" data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3> <span id="liveHomeCode" data-astro-cid-ptwhsag3>BPC</span> <strong id="liveHomeScore" data-astro-cid-ptwhsag3>0</strong> <small id="liveHomeMomentum" data-astro-cid-ptwhsag3>50 momentum</small> </div> <b id="liveClock" data-astro-cid-ptwhsag3>00'</b> <div data-astro-cid-ptwhsag3> <span id="liveAwayCode" data-astro-cid-ptwhsag3>PHC</span> <strong id="liveAwayScore" data-astro-cid-ptwhsag3>0</strong> <small id="liveAwayMomentum" data-astro-cid-ptwhsag3>50 momentum</small> </div> </div> <div class="button-row button-row--four" data-astro-cid-ptwhsag3> <button id="shiftButton" type="button" data-astro-cid-ptwhsag3>Run Agent Shift</button> <button id="loopButton" type="button" aria-pressed="true" data-astro-cid-ptwhsag3>Pause Loop</button> <button id="resetMatchButton" type="button" data-astro-cid-ptwhsag3>Reset Match</button> <a id="matchPageButton"`, ' data-astro-cid-ptwhsag3>Open Match Page</a> </div> <div class="proposal-grid" id="proposalGrid" aria-label="Agent proposals" data-astro-cid-ptwhsag3></div> <ol class="event-log" id="eventLog" aria-label="Agent event log" data-astro-cid-ptwhsag3></ol> </article> </section> <section class="table-grid" aria-label="League table and protocol" data-astro-cid-ptwhsag3> <article class="standings-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Club Table</span> <strong data-astro-cid-ptwhsag3>v0 desk</strong> </div> <div class="standings-list" id="standingsList" data-astro-cid-ptwhsag3></div> </article> <article class="protocol-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Protocol</span> <strong data-astro-cid-ptwhsag3>agent-readable</strong> </div> <div class="protocol-grid" data-astro-cid-ptwhsag3> <div data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>Packet shape</b> <code data-astro-cid-ptwhsag3>', "</code> </div> <div data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>Receipt shape</b> <code data-astro-cid-ptwhsag3>", "</code> </div> <div data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>Storage</b> <code data-astro-cid-ptwhsag3>", '</code> </div> <div data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>Discovery</b> <code data-astro-cid-ptwhsag3>/agents.json</code> </div> </div> </article> <article class="principles-panel" data-astro-cid-ptwhsag3> <div class="panel-head" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>Constitution</span> <strong data-astro-cid-ptwhsag3>why rival</strong> </div> <ul data-astro-cid-ptwhsag3> ', ' </ul> </article> </section> </main> <script type="application/json" id="noc-data">', "<\/script> <script>\n    const dataNode = document.querySelector('#noc-data');\n    const data = JSON.parse(dataNode?.textContent || '{}');\n    const clubs = data.clubs || [];\n    const fixtures = data.fixtures || [];\n    const agents = data.agents || [];\n    const agentShiftProtocol = data.agentShiftProtocol || [];\n    const agentHandoffs = data.agentHandoffs || [];\n    const formations = data.formations || [];\n    const policies = data.targetPolicies || [];\n    const storageKeys = data.storageKeys || {};\n    const AUTO_SHIFT_MS = 1450;\n    const MATCH_TURNS = 8;\n    let loopTimer = null;\n    const state = {\n      clubSlug: localStorage.getItem(storageKeys.club) || clubs[0]?.slug || '',\n      packetNonce: 0,\n      packet: null,\n      receipt: null,\n      liveState: null,\n      looping: true,\n    };\n\n    const $ = (selector) => document.querySelector(selector);\n    const clubButtons = [...document.querySelectorAll('[data-club]')];\n\n    function hashText(text) {\n      let hash = 2166136261;\n      for (let index = 0; index < text.length; index += 1) {\n        hash ^= text.charCodeAt(index);\n        hash = Math.imul(hash, 16777619);\n      }\n      return hash >>> 0;\n    }\n\n    function shortHash(value) {\n      return hashText(JSON.stringify(value)).toString(36).padStart(7, '0').slice(0, 9);\n    }\n\n    function clubBySlug(slug) {\n      return clubs.find((club) => club.slug === slug) || clubs[0];\n    }\n\n    function fixtureForClub(club) {\n      return fixtures.find((fixture) => fixture.home === club.slug || fixture.away === club.slug) || fixtures[0];\n    }\n\n    function opponentFor(club, fixture) {\n      const opponentSlug = fixture.home === club.slug ? fixture.away : fixture.home;\n      return clubBySlug(opponentSlug);\n    }\n\n    function selectedClub() {\n      return clubBySlug(state.clubSlug);\n    }\n\n    function clamp(value, min, max) {\n      return Math.max(min, Math.min(max, value));\n    }\n\n    function initialLiveState(fixture = fixtureForClub(selectedClub())) {\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      return {\n        fixtureId: fixture.id,\n        turn: 0,\n        clock: 0,\n        final: false,\n        home: { slug: home.slug, score: 0, momentum: 50 },\n        away: { slug: away.slug, score: 0, momentum: 50 },\n        log: [],\n      };\n    }\n\n    function liveStateForFixture(fixture) {\n      if (!state.liveState || state.liveState.fixtureId !== fixture.id) {\n        state.liveState = initialLiveState(fixture);\n      }\n      return state.liveState;\n    }\n\n    function buildAgentShift(liveState) {\n      const fixture = fixtures.find((item) => item.id === liveState.fixtureId) || fixtureForClub(selectedClub());\n      const protocol = agentShiftProtocol[liveState.turn % agentShiftProtocol.length];\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      const packet = state.packet || buildPacket();\n      const base = hashText(\\`\\${fixture.seed}:\\${packet.formation}:\\${protocol.agent}:\\${liveState.turn}\\`);\n      const actor = base % 2 === 0 ? home : away;\n      const target = actor.slug === home.slug ? away : home;\n      const delta = base % 5 === 0 ? 2 : base % 3 === 0 ? 1 : 0;\n      const minute = Math.min(90, 8 + liveState.turn * 11);\n      return {\n        minute,\n        phase: protocol.phase,\n        agent: protocol.agent,\n        action: protocol.action,\n        artifact: protocol.artifact,\n        actor: actor.slug,\n        target: target.slug,\n        delta,\n        note: \\`\\${protocol.agent} turns \\${protocol.artifact} into \\${protocol.scoreboardEffect} for \\${actor.code}\\`,\n      };\n    }\n\n    function applyAgentShift() {\n      const fixture = fixtureForClub(selectedClub());\n      const liveState = liveStateForFixture(fixture);\n      if (liveState.final) return;\n      const shift = buildAgentShift(liveState);\n      const side = shift.actor === liveState.home.slug ? liveState.home : liveState.away;\n      const other = shift.actor === liveState.home.slug ? liveState.away : liveState.home;\n      side.score += shift.delta;\n      side.momentum = clamp(side.momentum + 7 + shift.delta * 4, 0, 99);\n      other.momentum = clamp(other.momentum - 3, 0, 99);\n      liveState.turn += 1;\n      liveState.clock = shift.minute;\n      liveState.log.unshift(shift);\n      state.liveState = liveState;\n      localStorage.setItem(storageKeys.liveState, JSON.stringify(liveState));\n      localStorage.setItem(storageKeys.dispatch, JSON.stringify(shift));\n      if (liveState.turn >= MATCH_TURNS) finishLiveMatch(liveState);\n    }\n\n    function finishLiveMatch(liveState) {\n      if (liveState.final) return;\n      liveState.clock = 90;\n      liveState.final = true;\n      state.liveState = liveState;\n      state.receipt = buildReceipt(state.packet || buildPacket());\n      localStorage.setItem(storageKeys.liveState, JSON.stringify(liveState));\n      renderReceipt(state.receipt);\n    }\n\n    function buildPacket() {\n      const club = selectedClub();\n      const fixture = fixtureForClub(club);\n      const base = hashText(\\`\\${fixture.seed}:\\${club.slug}:\\${state.packetNonce}\\`);\n      const watchlist = club.nounIds.slice(0, 3).map((nounId, index) => ({\n        nounId,\n        note: \\`\\${agentShiftProtocol[index]?.phase || 'scout'} lane watch\\`,\n      }));\n      return {\n        schema: 'pointcast-nouns-open-circuit-tactics-v2',\n        fixtureId: fixture.id,\n        club: club.slug,\n        operatorAgent: club.operator,\n        nounPool: club.nounIds,\n        formation: formations[base % formations.length],\n        aggression: 42 + (base % 49),\n        riskBudget: 35 + ((base >> 4) % 46),\n        targetPolicy: policies[(base >> 3) % policies.length],\n        agentHandoffs,\n        watchlist,\n        proofRefs: [\n          data.endpoints.spriteManifest,\n          data.endpoints.json,\n          data.endpoints.incumbent,\n          \\`\\${data.endpoints.human}/match/\\${fixture.id}\\`,\n        ],\n      };\n    }\n\n    function buildReceipt(packet) {\n      const club = selectedClub();\n      const fixture = fixtureForClub(club);\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      const liveState = state.liveState?.fixtureId === fixture.id ? state.liveState : null;\n      const timeline = liveState?.log?.length ? [...liveState.log].reverse() : [];\n      const fallbackHome = 18 + (hashText(\\`\\${fixture.seed}:score:\\${home.slug}\\`) % 12);\n      const fallbackAway = Math.max(0, fallbackHome - (2 + (hashText(\\`\\${fixture.seed}:margin:\\${away.slug}\\`) % 9)));\n      const homeScore = liveState ? 12 + liveState.home.score : fallbackHome;\n      const awayScore = liveState ? 12 + liveState.away.score : fallbackAway;\n      const winner = homeScore >= awayScore ? home : away;\n      return {\n        schema: 'pointcast-nouns-open-circuit-receipt-v2',\n        fixtureId: fixture.id,\n        seed: fixture.seed,\n        winner: winner.slug,\n        score: \\`\\${homeScore}-\\${awayScore}\\`,\n        packetHash: shortHash(packet),\n        eventHash: shortHash({ fixture, winner: winner.slug, score: \\`\\${homeScore}-\\${awayScore}\\`, timeline }),\n        agentTrail: timeline.map((event) => ({\n          minute: event.minute,\n          agent: event.agent,\n          artifact: event.artifact,\n          hash: shortHash(event),\n        })),\n        timeline,\n        agents: agents.map((agent) => agent.slug),\n        citation: \\`\\${data.endpoints.human}/match/\\${fixture.id}\\`,\n      };\n    }\n\n    function renderFixture() {\n      const club = selectedClub();\n      const fixture = fixtureForClub(club);\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      $('#fixtureTag').textContent = fixture.tag;\n      $('#fixtureTitle').textContent = fixture.label;\n      $('#fixtureSeed').textContent = \\`seed: \\${fixture.seed}\\`;\n      $('#homeNoun').src = home.image;\n      $('#homeCode').textContent = home.code;\n      $('#awayNoun').src = away.image;\n      $('#awayCode').textContent = away.code;\n      $('#deskMatchLink').href = fixture.route;\n      $('#matchPageButton').href = fixture.route;\n    }\n\n    function renderClub() {\n      const club = selectedClub();\n      $('#clubName').textContent = club.name;\n      $('#clubImage').src = club.image;\n      $('#clubDoctrine').textContent = club.doctrine;\n      $('#clubOperator').textContent = club.operator;\n      $('#clubHome').textContent = club.home;\n      $('#clubRecord').textContent = club.record;\n      $('#clubHeat').textContent = club.heat;\n      $('#nounStrip').innerHTML = club.nounIds.map((id) => \\`\n        <span>\n          <img src=\"/games/nouns-nation-battler/assets/noun-\\${id}.svg\" alt=\"\" loading=\"lazy\" />\n          <b>#\\${String(id).padStart(2, '0')}</b>\n        </span>\n      \\`).join('');\n      clubButtons.forEach((button) => {\n        button.classList.toggle('is-active', button.dataset.club === club.slug);\n      });\n    }\n\n    function renderTable() {\n      const sorted = [...clubs].sort((a, b) => b.heat - a.heat || a.name.localeCompare(b.name));\n      $('#standingsList').innerHTML = sorted.map((club, index) => \\`\n        <div class=\"standing-row\" style=\"--club:\\${club.color};\">\n          <b>\\${index + 1}</b>\n          <strong>\\${club.code}</strong>\n          <span>\\${club.record}</span>\n          <span>\\${club.heat} heat</span>\n          <em>\\${club.operator}</em>\n        </div>\n      \\`).join('');\n    }\n\n    function renderPacket(packet = state.packet || buildPacket()) {\n      state.packet = packet;\n      $('#packetPreview').textContent = JSON.stringify(packet, null, 2);\n      localStorage.setItem(storageKeys.packet, JSON.stringify(packet));\n    }\n\n    function renderReceipt(receipt = state.receipt) {\n      if (!receipt) {\n        $('#receiptPreview').textContent = JSON.stringify({\n          schema: 'pointcast-nouns-open-circuit-receipt-v2',\n          status: 'pending',\n        }, null, 2);\n        return;\n      }\n      const winner = clubBySlug(receipt.winner);\n      $('#receiptPreview').textContent = JSON.stringify(receipt, null, 2);\n      $('#receiptStatus').textContent = receipt.packetHash;\n      $('#resultBanner').textContent = \\`\\${winner.code} win \\${receipt.score}\\`;\n      localStorage.setItem(storageKeys.receipt, JSON.stringify(receipt));\n    }\n\n    function renderLive() {\n      const fixture = fixtureForClub(selectedClub());\n      const home = clubBySlug(fixture.home);\n      const away = clubBySlug(fixture.away);\n      const liveState = liveStateForFixture(fixture);\n      $('#liveHomeCode').textContent = home.code;\n      $('#liveAwayCode').textContent = away.code;\n      $('#liveHomeScore').textContent = liveState.home.score;\n      $('#liveAwayScore').textContent = liveState.away.score;\n      $('#fixtureHomeLiveScore').textContent = liveState.home.score;\n      $('#fixtureAwayLiveScore').textContent = liveState.away.score;\n      $('#liveHomeMomentum').textContent = \\`\\${liveState.home.momentum} momentum\\`;\n      $('#liveAwayMomentum').textContent = \\`\\${liveState.away.momentum} momentum\\`;\n      $('#liveClock').textContent = \\`\\${String(liveState.clock).padStart(2, '0')}'\\`;\n      $('#fixtureRunnerClock').textContent = \\`\\${String(liveState.clock).padStart(2, '0')}'\\`;\n      $('#liveStatus').textContent = liveState.final ? 'final whistle' : \\`\\${state.looping ? 'loop' : 'paused'} shift \\${liveState.turn}/\\${MATCH_TURNS}\\`;\n      const nextProtocol = agentShiftProtocol[liveState.turn % agentShiftProtocol.length];\n      const latestEvent = liveState.log[0];\n      const latestActor = latestEvent ? clubBySlug(latestEvent.actor) : null;\n      $('#fixtureRunnerState').textContent = liveState.final\n        ? 'final receipt issued'\n        : state.looping\n          ? 'auto loop running'\n          : 'loop paused';\n      $('#fixtureRunnerDelta').textContent = latestEvent\n        ? \\`\\${latestEvent.agent} -> \\${latestActor.code} +\\${latestEvent.delta}\\`\n        : \\`waiting for \\${nextProtocol.agent}\\`;\n      $('#fixtureMomentumBar').style.width = \\`\\${clamp(liveState.home.momentum, 8, 92)}%\\`;\n      $('#proposalGrid').innerHTML = \\`\n        <div>\n          <b>\\${nextProtocol.agent}</b>\n          <strong>\\${nextProtocol.action}</strong>\n          <span>\\${nextProtocol.artifact}</span>\n        </div>\n        <div>\n          <b>\\${fixture.tempo || 'open tempo'}</b>\n          <strong>\\${home.code} vs \\${away.code}</strong>\n          <span>\\${fixture.seed}</span>\n        </div>\n        <div>\n          <b>\\${selectedClub().operator}</b>\n          <strong>\\${state.packet?.formation || 'packet pending'}</strong>\n          <span>\\${state.packet?.targetPolicy || 'target policy pending'}</span>\n        </div>\n      \\`;\n      $('#eventLog').innerHTML = liveState.log.length\n        ? liveState.log.map((event) => {\n            const actor = clubBySlug(event.actor);\n            return \\`<li><b>\\${event.minute}' \\${event.agent}</b><span>\\${event.note}</span><em>\\${actor.code} +\\${event.delta}</em></li>\\`;\n          }).join('')\n        : '<li><b>00&#39; DESK</b><span>Agents waiting for first shift.</span><em>ready</em></li>';\n      localStorage.setItem(storageKeys.liveState, JSON.stringify(liveState));\n    }\n\n    function renderLoopButton() {\n      const loopButton = $('#loopButton');\n      loopButton.textContent = state.looping ? 'Pause Loop' : 'Start Loop';\n      loopButton.setAttribute('aria-pressed', String(state.looping));\n    }\n\n    function renderAll() {\n      const activeFixture = fixtureForClub(selectedClub());\n      if (state.packet && (state.packet.fixtureId !== activeFixture.id || state.packet.club !== state.clubSlug)) {\n        state.packet = null;\n      }\n      if (state.receipt && state.receipt.fixtureId !== activeFixture.id) {\n        state.receipt = null;\n      }\n      localStorage.setItem(storageKeys.club, state.clubSlug);\n      renderFixture();\n      renderClub();\n      renderTable();\n      renderPacket();\n      renderReceipt(state.receipt);\n      renderLive();\n      renderLoopButton();\n    }\n\n    function runLoopStep() {\n      const fixture = fixtureForClub(selectedClub());\n      const liveState = liveStateForFixture(fixture);\n      if (liveState.final) {\n        state.liveState = initialLiveState(fixture);\n        state.receipt = null;\n        $('#receiptStatus').textContent = 'local preview';\n        $('#resultBanner').textContent = 'Looping next match run.';\n        renderReceipt(null);\n      } else {\n        applyAgentShift();\n      }\n      renderLive();\n    }\n\n    function startMatchLoop() {\n      if (loopTimer) return;\n      state.looping = true;\n      renderLoopButton();\n      loopTimer = window.setInterval(runLoopStep, AUTO_SHIFT_MS);\n    }\n\n    function stopMatchLoop() {\n      if (loopTimer) {\n        window.clearInterval(loopTimer);\n        loopTimer = null;\n      }\n      state.looping = false;\n      renderLoopButton();\n    }\n\n    clubButtons.forEach((button) => {\n      button.addEventListener('click', () => {\n        state.clubSlug = button.dataset.club;\n        state.packetNonce += 1;\n        state.receipt = null;\n        state.liveState = null;\n        $('#receiptStatus').textContent = 'local preview';\n        $('#resultBanner').textContent = 'Awaiting receipt.';\n        renderAll();\n      });\n    });\n\n    $('#packetButton').addEventListener('click', () => {\n      state.packetNonce += 1;\n      const packet = buildPacket();\n      $('#packetStatus').textContent = shortHash(packet);\n      renderPacket(packet);\n      renderLive();\n    });\n\n    $('#receiptButton').addEventListener('click', () => {\n      const packet = state.packet || buildPacket();\n      state.receipt = buildReceipt(packet);\n      renderReceipt(state.receipt);\n    });\n\n    $('#shiftButton').addEventListener('click', () => {\n      applyAgentShift();\n      renderLive();\n    });\n\n    $('#loopButton').addEventListener('click', () => {\n      if (state.looping) {\n        stopMatchLoop();\n      } else {\n        startMatchLoop();\n      }\n      renderLive();\n    });\n\n    $('#resetMatchButton').addEventListener('click', () => {\n      state.liveState = initialLiveState(fixtureForClub(selectedClub()));\n      state.receipt = null;\n      localStorage.setItem(storageKeys.liveState, JSON.stringify(state.liveState));\n      $('#receiptStatus').textContent = 'local preview';\n      $('#resultBanner').textContent = 'Awaiting receipt.';\n      renderReceipt(null);\n      renderLive();\n    });\n\n    try {\n      state.packet = JSON.parse(localStorage.getItem(storageKeys.packet) || 'null');\n      state.receipt = JSON.parse(localStorage.getItem(storageKeys.receipt) || 'null');\n      state.liveState = JSON.parse(localStorage.getItem(storageKeys.liveState) || 'null');\n    } catch {\n      state.packet = null;\n      state.receipt = null;\n      state.liveState = null;\n    }\n    if (state.packet?.schema !== 'pointcast-nouns-open-circuit-tactics-v2') state.packet = null;\n    if (state.receipt?.schema !== 'pointcast-nouns-open-circuit-receipt-v2') state.receipt = null;\n    if (!state.liveState?.fixtureId) state.liveState = null;\n    if (state.liveState && typeof state.liveState.final !== 'boolean') state.liveState.final = false;\n\n    renderAll();\n    startMatchLoop();\n  <\/script> "])), maybeRenderHead(), addAttribute(NOC_FIXTURES[0].route, "href"), addAttribute(NOC_CLUBS[0].image, "src"), addAttribute(NOC_CLUBS[5].image, "src"), NOC_CLUBS.map((club, index) => renderTemplate`<button${addAttribute(index === 0 ? "is-active" : "", "class")} type="button"${addAttribute(club.slug, "data-club")}${addAttribute(`--club:${club.color};`, "style")} data-astro-cid-ptwhsag3> <img${addAttribute(club.image, "src")} alt="" loading="lazy" data-astro-cid-ptwhsag3> <span data-astro-cid-ptwhsag3>${club.code}</span> <strong data-astro-cid-ptwhsag3>${club.name}</strong> </button>`), NOC_CLUBS[0].name, addAttribute(NOC_CLUBS[0].image, "src"), NOC_CLUBS[0].doctrine, NOC_CLUBS[0].operator, NOC_CLUBS[0].home, NOC_CLUBS[0].record, NOC_CLUBS[0].heat, NOC_AGENTS.length, NOC_AGENTS.map((agent) => renderTemplate`<div${addAttribute(`agent-row agent-row--${agent.status}`, "class")} data-astro-cid-ptwhsag3> <b data-astro-cid-ptwhsag3>${agent.callSign}</b> <strong data-astro-cid-ptwhsag3>${agent.label}</strong> <span data-astro-cid-ptwhsag3>${agent.output}</span> </div>`), addAttribute(NOC_FIXTURES[0].route, "href"), NOC_PACKET_SHAPE.schema, NOC_RECEIPT_SHAPE.schema, NOC_STORAGE_KEYS.packet, NOC_PRINCIPLES.map((principle) => renderTemplate`<li data-astro-cid-ptwhsag3>${principle}</li>`), unescapeHTML(JSON.stringify(clientPayload))) })}`;
}, "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/nouns-open-circuit.astro", void 0);

const $$file = "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/nouns-open-circuit.astro";
const $$url = "/nouns-open-circuit";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$NounsOpenCircuit,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
