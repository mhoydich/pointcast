import { c as createComponent } from './astro-component_DWMcTjG3.mjs';
import 'piccolore';
import { a as renderTemplate, r as renderComponent, m as maybeRenderHead, b as addAttribute } from './prerender_CmTjnOuJ.mjs';
import { $ as $$BlockLayout } from './BlockLayout_DHviHHrD.mjs';
import { $ as $$DrumNav } from './DrumNav_D5cUUl3f.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$DrumV15 = createComponent(($$result, $$props, $$slots) => {
  const title = "/drum-v15 — Hang Drum (handpan)";
  const description = "Eight steel tongues on a handpan. Tap to play. Calming melodic percussion. Multiplayer drum-hub variant on PointCast.";
  const jsonLd = { "@context": "https://schema.org", "@type": "WebPage", "@id": "https://pointcast.xyz/drum-v15", name: "PointCast Drum · v15 · Hang Drum", url: "https://pointcast.xyz/drum-v15", description };
  const TONGUES = [
    { id: "h0", note: "D3", hz: 146.83, kbd: "1", angle: 0 },
    { id: "h1", note: "A3", hz: 220, kbd: "2", angle: 45 },
    { id: "h2", note: "F3", hz: 174.61, kbd: "3", angle: 90 },
    { id: "h3", note: "C4", hz: 261.63, kbd: "4", angle: 135 },
    { id: "h4", note: "D4", hz: 293.66, kbd: "5", angle: 180 },
    { id: "h5", note: "A4", hz: 440, kbd: "6", angle: 225 },
    { id: "h6", note: "F4", hz: 349.23, kbd: "7", angle: 270 },
    { id: "h7", note: "C5", hz: 523.25, kbd: "8", angle: 315 }
  ];
  return renderTemplate(_a || (_a = __template(["", " <script>\n  (function () {\n    'use strict';\n    let ctx = null, master = null, lpf = null, conv = null, dryG = null, wetG = null;\n    let decayMul = 0.7, brightHz = 2400, reverbWet = 0.55;\n    function makeReverb(s, d) {\n      const rate = ctx.sampleRate, len = Math.floor(rate * s);\n      const buf = ctx.createBuffer(2, len, rate);\n      for (let ch = 0; ch < 2; ch++) {\n        const data = buf.getChannelData(ch);\n        for (let i = 0; i < len; i++) data[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, d);\n      }\n      return buf;\n    }\n    function ensure() {\n      if (ctx) return;\n      const AC = window.AudioContext || window.webkitAudioContext;\n      ctx = new AC();\n      lpf = ctx.createBiquadFilter(); lpf.type = 'lowpass'; lpf.frequency.value = brightHz; lpf.Q.value = 0.7;\n      master = ctx.createGain(); master.gain.value = 0.65;\n      conv = ctx.createConvolver(); conv.buffer = makeReverb(3.2, 2.4);\n      dryG = ctx.createGain(); dryG.gain.value = 1 - reverbWet * 0.5;\n      wetG = ctx.createGain(); wetG.gain.value = reverbWet;\n      lpf.connect(master); master.connect(dryG); master.connect(conv); conv.connect(wetG);\n      dryG.connect(ctx.destination); wetG.connect(ctx.destination);\n    }\n    function pluck(hz) {\n      ensure();\n      const t = ctx.currentTime;\n      const baseDecay = 2.4 * decayMul;\n      // metallic transient — short bandpass-shaped noise burst\n      const buf = ctx.createBuffer(1, ctx.sampleRate * 0.06, ctx.sampleRate);\n      const data = buf.getChannelData(0);\n      for (let i = 0; i < data.length; i++) data[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / data.length, 4);\n      const n = ctx.createBufferSource(); n.buffer = buf;\n      const nf = ctx.createBiquadFilter(); nf.type = 'bandpass'; nf.frequency.value = hz * 5; nf.Q.value = 6;\n      const ng = ctx.createGain();\n      ng.gain.setValueAtTime(0.001, t);\n      ng.gain.linearRampToValueAtTime(0.16, t + 0.001);\n      ng.gain.exponentialRampToValueAtTime(0.001, t + 0.08);\n      n.connect(nf); nf.connect(ng); ng.connect(lpf); n.start(t);\n      [[1, 0.4, baseDecay], [2.01, 0.18, baseDecay * 0.7], [3.97, 0.08, baseDecay * 0.5]].forEach(([mul, lvl, dec]) => {\n        const o = ctx.createOscillator(); o.type = 'sine'; o.frequency.value = hz * mul;\n        o.detune.value = (Math.random() * 2 - 1) * 5;\n        const bp = ctx.createBiquadFilter(); bp.type = 'bandpass'; bp.frequency.value = hz * mul; bp.Q.value = 12;\n        const g = ctx.createGain();\n        g.gain.setValueAtTime(0.001, t);\n        g.gain.linearRampToValueAtTime(lvl, t + 0.005);\n        g.gain.exponentialRampToValueAtTime(0.001, t + dec);\n        o.connect(bp); bp.connect(g); g.connect(lpf);\n        o.start(t); o.stop(t + dec + 0.05);\n      });\n    }\n    function sess() { try { const k='pc:session'; let s=localStorage.getItem(k); if(!s){s=Math.random().toString(36).slice(2)+Date.now().toString(36); localStorage.setItem(k,s);} return s; } catch { return 'hd-' + Math.random().toString(36).slice(2,10); } }\n    function broadcast(note, hz) { try { fetch('/api/sounds', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ type: 'hang', sessionId: sess(), note, hz: Math.round(hz) }) }); const el = document.getElementById('hd-broadcast'); if (el) el.textContent = `playing · ${note}`; } catch {} }\n    document.getElementById('hd-decay').addEventListener('input', (e) => { decayMul = Number(e.target.value) / 100; });\n    document.getElementById('hd-bright').addEventListener('input', (e) => { brightHz = Number(e.target.value); if (lpf && ctx) lpf.frequency.setTargetAtTime(brightHz, ctx.currentTime, 0.05); });\n    document.getElementById('hd-reverb').addEventListener('input', (e) => { reverbWet = Number(e.target.value) / 100; if (wetG && dryG && ctx) { wetG.gain.setTargetAtTime(reverbWet, ctx.currentTime, 0.05); dryG.gain.setTargetAtTime(1 - reverbWet * 0.5, ctx.currentTime, 0.05); } });\n    document.querySelectorAll('.hd__tongue').forEach((btn) => {\n      const hz = Number(btn.getAttribute('data-hz') || 0);\n      const note = btn.getAttribute('data-note') || '';\n      function play() { pluck(hz); btn.classList.remove('hd__tongue--hit'); void btn.offsetWidth; btn.classList.add('hd__tongue--hit'); broadcast(note, hz); }\n      btn.addEventListener('mousedown', play);\n      btn.addEventListener('touchstart', (e) => { play(); e.preventDefault(); }, { passive: false });\n      const kbd = (btn.getAttribute('data-kbd') || '').toLowerCase();\n      window.addEventListener('keydown', (e) => { if (!e.repeat && e.key.toLowerCase() === kbd) play(); });\n    });\n  })();\n<\/script>"], ["", " <script>\n  (function () {\n    'use strict';\n    let ctx = null, master = null, lpf = null, conv = null, dryG = null, wetG = null;\n    let decayMul = 0.7, brightHz = 2400, reverbWet = 0.55;\n    function makeReverb(s, d) {\n      const rate = ctx.sampleRate, len = Math.floor(rate * s);\n      const buf = ctx.createBuffer(2, len, rate);\n      for (let ch = 0; ch < 2; ch++) {\n        const data = buf.getChannelData(ch);\n        for (let i = 0; i < len; i++) data[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / len, d);\n      }\n      return buf;\n    }\n    function ensure() {\n      if (ctx) return;\n      const AC = window.AudioContext || window.webkitAudioContext;\n      ctx = new AC();\n      lpf = ctx.createBiquadFilter(); lpf.type = 'lowpass'; lpf.frequency.value = brightHz; lpf.Q.value = 0.7;\n      master = ctx.createGain(); master.gain.value = 0.65;\n      conv = ctx.createConvolver(); conv.buffer = makeReverb(3.2, 2.4);\n      dryG = ctx.createGain(); dryG.gain.value = 1 - reverbWet * 0.5;\n      wetG = ctx.createGain(); wetG.gain.value = reverbWet;\n      lpf.connect(master); master.connect(dryG); master.connect(conv); conv.connect(wetG);\n      dryG.connect(ctx.destination); wetG.connect(ctx.destination);\n    }\n    function pluck(hz) {\n      ensure();\n      const t = ctx.currentTime;\n      const baseDecay = 2.4 * decayMul;\n      // metallic transient — short bandpass-shaped noise burst\n      const buf = ctx.createBuffer(1, ctx.sampleRate * 0.06, ctx.sampleRate);\n      const data = buf.getChannelData(0);\n      for (let i = 0; i < data.length; i++) data[i] = (Math.random() * 2 - 1) * Math.pow(1 - i / data.length, 4);\n      const n = ctx.createBufferSource(); n.buffer = buf;\n      const nf = ctx.createBiquadFilter(); nf.type = 'bandpass'; nf.frequency.value = hz * 5; nf.Q.value = 6;\n      const ng = ctx.createGain();\n      ng.gain.setValueAtTime(0.001, t);\n      ng.gain.linearRampToValueAtTime(0.16, t + 0.001);\n      ng.gain.exponentialRampToValueAtTime(0.001, t + 0.08);\n      n.connect(nf); nf.connect(ng); ng.connect(lpf); n.start(t);\n      [[1, 0.4, baseDecay], [2.01, 0.18, baseDecay * 0.7], [3.97, 0.08, baseDecay * 0.5]].forEach(([mul, lvl, dec]) => {\n        const o = ctx.createOscillator(); o.type = 'sine'; o.frequency.value = hz * mul;\n        o.detune.value = (Math.random() * 2 - 1) * 5;\n        const bp = ctx.createBiquadFilter(); bp.type = 'bandpass'; bp.frequency.value = hz * mul; bp.Q.value = 12;\n        const g = ctx.createGain();\n        g.gain.setValueAtTime(0.001, t);\n        g.gain.linearRampToValueAtTime(lvl, t + 0.005);\n        g.gain.exponentialRampToValueAtTime(0.001, t + dec);\n        o.connect(bp); bp.connect(g); g.connect(lpf);\n        o.start(t); o.stop(t + dec + 0.05);\n      });\n    }\n    function sess() { try { const k='pc:session'; let s=localStorage.getItem(k); if(!s){s=Math.random().toString(36).slice(2)+Date.now().toString(36); localStorage.setItem(k,s);} return s; } catch { return 'hd-' + Math.random().toString(36).slice(2,10); } }\n    function broadcast(note, hz) { try { fetch('/api/sounds', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ type: 'hang', sessionId: sess(), note, hz: Math.round(hz) }) }); const el = document.getElementById('hd-broadcast'); if (el) el.textContent = \\`playing · \\${note}\\`; } catch {} }\n    document.getElementById('hd-decay').addEventListener('input', (e) => { decayMul = Number(e.target.value) / 100; });\n    document.getElementById('hd-bright').addEventListener('input', (e) => { brightHz = Number(e.target.value); if (lpf && ctx) lpf.frequency.setTargetAtTime(brightHz, ctx.currentTime, 0.05); });\n    document.getElementById('hd-reverb').addEventListener('input', (e) => { reverbWet = Number(e.target.value) / 100; if (wetG && dryG && ctx) { wetG.gain.setTargetAtTime(reverbWet, ctx.currentTime, 0.05); dryG.gain.setTargetAtTime(1 - reverbWet * 0.5, ctx.currentTime, 0.05); } });\n    document.querySelectorAll('.hd__tongue').forEach((btn) => {\n      const hz = Number(btn.getAttribute('data-hz') || 0);\n      const note = btn.getAttribute('data-note') || '';\n      function play() { pluck(hz); btn.classList.remove('hd__tongue--hit'); void btn.offsetWidth; btn.classList.add('hd__tongue--hit'); broadcast(note, hz); }\n      btn.addEventListener('mousedown', play);\n      btn.addEventListener('touchstart', (e) => { play(); e.preventDefault(); }, { passive: false });\n      const kbd = (btn.getAttribute('data-kbd') || '').toLowerCase();\n      window.addEventListener('keydown', (e) => { if (!e.repeat && e.key.toLowerCase() === kbd) play(); });\n    });\n  })();\n<\/script>"])), renderComponent($$result, "BlockLayout", $$BlockLayout, { "title": title, "description": description, "jsonLd": jsonLd, "data-astro-cid-ys4u3bda": true }, { "default": ($$result2) => renderTemplate` ${maybeRenderHead()}<main class="hd" id="hd-main" data-astro-cid-ys4u3bda> ${renderComponent($$result2, "DrumNav", $$DrumNav, { "current": "v14", "data-astro-cid-ys4u3bda": true })} <header class="hd__head" data-astro-cid-ys4u3bda> <p class="hd__kicker" data-astro-cid-ys4u3bda>DRUM HUB · V15 · HANG · HANDPAN · CALM · MULTIPLAYER</p> <h1 class="hd__title" data-astro-cid-ys4u3bda><em data-astro-cid-ys4u3bda>Eight tongues. Steel. Slow.</em></h1> <p class="hd__dek" data-astro-cid-ys4u3bda>A handpan in D minor. Tap a tongue. Let it ring. Multiplayer — every visitor's strike shows up on the cast surfaces.</p> </header> <section class="hd__stage" data-astro-cid-ys4u3bda> <div class="hd__pan" data-astro-cid-ys4u3bda> <div class="hd__pan-shell" aria-hidden="true" data-astro-cid-ys4u3bda></div> <div class="hd__pan-ring" aria-hidden="true" data-astro-cid-ys4u3bda></div> <div class="hd__pan-ding" aria-hidden="true" data-astro-cid-ys4u3bda></div> ${TONGUES.map((t) => renderTemplate`<button type="button" class="hd__tongue"${addAttribute(t.id, "data-id")}${addAttribute(t.note, "data-note")}${addAttribute(t.hz, "data-hz")}${addAttribute(t.kbd, "data-kbd")}${addAttribute(`--hd-angle: ${t.angle}deg`, "style")}${addAttribute(`Tongue ${t.note}`, "aria-label")} data-astro-cid-ys4u3bda> <span class="hd__tongue-shape" data-astro-cid-ys4u3bda></span> <span class="hd__tongue-note mono" data-astro-cid-ys4u3bda>${t.note}</span> <span class="hd__tongue-kbd mono" data-astro-cid-ys4u3bda>${t.kbd}</span> </button>`)} </div> <aside class="hd__panel" data-astro-cid-ys4u3bda> <div class="hd__panel-row" data-astro-cid-ys4u3bda><span class="hd__panel-label mono" data-astro-cid-ys4u3bda>decay</span><input id="hd-decay" type="range" min="20" max="100" value="70" data-astro-cid-ys4u3bda></div> <div class="hd__panel-row" data-astro-cid-ys4u3bda><span class="hd__panel-label mono" data-astro-cid-ys4u3bda>brightness</span><input id="hd-bright" type="range" min="500" max="6000" value="2400" data-astro-cid-ys4u3bda></div> <div class="hd__panel-row" data-astro-cid-ys4u3bda><span class="hd__panel-label mono" data-astro-cid-ys4u3bda>reverb</span><input id="hd-reverb" type="range" min="0" max="100" value="55" data-astro-cid-ys4u3bda></div> <p class="hd__broadcast mono" id="hd-broadcast" data-astro-cid-ys4u3bda>— ready —</p> </aside> </section> <footer class="hd__foot" data-astro-cid-ys4u3bda> <p data-astro-cid-ys4u3bda>Each strike fires <code data-astro-cid-ys4u3bda>type=hang</code> to <code data-astro-cid-ys4u3bda>/api/sounds</code>. Tongue layout follows the classic D-minor handpan scale (D / A / F / C / D / A / F / C).</p> <p class="hd__credit mono" data-astro-cid-ys4u3bda>v0.1 · 2026-04-28 · drum sprint v15 · audio batch 2/5</p> </footer> </main> ` }));
}, "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-v15.astro", void 0);

const $$file = "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-v15.astro";
const $$url = "/drum-v15";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$DrumV15,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
