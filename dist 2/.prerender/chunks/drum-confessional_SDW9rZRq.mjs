import { c as createComponent } from './astro-component_DWMcTjG3.mjs';
import 'piccolore';
import { a as renderTemplate, r as renderComponent, m as maybeRenderHead } from './prerender_CmTjnOuJ.mjs';
import { $ as $$BlockLayout } from './BlockLayout_DHviHHrD.mjs';
import { $ as $$DrumNav } from './DrumNav_D5cUUl3f.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$DrumConfessional = createComponent(async ($$result, $$props, $$slots) => {
  const title = "/drum-confessional — leave a one-line note";
  const description = "A soft room on the drum hub. Leave a one-line note; it floats up the screen for thirty seconds. Other visitors see it drift past. Low-stakes, easy to share, easy to forget.";
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "WebPage",
    "@id": "https://pointcast.xyz/drum-confessional",
    name: "PointCast Drum · Confessional",
    url: "https://pointcast.xyz/drum-confessional",
    description
  };
  return renderTemplate(_a || (_a = __template(["", " <script>\n  (function () {\n    'use strict';\n    const MOOD_COLORS = {\n      warm:   '#ff8a4a',\n      bright: '#ffd400',\n      ocean:  '#5fbafd',\n      garden: '#3b6e3b',\n      fog:    '#cfc6df',\n    };\n    let mood = 'warm';\n    const RATE_LIMIT_MS = 12 * 1000;\n    let lastPostAt = 0;\n    let lastTs = 0;\n    const seen = new Set();\n    const notesHost = document.getElementById('cf-notes');\n    const counter = document.getElementById('cf-counter');\n\n    function getSession() {\n      try {\n        const k = 'pc:session';\n        let s = localStorage.getItem(k);\n        if (!s) { s = Math.random().toString(36).slice(2) + Date.now().toString(36); localStorage.setItem(k, s); }\n        return s;\n      } catch { return 'cf-' + Math.random().toString(36).slice(2, 10); }\n    }\n    const session = getSession();\n\n    function sanitize(s) {\n      return String(s || '')\n        .replace(/[\\x00-\\x1f\\x7f]/g, '')\n        .slice(0, 140)\n        .trim();\n    }\n\n    function spawnNote(text, color) {\n      const el = document.createElement('div');\n      el.className = 'cf__note';\n      el.style.setProperty('--note-color', color);\n      el.style.left = `${5 + Math.random() * 80}%`;\n      el.style.bottom = `-60px`;\n      const dur = 26 + Math.random() * 8;\n      el.style.animation = `cf-float ${dur}s linear forwards`;\n      // Subtle horizontal sway\n      const sway = (Math.random() - 0.5) * 8;\n      el.style.setProperty('--cf-sway', `${sway}vw`);\n      const inner = document.createElement('div');\n      inner.className = 'cf__note-inner';\n      inner.textContent = text;\n      el.appendChild(inner);\n      notesHost.appendChild(el);\n      setTimeout(() => el.remove(), (dur + 0.5) * 1000);\n    }\n\n    document.querySelectorAll('.cf__mood').forEach((b) => {\n      b.addEventListener('click', () => {\n        document.querySelectorAll('.cf__mood').forEach((x) => x.classList.remove('cf__mood--on'));\n        b.classList.add('cf__mood--on');\n        mood = b.getAttribute('data-mood') || 'warm';\n      });\n    });\n\n    document.getElementById('cf-form').addEventListener('submit', async (e) => {\n      e.preventDefault();\n      const input = document.getElementById('cf-text');\n      const text = sanitize(input.value);\n      if (!text) return;\n      const now = Date.now();\n      if (now - lastPostAt < RATE_LIMIT_MS) {\n        const remain = Math.ceil((RATE_LIMIT_MS - (now - lastPostAt)) / 1000);\n        input.placeholder = `easy — ${remain}s left`;\n        setTimeout(() => { input.placeholder = 'one line · 140 chars · be gentle'; }, 1500);\n        return;\n      }\n      lastPostAt = now;\n      input.value = '';\n      // Optimistic spawn locally\n      spawnNote(text, MOOD_COLORS[mood] || '#ff8a4a');\n      try {\n        await fetch('/api/sounds', {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({ type: 'confessional', sessionId: session, text, mood }),\n        });\n      } catch {}\n    });\n\n    async function poll() {\n      try {\n        const r = await fetch(`/api/sounds?since=${lastTs}`, { cache: 'no-store' });\n        if (!r.ok) return;\n        const data = await r.json();\n        const events = Array.isArray(data.events) ? data.events : [];\n        if (events.length) lastTs = events[events.length - 1].t || Date.now();\n        let activeCount = 0;\n        for (const e of events) {\n          if (e.type !== 'confessional') continue;\n          // Dedupe by t+pid+text-fingerprint\n          const k = `${e.t}|${e.pid}|${(e.text || '').slice(0, 24)}`;\n          if (seen.has(k)) continue;\n          seen.add(k);\n          const text = sanitize(e.text);\n          if (!text) continue;\n          const color = MOOD_COLORS[e.mood] || MOOD_COLORS.warm;\n          // Don't double-render our own optimistic spawn — skip if pid matches us\n          // pid is 10-char hash of session; we don't have it here, but server returns it\n          // Quick heuristic: if event is within 2s of our last optimistic post, skip\n          if (Date.now() - lastPostAt < 2500 && e.text === text && e.mood === mood) continue;\n          spawnNote(text, color);\n          activeCount += 1;\n        }\n        // Counter shows how many notes have drifted today (rough)\n        // and how many bubbles are currently floating\n        const live = document.querySelectorAll('.cf__note').length;\n        counter.textContent = `${live} floating · ${seen.size} seen this session`;\n      } catch {}\n    }\n    poll();\n    setInterval(poll, 1500);\n  })();\n<\/script>"], ["", " <script>\n  (function () {\n    'use strict';\n    const MOOD_COLORS = {\n      warm:   '#ff8a4a',\n      bright: '#ffd400',\n      ocean:  '#5fbafd',\n      garden: '#3b6e3b',\n      fog:    '#cfc6df',\n    };\n    let mood = 'warm';\n    const RATE_LIMIT_MS = 12 * 1000;\n    let lastPostAt = 0;\n    let lastTs = 0;\n    const seen = new Set();\n    const notesHost = document.getElementById('cf-notes');\n    const counter = document.getElementById('cf-counter');\n\n    function getSession() {\n      try {\n        const k = 'pc:session';\n        let s = localStorage.getItem(k);\n        if (!s) { s = Math.random().toString(36).slice(2) + Date.now().toString(36); localStorage.setItem(k, s); }\n        return s;\n      } catch { return 'cf-' + Math.random().toString(36).slice(2, 10); }\n    }\n    const session = getSession();\n\n    function sanitize(s) {\n      return String(s || '')\n        .replace(/[\\\\x00-\\\\x1f\\\\x7f]/g, '')\n        .slice(0, 140)\n        .trim();\n    }\n\n    function spawnNote(text, color) {\n      const el = document.createElement('div');\n      el.className = 'cf__note';\n      el.style.setProperty('--note-color', color);\n      el.style.left = \\`\\${5 + Math.random() * 80}%\\`;\n      el.style.bottom = \\`-60px\\`;\n      const dur = 26 + Math.random() * 8;\n      el.style.animation = \\`cf-float \\${dur}s linear forwards\\`;\n      // Subtle horizontal sway\n      const sway = (Math.random() - 0.5) * 8;\n      el.style.setProperty('--cf-sway', \\`\\${sway}vw\\`);\n      const inner = document.createElement('div');\n      inner.className = 'cf__note-inner';\n      inner.textContent = text;\n      el.appendChild(inner);\n      notesHost.appendChild(el);\n      setTimeout(() => el.remove(), (dur + 0.5) * 1000);\n    }\n\n    document.querySelectorAll('.cf__mood').forEach((b) => {\n      b.addEventListener('click', () => {\n        document.querySelectorAll('.cf__mood').forEach((x) => x.classList.remove('cf__mood--on'));\n        b.classList.add('cf__mood--on');\n        mood = b.getAttribute('data-mood') || 'warm';\n      });\n    });\n\n    document.getElementById('cf-form').addEventListener('submit', async (e) => {\n      e.preventDefault();\n      const input = document.getElementById('cf-text');\n      const text = sanitize(input.value);\n      if (!text) return;\n      const now = Date.now();\n      if (now - lastPostAt < RATE_LIMIT_MS) {\n        const remain = Math.ceil((RATE_LIMIT_MS - (now - lastPostAt)) / 1000);\n        input.placeholder = \\`easy — \\${remain}s left\\`;\n        setTimeout(() => { input.placeholder = 'one line · 140 chars · be gentle'; }, 1500);\n        return;\n      }\n      lastPostAt = now;\n      input.value = '';\n      // Optimistic spawn locally\n      spawnNote(text, MOOD_COLORS[mood] || '#ff8a4a');\n      try {\n        await fetch('/api/sounds', {\n          method: 'POST',\n          headers: { 'Content-Type': 'application/json' },\n          body: JSON.stringify({ type: 'confessional', sessionId: session, text, mood }),\n        });\n      } catch {}\n    });\n\n    async function poll() {\n      try {\n        const r = await fetch(\\`/api/sounds?since=\\${lastTs}\\`, { cache: 'no-store' });\n        if (!r.ok) return;\n        const data = await r.json();\n        const events = Array.isArray(data.events) ? data.events : [];\n        if (events.length) lastTs = events[events.length - 1].t || Date.now();\n        let activeCount = 0;\n        for (const e of events) {\n          if (e.type !== 'confessional') continue;\n          // Dedupe by t+pid+text-fingerprint\n          const k = \\`\\${e.t}|\\${e.pid}|\\${(e.text || '').slice(0, 24)}\\`;\n          if (seen.has(k)) continue;\n          seen.add(k);\n          const text = sanitize(e.text);\n          if (!text) continue;\n          const color = MOOD_COLORS[e.mood] || MOOD_COLORS.warm;\n          // Don't double-render our own optimistic spawn — skip if pid matches us\n          // pid is 10-char hash of session; we don't have it here, but server returns it\n          // Quick heuristic: if event is within 2s of our last optimistic post, skip\n          if (Date.now() - lastPostAt < 2500 && e.text === text && e.mood === mood) continue;\n          spawnNote(text, color);\n          activeCount += 1;\n        }\n        // Counter shows how many notes have drifted today (rough)\n        // and how many bubbles are currently floating\n        const live = document.querySelectorAll('.cf__note').length;\n        counter.textContent = \\`\\${live} floating · \\${seen.size} seen this session\\`;\n      } catch {}\n    }\n    poll();\n    setInterval(poll, 1500);\n  })();\n<\/script>"])), renderComponent($$result, "BlockLayout", $$BlockLayout, { "title": title, "description": description, "jsonLd": jsonLd, "data-astro-cid-pc4dbnju": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<main class="cf" id="cf-main" data-astro-cid-pc4dbnju> ${renderComponent($$result2, "DrumNav", $$DrumNav, { "current": "confessional", "data-astro-cid-pc4dbnju": true })} <header class="cf__head" data-astro-cid-pc4dbnju> <p class="cf__kicker" data-astro-cid-pc4dbnju>DRUM HUB · CONFESSIONAL · SOFT ROOM</p> <h1 class="cf__title" data-astro-cid-pc4dbnju><em data-astro-cid-pc4dbnju>One line. Thirty seconds. Up and out.</em></h1> <p class="cf__dek" data-astro-cid-pc4dbnju>
Leave a small note about how today is going. Pick a mood. Press post. Your note rises across the screen for thirty seconds. Other visitors see it drift past in real time. Then it's gone.
</p> </header> <section class="cf__chapel" id="cf-chapel" data-astro-cid-pc4dbnju> <div class="cf__floor" aria-hidden="true" data-astro-cid-pc4dbnju></div> <div class="cf__lamp cf__lamp--l" aria-hidden="true" data-astro-cid-pc4dbnju> <span class="cf__lamp-bulb" data-astro-cid-pc4dbnju></span> <span class="cf__lamp-pole" data-astro-cid-pc4dbnju></span> </div> <div class="cf__lamp cf__lamp--r" aria-hidden="true" data-astro-cid-pc4dbnju> <span class="cf__lamp-bulb" data-astro-cid-pc4dbnju></span> <span class="cf__lamp-pole" data-astro-cid-pc4dbnju></span> </div> <div class="cf__notes" id="cf-notes" aria-live="polite" data-astro-cid-pc4dbnju></div> <form class="cf__form" id="cf-form" autocomplete="off" data-astro-cid-pc4dbnju> <div class="cf__moods" role="radiogroup" aria-label="Mood" data-astro-cid-pc4dbnju> <button type="button" class="cf__mood cf__mood--on" data-mood="warm" data-color="#ff8a4a" data-astro-cid-pc4dbnju>warm</button> <button type="button" class="cf__mood" data-mood="bright" data-color="#ffd400" data-astro-cid-pc4dbnju>bright</button> <button type="button" class="cf__mood" data-mood="ocean" data-color="#5fbafd" data-astro-cid-pc4dbnju>ocean</button> <button type="button" class="cf__mood" data-mood="garden" data-color="#3b6e3b" data-astro-cid-pc4dbnju>garden</button> <button type="button" class="cf__mood" data-mood="fog" data-color="#cfc6df" data-astro-cid-pc4dbnju>fog</button> </div> <input id="cf-text" class="cf__input mono" type="text" maxlength="140" placeholder="one line · 140 chars · be gentle" aria-label="Confessional message" data-astro-cid-pc4dbnju> <button type="submit" class="cf__post" data-astro-cid-pc4dbnju>POST →</button> </form> <p class="cf__counter mono" id="cf-counter" data-astro-cid-pc4dbnju>— · — · in the room</p> </section> <footer class="cf__foot" data-astro-cid-pc4dbnju> <p data-astro-cid-pc4dbnju>
Notes go through <code data-astro-cid-pc4dbnju>/api/sounds</code> like every other drum-hub
        event — public bus, anyone polling can see them. The framing
        here is "low-stakes" not "private confession." Self-rate-limit
        of one note per 12s. Notes float for ~30s and then disappear
        from the page; the underlying event stays in the bus history.
</p> <p class="cf__credit mono" data-astro-cid-pc4dbnju>v0.1 · 2026-04-28 · Mike Hoydich + Claude Code · El Segundo · sprint 4/4 (batch 2)</p> </footer> </main> ` }));
}, "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-confessional.astro", void 0);

const $$file = "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-confessional.astro";
const $$url = "/drum-confessional";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$DrumConfessional,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
