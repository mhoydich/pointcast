import { c as createComponent } from './astro-component_DWMcTjG3.mjs';
import 'piccolore';
import { a as renderTemplate, d as defineScriptVars, r as renderComponent, m as maybeRenderHead, b as addAttribute } from './prerender_CmTjnOuJ.mjs';
import { $ as $$BlockLayout } from './BlockLayout_DHviHHrD.mjs';
import { $ as $$DrumNav } from './DrumNav_D5cUUl3f.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$DrumEmojiMesh = createComponent(async ($$result, $$props, $$slots) => {
  const title = "/drum-emoji-mesh — emoji storm";
  const description = "Throw an emoji. The room sees it fly across every screen. Twenty-four emoji to choose from. Pure transient — no archive, just a real-time storm.";
  const jsonLd = { "@context": "https://schema.org", "@type": "WebPage", "@id": "https://pointcast.xyz/drum-emoji-mesh", name: "PointCast Drum · Emoji Mesh", url: "https://pointcast.xyz/drum-emoji-mesh", description };
  const EMOJI = [
    "🥁",
    "🎷",
    "🎺",
    "🎸",
    "🎻",
    "🎹",
    "🎤",
    "🎧",
    "👏",
    "🙌",
    "🤘",
    "✨",
    "⭐",
    "💫",
    "🌟",
    "🔥",
    "🌊",
    "🌅",
    "🌴",
    "🌻",
    "☀️",
    "🌙",
    "☕",
    "🫖"
  ];
  return renderTemplate(_a || (_a = __template(["", " <script>(function(){", "\n(function () { 'use strict';\n  const canvas = document.getElementById('em-canvas');\n  const ctx = canvas.getContext('2d');\n  const hud = {\n    thrown: document.getElementById('em-thrown'),\n    incoming: document.getElementById('em-incoming'),\n    flying: document.getElementById('em-flying'),\n  };\n  let dpr = window.devicePixelRatio || 1;\n  let W = window.innerWidth, H = window.innerHeight;\n  function resize() {\n    W = window.innerWidth; H = window.innerHeight;\n    canvas.width = W * dpr; canvas.height = H * dpr;\n    canvas.style.width = `${W}px`; canvas.style.height = `${H}px`;\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n  }\n  resize();\n  window.addEventListener('resize', resize);\n\n  const particles = [];\n  let thrown = 0;\n  let incoming = 0;\n\n  function spawnParticle(emoji, opts) {\n    // Spawn from a random edge, drift toward center-ish\n    opts = opts || {};\n    const edge = Math.floor(Math.random() * 4);\n    let x, y, vx, vy;\n    if (edge === 0) { x = Math.random() * W; y = -40;        vx = (Math.random() - 0.5) * 1.6; vy = 1 + Math.random() * 1.4; }\n    else if (edge === 1) { x = W + 40; y = Math.random() * H; vx = -(0.6 + Math.random() * 1.4); vy = (Math.random() - 0.5) * 1.6; }\n    else if (edge === 2) { x = Math.random() * W; y = H + 40; vx = (Math.random() - 0.5) * 1.6; vy = -(0.6 + Math.random() * 1.4); }\n    else { x = -40; y = Math.random() * H;       vx = 0.6 + Math.random() * 1.4; vy = (Math.random() - 0.5) * 1.6; }\n    particles.push({\n      emoji, x, y, vx, vy,\n      rot: (Math.random() - 0.5) * 0.6,\n      rotVel: (Math.random() - 0.5) * 0.05,\n      size: 36 + Math.random() * 36,\n      ttl: 280 + Math.random() * 120,\n      life: 1,\n    });\n  }\n\n  function frame() {\n    ctx.clearRect(0, 0, W, H);\n    let alive = 0;\n    for (let i = particles.length - 1; i >= 0; i--) {\n      const p = particles[i];\n      p.x += p.vx; p.y += p.vy;\n      p.vy += 0.012; // gentle gravity\n      p.rot += p.rotVel;\n      p.ttl -= 1;\n      p.life = Math.max(0, p.ttl / 360);\n      if (p.ttl <= 0 || p.x < -100 || p.x > W + 100 || p.y > H + 100) { particles.splice(i, 1); continue; }\n      ctx.save();\n      ctx.translate(p.x, p.y);\n      ctx.rotate(p.rot);\n      ctx.globalAlpha = Math.min(1, p.life * 1.4);\n      ctx.font = `${p.size}px serif`;\n      ctx.textAlign = 'center';\n      ctx.textBaseline = 'middle';\n      ctx.fillText(p.emoji, 0, 0);\n      ctx.restore();\n      alive += 1;\n    }\n    if (hud.flying) hud.flying.textContent = String(alive);\n    requestAnimationFrame(frame);\n  }\n  requestAnimationFrame(frame);\n\n  function getSession() { try { const k='pc:session'; let s=localStorage.getItem(k); if(!s){s=Math.random().toString(36).slice(2)+Date.now().toString(36); localStorage.setItem(k,s);} return s;} catch { return 'em-' + Math.random().toString(36).slice(2,10);} }\n  const sid = getSession();\n\n  let lastThrowAt = 0;\n  function throwEmoji(emoji, fromSelf) {\n    if (fromSelf) {\n      const now = Date.now();\n      if (now - lastThrowAt < 80) return; // 12.5/sec local rate cap\n      lastThrowAt = now;\n    }\n    spawnParticle(emoji);\n    if (fromSelf) {\n      thrown += 1;\n      if (hud.thrown) hud.thrown.textContent = String(thrown);\n      try {\n        fetch('/api/sounds', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ type: 'emoji', sessionId: sid, emoji }) });\n      } catch {}\n    } else {\n      incoming += 1;\n      if (hud.incoming) hud.incoming.textContent = String(incoming);\n    }\n  }\n\n  // Buttons\n  document.querySelectorAll('.em__btn').forEach((btn) => {\n    btn.addEventListener('click', (e) => {\n      const g = btn.getAttribute('data-emoji') || '';\n      throwEmoji(g, true);\n      btn.classList.remove('em__btn--hit'); void btn.offsetWidth; btn.classList.add('em__btn--hit');\n      e.preventDefault();\n    });\n  });\n\n  // Keyboard map: 1-9, 0 -> emoji 0..9; q-y (8) -> 10..17; a-h (8) -> 18..23 (we have 24)\n  const KEY_MAP = '1234567890qwertyuiasdfgh'.split('');\n  window.addEventListener('keydown', (e) => {\n    if (e.repeat) return;\n    const idx = KEY_MAP.indexOf(e.key.toLowerCase());\n    if (idx >= 0 && idx < EMOJI.length) {\n      const btn = document.querySelector(`.em__btn[data-emoji=\"${EMOJI[idx]}\"]`);\n      if (btn) btn.click();\n    }\n  });\n\n  // Poll for incoming emojis from other visitors\n  let lastTs = Date.now();\n  async function poll() {\n    try {\n      const r = await fetch(`/api/sounds?since=${lastTs}`, { cache: 'no-store' });\n      if (!r.ok) return;\n      const data = await r.json();\n      const events = Array.isArray(data.events) ? data.events : [];\n      if (!events.length) return;\n      lastTs = events[events.length - 1].t || Date.now();\n      for (const e of events) {\n        if (e.type !== 'emoji' || !e.emoji) continue;\n        // Skip events that match our own throws within the last second\n        if (Math.abs((e.t || 0) - lastThrowAt) < 600) continue;\n        throwEmoji(e.emoji, false);\n      }\n    } catch {}\n  }\n  setInterval(poll, 1500);\n  poll();\n})();\n})();<\/script>"], ["", " <script>(function(){", "\n(function () { 'use strict';\n  const canvas = document.getElementById('em-canvas');\n  const ctx = canvas.getContext('2d');\n  const hud = {\n    thrown: document.getElementById('em-thrown'),\n    incoming: document.getElementById('em-incoming'),\n    flying: document.getElementById('em-flying'),\n  };\n  let dpr = window.devicePixelRatio || 1;\n  let W = window.innerWidth, H = window.innerHeight;\n  function resize() {\n    W = window.innerWidth; H = window.innerHeight;\n    canvas.width = W * dpr; canvas.height = H * dpr;\n    canvas.style.width = \\`\\${W}px\\`; canvas.style.height = \\`\\${H}px\\`;\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n  }\n  resize();\n  window.addEventListener('resize', resize);\n\n  const particles = [];\n  let thrown = 0;\n  let incoming = 0;\n\n  function spawnParticle(emoji, opts) {\n    // Spawn from a random edge, drift toward center-ish\n    opts = opts || {};\n    const edge = Math.floor(Math.random() * 4);\n    let x, y, vx, vy;\n    if (edge === 0) { x = Math.random() * W; y = -40;        vx = (Math.random() - 0.5) * 1.6; vy = 1 + Math.random() * 1.4; }\n    else if (edge === 1) { x = W + 40; y = Math.random() * H; vx = -(0.6 + Math.random() * 1.4); vy = (Math.random() - 0.5) * 1.6; }\n    else if (edge === 2) { x = Math.random() * W; y = H + 40; vx = (Math.random() - 0.5) * 1.6; vy = -(0.6 + Math.random() * 1.4); }\n    else { x = -40; y = Math.random() * H;       vx = 0.6 + Math.random() * 1.4; vy = (Math.random() - 0.5) * 1.6; }\n    particles.push({\n      emoji, x, y, vx, vy,\n      rot: (Math.random() - 0.5) * 0.6,\n      rotVel: (Math.random() - 0.5) * 0.05,\n      size: 36 + Math.random() * 36,\n      ttl: 280 + Math.random() * 120,\n      life: 1,\n    });\n  }\n\n  function frame() {\n    ctx.clearRect(0, 0, W, H);\n    let alive = 0;\n    for (let i = particles.length - 1; i >= 0; i--) {\n      const p = particles[i];\n      p.x += p.vx; p.y += p.vy;\n      p.vy += 0.012; // gentle gravity\n      p.rot += p.rotVel;\n      p.ttl -= 1;\n      p.life = Math.max(0, p.ttl / 360);\n      if (p.ttl <= 0 || p.x < -100 || p.x > W + 100 || p.y > H + 100) { particles.splice(i, 1); continue; }\n      ctx.save();\n      ctx.translate(p.x, p.y);\n      ctx.rotate(p.rot);\n      ctx.globalAlpha = Math.min(1, p.life * 1.4);\n      ctx.font = \\`\\${p.size}px serif\\`;\n      ctx.textAlign = 'center';\n      ctx.textBaseline = 'middle';\n      ctx.fillText(p.emoji, 0, 0);\n      ctx.restore();\n      alive += 1;\n    }\n    if (hud.flying) hud.flying.textContent = String(alive);\n    requestAnimationFrame(frame);\n  }\n  requestAnimationFrame(frame);\n\n  function getSession() { try { const k='pc:session'; let s=localStorage.getItem(k); if(!s){s=Math.random().toString(36).slice(2)+Date.now().toString(36); localStorage.setItem(k,s);} return s;} catch { return 'em-' + Math.random().toString(36).slice(2,10);} }\n  const sid = getSession();\n\n  let lastThrowAt = 0;\n  function throwEmoji(emoji, fromSelf) {\n    if (fromSelf) {\n      const now = Date.now();\n      if (now - lastThrowAt < 80) return; // 12.5/sec local rate cap\n      lastThrowAt = now;\n    }\n    spawnParticle(emoji);\n    if (fromSelf) {\n      thrown += 1;\n      if (hud.thrown) hud.thrown.textContent = String(thrown);\n      try {\n        fetch('/api/sounds', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ type: 'emoji', sessionId: sid, emoji }) });\n      } catch {}\n    } else {\n      incoming += 1;\n      if (hud.incoming) hud.incoming.textContent = String(incoming);\n    }\n  }\n\n  // Buttons\n  document.querySelectorAll('.em__btn').forEach((btn) => {\n    btn.addEventListener('click', (e) => {\n      const g = btn.getAttribute('data-emoji') || '';\n      throwEmoji(g, true);\n      btn.classList.remove('em__btn--hit'); void btn.offsetWidth; btn.classList.add('em__btn--hit');\n      e.preventDefault();\n    });\n  });\n\n  // Keyboard map: 1-9, 0 -> emoji 0..9; q-y (8) -> 10..17; a-h (8) -> 18..23 (we have 24)\n  const KEY_MAP = '1234567890qwertyuiasdfgh'.split('');\n  window.addEventListener('keydown', (e) => {\n    if (e.repeat) return;\n    const idx = KEY_MAP.indexOf(e.key.toLowerCase());\n    if (idx >= 0 && idx < EMOJI.length) {\n      const btn = document.querySelector(\\`.em__btn[data-emoji=\"\\${EMOJI[idx]}\"]\\`);\n      if (btn) btn.click();\n    }\n  });\n\n  // Poll for incoming emojis from other visitors\n  let lastTs = Date.now();\n  async function poll() {\n    try {\n      const r = await fetch(\\`/api/sounds?since=\\${lastTs}\\`, { cache: 'no-store' });\n      if (!r.ok) return;\n      const data = await r.json();\n      const events = Array.isArray(data.events) ? data.events : [];\n      if (!events.length) return;\n      lastTs = events[events.length - 1].t || Date.now();\n      for (const e of events) {\n        if (e.type !== 'emoji' || !e.emoji) continue;\n        // Skip events that match our own throws within the last second\n        if (Math.abs((e.t || 0) - lastThrowAt) < 600) continue;\n        throwEmoji(e.emoji, false);\n      }\n    } catch {}\n  }\n  setInterval(poll, 1500);\n  poll();\n})();\n})();<\/script>"])), renderComponent($$result, "BlockLayout", $$BlockLayout, { "title": title, "description": description, "jsonLd": jsonLd, "data-astro-cid-ikl754gv": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<main class="em" id="em-main" data-astro-cid-ikl754gv> ${renderComponent($$result2, "DrumNav", $$DrumNav, { "current": "emoji-mesh", "data-astro-cid-ikl754gv": true })} <header class="em__head" data-astro-cid-ikl754gv> <p class="em__kicker" data-astro-cid-ikl754gv>DRUM HUB · COMMS 3 · EMOJI MESH · LIVE · MULTIPLAYER</p> <h1 class="em__title" data-astro-cid-ikl754gv><em data-astro-cid-ikl754gv>Throw it. The room sees it.</em></h1> <p class="em__dek" data-astro-cid-ikl754gv>Twenty-four emoji to choose from. Click one to throw. The room throws too. Every emoji flies across every screen for about five seconds, then disappears. No archive. Pure storm.</p> </header> <section class="em__chooser" data-astro-cid-ikl754gv> <p class="em__chooser-label mono" data-astro-cid-ikl754gv>PICK ONE</p> <div class="em__grid" data-astro-cid-ikl754gv> ${EMOJI.map((g) => renderTemplate`<button type="button" class="em__btn"${addAttribute(g, "data-emoji")}${addAttribute(`Throw ${g}`, "aria-label")} data-astro-cid-ikl754gv> <span class="em__btn-glyph" data-astro-cid-ikl754gv>${g}</span> </button>`)} </div> <p class="em__hint mono" id="em-hint" data-astro-cid-ikl754gv>click an emoji · or press 1-9, 0, q-y, a-h</p> </section> <div class="em__hud mono" data-astro-cid-ikl754gv> <span data-astro-cid-ikl754gv>thrown <strong id="em-thrown" data-astro-cid-ikl754gv>0</strong></span> <span data-astro-cid-ikl754gv>incoming <strong id="em-incoming" data-astro-cid-ikl754gv>0</strong></span> <span data-astro-cid-ikl754gv>flying now <strong id="em-flying" data-astro-cid-ikl754gv>0</strong></span> </div> <canvas class="em__canvas" id="em-canvas" aria-hidden="true" data-astro-cid-ikl754gv></canvas> <footer class="em__foot" data-astro-cid-ikl754gv> <p data-astro-cid-ikl754gv>Each throw fires <code data-astro-cid-ikl754gv>type=emoji</code> to <code data-astro-cid-ikl754gv>/api/sounds</code> with the emoji char. Particles render in a fixed-position canvas with random spawn edges, drift vectors, and rotation. ~5s fade.</p> <p class="em__credit mono" data-astro-cid-ikl754gv>v0.1 · 2026-04-28 · drum sprint · comms 3</p> </footer> </main> ` }), defineScriptVars({ EMOJI }));
}, "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-emoji-mesh.astro", void 0);

const $$file = "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-emoji-mesh.astro";
const $$url = "/drum-emoji-mesh";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$DrumEmojiMesh,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
