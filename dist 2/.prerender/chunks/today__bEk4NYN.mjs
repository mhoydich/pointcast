import { c as createComponent } from './astro-component_DWMcTjG3.mjs';
import 'piccolore';
import { a as renderTemplate, r as renderComponent, m as maybeRenderHead, b as addAttribute } from './prerender_CmTjnOuJ.mjs';
import { g as getCollection } from './_astro_content_kC0GrL8i.mjs';
import { $ as $$BlockLayout } from './BlockLayout_DHviHHrD.mjs';
import { C as CHANNELS } from './channels_C2qW9mSV.mjs';
import { p as pickDailyBlock, t as todayPT, d as daySeed } from './daily_2eiOMuEj.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$Today = createComponent(async ($$result, $$props, $$slots) => {
  const blocks = await getCollection("blocks", ({ data }) => !data.draft);
  const pick = pickDailyBlock(blocks);
  const todayStr = todayPT();
  const seedNum = daySeed();
  if (!pick) {
    throw new Error("/today — no blocks available for daily pick");
  }
  const ch = CHANNELS[pick.data.channel];
  const thumbnail = pick.data.media?.thumbnail ?? (pick.data.noun ? `https://noun.pics/${pick.data.noun}.svg` : null);
  const prettyDate = new Intl.DateTimeFormat("en-US", {
    weekday: "long",
    month: "long",
    day: "numeric",
    year: "numeric",
    timeZone: "America/Los_Angeles"
  }).format(/* @__PURE__ */ new Date());
  const qrTarget = `https://pointcast.xyz/b/${pick.data.id}`;
  const qrSrc = `https://api.qrserver.com/v1/create-qr-code/?size=216x216&margin=2&data=${encodeURIComponent(qrTarget)}`;
  const title = `Today · ${prettyDate} · № ${pick.data.id}`;
  const description = `The daily drop for ${prettyDate}. Today's pick: block № ${pick.data.id} — ${pick.data.title}. One block per day, chosen deterministically so every visitor sees the same drop on the same day. Tap to collect.`;
  const jsonLd = {
    "@context": "https://schema.org",
    "@type": "CreativeWork",
    "@id": `https://pointcast.xyz/today`,
    name: title,
    description,
    url: "https://pointcast.xyz/today",
    dateModified: todayStr,
    mainEntity: {
      "@type": "CreativeWork",
      "@id": `https://pointcast.xyz/b/${pick.data.id}`,
      name: pick.data.title,
      url: `https://pointcast.xyz/b/${pick.data.id}`
    }
  };
  return renderTemplate(_a || (_a = __template(["", " <script>\n  (function () {\n    var article = document.querySelector('.pick');\n    if (!article) return;\n\n    var blockId = article.dataset.blockId || '';\n    var today = article.dataset.today || '';\n\n    var btn = document.getElementById('collect-btn');\n    var btnLabel = btn ? btn.querySelector('.collect-btn__label') : null;\n    var btnHint = btn ? btn.querySelector('.collect-btn__hint') : null;\n    var totalEl = document.getElementById('collect-total');\n    var streakEl = document.getElementById('collect-streak');\n\n    var LS_KEY = 'pc:daily:collected';\n\n    function readCollected() {\n      try {\n        var raw = localStorage.getItem(LS_KEY);\n        if (!raw) return [];\n        var parsed = JSON.parse(raw);\n        return Array.isArray(parsed) ? parsed : [];\n      } catch (e) { return []; }\n    }\n\n    function writeCollected(arr) {\n      try { localStorage.setItem(LS_KEY, JSON.stringify(arr)); } catch (e) {}\n    }\n\n    /** Compute the current consecutive-day streak ending at `today`.\n     *  A day counts if there's a claim with that exact PT date. */\n    function computeStreak(arr, todayStr) {\n      var dates = {};\n      for (var i = 0; i < arr.length; i++) {\n        if (arr[i] && arr[i].date) dates[arr[i].date] = true;\n      }\n      var streak = 0;\n      var cur = todayStr;\n      while (dates[cur]) {\n        streak++;\n        // Step back one day.\n        var parts = cur.split('-').map(function (s) { return parseInt(s, 10); });\n        var d = new Date(Date.UTC(parts[0], parts[1] - 1, parts[2]));\n        d.setUTCDate(d.getUTCDate() - 1);\n        cur = d.getUTCFullYear() + '-' + String(d.getUTCMonth() + 1).padStart(2, '0') + '-' + String(d.getUTCDate()).padStart(2, '0');\n      }\n      return streak;\n    }\n\n    function paint() {\n      var arr = readCollected();\n      if (totalEl) totalEl.textContent = String(arr.length);\n      if (streakEl) streakEl.textContent = String(computeStreak(arr, today));\n      var has = arr.some(function (c) { return c && c.date === today; });\n      if (has && btn) {\n        btn.classList.add('collect-btn--claimed');\n        btn.disabled = true;\n        if (btnLabel) btnLabel.textContent = '✓ COLLECTED';\n        if (btnHint) btnHint.textContent = 'COME BACK TOMORROW';\n      }\n    }\n\n    function floater(msg) {\n      if (!btn) return;\n      var f = document.createElement('span');\n      f.className = 'collect-float';\n      f.textContent = msg;\n      btn.appendChild(f);\n      setTimeout(function () { if (f.parentNode) f.parentNode.removeChild(f); }, 1800);\n    }\n\n    function chime(freq, dur) {\n      try {\n        var ctx = new (window.AudioContext || window.webkitAudioContext)();\n        var o = ctx.createOscillator(), g = ctx.createGain();\n        o.type = 'sine'; o.frequency.value = freq || 720;\n        g.gain.setValueAtTime(0, ctx.currentTime);\n        g.gain.linearRampToValueAtTime(0.12, ctx.currentTime + 0.01);\n        g.gain.exponentialRampToValueAtTime(0.0005, ctx.currentTime + (dur || 0.18));\n        o.connect(g).connect(ctx.destination);\n        o.start(); o.stop(ctx.currentTime + (dur || 0.18) + 0.05);\n      } catch (e) {}\n    }\n\n    function buzz(ms) { try { if (navigator.vibrate) navigator.vibrate(ms); } catch (e) {} }\n\n    if (btn) {\n      btn.addEventListener('click', function () {\n        if (btn.disabled) return;\n        var arr = readCollected();\n        if (arr.some(function (c) { return c && c.date === today; })) {\n          paint();  // already collected; just repaint\n          return;\n        }\n        arr.push({ date: today, blockId: blockId, at: new Date().toISOString() });\n        writeCollected(arr);\n        chime(720, 0.16);\n        setTimeout(function () { chime(900, 0.14); }, 140);\n        buzz(20);\n        floater('+1 COLLECTED');\n        article.classList.add('pick--claimed-anim');\n        setTimeout(function () { article.classList.remove('pick--claimed-anim'); }, 900);\n        paint();\n      });\n    }\n\n    paint();\n  })();\n<\/script>"], ["", " <script>\n  (function () {\n    var article = document.querySelector('.pick');\n    if (!article) return;\n\n    var blockId = article.dataset.blockId || '';\n    var today = article.dataset.today || '';\n\n    var btn = document.getElementById('collect-btn');\n    var btnLabel = btn ? btn.querySelector('.collect-btn__label') : null;\n    var btnHint = btn ? btn.querySelector('.collect-btn__hint') : null;\n    var totalEl = document.getElementById('collect-total');\n    var streakEl = document.getElementById('collect-streak');\n\n    var LS_KEY = 'pc:daily:collected';\n\n    function readCollected() {\n      try {\n        var raw = localStorage.getItem(LS_KEY);\n        if (!raw) return [];\n        var parsed = JSON.parse(raw);\n        return Array.isArray(parsed) ? parsed : [];\n      } catch (e) { return []; }\n    }\n\n    function writeCollected(arr) {\n      try { localStorage.setItem(LS_KEY, JSON.stringify(arr)); } catch (e) {}\n    }\n\n    /** Compute the current consecutive-day streak ending at \\`today\\`.\n     *  A day counts if there's a claim with that exact PT date. */\n    function computeStreak(arr, todayStr) {\n      var dates = {};\n      for (var i = 0; i < arr.length; i++) {\n        if (arr[i] && arr[i].date) dates[arr[i].date] = true;\n      }\n      var streak = 0;\n      var cur = todayStr;\n      while (dates[cur]) {\n        streak++;\n        // Step back one day.\n        var parts = cur.split('-').map(function (s) { return parseInt(s, 10); });\n        var d = new Date(Date.UTC(parts[0], parts[1] - 1, parts[2]));\n        d.setUTCDate(d.getUTCDate() - 1);\n        cur = d.getUTCFullYear() + '-' + String(d.getUTCMonth() + 1).padStart(2, '0') + '-' + String(d.getUTCDate()).padStart(2, '0');\n      }\n      return streak;\n    }\n\n    function paint() {\n      var arr = readCollected();\n      if (totalEl) totalEl.textContent = String(arr.length);\n      if (streakEl) streakEl.textContent = String(computeStreak(arr, today));\n      var has = arr.some(function (c) { return c && c.date === today; });\n      if (has && btn) {\n        btn.classList.add('collect-btn--claimed');\n        btn.disabled = true;\n        if (btnLabel) btnLabel.textContent = '✓ COLLECTED';\n        if (btnHint) btnHint.textContent = 'COME BACK TOMORROW';\n      }\n    }\n\n    function floater(msg) {\n      if (!btn) return;\n      var f = document.createElement('span');\n      f.className = 'collect-float';\n      f.textContent = msg;\n      btn.appendChild(f);\n      setTimeout(function () { if (f.parentNode) f.parentNode.removeChild(f); }, 1800);\n    }\n\n    function chime(freq, dur) {\n      try {\n        var ctx = new (window.AudioContext || window.webkitAudioContext)();\n        var o = ctx.createOscillator(), g = ctx.createGain();\n        o.type = 'sine'; o.frequency.value = freq || 720;\n        g.gain.setValueAtTime(0, ctx.currentTime);\n        g.gain.linearRampToValueAtTime(0.12, ctx.currentTime + 0.01);\n        g.gain.exponentialRampToValueAtTime(0.0005, ctx.currentTime + (dur || 0.18));\n        o.connect(g).connect(ctx.destination);\n        o.start(); o.stop(ctx.currentTime + (dur || 0.18) + 0.05);\n      } catch (e) {}\n    }\n\n    function buzz(ms) { try { if (navigator.vibrate) navigator.vibrate(ms); } catch (e) {} }\n\n    if (btn) {\n      btn.addEventListener('click', function () {\n        if (btn.disabled) return;\n        var arr = readCollected();\n        if (arr.some(function (c) { return c && c.date === today; })) {\n          paint();  // already collected; just repaint\n          return;\n        }\n        arr.push({ date: today, blockId: blockId, at: new Date().toISOString() });\n        writeCollected(arr);\n        chime(720, 0.16);\n        setTimeout(function () { chime(900, 0.14); }, 140);\n        buzz(20);\n        floater('+1 COLLECTED');\n        article.classList.add('pick--claimed-anim');\n        setTimeout(function () { article.classList.remove('pick--claimed-anim'); }, 900);\n        paint();\n      });\n    }\n\n    paint();\n  })();\n<\/script>"])), renderComponent($$result, "BlockLayout", $$BlockLayout, { "title": title, "description": description, "jsonLd": jsonLd, "data-astro-cid-eipbybc2": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<div class="page" data-astro-cid-eipbybc2> <nav class="breadcrumb" aria-label="Breadcrumb" data-astro-cid-eipbybc2> <a href="/" data-astro-cid-eipbybc2>← All blocks</a> <span aria-hidden="true" data-astro-cid-eipbybc2>/</span> <span data-astro-cid-eipbybc2>today</span> </nav> <header class="masthead" data-astro-cid-eipbybc2> <p class="masthead__kicker mono" data-astro-cid-eipbybc2>TODAY · <time${addAttribute(todayStr, "datetime")} data-astro-cid-eipbybc2>${prettyDate}</time></p> <h1 class="masthead__title" data-astro-cid-eipbybc2>The daily drop</h1> <p class="masthead__lede" data-astro-cid-eipbybc2>
One block per day, chosen the moment the PT calendar rolls over.
        Everyone sees the same pick — a shared morning bell. Tap to collect
        and build a streak; the count is local to this browser today, a
        server tally later, a Tezos artifact when Mike says the word.
</p> </header> <article class="pick"${addAttribute(pick.data.id, "data-block-id")}${addAttribute(todayStr, "data-today")}${addAttribute(seedNum, "data-seed")} data-astro-cid-eipbybc2> <div class="pick__head" data-astro-cid-eipbybc2> <span class="pick__channel mono"${addAttribute(`background: color-mix(in oklab, ${ch.color600} 16%, transparent); color: ${ch.color800};`, "style")} data-astro-cid-eipbybc2> <span class="pick__channel-dot" aria-hidden="true" data-astro-cid-eipbybc2></span>
CH.${ch.code} · ${ch.name} </span> <span class="pick__id mono" data-astro-cid-eipbybc2>№ ${pick.data.id} · ${pick.data.type}</span> </div> <h2 class="pick__title" data-astro-cid-eipbybc2>${pick.data.title}</h2> ${pick.data.dek && renderTemplate`<p class="pick__dek" data-astro-cid-eipbybc2>${pick.data.dek}</p>`} <div class="pick__body" data-astro-cid-eipbybc2> <div class="pick__col" data-astro-cid-eipbybc2> ${thumbnail && renderTemplate`<a${addAttribute(`/b/${pick.data.id}`, "href")} class="pick__art"${addAttribute(`Open block № ${pick.data.id}`, "aria-label")} data-astro-cid-eipbybc2> <img${addAttribute(thumbnail, "src")} alt="" loading="eager" data-astro-cid-eipbybc2> </a>`} <div class="pick__meta mono" data-astro-cid-eipbybc2> ${pick.data.mood && renderTemplate`<a class="pick__mood"${addAttribute(`/mood/${pick.data.mood}`, "href")} data-astro-cid-eipbybc2>★ ${pick.data.mood.replace(/-/g, " ")}</a>`} ${pick.data.meta?.location && renderTemplate`<span class="pick__loc" data-astro-cid-eipbybc2>📍 ${pick.data.meta.location}</span>`} ${pick.data.readingTime && renderTemplate`<span class="pick__rt" data-astro-cid-eipbybc2>⏱ ${pick.data.readingTime}</span>`} </div> </div> <div class="pick__col pick__col--actions" data-astro-cid-eipbybc2> <button class="collect-btn" id="collect-btn" type="button" aria-label="Collect today's drop" data-astro-cid-eipbybc2> <span class="collect-btn__icon" aria-hidden="true" data-astro-cid-eipbybc2>✦</span> <span class="collect-btn__label" data-astro-cid-eipbybc2>COLLECT TODAY</span> <span class="collect-btn__hint mono" data-astro-cid-eipbybc2>TAP TO CLAIM</span> </button> <div class="collect-stats mono" id="collect-stats" aria-live="polite" data-astro-cid-eipbybc2> <span class="collect-stats__total" data-astro-cid-eipbybc2> <span class="collect-stats__num" id="collect-total" data-astro-cid-eipbybc2>0</span> <span class="collect-stats__label" data-astro-cid-eipbybc2>COLLECTED</span> </span> <span class="collect-stats__sep" aria-hidden="true" data-astro-cid-eipbybc2>·</span> <span class="collect-stats__streak" data-astro-cid-eipbybc2> <span class="collect-stats__num" id="collect-streak" data-astro-cid-eipbybc2>0</span> <span class="collect-stats__label" data-astro-cid-eipbybc2>DAY STREAK</span> </span> </div> <a class="pick__open mono"${addAttribute(`/b/${pick.data.id}`, "href")} data-astro-cid-eipbybc2>OPEN BLOCK →</a> <div class="pick__qr" data-astro-cid-eipbybc2> <img${addAttribute(qrSrc, "src")}${addAttribute(`QR to ${qrTarget}`, "alt")} data-astro-cid-eipbybc2> <span class="mono" data-astro-cid-eipbybc2>→ phone · /b/${pick.data.id}</span> </div> </div> </div> </article> <section class="next-note" data-astro-cid-eipbybc2> <p class="mono next-note__kicker" data-astro-cid-eipbybc2>COMES NEXT</p> <p data-astro-cid-eipbybc2>
A new drop lands every day at midnight PT. The pick is deterministic —
        the same block appears on /today across every browser, every device.
        Come back tomorrow for the next. If you missed yesterday's, it'll
        come back around when the rotation wraps.
</p> </section> <aside class="agent-strip" data-astro-cid-eipbybc2> <p class="agent-strip__label" data-astro-cid-eipbybc2>MACHINE-READABLE</p> <ul data-astro-cid-eipbybc2> <li data-astro-cid-eipbybc2><a href="/today.json" data-astro-cid-eipbybc2>/today.json</a></li> <li data-astro-cid-eipbybc2><a${addAttribute(`/b/${pick.data.id}.json`, "href")} data-astro-cid-eipbybc2>/b/${pick.data.id}.json</a></li> <li data-astro-cid-eipbybc2><a href="/blocks.json" data-astro-cid-eipbybc2>/blocks.json</a></li> <li data-astro-cid-eipbybc2><a href="/for-agents" data-astro-cid-eipbybc2>/for-agents</a></li> </ul> </aside> </div> ` }));
}, "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/today.astro", void 0);

const $$file = "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/today.astro";
const $$url = "/today";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$Today,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
