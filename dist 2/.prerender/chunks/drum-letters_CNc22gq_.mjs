import { c as createComponent } from './astro-component_DWMcTjG3.mjs';
import 'piccolore';
import { a as renderTemplate, r as renderComponent, m as maybeRenderHead, b as addAttribute } from './prerender_CmTjnOuJ.mjs';
import { $ as $$BlockLayout } from './BlockLayout_DHviHHrD.mjs';
import { $ as $$DrumNav } from './DrumNav_D5cUUl3f.mjs';

var __freeze = Object.freeze;
var __defProp = Object.defineProperty;
var __template = (cooked, raw) => __freeze(__defProp(cooked, "raw", { value: __freeze(raw || cooked.slice()) }));
var _a;
const $$DrumLetters = createComponent(async ($$result, $$props, $$slots) => {
  const title = "/drum-letters — write to the next visitor";
  const description = "Leave a letter for the next visitor to /drum-letters. Read whatever they left for you. KV-backed, 500 chars, six mood colors. Small letters between strangers.";
  const jsonLd = { "@context": "https://schema.org", "@type": "WebPage", "@id": "https://pointcast.xyz/drum-letters", name: "PointCast Drum · Letters", url: "https://pointcast.xyz/drum-letters", description };
  const COLORS = [
    { id: "warm", label: "WARM", hex: "#ff8a4a" },
    { id: "bright", label: "BRIGHT", hex: "#ffd400" },
    { id: "ocean", label: "OCEAN", hex: "#5fbafd" },
    { id: "garden", label: "GARDEN", hex: "#3b6e3b" },
    { id: "fog", label: "FOG", hex: "#cfc6df" },
    { id: "rose", label: "ROSE", hex: "#d6745f" }
  ];
  return renderTemplate(_a || (_a = __template(["", " <script>\n(function () { 'use strict';\n  const SEEN_KEY = 'pc:drum-letters:seen';\n  const COLORS = { warm: '#ff8a4a', bright: '#ffd400', ocean: '#5fbafd', garden: '#3b6e3b', fog: '#cfc6df', rose: '#d6745f' };\n  const cardEl = document.getElementById('lt-card');\n  const bodyEl = document.getElementById('lt-body');\n  const metaEl = document.getElementById('lt-meta');\n  const footEl = document.getElementById('lt-foot');\n  const nounEl = document.getElementById('lt-noun');\n  const countEl = document.getElementById('lt-count');\n  const input = document.getElementById('lt-input');\n  const cc = document.getElementById('lt-cc');\n  const send = document.getElementById('lt-send');\n  const next = document.getElementById('lt-next');\n  const sig = document.getElementById('lt-sig');\n\n  function getSeen() {\n    try { return new Set(JSON.parse(localStorage.getItem(SEEN_KEY) || '[]')); }\n    catch { return new Set(); }\n  }\n  function saveSeen(set) {\n    try { localStorage.setItem(SEEN_KEY, JSON.stringify([...set].slice(-200))); } catch {}\n  }\n  function getSession() {\n    try { const k='pc:session'; let s=localStorage.getItem(k); if(!s){s=Math.random().toString(36).slice(2)+Date.now().toString(36); localStorage.setItem(k,s);} return s;} catch { return 'lt-' + Math.random().toString(36).slice(2,10);}\n  }\n  function nounIdFor(sid) { let h=5381; for(let i=0;i<sid.length;i++) h=(h*33+sid.charCodeAt(i))&0x7fffffff; return h%1200; }\n  const sid = getSession();\n  let storedNoun = 0;\n  try { const s = localStorage.getItem('pc:nounId'); storedNoun = s ? Number(s) : nounIdFor(sid); }\n  catch { storedNoun = nounIdFor(sid); }\n  if (!Number.isFinite(storedNoun) || storedNoun < 0 || storedNoun > 1199) storedNoun = nounIdFor(sid);\n  try { localStorage.setItem('pc:nounId', String(storedNoun)); } catch {}\n  sig.textContent = `signed: noun #${storedNoun}`;\n\n  let seen = getSeen();\n  let currentColor = 'warm';\n\n  function fmtTimeAgo(ts) {\n    const s = Math.floor((Date.now() - ts) / 1000);\n    if (s < 60) return `${s}s ago`;\n    if (s < 3600) return `${Math.floor(s / 60)}m ago`;\n    if (s < 86400) return `${Math.floor(s / 3600)}h ago`;\n    return `${Math.floor(s / 86400)}d ago`;\n  }\n  function paintLetter(letter) {\n    if (!letter) {\n      bodyEl.textContent = 'No letters yet, or you\\'ve seen them all this session. Be the first to write one.';\n      metaEl.textContent = '— empty box —';\n      footEl.textContent = '';\n      cardEl.dataset.color = 'fog';\n      cardEl.style.setProperty('--lt-card-color', COLORS.fog);\n      nounEl.removeAttribute('src');\n      return;\n    }\n    seen.add(letter.id); saveSeen(seen);\n    bodyEl.textContent = letter.body || '';\n    metaEl.textContent = `from noun #${letter.nounId} · ${fmtTimeAgo(letter.t)}`;\n    footEl.textContent = `read ${letter.delivered_count || 0}× · color ${(letter.color || 'warm').toLowerCase()}`;\n    cardEl.dataset.color = letter.color || 'warm';\n    cardEl.style.setProperty('--lt-card-color', COLORS[letter.color] || COLORS.warm);\n    nounEl.src = `https://noun.pics/${letter.nounId || 0}.svg`;\n  }\n  async function fetchOne() {\n    metaEl.textContent = '— drawing a letter —';\n    try {\n      const exclude = [...seen].slice(-150).join(',');\n      const r = await fetch(`/api/letters?exclude=${encodeURIComponent(exclude)}`, { cache: 'no-store' });\n      if (!r.ok) { paintLetter(null); return; }\n      const data = await r.json();\n      countEl.textContent = `${data.count || 0} letter${(data.count || 0) === 1 ? '' : 's'} in the box`;\n      paintLetter(data.letter);\n    } catch { paintLetter(null); }\n  }\n  fetchOne();\n\n  next.addEventListener('click', () => fetchOne());\n\n  input.addEventListener('input', () => { cc.textContent = `${input.value.length} / 500`; });\n\n  document.querySelectorAll('.lt__color').forEach((b) => {\n    b.addEventListener('click', () => {\n      document.querySelectorAll('.lt__color').forEach((x) => x.classList.remove('lt__color--on'));\n      b.classList.add('lt__color--on');\n      currentColor = b.getAttribute('data-color') || 'warm';\n    });\n  });\n\n  document.getElementById('lt-form').addEventListener('submit', async (e) => {\n    e.preventDefault();\n    const text = (input.value || '').trim().slice(0, 500);\n    if (!text) { input.focus(); return; }\n    send.disabled = true; send.textContent = 'DROPPING…';\n    try {\n      const r = await fetch('/api/letters', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ body: text, color: currentColor, nounId: storedNoun }),\n      });\n      const data = await r.json();\n      if (data && data.ok) {\n        input.value = ''; cc.textContent = '0 / 500';\n        send.textContent = 'DROPPED ✓';\n        if (typeof data.count === 'number') countEl.textContent = `${data.count} letter${data.count === 1 ? '' : 's'} in the box`;\n        // Mark as seen so we don't immediately re-draw our own\n        if (data.letter && data.letter.id) { seen.add(data.letter.id); saveSeen(seen); }\n        // Also broadcast a tiny confessional-style ping so cast surfaces flash\n        try { fetch('/api/sounds', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ type: 'letter', sessionId: sid, color: currentColor, nounId: storedNoun }) }); } catch {}\n        setTimeout(() => { send.textContent = 'DROP IN THE BOX →'; send.disabled = false; }, 1600);\n      } else {\n        send.textContent = 'TRY AGAIN'; setTimeout(() => { send.textContent = 'DROP IN THE BOX →'; send.disabled = false; }, 1800);\n      }\n    } catch {\n      send.textContent = 'OFFLINE'; setTimeout(() => { send.textContent = 'DROP IN THE BOX →'; send.disabled = false; }, 1800);\n    }\n  });\n})();\n<\/script>"], ["", " <script>\n(function () { 'use strict';\n  const SEEN_KEY = 'pc:drum-letters:seen';\n  const COLORS = { warm: '#ff8a4a', bright: '#ffd400', ocean: '#5fbafd', garden: '#3b6e3b', fog: '#cfc6df', rose: '#d6745f' };\n  const cardEl = document.getElementById('lt-card');\n  const bodyEl = document.getElementById('lt-body');\n  const metaEl = document.getElementById('lt-meta');\n  const footEl = document.getElementById('lt-foot');\n  const nounEl = document.getElementById('lt-noun');\n  const countEl = document.getElementById('lt-count');\n  const input = document.getElementById('lt-input');\n  const cc = document.getElementById('lt-cc');\n  const send = document.getElementById('lt-send');\n  const next = document.getElementById('lt-next');\n  const sig = document.getElementById('lt-sig');\n\n  function getSeen() {\n    try { return new Set(JSON.parse(localStorage.getItem(SEEN_KEY) || '[]')); }\n    catch { return new Set(); }\n  }\n  function saveSeen(set) {\n    try { localStorage.setItem(SEEN_KEY, JSON.stringify([...set].slice(-200))); } catch {}\n  }\n  function getSession() {\n    try { const k='pc:session'; let s=localStorage.getItem(k); if(!s){s=Math.random().toString(36).slice(2)+Date.now().toString(36); localStorage.setItem(k,s);} return s;} catch { return 'lt-' + Math.random().toString(36).slice(2,10);}\n  }\n  function nounIdFor(sid) { let h=5381; for(let i=0;i<sid.length;i++) h=(h*33+sid.charCodeAt(i))&0x7fffffff; return h%1200; }\n  const sid = getSession();\n  let storedNoun = 0;\n  try { const s = localStorage.getItem('pc:nounId'); storedNoun = s ? Number(s) : nounIdFor(sid); }\n  catch { storedNoun = nounIdFor(sid); }\n  if (!Number.isFinite(storedNoun) || storedNoun < 0 || storedNoun > 1199) storedNoun = nounIdFor(sid);\n  try { localStorage.setItem('pc:nounId', String(storedNoun)); } catch {}\n  sig.textContent = \\`signed: noun #\\${storedNoun}\\`;\n\n  let seen = getSeen();\n  let currentColor = 'warm';\n\n  function fmtTimeAgo(ts) {\n    const s = Math.floor((Date.now() - ts) / 1000);\n    if (s < 60) return \\`\\${s}s ago\\`;\n    if (s < 3600) return \\`\\${Math.floor(s / 60)}m ago\\`;\n    if (s < 86400) return \\`\\${Math.floor(s / 3600)}h ago\\`;\n    return \\`\\${Math.floor(s / 86400)}d ago\\`;\n  }\n  function paintLetter(letter) {\n    if (!letter) {\n      bodyEl.textContent = 'No letters yet, or you\\\\'ve seen them all this session. Be the first to write one.';\n      metaEl.textContent = '— empty box —';\n      footEl.textContent = '';\n      cardEl.dataset.color = 'fog';\n      cardEl.style.setProperty('--lt-card-color', COLORS.fog);\n      nounEl.removeAttribute('src');\n      return;\n    }\n    seen.add(letter.id); saveSeen(seen);\n    bodyEl.textContent = letter.body || '';\n    metaEl.textContent = \\`from noun #\\${letter.nounId} · \\${fmtTimeAgo(letter.t)}\\`;\n    footEl.textContent = \\`read \\${letter.delivered_count || 0}× · color \\${(letter.color || 'warm').toLowerCase()}\\`;\n    cardEl.dataset.color = letter.color || 'warm';\n    cardEl.style.setProperty('--lt-card-color', COLORS[letter.color] || COLORS.warm);\n    nounEl.src = \\`https://noun.pics/\\${letter.nounId || 0}.svg\\`;\n  }\n  async function fetchOne() {\n    metaEl.textContent = '— drawing a letter —';\n    try {\n      const exclude = [...seen].slice(-150).join(',');\n      const r = await fetch(\\`/api/letters?exclude=\\${encodeURIComponent(exclude)}\\`, { cache: 'no-store' });\n      if (!r.ok) { paintLetter(null); return; }\n      const data = await r.json();\n      countEl.textContent = \\`\\${data.count || 0} letter\\${(data.count || 0) === 1 ? '' : 's'} in the box\\`;\n      paintLetter(data.letter);\n    } catch { paintLetter(null); }\n  }\n  fetchOne();\n\n  next.addEventListener('click', () => fetchOne());\n\n  input.addEventListener('input', () => { cc.textContent = \\`\\${input.value.length} / 500\\`; });\n\n  document.querySelectorAll('.lt__color').forEach((b) => {\n    b.addEventListener('click', () => {\n      document.querySelectorAll('.lt__color').forEach((x) => x.classList.remove('lt__color--on'));\n      b.classList.add('lt__color--on');\n      currentColor = b.getAttribute('data-color') || 'warm';\n    });\n  });\n\n  document.getElementById('lt-form').addEventListener('submit', async (e) => {\n    e.preventDefault();\n    const text = (input.value || '').trim().slice(0, 500);\n    if (!text) { input.focus(); return; }\n    send.disabled = true; send.textContent = 'DROPPING…';\n    try {\n      const r = await fetch('/api/letters', {\n        method: 'POST',\n        headers: { 'Content-Type': 'application/json' },\n        body: JSON.stringify({ body: text, color: currentColor, nounId: storedNoun }),\n      });\n      const data = await r.json();\n      if (data && data.ok) {\n        input.value = ''; cc.textContent = '0 / 500';\n        send.textContent = 'DROPPED ✓';\n        if (typeof data.count === 'number') countEl.textContent = \\`\\${data.count} letter\\${data.count === 1 ? '' : 's'} in the box\\`;\n        // Mark as seen so we don't immediately re-draw our own\n        if (data.letter && data.letter.id) { seen.add(data.letter.id); saveSeen(seen); }\n        // Also broadcast a tiny confessional-style ping so cast surfaces flash\n        try { fetch('/api/sounds', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ type: 'letter', sessionId: sid, color: currentColor, nounId: storedNoun }) }); } catch {}\n        setTimeout(() => { send.textContent = 'DROP IN THE BOX →'; send.disabled = false; }, 1600);\n      } else {\n        send.textContent = 'TRY AGAIN'; setTimeout(() => { send.textContent = 'DROP IN THE BOX →'; send.disabled = false; }, 1800);\n      }\n    } catch {\n      send.textContent = 'OFFLINE'; setTimeout(() => { send.textContent = 'DROP IN THE BOX →'; send.disabled = false; }, 1800);\n    }\n  });\n})();\n<\/script>"])), renderComponent($$result, "BlockLayout", $$BlockLayout, { "title": title, "description": description, "jsonLd": jsonLd, "data-astro-cid-ase2ce4o": true }, { "default": async ($$result2) => renderTemplate` ${maybeRenderHead()}<main class="lt" id="lt-main" data-astro-cid-ase2ce4o> ${renderComponent($$result2, "DrumNav", $$DrumNav, { "current": "letters", "data-astro-cid-ase2ce4o": true })} <header class="lt__head" data-astro-cid-ase2ce4o> <p class="lt__kicker" data-astro-cid-ase2ce4o>DRUM HUB · COMMS 4 · LETTERS · 500 CHAR · KV</p> <h1 class="lt__title" data-astro-cid-ase2ce4o><em data-astro-cid-ase2ce4o>Leave a letter for the next visitor.</em></h1> <p class="lt__dek" data-astro-cid-ase2ce4o>Write something to whoever comes through next. They might be a stranger. They might be you, six months from now. Read whatever the last visitor left. Letters last 200 deep before they roll off.</p> </header> <section class="lt__stage" data-astro-cid-ase2ce4o> <article class="lt__received" id="lt-received" aria-label="Letter received" data-astro-cid-ase2ce4o> <div class="lt__received-head" data-astro-cid-ase2ce4o> <p class="lt__received-eyebrow mono" data-astro-cid-ase2ce4o>★ A LETTER FOR YOU ★</p> <p class="lt__received-meta mono" id="lt-meta" data-astro-cid-ase2ce4o>— loading —</p> </div> <div class="lt__received-card" id="lt-card" data-color="warm" data-astro-cid-ase2ce4o> <img class="lt__received-noun" id="lt-noun" alt="" width="56" height="56" data-astro-cid-ase2ce4o> <p class="lt__received-body" id="lt-body" data-astro-cid-ase2ce4o>…</p> <p class="lt__received-foot mono" id="lt-foot" data-astro-cid-ase2ce4o>—</p> </div> <div class="lt__received-actions" data-astro-cid-ase2ce4o> <button type="button" class="lt__next mono" id="lt-next" data-astro-cid-ase2ce4o>DRAW ANOTHER →</button> <span class="lt__count mono" id="lt-count" data-astro-cid-ase2ce4o>— letters in the box</span> </div> </article> <form class="lt__compose" id="lt-form" autocomplete="off" data-astro-cid-ase2ce4o> <p class="lt__compose-eyebrow mono" data-astro-cid-ase2ce4o>★ WRITE TO THE NEXT VISITOR ★</p> <textarea class="lt__compose-body" id="lt-input" maxlength="500" placeholder="Dear stranger,

…" rows="6" aria-label="Letter body" data-astro-cid-ase2ce4o></textarea> <div class="lt__compose-row" data-astro-cid-ase2ce4o> <span class="lt__compose-count mono" id="lt-cc" data-astro-cid-ase2ce4o>0 / 500</span> <div class="lt__compose-colors" role="radiogroup" aria-label="Letter color" data-astro-cid-ase2ce4o> ${COLORS.map((c, i) => renderTemplate`<button type="button"${addAttribute(`lt__color ${i === 0 ? "lt__color--on" : ""}`, "class")}${addAttribute(c.id, "data-color")}${addAttribute(`--lt-color: ${c.hex}`, "style")}${addAttribute(`Color ${c.label}`, "aria-label")} data-astro-cid-ase2ce4o> <span class="lt__color-swatch" data-astro-cid-ase2ce4o></span> <span class="lt__color-label mono" data-astro-cid-ase2ce4o>${c.label}</span> </button>`)} </div> </div> <div class="lt__compose-foot" data-astro-cid-ase2ce4o> <span class="lt__sig mono" id="lt-sig" data-astro-cid-ase2ce4o>signed: noun #—</span> <button type="submit" class="lt__send mono" id="lt-send" data-astro-cid-ase2ce4o>DROP IN THE BOX →</button> </div> </form> </section> <footer class="lt__foot" data-astro-cid-ase2ce4o> <p data-astro-cid-ase2ce4o>Letters live in <code data-astro-cid-ase2ce4o>env.VISITS</code> KV (key prefix <code data-astro-cid-ase2ce4o>letter:*</code>) with an index at <code data-astro-cid-ase2ce4o>letters:index</code>, capped at 200 entries. POST <code data-astro-cid-ase2ce4o>/api/letters</code> writes; GET picks one at random excluding ids already seen this session and increments <code data-astro-cid-ase2ce4o>delivered_count</code>.</p> <p class="lt__credit mono" data-astro-cid-ase2ce4o>v0.1 · 2026-04-28 · drum sprint · comms 4</p> </footer> </main> ` }));
}, "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-letters.astro", void 0);

const $$file = "/Users/michaelhoydich/Documents/join us yee/pointcast-shipping/src/pages/drum-letters.astro";
const $$url = "/drum-letters";

const _page = /*#__PURE__*/Object.freeze(/*#__PURE__*/Object.defineProperty({
  __proto__: null,
  default: $$DrumLetters,
  file: $$file,
  url: $$url
}, Symbol.toStringTag, { value: 'Module' }));

const page = () => _page;

export { page };
