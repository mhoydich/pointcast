# build output
dist/
dist.stale-*/
# generated types
.astro/

# local scratch
tmp/visits/
.pointcast/
.wrangler/

# Generated from the preserved Midjourney ZIPs by the gallery build script.
# Cloudflare receives these through direct upload; keep 258 MB of renditions out of Git.
public/images/el-segundo-school/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production
.env.*
.dev.vars
.dev.vars.*

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Local secrets — NEVER commit. Used by scripts/manus.mjs + local dev.
.env.local
.secrets/

# Finder-style duplicate build outputs ("dist 2", "dist 3") — never ship these.
dist [0-9]*/
